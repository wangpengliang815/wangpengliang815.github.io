<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>泛型</title>
    <url>/2020/11/17/csharp/Genericity/</url>
    <content><![CDATA[<p>.Net自从2.0版 开始支持泛型。泛型允许你在编译时实现类型安全。它们允许你创建一个数据结构而不限于一特定的数据类型。然而，当使用该数据结构时，编译器保证它使用的类型与类型安全是相一致的。泛型提供了类型安全,但是没有造成任何性能损失和代码臃肿。泛型不仅限于类,还可用于接口、方法、委托…</p>
<a id="more"></a>
<h2 id="泛型优势">泛型优势</h2>
<pre><code> ArrayList arrs = new ArrayList
 {
     &quot;wang&quot;,
     100
 };
</code></pre>
<h3 id="类型安全">类型安全</h3>
<p>这里如果将item设置为int类型遍历集合,会抛出异常,因为集合中存在string</p>
<pre><code>foreach (var item in arrs)
{
    Console.WriteLine(item);
}
</code></pre>
<h3 id="避免装箱拆箱带来的性能损耗">避免装箱拆箱带来的性能损耗</h3>
<p>遍历时不可避免装箱/拆箱操作</p>
<pre><code>foreach (var item in arrs)
{
    Console.WriteLine(item);
}

</code></pre>
<h3 id="二进制代码重用">二进制代码重用</h3>
<p>非泛型集合</p>
<pre><code class="language-cs">var arr1 = new ArrayList();
arr1.Add(10);//Add方法参数值是object类型，这里将int-&gt;object
arr1.Add(&quot;wang&quot;);//Add方法参数值是object类型，这里将int-&gt;object
foreach (int item in arr1)//这里如果用int类型遍历集合,就挂掉了
{
    Console.WriteLine(item);//读取时要进行拆箱
}
</code></pre>
<p>泛型集合</p>
<pre><code class="language-cs">/*泛型集合在使用时定义好类型,之后就不存在装箱拆箱;
 * 因为已经定义当前集合只能存入int类型，也就不能存入其他类型
 * 编译时就会报错，错误应及早发现
 */
var arr2 = new List&lt;int&gt;();
arr2.Add(10);
foreach (var item in arr2)
{
    Console.WriteLine(item);
} 
</code></pre>
<p>通过参数化类型来实现在同一份代码上操作多种数据类型，利用“参数化类型”将类型抽象化，从而实现灵活的复用。每个集合的详细规范可以在<code>System.Collection.Generic</code>名称空间下找到</p>
<h2 id="泛型方法">泛型方法</h2>
<pre><code class="language-cs">public class Print
{
    public void PrintType&lt;Tentity&gt;()
        where Tentity : new()
    {
        Tentity t = new Tentity();
        Console.WriteLine(t.GetType());
    }
}
</code></pre>
<h2 id="泛型类">泛型类</h2>
<pre><code class="language-cs">public class MyArray&lt;T&gt;
      where T : new()
{
    private readonly T[] array;

    public MyArray(int size)
        {
            array = new T[size + 1];
        }

    public T GetItem(int index)
        {
            return array[index];
        }

    public void SetItem(int index, T value)
        {
            array[index] = value;
        }

}
</code></pre>
<h2 id="default">default</h2>
<p>泛型<code>default</code>用于将泛型类型初始化为null或0</p>
<p><code>T t=default(T);</code></p>
<h2 id="泛型接口">泛型接口</h2>
<pre><code class="language-cs">public interface IPrint&lt;T&gt;
{
    void Add(T t);
}
</code></pre>
<h2 id="泛型委托">泛型委托</h2>
<pre><code>public delegate void Print&lt;T&gt;(T t);
</code></pre>
<p>泛型在声明的时候可以不指定具体的类型，但是在使用的时候必须指定具体类型</p>
<pre><code>Print&lt;int&gt; iPrint = new Print&lt;int&gt;();
</code></pre>
<p>这里的类型是int,如果存在继承并且子类也是泛型的，那么继承的时候可以不指定具体类型</p>
<pre><code class="language-cs">class Print&lt;T&gt;
{
    public T t;
}

//这里继承时没有指定泛型类型
class ConsolePrint&lt;T&gt; : Print&lt;T&gt;
{

}
</code></pre>
<p>同上,类实现泛型接口也是如此</p>
<h2 id="泛型约束">泛型约束</h2>
<p>所谓的泛型约束，就是约束的类型T。使T必须遵循一定的规则。比如T必须继承自某个类，或者T必须实现某个接口等</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>T:calss</td>
<td>类型参数必须是引用类型，包括任何类、接口、委托或数组类型</td>
</tr>
<tr>
<td>T:stauct</td>
<td>类型参数必须是值类型。可以指定除 Nullable 以外的任何值类型</td>
</tr>
<tr>
<td>T:new()</td>
<td>类型参数必须具有无参数的公共构造函数。当与其他约束一起使用时，new() 约束必须最后指定</td>
</tr>
<tr>
<td>T:&lt;基类名&gt;</td>
<td>类型参数必须是指定的基类或派生自指定的基类</td>
</tr>
<tr>
<td>T:&lt;接口名称&gt;</td>
<td>类型参数必须是指定的接口或实现指定的接口。可以指定多个接口约束。约束接口也可以是泛型的</td>
</tr>
<tr>
<td>T1:T2</td>
<td>类型T1派生自泛型类型T2,该约束也被称为裸类型约束</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">class Person
{
    public static void PintSayHello()
    {
    }
}

interface ISport
{
    void Sport();
}

/// &lt;summary&gt;
/// 约束T必须是Person类型或者是Person的子类并且实现了ISport接口且T类型中必须有无参构造函数
/// &lt;/summary&gt;
/// &lt;typeparam name=&quot;T&quot;&gt;&lt;/typeparam&gt;
class Show&lt;T&gt; where T : Person, ISport, new()
{
}

/// &lt;summary&gt;
/// 约束T必须是引用类型并且实现了ISport接口且T类型中必须有无参构造函数
/// &lt;/summary&gt;
/// &lt;typeparam name=&quot;T&quot;&gt;&lt;/typeparam&gt;
class Show2&lt;T&gt; where T : class, ISport, new()
{
}

/// &lt;summary&gt;
/// 约束T必须是值类型并且实现了ISport接口
/// &lt;/summary&gt;
/// &lt;typeparam name=&quot;T&quot;&gt;&lt;/typeparam&gt;
class Show3&lt;T&gt; where T : struct, ISport
{
}
</code></pre>
<p>注意:泛型约束可以同时约束多个,有多个泛型约束时，<code>new()</code>约束一定是在最后</p>
<h2 id="泛型协变与逆变">泛型协变与逆变</h2>
<p><a href="http://xn--ykr6la33ei5hvr2j.NET" target="_blank" rel="noopener">协变和逆变在.NET</a> 4.0的时候出现，<strong>只能放在接口或者委托的泛型参数前</strong>，<code>out</code> 协变<code>covariant</code>，用来修饰返回值；<code>in</code>：逆变<code>contravariant</code>，用来修饰传入参数;在C#中声明泛型接口时，可以使用 <code>in</code> 和 <code>out</code> 参数来控制这个泛型是协变还是逆变的，这里逆变有时也被翻译成抗变，协变和逆变是用来描述<strong>如果泛型存在继承关系时，两个泛型类是否能够直接赋值的问题</strong>。比如派生泛型<code>IInterface&lt;Child&gt;</code>是否能被赋值给<code>IInterface&lt;Parent&gt;</code></p>
<p>协变：只能是方法参数</p>
<pre><code>public interface ICustomerListIn&lt;in T&gt;
{
    void Show(T t);
}
</code></pre>
<pre><code>        /// &lt;summary&gt;
        /// 泛型协变(子类到父类的转换)
        /// &lt;/summary&gt;
        /// &lt;remarks&gt;
        /// out：协变covariant，用来修饰返回值
        /// &lt;/remarks&gt;
        [TestMethod]
        public void GenericityCovariant()
        {
            // 直接声明Animal类
            Animal animal = new Animal();
            Console.WriteLine($&quot;{animal.GetType()}&quot;);

            // 直接声明Cat类
            Cat cat = new Cat();
            Console.WriteLine($&quot;{cat.GetType()}&quot;);

            // 声明子类对象指向父类
            Animal animal2 = new Cat();
            Console.WriteLine($&quot;{animal2.GetType()}&quot;);

            // 声明Animal类的集合
            List&lt;Animal&gt; listAnimal = new List&lt;Animal&gt;();
            foreach (var item in listAnimal)
            {
                Console.WriteLine($&quot;{item.GetType()}&quot;);
            }

            // 声明Cat类的集合
            List&lt;Cat&gt; listCat = new List&lt;Cat&gt;();
            foreach (var item in listCat)
            {
                Console.WriteLine($&quot;{item.GetType()}&quot;);
            }

#if debug
            // 一只Cat属于Animal，一群Cat也应该属于Animal。但实际上这样声明是错误的：因为List&lt;Cat&gt;和List&lt;Animal&gt;之间没有父子关系
            List&lt;Animal&gt; list = new List&lt;Cat&gt;();
#endif
            // 泛型协变，IEnumerable泛型参数类型使用了out修饰
            ICustomerListOut&lt;Animal&gt; customerList1 = new CustomerListOut&lt;Animal&gt;();
            Console.WriteLine($&quot;{customerList1.GetType()}&quot;);

            ICustomerListOut&lt;Animal&gt; customerList2 = new CustomerListOut&lt;Cat&gt;();
            Console.WriteLine($&quot;{customerList2.GetType()}&quot;);
            Assert.IsTrue(true);
        }
</code></pre>
<p>逆变：逆变 只能是方法参数</p>
<pre><code> public interface ICustomerListIn&lt;in T&gt;
 {
     void Show(T t);
 }
</code></pre>
<pre><code>/// &lt;summary&gt;
        /// 泛型逆变(父类到子类的转换)
        /// &lt;/summary&gt;
        /// &lt;remarks&gt;
        /// int：协变contravariant，用来修饰返回值(子类到父类的转换)
        /// &lt;/remarks&gt;
[TestMethod]
public void GenericityContravariant()
{
    // 直接声明Animal类
    Animal animal = new Animal();
    Console.WriteLine($&quot;{animal.GetType()}&quot;);

    // 直接声明Cat类
    Cat cat = new Cat();
    Console.WriteLine($&quot;{cat.GetType()}&quot;);

    // 声明子类对象指向父类
    Animal animal2 = new Cat();
    Console.WriteLine($&quot;{animal2.GetType()}&quot;);

    // 声明Animal类的集合
    List&lt;Animal&gt; listAnimal = new List&lt;Animal&gt;();
    foreach (var item in listAnimal)
    {
        Console.WriteLine($&quot;{item.GetType()}&quot;);
    }

    // 声明Cat类的集合
    List&lt;Cat&gt; listCat = new List&lt;Cat&gt;();
    foreach (var item in listCat)
    {
        Console.WriteLine($&quot;{item.GetType()}&quot;);
    }

    ICustomerListIn&lt;Cat&gt; customerListCat1 = new CustomerListIn&lt;Cat&gt;();
    Console.WriteLine($&quot;{customerListCat1.GetType()}&quot;);

    ICustomerListIn&lt;Cat&gt; customerListCat2 = new CustomerListIn&lt;Animal&gt;();
    Console.WriteLine($&quot;{customerListCat2.GetType()}&quot;);
    Assert.IsTrue(true);
}

</code></pre>
<h2 id="高性能泛型缓存">高性能泛型缓存</h2>
<p>静态字典缓存和常用的泛型缓存的性能相比，泛型缓存性能是非常优异的。泛型缓存是JIT产生全新的类，内存直接分配，由CPU查找内存地址。<br>
静态字典缓存需要根据地址去寻址查找</p>
<pre><code>/// &lt;summary&gt;
/// 类中的静态类型无论实例化多少次，在内存中只会有一个
/// 静态构造函数只会执行一次
/// 在泛型类中，T类型不同
/// 每个不同的T类型，都会产生一个不同的副本，所以会产生不同的静态属性、不同的静态构造函数
/// &lt;/summary&gt;
/// &lt;typeparam name=&quot;T&quot;&gt;&lt;/typeparam&gt;
gma warning disable S1118 // Utility classes should not have public constructors
public class GenericCache&lt;T&gt;
gma warning restore S1118 // Utility classes should not have public constructors
{
gma warning disable S3963 // &quot;static&quot; fields should be initialized inline
    static GenericCache()
    {
        _CachedValue = string.Format(&quot;{0}_{1}&quot;,
            typeof(T).FullName, DateTime.Now.ToString(&quot;yyyyMMddHHmmss.fff&quot;));
    }
gma warning restore S3963 // &quot;static&quot; fields should be initialized inline

gma warning disable S2743 // Static fields should not be used in generic types
    private static readonly string _CachedValue = &quot;&quot;;
gma warning restore S2743 // Static fields should not be used in generic types

    public static string GetCache()
    {
        return _CachedValue;
    }
}
</code></pre>
<pre><code>/// &lt;summary&gt;
/// 泛型会为不同的类型都创建一个副本
/// 所以静态构造函数会执行4次,而且每次静态属性的值都是一样的
/// 利用泛型的这一特性，可以实现缓存
/// 注意：只能为不同的类型缓存一次。泛型缓存比字典缓存效率高。泛型缓存不能主动释放
/// &lt;/summary&gt;
[TestMethod]
public void GenericityCache()
{
    for (int i = 0; i &lt; 5; i++)
    {
        Console.WriteLine(GenericCache&lt;int&gt;.GetCache());
        Console.WriteLine(GenericCache&lt;long&gt;.GetCache());
        Console.WriteLine(GenericCache&lt;DateTime&gt;.GetCache());
        Console.WriteLine(GenericCache&lt;string&gt;.GetCache());
    }
    Assert.IsTrue(true);
}

</code></pre>
<p><img src="/images/2020-11-17-21-32-27.png" alt=""></p>
<p>参考</p>
<ul>
<li><a href="https://www.cnblogs.com/dotnet261010/p/9034594.html" target="_blank" rel="noopener">https://www.cnblogs.com/dotnet261010/p/9034594.html</a></li>
<li><a href="https://www.cnblogs.com/yilezhu/p/10029782.html" target="_blank" rel="noopener">https://www.cnblogs.com/yilezhu/p/10029782.html</a></li>
<li><a href="https://www.cnblogs.com/aehyok/p/3384637.html" target="_blank" rel="noopener">https://www.cnblogs.com/aehyok/p/3384637.html</a></li>
</ul>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>基础合集</title>
    <url>/2020/11/17/csharp/BasicCollection/</url>
    <content><![CDATA[<p>C#中的基础知识点，基础必须要扎实掌握，勿在浮沙建高塔</p>
<a id="more"></a>
<h2 id="预定义数据类型">预定义数据类型</h2>
<blockquote>
<p>C#是一种面向对象的强类型高级语言,内置了用于存储不同类型数据的内置数据类型,每种数据类型都包含特定的取值范围,使用这些数据类型来表示在应用程序中存储的数据,比如人的姓名是个字符串,对应的<code>.Net</code>平台中的<code>string</code>类型,年龄是个整数类型值,对应<code>.Net</code>平台中的<code>int</code>类型等</p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:center">类型别名</th>
<th style="text-align:center">.实际.Net 类型</th>
<th style="text-align:center">Type</th>
<th style="text-align:center">Size (bits)</th>
<th style="text-align:center">Range (values)</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">byte</td>
<td style="text-align:center">Byte</td>
<td style="text-align:center">Unsigned integer</td>
<td style="text-align:center">8</td>
<td style="text-align:center">0 to 255</td>
</tr>
<tr>
<td style="text-align:center">sbyte</td>
<td style="text-align:center">SByte</td>
<td style="text-align:center">Signed integer</td>
<td style="text-align:center">8</td>
<td style="text-align:center">-128 to 127</td>
</tr>
<tr>
<td style="text-align:center">int</td>
<td style="text-align:center">Int32</td>
<td style="text-align:center">Signed integer</td>
<td style="text-align:center">32</td>
<td style="text-align:center">-2,147,483,648 to 2,147,483,647</td>
</tr>
<tr>
<td style="text-align:center">uint</td>
<td style="text-align:center">UInt32</td>
<td style="text-align:center">Unsigned integer</td>
<td style="text-align:center">32</td>
<td style="text-align:center">0 to 4294967295</td>
</tr>
<tr>
<td style="text-align:center">short</td>
<td style="text-align:center">Int16</td>
<td style="text-align:center">Signed integer</td>
<td style="text-align:center">16</td>
<td style="text-align:center">-32,768 to 32,767</td>
</tr>
<tr>
<td style="text-align:center">ushort</td>
<td style="text-align:center">UInt16</td>
<td style="text-align:center">Unsigned integer</td>
<td style="text-align:center">16</td>
<td style="text-align:center">0 to 65,535</td>
</tr>
<tr>
<td style="text-align:center">long</td>
<td style="text-align:center">Int64</td>
<td style="text-align:center">Signed integer</td>
<td style="text-align:center">64</td>
<td style="text-align:center">-9,223,372,036,854,775,808 to 9,223,372,036,854,775,807</td>
</tr>
<tr>
<td style="text-align:center">ulong</td>
<td style="text-align:center">UInt64</td>
<td style="text-align:center">Unsigned integer</td>
<td style="text-align:center">64</td>
<td style="text-align:center">0 to 18,446,744,073,709,551,615</td>
</tr>
<tr>
<td style="text-align:center">float</td>
<td style="text-align:center">Single</td>
<td style="text-align:center">Single-precision floating point type</td>
<td style="text-align:center">32</td>
<td style="text-align:center">-3.402823e38 to 3.402823e38</td>
</tr>
<tr>
<td style="text-align:center">double</td>
<td style="text-align:center">Double</td>
<td style="text-align:center">Double-precision floating point type</td>
<td style="text-align:center">64</td>
<td style="text-align:center">-1.79769313486232e308 to 1.79769313486232e308</td>
</tr>
<tr>
<td style="text-align:center">char</td>
<td style="text-align:center">Char</td>
<td style="text-align:center">A single Unicode character</td>
<td style="text-align:center">16</td>
<td style="text-align:center">Unicode symbols used in text</td>
</tr>
<tr>
<td style="text-align:center">bool</td>
<td style="text-align:center">Boolean</td>
<td style="text-align:center">Logical Boolean type</td>
<td style="text-align:center">8</td>
<td style="text-align:center">True or False</td>
</tr>
<tr>
<td style="text-align:center">object</td>
<td style="text-align:center">Object</td>
<td style="text-align:center">Base type of all other types</td>
<td style="text-align:center"></td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center">string</td>
<td style="text-align:center">String</td>
<td style="text-align:center">A sequence of characters</td>
<td style="text-align:center"></td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center">decimal</td>
<td style="text-align:center">Decimal</td>
<td style="text-align:center">Precise fractional or integral type that can represent decimal numbers with 29 significant digits</td>
<td style="text-align:center">128</td>
<td style="text-align:center">(+ or -)1.0 x 10e-28 to 7.9 x 10e28</td>
</tr>
<tr>
<td style="text-align:center">DateTime</td>
<td style="text-align:center">DateTime</td>
<td style="text-align:center">Represents date and time</td>
<td style="text-align:center"></td>
<td style="text-align:center">0:00:00am 1/1/01 to 11:59:59pm 12/31/9999</td>
</tr>
</tbody>
</table>
<p>数据类型进一步又被分类为值类型或引用类型,这取决于特定类型的变量是存储其自己的数据还是指向存储器中的数据的引用,理解这一点很重要</p>
<table>
<thead>
<tr>
<th style="text-align:center">Alias</th>
<th style="text-align:center">Type Name</th>
<th style="text-align:center">.Net Type</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">byte</td>
<td style="text-align:center">System.Byte</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">sbyte</td>
<td style="text-align:center">System.SByte</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">int</td>
<td style="text-align:center">System.Int32</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">uint</td>
<td style="text-align:center">System.UInt32</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">short</td>
<td style="text-align:center">System.Int16</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">ushort</td>
<td style="text-align:center">System.UInt16</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">long</td>
<td style="text-align:center">System.Int64</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">ulong</td>
<td style="text-align:center">System.UInt64</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">float</td>
<td style="text-align:center">System.Single</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">double</td>
<td style="text-align:center">System.Double</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">char</td>
<td style="text-align:center">System.Char</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">bool</td>
<td style="text-align:center">System.Boolean</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">object</td>
<td style="text-align:center">System.Object</td>
<td style="text-align:center">Class</td>
</tr>
<tr>
<td style="text-align:center">string</td>
<td style="text-align:center">System.String</td>
<td style="text-align:center">Class</td>
</tr>
<tr>
<td style="text-align:center">decimal</td>
<td style="text-align:center">System.Decimal</td>
<td style="text-align:center">struct</td>
</tr>
<tr>
<td style="text-align:center">DateTime</td>
<td style="text-align:center">System.DateTime</td>
<td style="text-align:center">struct</td>
</tr>
</tbody>
</table>
<h3 id="值类型">值类型</h3>
<blockquote>
<p>值类型的特点是变量直接存储其值,以下数据类型都是值类型</p>
</blockquote>
<ul>
<li><code>bool</code> <code>byte</code> <code>char</code> <code>decimal</code> <code>double</code> <code>enum</code> <code>float</code> <code>int</code> <code>long</code> <code>sbyte</code> <code>short</code> <code>struct</code> <code>uint</code> <code>ulong</code> <code>ushort</code></li>
</ul>
<h3 id="引用类型">引用类型</h3>
<blockquote>
<p>与值类型不同,引用类型不直接存储其值,它存储值的引用内存地址。以下数据类型都是引用类型</p>
</blockquote>
<ul>
<li><code>String</code></li>
<li><code>All arrays, even if their elements are value types</code>(所有数组,即使它们的元素是值类型)</li>
<li><code>Class</code></li>
<li><code>Delegates</code></li>
<li>…</li>
</ul>
<h2 id="变量">变量</h2>
<blockquote>
<p>了解dotnet平台的数据类型后,第二步就是使用变量来保存数据</p>
</blockquote>
<p>变量声明语法</p>
<pre><code class="language-cs">&lt;datatype&gt;&lt;variablename&gt;=&lt;value&gt;;
</code></pre>
<p>结合数据类型的例子,名称是个字符串,<a href="http://xn--ceto4d.Net" target="_blank" rel="noopener">对应.Net</a> 平台下<code>string</code>类型,创建一个<code>string</code>类型的变量来保存名称</p>
<pre><code class="language-cs">string name = &quot;wang&quot;;
</code></pre>
<p>可同时声明多个</p>
<pre><code class="language-cs">string name1, name2 = &quot;wang&quot;;
</code></pre>
<p>编译器需要用某个初始值对变量进行初始化,之后才能在操作中引用该变量。这里需要注意</p>
<ul>
<li>变量是类或结构中的字段,如果没有显式初始化,创建这些变量时,其默认值就是类型默认值</li>
<li>方法的局部变量必须在代码中显式初始化,之后才能在语句中使用。此时,初始化不是在声明该变量时进行的,但编译器会通过方法检查所有可能的路径,如果检测到局部变量在初始化之前就使用了它的值,就会产生错误</li>
<li>在 C#中实例化一个引用对象需要使用<code>new</code>关键字把该引用指向存储在堆上的一个对象</li>
</ul>
<h2 id="类型推断">类型推断</h2>
<p>传统定义变量是已经知道变量的类型,如之前的</p>
<pre><code class="language-cs">string name = &quot;wang&quot;;
</code></pre>
<p>使用<code>var</code>类型预先不用知道变量的类型,编译器可以根据变量的初始化值 “推断” 变量的类型</p>
<pre><code class="language-cs">var name = &quot;wang&quot;;
</code></pre>
<p>但是需要遵循一些规则:</p>
<ul>
<li>变量必须初始化。否则,编译器就没有推断变量类型的依据</li>
<li>初始化器不能为空</li>
<li>初始化器必须放在表达式中</li>
<li>不能把初始化器设置为一个对象,除非在初始化器中创建了一个新对象</li>
</ul>
<h2 id="变量作用域">变量作用域</h2>
<blockquote>
<p>变量的作用域是可以访问该变量的代码区域</p>
</blockquote>
<p>一般情况下,确定作用域遵循以下规则:</p>
<ul>
<li>只要类在某个作用域内,其字段(也称为成员变量)也在该作用域内</li>
<li>局部变量存在于表示声明该变量的块语句或方法结束的右花括号之前的作用域内</li>
<li>在<code>for、while</code>或类似语旬中声明的局部变量存在于该循环体内</li>
</ul>
<h2 id="常量">常量</h2>
<blockquote>
<p>常量是其值在使用过程中不会发生变化的变量,在声明和初始化变量时,在变量的前面加上关键字<code>const</code>,就可以把该变量指定为一个常量</p>
</blockquote>
<p>常量具有如下特点:</p>
<ul>
<li>常量必须在声明时初始化。指定了其值后,就不能再改写了</li>
<li>常量的值必须能在编译时用于计算。因此,不能用从一个变量中提取的值来初始化常量。如果需要这么做,应使用只读字段<code>readonly</code></li>
<li>常量总是静态的,但注意,不必(实际上,是不允许)在常量声明中包含修饰符</li>
</ul>
<p>在程序中使用常量有以下好处:</p>
<ul>
<li>由于使用易于读取的名称(名称的值易于理解)替代了较难读取的数字或字符串,常量使程序变得更易于阅读</li>
<li>常量使程序更易于修改</li>
<li>常量更容易避免程序出现错误,如果在声明常量的位置以外的某个地方将另一个值赋给常量,编译器就会报告错误</li>
</ul>
<pre><code class="language-cs">const string conntionName = &quot;testConntion&quot;;
</code></pre>
<h2 id="只读字段">只读字段</h2>
<blockquote>
<p>常量的概念是一个包含不能修改的值的变量,但是,有时可能需要一些变量,其值不应改变,但在运行之前其值是未知的,C#为这种情形提供了另一种类型的变量:只读字段。<code>readonly</code>关键字比<code>const</code>灵活,允许把一个字段设置为常量,但还需要执行一些计算,以确定它的初始值。其规则是可以在构造函数中给只读段赋值,但不能在其他地方赋值。只读字段还可以是一个实例字段,而不是静态字段,类的每个实例可以有不同的值。与<code>const</code>字段不同,如果要把只读字段设置为静态,就必须显式声明它</p>
</blockquote>
<pre><code class="language-cs">//实例只读字段
readonly double taxRate;

//静态字段字段
static readonly double taxRate1;
</code></pre>
<pre><code class="language-cs">BasicsTest()
{
    //只读字段可以在声明时赋值,也可以在构造函数中赋值
    taxRate = 0.8;
}
static BasicsTest()
{
    //静态只读字段在类的静态构造函数中赋值
    taxRate1 = 0.9;
}
</code></pre>
<h2 id="常量和只读字段的区别">常量和只读字段的区别</h2>
<ul>
<li><code>const</code>字段只能在声明语句中初始化,而且必须初始化,初始化之后在任何地方都不能改变；<code>readonly</code>字段既可以在声明时初始化,也可以在构造函数中改变它的值：如果是<code>实例只读字段</code>,可以在<code>实例构造函数</code>中改变它的值,如果是<code>静态只读字段</code>,则可以在<code>静态构造函数</code>中改变它的值</li>
<li><code>const</code>字段的值必须在编译时决定,编译完成之后它的值就被替换为字面量;<code>readonly</code>字段的值可以在运行时决定,可以在不同的构造函数中设置不同的值</li>
<li><code>const</code>总是像静态字段,在类的外部要通过&quot;类名.常量名&quot;的方式访问；<code>readonly</code>字段既可以是静态字段,也可以是实例字段。</li>
<li><code>const</code>在内存中没有存储位置,而<code>readonly</code>字段在内存中有存储位置。</li>
</ul>
<h2 id="值类型和引用类型空值">值类型和引用类型空值</h2>
<blockquote>
<p>默认情况下,引用类型在未初始化时具有空值。例如,一个字符串变量(或引用类型数据类型的任何其他变量),但没有赋值。在这种情况下,它具有空值,这意味着它不指向任何其他内存位置,因为它还没有值。值类型变量不能为<code>null</code>,因为它包含值而不是内存地址。所以必须在使用前为值类型变量分配值</p>
</blockquote>
<h2 id="可空类型">可空类型</h2>
<blockquote>
<p><code>C＃2.0</code>为值类型引入了可空类型,允许为值类型变量赋值<code>null</code>或声明值类型变量而不为其赋值</p>
</blockquote>
<pre><code class="language-cs">int? age = null;
</code></pre>
<h2 id="类型转换">类型转换</h2>
<blockquote>
<p><code>C#</code>是一门强类型语言,对类型要求比较严格,但是在一定的条件下是可以相互转换的,如将<code>int</code>型数据转换成<code>double</code>型数据。<code>C#</code>允许使用两种转换方式：隐式转换和显式转换</p>
</blockquote>
<h3 id="隐式转换">隐式转换</h3>
<blockquote>
<p>隐式转换是系统默认的,不需要加以声明就会自动执行隐式类型转换,在隐式转换过程,编译器无需对转换进行详细检查就能够安全的执行.隐式类型转换是从低精度数值类型=&gt;高精度数值类型</p>
</blockquote>
<pre><code class="language-cs">int a = 10;
double b = a;//自动隐式类型转换
</code></pre>
<h3 id="显式转换">显式转换</h3>
<blockquote>
<p>将高精度值=&gt;低精度进行数据转换时,可能会丢失数据,这时候需要使用显式转换,并且要考虑到可能出现算术溢出;显式转换需要明确指出指定要转换的类型</p>
</blockquote>
<p><strong>注意</strong>：显式转换可能导致错误,进行这种转换时编译器会对转换进行溢出检测,如果有溢出说明转换失败,表示源类型不是一个合法的目标类型无法进行类型转换<br>
强制类型转换会造成数据精度丢失</p>
<pre><code class="language-cs">double a = 10;
int b = (int)a;//显式将double类型转换为int
</code></pre>
<p>可空类型数据转换=&gt;非可空类型或者另一个可空类型,其中可能会丢失数据,就必须使用显式类型转换,并且如果从可空类型转换为非可空类型时.且变量值为<code>null</code>.就会抛出<code>InvalidOperationException</code>异常</p>
<pre><code class="language-cs">int? a = null;
int b = (int)a;
//System.InvalidOperationException:“可为空的对象必须具有一个值。
</code></pre>
<h3 id="通过方法进行转换">通过方法进行转换</h3>
<h4 id="ToString"><code>ToString()</code></h4>
<blockquote>
<p>C#中的类型基类都继承自<code>Object</code>类,所以都可以使用<code>ToString()</code>来转换成字符串</p>
</blockquote>
<pre><code class="language-cs">int a = 10;
string s = a.ToString();
</code></pre>
<h4 id="int-Parse方法"><code>int.Parse方法()</code></h4>
<blockquote>
<p>用于将<code>string</code>类型参数转换为<code>int</code>,需要注意:<code>string</code>类型参数不能为<code>null</code>,并且也只能是各种整型,不能是浮点型</p>
</blockquote>
<pre><code class="language-cs">string a = &quot;2&quot;;
string b = &quot;2.6&quot;;
string c = null;
int a1 = int.Parse(a);//正常
int a2 = int.Parse(b);//错误:输入字符串格式错误
int a3 = int.Parse(c);//值不能为null
</code></pre>
<h4 id="int-TryParse方法"><code>int.TryParse方法()</code></h4>
<blockquote>
<p>该方法与<code>int.Parse()</code>方法类似,不同点在于<code>int.Parse()</code>方法无法转换成功时会抛出异常.而<code>int.TryParse()</code>方法在无法进行转换时会返回<code>false</code>,<code>int.TryParse()</code>方法需要一个<code>out</code>类型的参数,如果转换成功,<code>out</code>参数的值就是正常转换的值,否则,返回<code>false</code></p>
</blockquote>
<pre><code class="language-cs">string a = &quot;2&quot;;
string b = &quot;2.6&quot;;
string c = null;
int i;
bool a1 = int.TryParse(a,out i);//转换成功,i=2
bool a2 = int.TryParse(b, out i);//转换失败,a2=false
bool a3 = int.TryParse(c, out i);//转换失败,a3=false
</code></pre>
<h3 id="通过Convert类进行转换">通过<code>Convert</code>类进行转换</h3>
<pre><code class="language-cs">string a = &quot;2&quot;;
int a1 = Convert.ToInt32(a);
</code></pre>
<p><code>Convert</code>类常用的类型转换方法</p>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Convert.ToInt32()</td>
<td style="text-align:left">转换为整型(int)</td>
</tr>
<tr>
<td style="text-align:left">Convert.ToChar()</td>
<td style="text-align:left">转换为字符型(char)</td>
</tr>
<tr>
<td style="text-align:left">Convert.ToString()</td>
<td style="text-align:left">转换为字符串型(string)</td>
</tr>
<tr>
<td style="text-align:left">Convert.ToDateTime()</td>
<td style="text-align:left">转换为日期型(datetime)</td>
</tr>
<tr>
<td style="text-align:left">Convert.ToDouble()</td>
<td style="text-align:left">转换为双精度浮点型(double)</td>
</tr>
<tr>
<td style="text-align:left">Conert.ToSingle()</td>
<td style="text-align:left">转换为单精度浮点型(float)</td>
</tr>
</tbody>
</table>
<h3 id="实现自定义转换">实现自定义转换</h3>
<blockquote>
<p>通过继承接口<code>IConventible</code>或者<code>TypeConventer</code>类,可以实现自定义转换</p>
</blockquote>
<h3 id="使用-as-运算符转换">使用 as 运算符转换</h3>
<blockquote>
<p>使用<code>as</code>操作符转换,但是<code>as</code>只能用于引用类型和可为空的类型。使用<code>as</code>有很多好处,当无法进行类型转换时,会将对象赋值为<code>NULL</code>,避免类型转换时报错或是出异常。<code>C#</code>抛出异常在进行捕获异常并进行处理是很消耗资源的,如果只是将对象赋值为<code>NULL</code>的话是几乎不消耗资源的(消耗很小的资源)</p>
</blockquote>
<pre><code class="language-cs">object o = &quot;abc&quot;;
string s = o as string; //执行第一次类型兼容性检查,并返回结果
if (s != null)
Console.WriteLine(&quot;转换成功！&quot;);
else
Console.WriteLine(&quot;转换失败！&quot;);
</code></pre>
<h2 id="装箱和拆箱">装箱和拆箱</h2>
<blockquote>
<p>装箱和拆箱在值类型和引用类型之间架起了一座桥梁,使得任何<code>value-type</code>的值都可以转换为<code>object</code> 类型的值,反过来转换也可以</p>
</blockquote>
<h3 id="装箱">装箱</h3>
<blockquote>
<p>装箱是指将一个值类型的数据隐式地转换成一个对象类型(<code>object</code>)的数据。执行装箱操作时不可避免的要在堆上申请内存空间,并将堆栈上的值类型数据复制到申请的堆内存空间上,这肯定是要消耗内存和 cpu 资源的。注意：在执行装箱转换时,也可以使用显式转换</p>
</blockquote>
<pre><code class="language-cs">int i = 0;
object obj = i;   //装箱：值类型转换为引用类型
</code></pre>
<h3 id="拆箱">拆箱</h3>
<blockquote>
<p>拆箱是指将一个对象类型的数据显式地转换成一个值类型数据。拆箱过程是装箱的逆过程,是将存储在堆上的引用类型值转换为值类型并赋给值类型变量。拆箱操作分为两步：一是检查对象实例,确保它是给定值类型的一个装箱值；而是将该值从实例复制到值类型变量中</p>
</blockquote>
<pre><code class="language-cs">int i = 0;
object obj = i;   //装箱：值类型转换为引用类型
int j = (int)obj;  //拆箱：引用类型转换为值类型
</code></pre>
<p><strong>装箱拆箱注意事项</strong></p>
<ul>
<li>装箱操作可以隐式进行,但拆箱操作必须显示</li>
<li>在装箱的时候,并不需要显示类型转换。但在拆箱时需要类型转换</li>
<li>这是因为在拆箱时对象可以被转换为任何类型</li>
<li>装什么拆什么</li>
<li>装箱就是要在托管堆重开辟空间,不但要装数值而且还要装类型</li>
<li>所以说装什么拆什么,也就是用什么值类型装箱,就要用什么值类型拆箱</li>
</ul>
<h2 id="常见运算符">常见运算符</h2>
<h3 id="三元运算符">三元运算符</h3>
<blockquote>
<p><code>if...else</code>的简化形式,首先判断一个条件,如果为真,返回第一个值.为假返回后一个值</p>
</blockquote>
<pre><code class="language-cs">int a = 3;
bool result = a &gt; 10 ? true : false;//a&gt;10?如果大于返回true否则返回false
</code></pre>
<h3 id="checked和unchecked"><code>checked</code>和<code>unchecked</code></h3>
<blockquote>
<p>如果把代码块标记为<code>checked</code>,<code>CLR</code>就会执行栈溢出检测,如果要禁止栈溢出,则可以把代码标记<code>unchecked</code></p>
</blockquote>
<pre><code class="language-cs">//byte类型最大取值255
byte a = 255;
checked
{
     a++;
}
//这里如果不加checed.++后输出0(不会抛异常,但会丢失数据,溢出的位会被舍弃,所以值为0),加上后会抛出栈溢出异常
Console.WriteLine(a);
</code></pre>
<h3 id="is"><code>is</code></h3>
<blockquote>
<p><code>is</code> 运算符可以检测对象是否与特定类型兼容,兼容表示对象是该类型或者派生自该类型,转换规则如下：</p>
</blockquote>
<ul>
<li>1.检查对象类型的兼容性,并返回结果 true(false)</li>
<li>2.不会抛出异常</li>
<li>3.如果对象为 null,刚返回 false</li>
</ul>
<pre><code class="language-cs">object o = &quot;abc&quot;;
if (o is string) //执行第一次类型兼容性检查
{
    string s = (string)o; //执行第二次类型兼容性检查,并转换
    Console.WriteLine(&quot;转换成功！&quot;);
}
else
{
    Console.WriteLine(&quot;转换失败！&quot;);
}
</code></pre>
<h3 id="as"><code>as</code></h3>
<p><code>as</code> 转换规则如下：</p>
<ul>
<li>1.检查对象类型的兼容性,并返回转换结果,如果不兼容则返回 null</li>
<li>2.不会抛出异常</li>
<li>3.如果结果判断为空,则强制执行类型转换将抛出 NullReferenceException 异常</li>
</ul>
<pre><code class="language-cs">object o = &quot;abc&quot;;
string s = o as string; //执行第一次类型兼容性检查,并返回结果
if (s != null)
    Console.WriteLine(&quot;转换成功！&quot;);
else
    Console.WriteLine(&quot;转换失败！&quot;);

</code></pre>
<h3 id="sizeof"><code>sizeof</code></h3>
<blockquote>
<p><code>sizeof</code> 运算符可以确定栈中值类型需要的长度(单位为字节)</p>
</blockquote>
<pre><code class="language-cs"> Console.WriteLine(sizeof(int));//4个字节
 Console.WriteLine(sizeof(byte));//1个字节
</code></pre>
<h3 id="typeof"><code>typeof</code></h3>
<blockquote>
<p>返回一个表示特定类型的<code>Systen.Type</code>对象</p>
</blockquote>
<pre><code class="language-cs">Console.WriteLine(typeof(int));//System.Int32
Console.WriteLine(typeof(byte));//System.Byte
</code></pre>
<h3 id="可空类型和运算符">可空类型和运算符</h3>
<blockquote>
<p>在 C# 2.0 中出现了可空类型,允许值类型也可以为空<code>null</code>,可空类型的实现基于 C#泛型。需要注意的是:在程序中使用可空类型就必须考虑<code>null</code>值在各种运算符一起使用的影响,通常可空类型与一元或二元运算符一起使用时,如果一个操作数为<code>null</code>或两个操作数为<code>null</code>.结果就是<code>null</code></p>
</blockquote>
<pre><code class="language-cs"> int? a = null;
 int? c = a + 4; //c=null
</code></pre>
<h3 id="空合并运算符">空合并运算符</h3>
<blockquote>
<p>空合并运算符<code>??</code>提供了快捷方式,处理可控类型和引用类型时表示<code>null</code>可能的值,需要注意:只能针对引用类型处理,规则是</p>
</blockquote>
<ul>
<li>如果第一个操作数不是<code>null</code>,值就等于第一个操作数的值</li>
<li>如果第一个操作数是<code>null</code>,值就等于第二个操作数的值</li>
</ul>
<pre><code class="language-cs">int? a = null;
int b;
b = a ?? 10;//第一个操作数是null,值为第二个操作数.10
a = 3;
b = a ?? 10;//第一个操作数不是null,值为第一个操作数.3
</code></pre>
<h2 id="关键字">关键字</h2>
<blockquote>
<p>C＃包含保留字,对编译器有特殊意义。这些保留字称为“关键字”。关键字不能用作变量,类,接口等的名称(标识符),关键字不能用作标识符(变量名,类,接口等)。但是,它们可以与前缀“@”一起使用。例如,class 是保留关键字,因此不能用作标识符,但可以使用@class。<a href="https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/index" target="_blank" rel="noopener"><strong>有关关键字的更多信息,访问 MSDN</strong></a></p>
</blockquote>
<h2 id="枚举">枚举</h2>
<blockquote>
<p>C＃中,<code>enum</code>是值类型数据类型。枚举用于声明命名整数常量的列表,可以直接在命名空间,类或结构中使用<code>enum</code>关键字定义</p>
</blockquote>
<ul>
<li>枚举用于为每个常量指定一个名称,以便可以使用其名称引用常量整数;默认情况下,枚举的第一个成员的值为 0,每个连续的枚举成员的值增加 1.</li>
<li>枚举可以包括数字数据类型的命名常量,例如<code>byte</code>,<code>sbyte</code>,<code>short</code>,<code>ushort</code>,<code>int</code>,<code>uint</code>,<code>long</code>或<code>ulong</code></li>
<li><strong>枚举不能与字符串类型一起使用</strong></li>
</ul>
<p><code>Enum</code>是一个抽象类,包含用于枚举的静态帮助器方法</p>
<table>
<thead>
<tr>
<th style="text-align:center">Enum method</th>
<th style="text-align:center">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">Format</td>
<td style="text-align:center">将指定的枚举类型值转换为指定的字符串格式</td>
</tr>
<tr>
<td style="text-align:center">GetName</td>
<td style="text-align:center">返回指定枚举的指定值的常量的名称</td>
</tr>
<tr>
<td style="text-align:center">GetNames</td>
<td style="text-align:center">返回指定枚举的所有常量的字符串名称数组</td>
</tr>
<tr>
<td style="text-align:center">GetValues</td>
<td style="text-align:center">返回指定枚举的所有常量值的数组</td>
</tr>
<tr>
<td style="text-align:center">object Parse(type, string)</td>
<td style="text-align:center">将一个或多个枚举常量的名称或数值的字符串表示形式转换为等效的枚举对象</td>
</tr>
<tr>
<td style="text-align:center">bool TryParse(string, out TEnum)</td>
<td style="text-align:center">将一个或多个枚举常量的名称或数值的字符串表示形式转换为等效的枚举对象，返回值表示转换是否成功</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">enum Color
{
    Red,
    Green,
    Blue
}
</code></pre>
<h2 id="预处理器指令">预处理器指令</h2>
<ul>
<li><code>#region/#endregion</code>指令用于把一段代码标记为有给定名称的一个块</li>
<li><code>#define/#undef</code> 结合 <code>#if/#elif/endif</code> 实现条件编译</li>
<li>…</li>
</ul>
<pre><code>#define debug
using System;

namespace CSharp.Study.Test
{
    class Program
    {
        static void Main(string[] args)
        {
#if debug
            Console.WriteLine(&quot;debug&quot;);
#else
          Console.WriteLine(&quot;other&quot;);
#endif
        }

    }
}
</code></pre>
<h1>类和对象</h1>
<p>类和对象将琐碎的C#知识点系统的组合到一起，构成一个完整的程序</p>
<h2 id="类和结构">类和结构</h2>
<p>类和结构实际上都是创建对象的模板,每个对象都包含数据,并提供了处理和访问数据的方法。类定义了类的每个对象(称为实例)可以包含什么数据和功能。还可以定义处理在这些字段中存储的数据的功能</p>
<h2 id="类">类</h2>
<p>类中的数据和函数称为类的成员,除了这些成员外,类还可以包含嵌套的类型(如其它类),成员的可访问性可以是<code>public</code>、<code>protected</code>、<code>internalprotected</code>、<code>private</code>、<code>internal</code></p>
<h3 id="数据成员">数据成员</h3>
<p>数据成员是包含类的数据—&gt;字段、常量和事件的成员,数据成员可以是静态数据,类成员总是实例成员,除非用<code>static</code>进行显式的声明</p>
<h3 id="函数成员">函数成员</h3>
<p>函数成员提供了操作类中数据的某些功能,包括方法、属性、构造函数和终结器、运算符以及索引器</p>
<ul>
<li>方法是与某个类相关的函数,与数据成员一样,函数成员默认为实例成员,使用<code>static</code>修饰符可以把方法定义为静态方法</li>
<li>属性是可以从客户端访问的函数组,其访问方式与访问类的公共字段类似,<code>C#</code>为读写类中的属性提供了专用语法,所以不必使用那些名称中嵌有<code>Get</code>或<code>set</code>的方法</li>
<li>构造函数是在实例化对象时自动调用的特殊函数,必须与所属的类同名,且不能有返回类型,构造函数用于初始化字段的值</li>
<li>终结器类似于构造函数,在<code>CLR</code>检测到不再需要某个对象时调用它,它们的名称与类相同,但前面有一个 <code>~</code> 符号</li>
<li>运算符执行的最简单的操作就是加法和减法。 在两个整数相加时,严格地说,就是对整数使用<code>+</code>运算符,C#允许指定把已有的运算符应用于自己的类(运算符重载)</li>
<li>索引器允许对象以数组或集合的方式进行索引</li>
</ul>
<h3 id="方法">方法</h3>
<h4 id="声明">声明</h4>
<p>在 C#中 ,方法的定义包括任意方法修饰符(如方法的可访问性)、返回值的类型,然后依次是方法名和输入参数的列表,和方法体。每个参数都包括参数的类型名和在方法体中的引用名称。 如果方法有返回值,<code>return</code>语句就必须与返回值一起使用</p>
<h4 id="语法">语法</h4>
<pre><code class="language-cs">[modifiers] return_type MethodName([parameters])
{
      // Method body
}
</code></pre>
<h4 id="实例">实例</h4>
<pre><code class="language-cs">class Test
{
    /// &lt;summary&gt;
    /// 实例方法
    /// &lt;/summary&gt;
    public void Show()
    {
        Console.WriteLine(&quot;Test下实例方法&quot;);
    }

    /// &lt;summary&gt;
    /// 静态方法属于类本身
    /// &lt;/summary&gt;
    public static void Show2()
    {
        Console.WriteLine(&quot;Test下静态方法&quot;);
    }
}
</code></pre>
<h4 id="方法调用">方法调用</h4>
<p>在实例化类得到类的对象后,即可通过对象<code>.</code>方法名称进行方法的调用,需要注意的是使用<code>static</code>修饰的方法属于类的本身,无法使用类的实例化对象进行调用,使用类名“.”方法名即可</p>
<h4 id="实例方法调用">实例方法调用</h4>
<pre><code class="language-cs">Test test = new Test();
test.Show();
</code></pre>
<h4 id="静态方法调用">静态方法调用</h4>
<pre><code class="language-cs">Test.Show2();
</code></pre>
<h4 id="表达式方法体">表达式方法体</h4>
<p>如果方法的实现只有一条语句,C#为方法定义提供了一个简化的语法：表达式体方法,使用<code>=&gt;</code>区分操作符左边的声明和操作符右边的实现代码</p>
<h4 id="命名参数">命名参数</h4>
<p>参数一般需要按定义的顺序传送给方法,命名参数允许按任意顺序传递</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            //下面两种调用方式结果是一样的
            Print(&quot;hello&quot;, &quot;world&quot;);
            Print(b: &quot;world&quot;, a: &quot;hello&quot;);
            Console.ReadKey();
        }
        static void Print(string a, string b)
        {
            Console.WriteLine(&quot;{0},{1}&quot;, a, b);
        }
    }
</code></pre>
<h4 id="可选参数">可选参数</h4>
<p>参数也可以是可选的,必须为可选参数提供默认值,可选参数还必须是方法定义的最后一个参数</p>
<pre><code class="language-cs">static void Print(string a, string b = &quot;world&quot;)
{
    Console.WriteLine(&quot;{0},{1}&quot;, a, b);
}
</code></pre>
<pre><code class="language-cs">Print(&quot;hello&quot;);//输出hello,world
Print(&quot;hello&quot;,&quot;wang&quot;);//输出hello,wang
</code></pre>
<h4 id="个数可变的参数">个数可变的参数</h4>
<p>使用<code>params[]</code>数组的方式可以定义数量可变的参数,如果<code>params[]</code>关键字与方法签名定义的多个参数一起使用,则<code>params[]</code>只能使用一次,而且必须是最后一个参数</p>
<h4 id="方法的重载">方法的重载</h4>
<p>方法名相同,但参数的个数或类型不同</p>
<pre><code class="language-cs">        static void Print(string a)
        {
            Console.WriteLine(&quot;{0}&quot;, a);
        }
        static void Print(string a, string b = &quot;world&quot;)
        {
            Console.WriteLine(&quot;{0},{1}&quot;, a, b);
        }
</code></pre>
<p>注意:</p>
<ul>
<li>两个方法不能仅在返回类型上有区别</li>
<li>两个方法不能仅根据参数是声明为<code>ref</code>还是<code>out</code>来区分</li>
</ul>
<h3 id="属性">属性</h3>
<p>属性<code>property</code>的概念是:它是一个方法或一对方法,在客户端代码看来,它(们)是一个字段</p>
<h4 id="属性定义">属性定义</h4>
<pre><code class="language-cs">public string SomeProperty
{
    get
    {
        return &quot;This is the property value&quot;;
    }
    set
    {
        //type string
    }
}
</code></pre>
<h4 id="只读和只写属性">只读和只写属性</h4>
<p>在属性定义中省略<code>set</code>访问器,就可以创建只读属性;同样,在属性定义中省略<code>get</code>访问器,就可以创建只写属性</p>
<pre><code class="language-cs">    class Program
    {
        private string age;
        private string name;
        //只读属性
        public string Age
        {        
            get
            {
                return age;
            }           
        }
        //只写属性
        public string Name
        {        
            set
            {
                value= name;
            }           
        }
    }
</code></pre>
<h4 id="属性的访问修饰符">属性的访问修饰符</h4>
<p>C#允许给属性的<code>get</code>和<code>set</code>访问器设置不同的访问修饰符,所以属性可以有公有的<code>get</code>访问器和私有或受保护的<code>set</code>访问器。这有助于控制属性的设置方式或时间</p>
<h4 id="自动实现的属性">自动实现的属性</h4>
<p>如果属性的<code>set</code>和<code>get</code>访问器中没有任何逻辑,就可以使用自动实现的属性,这种属性会自动实现后备成员变量,使用自动实现的属性,就不能在属性设置中验证属性的有效性</p>
<pre><code class="language-cs">    class Program
    {
        public string Age { get; set; }
        public string Name { get; set; }
        static void Main(string[] args)
        {
            Console.ReadKey();
        }
    }
</code></pre>
<h3 id="构造函数">构造函数</h3>
<p>声明基本构造函数的语法就是声明一个与包含的类同名的方法,但该方法没有返回类型</p>
<h4 id="构造函数声明">构造函数声明</h4>
<pre><code class="language-cs">    class Person
    {
        Person()
        {

        }
    }
</code></pre>
<p><strong>其实没有必要给类提供构造函数,原因在于如果没有在类中没有提供任何构造函数,编译器会在后台创建一个默认的无参构造函数用来把所有的成员字段初始化为标准的默认值</strong></p>
<h4 id="构造函数重载">构造函数重载</h4>
<p>构造函数重载遵循与其他方法相同的规则,就是说,允许为构造函数提供任意多的重载,但是需要注意：如果提供了带参数的构造函数,编译器就不会隐式的自动创建默认的构造函数</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            //调用无参构造函数实例化对象
            Person person1 = new Person();

            //调用带参数的构造函数实例化对象
            Person person2 = new Person(3);

            //因为带name参数的构造函数是private的,所以这里无法实例化
            //Person person3 = new Person(20, &quot;wang&quot;);
            Console.ReadKey();
        }
    }
    class Person
    {
        private int age;

        private string name;
        public Person()
        {

        }
        public Person(int age)
        {
            //使用this关键字区分成员字段和同名参数
            this.age = age;
        }

        private Person(int age, string name)
        {
            this.age = age;
            this.name = name;
        }
    }
</code></pre>
<pre><code class="language-cs">    class Person
    {
        private int age;

        private string name;       
        private Person(int age, string name)
        {
            this.age = age;
            this.name = name;
        }
    }
</code></pre>
<p>这个例子没有为<code>Person</code>类定义任何公有的或受保护的构造函数。这就使<code>Person</code>不能使用<code>new</code>运算符在外部代码中实例化(但可以在<code>Person</code>中编写一个公有静态属性或方法,以实例化该类)。 这在下面两种情况下是有用的:</p>
<ul>
<li>类仅用作某些静态成员或属性的容器,因此永远不会实例化它</li>
<li>希望类仅通过调用某个静态成员函数来实例化(单例模式）</li>
</ul>
<h4 id="构造函数初始化器">构造函数初始化器</h4>
<p>有时,在一个类中有几个构造函数,以容纳某些可选参数,这些构造函数包含一些共同的代码。需要做到从构造函数中调用其他构造函数就可以使用构造函数初始化器</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            Person person1 = new Person(20);

            Person person2 = new Person(20, &quot;li&quot;);

            Console.WriteLine(&quot;person1:age={0},name={1}&quot;, person1.age, person1.name);
            Console.WriteLine(&quot;person2:age={0},name={1}&quot;, person2.age, person2.name);

            Console.ReadKey();
        }
    }
    class Person
    {
        public int age;

        public string name;

        public Person(int age)
        {
            this.age = age;
            this.name = &quot;wang&quot;;
        }

        public Person(int age, string name)
        {
            this.age = age;
            this.name = name;
        }
    }
</code></pre>
<p>上面的例子是一个简单的构造函数重载,然后通过调用不同的构造函数实例化对象,<code>Person</code>类的两个构造函数初始化了相同的字段<code>age</code>,显然,最好把所有的代码放在一个地方<br>
C#中使用构造函数初始化器,可以实现此目的</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            Person person1 = new Person(20);

            Person person2 = new Person(20, &quot;li&quot;);

            Console.WriteLine(&quot;person1:age={0},name={1}&quot;, person1.age, person1.name);
            Console.WriteLine(&quot;person2:age={0},name={1}&quot;, person2.age, person2.name);

            Console.ReadKey();
        }
    }
    class Person
    {
        public int age;

        public string name;

        public Person(int age)
        {
            this.age = age;
            this.name = &quot;wang&quot;;
        }

        public Person(int age, string name) : this(age)
        {
            this.age = age;
            this.name = name;
        }
    }
</code></pre>
<p>这里<code>this</code>关键字仅调用参数最匹配的那个构造函数。 注意,构造函数初始化器在构造函数的函数体之前执行。C#构造函数初始化器可以包含对同一个类的另一个构造函数的调用(使用前面介绍的语法),也可以包含对直接基类的构造函数的调用(使用相同的语法,但应使用<code>base</code>关键字代替<code>this</code>。初始化器中不能有多个调用</p>
<h3 id="静态类">静态类</h3>
<p>如果类只包含静态的方法和属性,该类就是静态的。静态类在功能上与使用私有静态函数创建的类相同,不能创建静态类的实例</p>
<h3 id="静态构造函数">静态构造函数</h3>
<p>静态构造函数只执行一次,而前面的构造函数是实例构造函数,只要创建类的对象,就会被执行。编写静态构造函数的一个原因是,类有一些静态字段或属性,需要在第一次使用类之前,从外部源中初始化这些静态字段和属性</p>
<p>注意：<code>.Net</code>运行库没有确保什么时候执行静态构造函数,所以不应把要求在某个特定时刻(例如,加载程序集时)执行的代码放在静态构造函数中。也不能预计不同类的静态构造函数按照什么顺序执行。但是,可以确保静态构造函数至多运行一次,即在代码引用类之前调用它。在C#中,通常在第一次调用类的任何成员之前执行静态构造函数。静态构造函数没有访问修饰符,其他C#代码从来不调用它,但在加载类时,总是由.NET运行库调用它,所以像<code>public</code>或<code>private</code>这样的访问修饰符就没有任何意义。出于同样原因,静态构造函数不能带任何参数,一个类也只能有一个静态构造函数。很显然,静态构造函数只能访问类的静态成员,不能访问类的实例成员。</p>
<p>无参数的实例构造函数与静态构造函数可以在同一个类中同时定义。尽管参数列表相同,但这并不矛盾,因为在加载类时执行静态构造函数,而在创建实例时执行实例构造函数,所以何时执行哪个构造函数不会有冲突。如果多个类都有静态构造函数,先执行哪个静态构造函数就不确定。此时静态构造函数中的代码不应依赖于其他静态构造函数的执行情况。 另一方面,如果任何静态字段有默认值,就在调用静态构造函数之前指定它们。</p>
<h2 id="结构">结构</h2>
<h3 id="结构是值类型">结构是值类型</h3>
<p>结构是会影响性能的值类型,但根据使用结构的方式,这种影响可能是正面的,也可能是负面的。 正面的影响是为结构分配内存时,速度非常快,因为它们将内联或者保存在栈中。 在结构超出了作用域被删除时,速度也很快。负面影响是,只要把结构作为参数来传递或者把一个结构赋予另一个结构(如A-B,其 中A和 B是结构),结构的所有内容就被复制,而对于类,则只复制引用。 这样就会有性能损失,根据结构的大小,性能损失也不同。注意,结构主要用于小的数据结构。但当把结构作为参数传递给方法时,应把它作为 <code>ref</code>参数传递,以避免性能损失,此时只传递了结构在内存中的地址,这样传递速度就与在类中的传递速度工样快了。但如果这样做,就必须注意被调用的方法可以改变结构的值</p>
<h3 id="结构不支持继承">结构不支持继承</h3>
<p>结构(和C#中的其他类型一样)最终派生于类<code>system.Object</code>因此,结构也可以访问<code>system.Object</code>的方法。在结构中,甚至可以重写<code>system.Object</code>中的方法—如重写<code>Tostring()</code>方法。 结构的继承链是:每个结构派生自<code>system.ValueType</code>类 ,<code>system,ValueType</code>类又派生自<code>system.Object</code>。 <code>ValueType</code>并没有给<code>Object</code>添加任何新成员,但提供了一些更适合结构的实现方式。注意,不能为结构提供其他基类:每个结构都派生自<code>ValueType</code></p>
<h3 id="结构的构造函数">结构的构造函数</h3>
<p>为结构定义构造函数的方式与为类定义构造函数的方式相同,但不允许定义无参数的构造函数。.Net运行库禁止在C#结构内定义无参的构造函数</p>
<h2 id="类和结构的区别">类和结构的区别</h2>
<p>结构与类的区别是它们在内存中的存储方式、访问方式(类是存储在堆上的引用类型,而结构是存储在栈上的值类型)和它们的一些特征(如结构不支持继承。较小的数据类型使用结构可提高性能。但在语法上,结构与类非常相似,主要的区别是使用关键字<code>struct</code>代替<code>class</code>来声明结构。对于类和结构,都使用关键字<code>new</code>来声明实例:这个关键字创建对象并对其进行初始化</p>
<h2 id="匿名类型">匿名类型</h2>
<p>匿名类型只是一个继承自<code>Object</code>且没有名称的类。该类的定义从初始化器中推断,类似于隐式类型化的变量</p>
<pre><code class="language-cs"> var person = new { Name = &quot;wang&quot;, Age = 22 };
</code></pre>
<h2 id="运算符重载">运算符重载</h2>
<p>重载运算符是具有特殊名称的函数,是通过关键字<code>operator</code>后跟运算符的符号来定义的。与其他函数一样,重载运算符有返回类型和参数列表</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            Person p1 = new Person { Age = 1, Name = &quot;wang&quot; };
            Person p2 = new Person { Age = 2, Name = &quot;li&quot; };
            Person p3 = p1 + p2;
            Console.WriteLine(p3.Age);
            Console.WriteLine(p3.Name);
            Console.ReadKey();
        }
    }
    class Person
    {
        public int Age { get; set; }
        public string Name { get; set; }
        /// &lt;summary&gt;
        /// 重载“+”号运算符,实现两个Person对象的相加
        /// &lt;/summary&gt;
        /// &lt;param name=&quot;a&quot;&gt;&lt;/param&gt;
        /// &lt;param name=&quot;b&quot;&gt;&lt;/param&gt;
        /// &lt;returns&gt;&lt;/returns&gt;
        public static Person operator +(Person a, Person b)
        {
            Person person = new Person();
            person.Age = a.Age + b.Age;
            person.Name = a.Name + b.Name;
            return person;
        }
    }
</code></pre>
<p>输出：<br>
3<br>
wangli</p>
<p>上面只是简单的举个例子,需要注意的是：1:不是所有的运算符都支持重载,2：比较运算符需要成对的重载,如果重载了<code>==</code>,则也必须重载<code>!=</code>,否则产生编译错误。</p>
<h2 id="扩展方法">扩展方法</h2>
<p>有许多扩展类的方式。 如果有类的源代码,继承是给对象添加功能的好方法。但如果没有源代码,该怎么办?此时可以使用扩展方法,它允许改变一个类,但不需要该类的源代码。扩展方法是静态方法,它是类的一部分,但实际上没有放在类的源代码中</p>
<pre><code class="language-cs">   class Program
    {
        static void Main(string[] args)
        {
            Person person = new Person();
            //调用Person类的扩展方法
            person.UpdatePerson(&quot;wang&quot;, 3);
            Console.WriteLine(person.Name);
            Console.WriteLine(person.Age);
            Console.ReadKey();
        }
    }
    static class Extension
    {
        public static void UpdatePerson(this Person person, string name, int age)
        {
            person.Name = name + &quot;,Hello&quot;;
            person.Age += age;
        }
    }
    class Person
    {
        public int Age { get; set; }
        public string Name { get; set; }

        /// &lt;summary&gt;
        /// 重载“+”号运算符,实现两个Person对象的相加
        /// &lt;/summary&gt;
        /// &lt;param name=&quot;a&quot;&gt;&lt;/param&gt;
        /// &lt;param name=&quot;b&quot;&gt;&lt;/param&gt;
        /// &lt;returns&gt;&lt;/returns&gt;
        public static Person operator +(Person a, Person b)
        {
            Person person = new Person();
            person.Age = a.Age + b.Age;
            person.Name = a.Name + b.Name;
            return person;
        }
    }
</code></pre>
<p><strong>注意</strong>：扩展方法是静态方法;如果扩展方法与类中的某个方法同名,就从来不会调用扩展方法。类中已有的任何实例方法优先</p>
<h2 id="Object类">Object类</h2>
<p>之前说到,所有的<code>.Net</code>类都派生自<code>System.Object</code>,如果在定义时没有指定基类编译器就会假定该类派生自<code>Object</code>,实际意义在于除了使用自己定义的方法和属性外,还可以访问<code>Object</code>类定义的许多公有的受保护的成员方法,比如<code>ToString()</code>/<code>GetHashCode()</code>/<code>Equals()</code>/<code>ReferenceEquals()</code>/<code>Finalize()</code>等</p>
<h2 id="给方法传递参数详解">给方法传递参数详解</h2>
<h3 id="值类型和引用类型">值类型和引用类型</h3>
<p>C#中的类型一共分为两类,一类是值类型<code>Value Type</code>,一类是引用类型(<code>Reference Type</code>),值类型包括结构体<code>struct</code>和枚举<code>enum</code>,引用类型包括类<code>class</code>、接口<code>interface</code>、委托<code>delegate</code>、数组<code>array</code>等</p>
<p>常见的简单类型如<code>short</code>、<code>int</code>、<code>long</code>、<code>float</code>、<code>double</code>、<code>byte</code>、<code>char</code>等其本质上都是结构体,对应<code>struct</code>、<code>System.Int16</code>、<code>System.Int32</code>、<code>System.Int64</code>、<code>System.Single</code>、<code>System.Double</code>、<code>Syetem.Byte</code>、<code>System.Char</code>,因此它们都是值类型。但<code>string</code>和<code>object</code>例外,它们本质上是类,对应class<code>System.String</code>和<code>System.Object</code>,所以它们是引用类型</p>
<h4 id="值类型-2">值类型</h4>
<p>值类型变量本身保存了该类型的全部数据,当声明一个值类型的变量时,该变量会被分配到栈<code>Stack</code>上</p>
<h4 id="引用类型-2">引用类型</h4>
<p>引用类型变量本身保存的是位于堆<code>Heap</code>上的该类型的实例的内存地址,并不包含数据。当声明一个引用类型变量时,该变量会被分配到栈上。如果仅仅只是声明这样一个变量,由于在堆上还没有创建该类型的实例,因此,变量值为<code>null</code>,意思是不指向任何类型实例(堆上的对象)。对于变量的类型声明,用于限制此变量可以保存的类型</p>
<h3 id="值传递和引用传递">值传递和引用传递</h3>
<p>C#函数的参数如果不加<code>ref</code>,<code>out</code>这样的修饰符显式声明参数是通过引用传递外,默认都是值传递</p>
<p><strong>注意</strong><br>
<code>参数的类型是值类型还是引用类型</code>和<code>传参数时用值传递还是引用传递</code>是两个不同的概念</p>
<h4 id="值传递参数">值传递参数</h4>
<p>此时传递是原变量的拷贝,值传递参数和原变量的内存地址不同,因此方法中对值传递参数的修改不会改变原变量</p>
<h4 id="引用传递参数">引用传递参数</h4>
<p>引用传递参数是原变量的指针,引用传递参数和原变量的内存地址相同,相应的方法中对引用传递参数的修改会改变原变量</p>
<h4 id="ref参数">ref参数</h4>
<p>如前所述,通过值传送变量是默认的,也可以使值参数通过引用传送给方法。为此,要使用<code>ref</code>关键字如果把一个参数传递给方法,且这个方法的输入参数前带有<code>ref</code>关键字,则该方法对变量所做的任何改变都会影响原始对象的值</p>
<h4 id="out-参数">out 参数</h4>
<p>在方法的输入参数前面加上<code>out</code>前缀时,传递给该方法的变量可以不初始化。该变量通过引用传递,所以在从被调用的方法中返回时,对应方法对该变量进行的任何改变都会保留下来。输出参数函数必须在函数内部进行初始化赋值</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            string value;//并未对变量value进行初始化
            Print(out value);
            Console.WriteLine(&quot;value:{0}&quot;, value);

            Console.ReadKey();
        }

        static string Print(out string value)
        {
            return value = &quot;b&quot;;
        }
    }
</code></pre>
<h3 id="ref和out参数的区别"><code>ref</code>和<code>out</code>参数的区别</h3>
<p>首先：两者都是按地址传递的,使用后都将改变原来参数的数值<br>
其次：<code>ref</code>可以把参数的数值传递进函数,但是<code>out</code>是要把参数清空,就是说你无法把一个数值从<code>out</code>传递进去的,<code>out</code>进去后,参数的数值为空,所以你必须初始化一次。这个就是两个的区别,或者说就像有的网友说的,<code>ref</code>是有进有出,<code>out</code>是只出不进</p>
<p>参考</p>
<p><a href="https://blog.csdn.net/lsl277879661/article/details/56481650" target="_blank" rel="noopener">https://blog.csdn.net/lsl277879661/article/details/56481650</a><br>
<a href="https://blog.csdn.net/weiwenhp/article/details/7644790" target="_blank" rel="noopener">https://blog.csdn.net/weiwenhp/article/details/7644790</a><br>
<a href="https://www.csdndoc.com/article/461002" target="_blank" rel="noopener">https://www.csdndoc.com/article/461002</a></p>
<p>如果需要使用同一类型的多个对象，就可使用数组，数组大小是固定的，如果元素个数是动态的，就应使用集合</p>
<h1>数组和集合</h1>
<h2 id="数组">数组</h2>
<p>已知道变量只能包含一个文字值<code>int x = 1;</code>只能为变量<code>x</code>分配一个值。假设要存储100个不同的值，那么创建100个不同的变量将非常麻烦。为了解决这个问题，C#引入了一个数组。数组是一种特殊类型的数据类型，它可以使用特殊语法顺序存储固定数量的值。下图显示了数组如何按顺序存储值</p>
<p><img src="http://www.tutorialsteacher.com/Content/images/csharp/array.png" alt=""></p>
<p><strong>数组声明</strong></p>
<pre><code class="language-cs">int [] intArr;
</code></pre>
<p><strong>数组初始化</strong>:可以使用<code>new</code>关键字同时声明和初始化数组，下面三种方式等价</p>
<pre><code class="language-cs">int[] intArr = new int[5]; 

int[] intArr = new int[5]{1, 2, 3, 4, 5};

int[] intArr = {1, 2, 3, 4, 5};
</code></pre>
<p><strong>延迟初始化</strong>:可以先声明后再初始化数组</p>
<pre><code class="language-cs">string[] strArr, strArr;

strArr = new string[5]{ &quot;1st Element&quot;,&quot;2nd Element&quot;, &quot;3rd Element&quot;};

strArr = new string[]{ &quot;1st Element&quot;,&quot;2nd Element&quot;, &quot;3rd Element&quot;};
</code></pre>
<p>通过索引访问数组中的元素</p>
<pre><code class="language-cs">intArr[索引位置];
</code></pre>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th>方法名称</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>GetLength(int维度)</td>
<td>返回指定维度中的元素数。</td>
</tr>
<tr>
<td>GetLowerBound(int维度)</td>
<td>返回指定维度的最低索引。</td>
</tr>
<tr>
<td>GetUpperBound(int维度)</td>
<td>返回指定维度的最高索引。</td>
</tr>
<tr>
<td>GetValue(int index)</td>
<td>返回指定索引处的值。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Length</td>
<td>返回数组中元素的总数。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">//数组可以先声明后赋值，也可以声明同时赋值,下面的方式是等价的,数组中必须存储同一类型数据，这在数组被定义时就已经确定
//第一种
int[] intArr = new int[5];
intArr[0] = 1;
intArr[1] = 2;
intArr[2] = 3;
intArr[3] = 4;
intArr[4] = 5;
//第二种
//int[] intArr = new int[5] { 1, 2, 3, 4, 5 };
//第三种
//int[] intArr = { 1, 2, 3, 4, 5 };
//注意这里是维度不是索引
Console.WriteLine(&quot;GetLength(int维度):返回指定维度中的元素数,值为：{0}&quot;, intArr.GetLength(0));
Console.WriteLine(&quot;GetLowerBound(int维度):返回指定维度的最低索引,值为：{0}&quot;, intArr.GetLowerBound(0));
Console.WriteLine(&quot;GetUpperBound(int维度):返回指定维度的最高索引,值为：{0}&quot;, intArr.GetUpperBound(0));
Console.WriteLine(&quot;GetValue(int index):	返回指定索引处的值,值为：{0}&quot;, intArr.GetValue(2));
Console.WriteLine(&quot;属性:Length:返回数组中元素的总数,值为：{0}&quot;, intArr.Length);

//使用索引来访问数组元素
Console.WriteLine(&quot;使用索引访问数组元素,索引为2的值为：{0}&quot;, intArr[2]);
//试图访问数组中不存在的索引元素，会发生数组越界
Console.WriteLine(&quot;使用索引访问数组元素,索引为10的值为：{0}&quot;, intArr[10]);
for (int i = 0; i &lt; intArr.Length; i++)
{
    Console.WriteLine(intArr[i]);
}
foreach (var item in intArr)
{
    Console.WriteLine(item);
}
</code></pre>
<h3 id="多维数组">多维数组</h3>
<p>多维数组,多维数组是像行和列的二维系列,多维数组又称为矩形数组;在本质上，是一个一维数组的列表</p>
<pre><code class="language-cs">//下面的两种创建方式等价
//第一种
//int[,] intArray = { { 1, 1 }, { 1, 2 }, { 1, 3 } };

//第二种
int[,] intArray = new int[3, 4]{    //初始化化一个三行四列的数组
                   {0, 1, 2, 3} ,   /*  初始化索引号为 0 的行 */
                   {4, 5, 6, 7} ,   /*  初始化索引号为 1 的行 */
                   {8, 9, 10, 11}   /*  初始化索引号为 2 的行 */
                };
//下面的语句将获取数组中第3行第4个元素                                
Console.WriteLine(&quot;二维数组中的元素是通过使用下标（即数组的行索引和列索引）来访问,值为：{0}&quot;, intArray[2, 3]);
Console.WriteLine(&quot;属性:Length:返回数组中元素的总数,值为：{0}&quot;, intArray.Length);

</code></pre>
<p>可以使用两个索引访问多维数组的值。第一个索引用于行，第二个索引用于列。两个索引都从零开始。</p>
<h3 id="锯齿状数组">锯齿状数组</h3>
<p>锯齿状数组是数组的数组。<code>Jagged</code>数组直接存储数组而不是任何其他数据类型值。锯齿状数组用两个方括号[][]初始化。第一个括号指定数组的大小，第二个括号指定将作为值存储的数组的维度。(锯齿状数组总是存储一个数组),二维数组的大小是矩形的，例如3×3个元素。而锯齿数组的大小设置是比较灵活的，在锯齿数组中，每一行都可以有不同的大小</p>
<pre><code class="language-cs">int[][] intJaggedArray = new int[2][];

intJaggedArray[0] = new int[3]{1,2,3};

intJaggedArray[1] = new int[2]{4,5};

Console.WriteLine(intJaggedArray[0][0]); // 1

Console.WriteLine(intJaggedArray[0][2]); // 3
    
Console.WriteLine(intJaggedArray[1][1]); // 5
</code></pre>
<h2 id="Array">Array</h2>
<p>.NET提供了一个抽象类<code>Array</code>，作为所有数组的基类。它提供了用于创建，操作，搜索和排序数组的静态方法</p>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th style="text-align:left">属性</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">IsFixedSize</td>
<td style="text-align:left">获取一个值，该值指示数组是否带有固定大小</td>
</tr>
<tr>
<td style="text-align:left">IsReadOnly</td>
<td style="text-align:left">获取一个值，该值指示数组是否只读</td>
</tr>
<tr>
<td style="text-align:left">Length</td>
<td style="text-align:left">获取一个 32 位整数，该值表示所有维度的数组中的元素总数</td>
</tr>
<tr>
<td style="text-align:left">LongLength</td>
<td style="text-align:left">获取一个 64 位整数，该值表示所有维度的数组中的元素总数</td>
</tr>
<tr>
<td style="text-align:left">Rank</td>
<td style="text-align:left">获取数组的秩（维度）</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Clear</td>
<td style="text-align:left">根据元素的类型，设置数组中某个范围的元素为零、为 false 或者为 null。</td>
</tr>
<tr>
<td style="text-align:left">Copy(Array, Array, Int32)</td>
<td style="text-align:left">从数组的第一个元素开始复制某个范围的元素到另一个数组的第一个元素位置。长度由一个 32 位整数指定。</td>
</tr>
<tr>
<td style="text-align:left">CopyTo(Array, Int32)</td>
<td style="text-align:left">从当前的一维数组中复制所有的元素到一个指定的一维数组的指定索引位置。索引由一个 32 位整数指定。</td>
</tr>
<tr>
<td style="text-align:left">GetLength</td>
<td style="text-align:left">获取一个 32 位整数，该值表示指定维度的数组中的元素总数。</td>
</tr>
<tr>
<td style="text-align:left">GetLongLength</td>
<td style="text-align:left">获取一个 64 位整数，该值表示指定维度的数组中的元素总数。</td>
</tr>
<tr>
<td style="text-align:left">GetLowerBound</td>
<td style="text-align:left">获取数组中指定维度的下界。</td>
</tr>
<tr>
<td style="text-align:left">GetType</td>
<td style="text-align:left">获取当前实例的类型。从对象（Object）继承。</td>
</tr>
<tr>
<td style="text-align:left">GetUpperBound</td>
<td style="text-align:left">获取数组中指定维度的上界。</td>
</tr>
<tr>
<td style="text-align:left">GetValue(Int32)</td>
<td style="text-align:left">获取一维数组中指定位置的值。索引由一个 32 位整数指定。</td>
</tr>
<tr>
<td style="text-align:left">IndexOf(Array, Object)</td>
<td style="text-align:left">搜索指定的对象，返回整个一维数组中第一次出现的索引。</td>
</tr>
<tr>
<td style="text-align:left">Reverse(Array)</td>
<td style="text-align:left">逆转整个一维数组中元素的顺序。</td>
</tr>
<tr>
<td style="text-align:left">SetValue(Object, Int32)</td>
<td style="text-align:left">给一维数组中指定位置的元素设置值。索引由一个 32 位整数指定。</td>
</tr>
<tr>
<td style="text-align:left">Sort(Array)</td>
<td style="text-align:left">使用数组的每个元素的 IComparable 实现来排序整个一维数组中的元素。</td>
</tr>
<tr>
<td style="text-align:left">ToString</td>
<td style="text-align:left">返回一个表示当前对象的字符串。从对象（Object）继承。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">Array arr = Array.CreateInstance(Type.GetType(&quot;System.Int32&quot;), 3);           
arr.SetValue(1, 0);
arr.SetValue(3, 1);
Console.WriteLine(&quot;IsFixedSize:取一个值，该值指示数组是否带有固定大小,值为:{0}&quot;, arr.IsFixedSize);
Console.WriteLine(&quot;IsReadOnly:取一个值，该值指示数组是否只读,值为:{0}&quot;, arr.IsReadOnly);
Console.WriteLine(&quot;Length:获取一个 32 位整数，该值表示所有维度的数组中的元素总数,值为:{0}&quot;, arr.Length);
Console.WriteLine(&quot;LongLength:获取一个 64 位整数，该值表示所有维度的数组中的元素总数,值为:{0}&quot;, arr.LongLength);
Console.WriteLine(&quot;Rank:获取数组的秩（维度）,值为:{0}&quot;, arr.Rank);
foreach (var item in arr)
{
    Console.WriteLine(item);
}
</code></pre>
<h2 id="集合Collections">集合<code>Collections</code></h2>
<p>C＃包括在特定系列中包含许多值或对象的专用类，称为“集合”。C＃中有两种类型的集合：<code>非泛型集合</code>和<code>泛型集合</code>。每个集合类都实现<code>IEnumerable</code>接口，因此可以使用<code>foreach</code>循环访问集合中的值</p>
<h3 id="非泛型集合">非泛型集合</h3>
<p><code>System.Collections</code>中命名空间包括以下非泛型集合</p>
<table>
<thead>
<tr>
<th style="text-align:center">类型</th>
<th style="text-align:center">用法</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">ArrayList</td>
<td style="text-align:center">ArrayList存储任何类型的对象，如数组。但是，当数组自动增长时，无需像数组那样指定ArrayList的大小。</td>
</tr>
<tr>
<td style="text-align:center">SortedList</td>
<td style="text-align:center">SortedList存储键和值对。它默认按键的升序自动排列元素。C＃包括泛型和非泛型SortedList集合。</td>
</tr>
<tr>
<td style="text-align:center">Stack</td>
<td style="text-align:center">Stack以LIFO样式存储值(后进先出)。它提供了一个Push()方法来添加一个值，Pop()和Peek()方法来检索值。C＃包括通用和非通用堆栈。</td>
</tr>
<tr>
<td style="text-align:center">Queue</td>
<td style="text-align:center">队列以FIFO样式(先进先出)存储值。它保持添加值的顺序。它提供了一个Enqueue()方法来添加值，还提供了一个Dequeue()方法来从集合中检索值。C＃包括通用和非通用队列。</td>
</tr>
<tr>
<td style="text-align:center">Hashtable</td>
<td style="text-align:center">Hashtable存储键和值对。它通过比较键的哈希值来检索值。</td>
</tr>
<tr>
<td style="text-align:center">BitArray</td>
<td style="text-align:center">BitArray管理一个紧凑的位值数组，表示为布尔值，其中true表示该位为on(1)，false表示该位为off(0)。</td>
</tr>
</tbody>
</table>
<h4 id="ArrayList"><code>ArrayList</code></h4>
<p><code>ArrayList</code>是C＃中的非泛型类型的集合。它可以包含任何数据类型的元素。它类似于数组，除了它在您添加项目时自动增长。与数组不同，不需要指定<code>ArrayList</code>的大小</p>
<p>要点：</p>
<ul>
<li><code>ArrayList</code>可以存储任何数据类型的项(元素)</li>
<li>添加元素时，<code>ArrayList</code>会自动调整大小</li>
<li><code>ArrayList</code>可以包含多个<code>null</code></li>
<li>可以使用<code>foreach</code>或<code>for</code>循环或索引器访问<code>ArrayList</code></li>
</ul>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Capacity</td>
<td>获取或设置ArrayList可以包含的元素数。</td>
</tr>
<tr>
<td>Count</td>
<td>获取ArrayList中实际包含的元素数。</td>
</tr>
<tr>
<td>IsFixedSize</td>
<td>获取一个值，该值指示ArrayList是否具有固定大小。</td>
</tr>
<tr>
<td>IsReadOnly</td>
<td>获取一个值，该值指示ArrayList是否为只读。</td>
</tr>
<tr>
<td>Item</td>
<td>获取或设置指定索引处的元素。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Add()/AddRange()</td>
<td>Add()方法在ArrayList的末尾添加单个元素。 AddRange()方法将指定集合中的所有元素添加到ArrayList中</td>
</tr>
<tr>
<td>Insert()/InsertRange()</td>
<td>Insert()方法在ArrayList中的指定索引处插入单个元素。 InsertRange()方法从ArrayList中的指定索引开始插入指定collection的所有元素。</td>
</tr>
<tr>
<td>Remove()/RemoveRange()</td>
<td>Remove()方法从ArrayList中删除指定的元素。 RemoveRange()方法从ArrayList中删除一系列元素。</td>
</tr>
<tr>
<td>RemoveAt()</td>
<td>从ArrayList中删除指定索引处的元素。</td>
</tr>
<tr>
<td>Sort()</td>
<td>对ArrayList的整个元素进行排序。</td>
</tr>
<tr>
<td>Reverse()</td>
<td>反转整个ArrayList中元素的顺序。</td>
</tr>
<tr>
<td>Contains</td>
<td>检查ArrayList中是否存在指定的元素。如果存在则返回true，否则返回false。</td>
</tr>
<tr>
<td>Clear</td>
<td>删除ArrayList中的所有元素。</td>
</tr>
<tr>
<td>CopyTo</td>
<td>将所有元素或元素范围复制到compitible Array。</td>
</tr>
<tr>
<td>GetRange</td>
<td>从ArrayList返回指定索引中指定数量的元素。</td>
</tr>
<tr>
<td>IndexOf</td>
<td>搜索指定的元素并返回零基索引(如果找到)。如果找不到元素，则返回-1。</td>
</tr>
<tr>
<td>ToArray</td>
<td>从ArrayList返回compitible数组。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">ArrayList arrayList = new ArrayList();
arrayList.Add(&quot;wang&quot;);
arrayList.Add(1);
//ArrayList允许插入null
arrayList.Add(null);

foreach (var item in arrayList)
{
    Console.WriteLine(item);
}

</code></pre>
<h4 id="SortedList">SortedList</h4>
<p><code>SortedList</code>集合默认按键的升序存储键值对。<code>SortedList</code>类实现<code>IDictionary</code>和<code>ICollection</code>接口，因此可以通过键和索引访问元素。C＃包括两种类型的<code>SortedList</code>，泛型<code>SortedList</code>和非泛型<code>SortedList</code>。先了解非泛型<code>SortedList</code>。</p>
<p>要点：</p>
<ul>
<li>C#具有泛型和非泛型<code>SortedList</code></li>
<li><code>SortedList</code>按键的升序存储键值对。键必须是唯一的，不能为<code>null</code>，而值可以为<code>null</code>或重复</li>
<li>非泛型<code>SortedList</code>存储任何数据类型的键和值。因此，需要将值转换为适当的数据类型</li>
<li>键值对可以强制转换为<code>DictionaryEntry</code></li>
<li>使用索引器访问单个值。<code>SortedList</code>索引器接受键以返回与之关联的值</li>
</ul>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Capacity</td>
<td>获取或设置SortedList实例可以存储的元素数。</td>
</tr>
<tr>
<td>Count</td>
<td>获取SortedList中实际包含的元素数。</td>
</tr>
<tr>
<td>IsFixedSize</td>
<td>获取SortedList中实际包含的元素数。</td>
</tr>
<tr>
<td>IsReadOnly</td>
<td>获取一个值，该值指示SortedList是否为只读。</td>
</tr>
<tr>
<td>Item</td>
<td>获取或设置SortedList中指定键的元素。</td>
</tr>
<tr>
<td>Keys</td>
<td>获取SortedList的键列表。</td>
</tr>
<tr>
<td>Values</td>
<td>获取SortedList中的值列表。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Add(object key, object value)</td>
<td>将键值对添加到SortedList中</td>
</tr>
<tr>
<td>Remove(object key)</td>
<td>删除具有指定键的元素</td>
</tr>
<tr>
<td>RemoveAt(int index)</td>
<td>删除指定索引处的元素</td>
</tr>
<tr>
<td>Contains(object key)</td>
<td>检查SortedList中是否存在指定的键</td>
</tr>
<tr>
<td>Clear()</td>
<td>从SortedList中删除所有元素</td>
</tr>
<tr>
<td>GetByIndex(int index)</td>
<td>返回存储在内部数组中的索引值</td>
</tr>
<tr>
<td>GetKey(int index)</td>
<td>检返回存储在内部数组中指定索引处的键</td>
</tr>
<tr>
<td>IndexOfKey(object key)</td>
<td>返回存储在内部数组中的指定键的索引</td>
</tr>
<tr>
<td>IndexOfValue(object value)</td>
<td>返回存储在内部数组中的指定值的索引</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">SortedList sortedList = new SortedList();
sortedList.Add(2, &quot;wang&quot;);
sortedList.Add(5, &quot;li&quot;);
sortedList.Add(3, 5);
//SortedList键可以是任何数据类型，但不能在同一SortedList中添加不同数据类型的键。
sortedList.Add(&quot;wang&quot;, 32);

for (int i = 0; i &lt; sortedList.Count; i++)
{
    Console.WriteLine(&quot;key:{0},value:{1}&quot;, sortedList.GetKey(i), sortedList.GetByIndex(i));
}

foreach (DictionaryEntry item in sortedList)
{
    Console.WriteLine(&quot;key:{0},value:{1}&quot;, item.Key, item.Value);
}
</code></pre>
<h4 id="Stack"><code>Stack</code></h4>
<p>C＃包含一种特殊类型的集合，它以<code>LIFO</code>样式存储元素(后进先出)。C#包括通用和非通用堆栈,非泛型堆栈。<code>Stack</code>允许空值以及重复值。它提供了一个<code>Push()</code>方法来添加一个值，<code>Pop()</code>或<code>Peek()</code>方法来检索值。</p>
<p>要点：</p>
<ul>
<li><code>Stack</code>将值存储在<code>LIFO(后进先出)</code>样式中。最后添加的元素将是首先出现的元素。</li>
<li>使用<code>Push()</code>方法将元素添加到Stack中。</li>
<li><code>Pop()</code>方法返回并从堆栈顶部删除元素。在空<code>Stack</code>上调用<code>Pop()</code>方法将引发异常。</li>
<li><code>Peek()</code>方法总是返回<code>Stack</code>中最顶层的元素。</li>
</ul>
<p><img src="http://www.tutorialsteacher.com/Content/images/csharp/stack.png" alt=""></p>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Count</td>
<td>返回Stack中元素的总数。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Push</td>
<td>在堆栈顶部插入一个项目。</td>
</tr>
<tr>
<td>Peek</td>
<td>返回堆栈中的顶部项。</td>
</tr>
<tr>
<td>Pop</td>
<td>从堆栈顶部删除并返回项目。</td>
</tr>
<tr>
<td>Contains</td>
<td>检查堆栈中是否存在项目。</td>
</tr>
<tr>
<td>Clear</td>
<td>从堆栈中删除所有项目。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">Stack stack = new Stack();
stack.Push(&quot;1&quot;);
stack.Push(1);
stack.Push(false);

foreach (var item in stack)
{
    Console.WriteLine(item);
}
</code></pre>
<h4 id="Queue"><code>Queue</code></h4>
<p>C＃包含<code>System.Collection</code>命名空间中的<code>Queue</code>集合类。队列以<code>FIFO样式(先进先出)</code>存储元素，与<code>Stack</code>集合完全相反。它按照添加顺序包含元素。队列集合允许多个空值和重复值。使用<code>Enqueue()</code>方法添加值，使用<code>Dequeue()</code>方法从队列中检索值。</p>
<p>要点：</p>
<ul>
<li>队列将值存储在<code>FIFO(先进先出)</code>样式中。首先添加的元素将首先出现。</li>
<li>使用<code>Enqueue()</code>方法将元素添加到Queue中</li>
<li><code>Dequeue()</code>方法返回并从队列的开头删除元素。在空队列上调用<code>Dequeue()</code>方法将引发异常。</li>
<li><code>Peek()</code>方法总是返回最顶层的元素。</li>
</ul>
<p><img src="http://www.tutorialsteacher.com/Content/images/csharp/csharp-queue.png" alt=""></p>
<p><strong>常用属性和方法</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Count</td>
<td>返回Stack中元素的总数。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Enqueue</td>
<td>将项添加到队列中。</td>
</tr>
<tr>
<td>Dequeue</td>
<td>从队列的开头删除并返回一个项目。</td>
</tr>
<tr>
<td>Peek</td>
<td>返回队列中的第一个项目</td>
</tr>
<tr>
<td>Contains</td>
<td>检查项目是否在队列中</td>
</tr>
<tr>
<td>Clear</td>
<td>从队列中删除所有项目。</td>
</tr>
<tr>
<td>TrimToSize</td>
<td>将队列的容量设置为队列中的实际项目数。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">Queue queue = new Queue();
queue.Enqueue(&quot;1&quot;);
queue.Enqueue(1);
queue.Enqueue(false);

foreach (var item in queue)
{
    Console.WriteLine(item);
}

</code></pre>
<h4 id="Hashtable"><code>Hashtable</code></h4>
<p>C＃包含<code>System.Collections</code>命名空间中的<code>Hashtable</code>集合，类似于通用字典集合。<code>Hashtable</code>集合存储键值对。它通过计算每个密钥的哈希码来优化查找，并在内部将其存储在不同的存储桶中，然后在访问值时匹配指定密钥的哈希码。</p>
<p>要点：</p>
<ul>
<li>Hashtable存储<code>Key</code>必须唯一的任何数据类型的键值对</li>
<li>Hashtable键不能为<code>null</code>，而值可以为<code>null</code></li>
<li>Hashtable通过比较键的哈希码来检索项目。因此它的性能比<code>Dictionary</code>集合慢</li>
<li>Hashtable使用默认的哈希码提供程序，即<code>object.GetHash()</code>。您还可以使用自定义哈希码提供程序。</li>
<li>将<code>DictionaryEntry</code>与<code>foreach</code>语句一起使用以迭代<code>Hashtable</code>。</li>
</ul>
<p>Hashtable的重要属性和方法：</p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Count</td>
<td>获取Hashtable中键/值对的总数。</td>
</tr>
<tr>
<td>IsReadOnly</td>
<td>获取布尔值，指示Hashtable是否为只读。</td>
</tr>
<tr>
<td>Item</td>
<td>获取或设置与指定键关联的值。</td>
</tr>
<tr>
<td>Keys</td>
<td>获取Hashtable中的键的ICollection。</td>
</tr>
<tr>
<td>Values</td>
<td>获取Hashtable中值的ICollection。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Add</td>
<td>将具有键和值的项添加到哈希表中</td>
</tr>
<tr>
<td>Remove</td>
<td>从散列表中删除具有指定键的项。</td>
</tr>
<tr>
<td>Clear</td>
<td>从哈希表中删除所有项目。</td>
</tr>
<tr>
<td>Contains</td>
<td>检查哈希表是否包含特定密钥。</td>
</tr>
<tr>
<td>ContainsKey</td>
<td>检查哈希表是否包含特定密钥。</td>
</tr>
<tr>
<td>ContainsValue</td>
<td>检查哈希表是否包含特定值。</td>
</tr>
<tr>
<td>GetHash</td>
<td>返回指定键的哈希码。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">Hashtable hashtable = new Hashtable();
hashtable.Add(1, &quot;wang&quot;);
hashtable.Add(3, false);
hashtable.Add(2, &quot;li&quot;);

foreach (DictionaryEntry item in hashtable)
{
    Console.WriteLine(&quot;key:{0}, value:{1}&quot;, item.Key, item.Value);
}
</code></pre>
<h4 id="BitArray"><code>BitArray</code></h4>
<p><code>BitArray</code> 类管理一个紧凑型的位值数组，它使用布尔值来表示，其中<code>true</code>表示位是开启的(1)，false 表示位是关闭的(0)。当需要存储位，但是事先不知道位数时，则使用点阵列。可以使用整型索引从点阵列集合中访问各项，索引从零开始。</p>
<p><code>BitArray</code>的重要属性和方法：</p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Count</td>
<td>获取 BitArray 中包含的元素个数。</td>
</tr>
<tr>
<td>IsReadOnly</td>
<td>获取一个值，表示 BitArray 是否只读。</td>
</tr>
<tr>
<td>Item</td>
<td>获取或设置 BitArray 中指定位置的位的值。</td>
</tr>
<tr>
<td>Length</td>
<td>获取或设置 BitArray 中的元素个数。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>public BitArray And( BitArray value );</td>
<td>对当前的 BitArray 中的元素和指定的 BitArray 中的相对应的元素执行按位与操作。</td>
</tr>
<tr>
<td>public bool Get( int index );</td>
<td>获取 BitArray 中指定位置的位的值。</td>
</tr>
<tr>
<td>public BitArray Not();</td>
<td>把当前的 BitArray 中的位值反转，以便设置为 true 的元素变为 false，设置为 false 的元素变为 true。</td>
</tr>
<tr>
<td>public BitArray Or( BitArray value );</td>
<td>对当前的 BitArray 中的元素和指定的 BitArray 中的相对应的元素执行按位或操作。</td>
</tr>
<tr>
<td>public void Set( int index, bool value );</td>
<td>把 BitArray 中指定位置的位设置为指定的值。</td>
</tr>
<tr>
<td>public void SetAll( bool value );</td>
<td>把 BitArray 中的所有位设置为指定的值。</td>
</tr>
<tr>
<td>public BitArray Xor( BitArray value );</td>
<td>对当前的 BitArray 中的元素和指定的 BitArray 中的相对应的元素执行按位异或操作。</td>
</tr>
</tbody>
</table>
<h3 id="泛型集合">泛型集合</h3>
<p>同传统的集合相比，泛型集合是一种强类型的集合，它解决了类型安全问题，同时避免了集合中每次的装箱与拆箱的操作，提升了性能</p>
<h4 id="List-T"><code>List&lt;T&gt;</code></h4>
<p><code>List&lt;T&gt;</code>在C#应用程序中是一种快捷、易于使用的泛型集合类型，使用泛型编程为编写面向对象程序增加了极大的效率和灵活性，不会强行对值类型进行装箱和拆箱，或对引用类型进行向下强制类型转换</p>
<p>常用方法：</p>
<table>
<thead>
<tr>
<th style="text-align:left">名称</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Add</td>
<td style="text-align:left">将对象添加到 List&lt;T的结尾处</td>
</tr>
<tr>
<td style="text-align:left">AddRange</td>
<td style="text-align:left">将指定集合的元素添加到 List&lt;T的末尾</td>
</tr>
<tr>
<td style="text-align:left">AsReadOnly</td>
<td style="text-align:left">返回当前集合的只读 IList&lt;T包装</td>
</tr>
<tr>
<td style="text-align:left">BinarySearch(T)</td>
<td style="text-align:left">使用默认的比较器在整个已排序的 List&lt;T中搜索元素，并返回该元素从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">BinarySearch(T, IComparer<T>)</td>
<td style="text-align:left">使用指定的比较器在整个已排序的 List&lt;T中搜索元素，并返回该元素从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">BinarySearch(Int32, Int32, T, IComparer<T>)</td>
<td style="text-align:left">使用指定的比较器在已排序 List&lt;T的某个元素范围中搜索元素，并返回该元素从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">Clear</td>
<td style="text-align:left">从 List&lt;T中移除所有元素。</td>
</tr>
<tr>
<td style="text-align:left">Contains</td>
<td style="text-align:left">确定某元素是否在 List&lt;T中。</td>
</tr>
<tr>
<td style="text-align:left">ConvertAll&lt;TOutput</td>
<td style="text-align:left">将当前 List&lt;T中的元素转换为另一种类型，并返回包含转换后的元素的列表。</td>
</tr>
<tr>
<td style="text-align:left">CopyTo(T[])</td>
<td style="text-align:left">将整个 List&lt;T复制到兼容的一维数组中，从目标数组的开头开始放置。</td>
</tr>
<tr>
<td style="text-align:left">Exists</td>
<td style="text-align:left">确定 List&lt;T是否包含与指定谓词所定义的条件相匹配的元素。</td>
</tr>
<tr>
<td style="text-align:left">Find</td>
<td style="text-align:left">搜索与指定谓词所定义的条件相匹配的元素，并返回整个 List&lt;T中的第一个匹配 元素。</td>
</tr>
<tr>
<td style="text-align:left">FindIndex(Predicate<T>)</td>
<td style="text-align:left">搜索与指定谓词所定义的条件相匹配的元素，并返回整个List &lt;T中第一个匹配元素的从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">ForEach</td>
<td style="text-align:left">对 List&lt;T的每个元素执行指定操作。  GetEnumerator    返回循环访问 List&lt;T的枚举器。</td>
</tr>
<tr>
<td style="text-align:left">IndexOf(T)</td>
<td style="text-align:left">搜索指定的对象，并返回整个 List&lt;T中第一个匹配项的从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">Insert</td>
<td style="text-align:left">将元素插入 List&lt;T的指定索引处。</td>
</tr>
<tr>
<td style="text-align:left">InsertRange</td>
<td style="text-align:left">将集合中的某个元素插入 List&lt;T的指定索引处。</td>
</tr>
<tr>
<td style="text-align:left">LastIndexOf(T)</td>
<td style="text-align:left">搜索指定的对象，并返回整个 List&lt;T中最后一个匹配项的从零开始的索引。</td>
</tr>
<tr>
<td style="text-align:left">Remove</td>
<td style="text-align:left">从 List&lt;T中移除特定对象的第一个匹配项。</td>
</tr>
<tr>
<td style="text-align:left">Reverse()</td>
<td style="text-align:left">将整个 List&lt;T中元素的顺序反转。</td>
</tr>
<tr>
<td style="text-align:left">Sort()</td>
<td style="text-align:left">使用默认比较器对整个 List&lt;T中的元素进行排序。</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">List&lt;intintList = new List&lt;int&gt;();
intList.Add(3);
intList.Add(54);
intList.Add(14);
foreach (var item in intList)
{
    Console.WriteLine(item);
}

</code></pre>
<h4 id="Stack-T"><code>Stack&lt;T&gt;</code></h4>
<p>类以后进先出的方式维护数据的集合，包含pop()和push(),从栈内压入或移除数据</p>
<h4 id="Quenue-T"><code>Quenue&lt;T&gt;</code></h4>
<p>队列以先进先出的方式访问数据，使用Enqueue()和Dequeue()添加数据和移除数据</p>
<h4 id="SortedSet-T"><code>SortedSet&lt;T&gt;</code></h4>
<p>这个类中的数据是排序的，在插入和移除数据之后仍然能自动排序，需要向其构造函数中传递一个实现了IComparer<T>,该接口定义了Compare方法。</p>
<h4 id="ObservableCollection-T"><code>ObservableCollection&lt;T&gt;</code></h4>
<p>类表示能在添加、移除或者刷新整个列表时提供通知的动态数据集，合，ReadOnlyObservableCollection<T>的操作与之类似，不过是只读的。ObservableCollection<T>实现了一个名为CollectionChanged事件，该事件在插入新的数据或者移除数据时触发</p>
<h4 id="Dictionary-k-v"><code>Dictionary&lt;k,v&gt;</code></h4>
<p><code>Dictionary</code>提供快速的基于键值的元素查找。结构是：<code>Dictionary &lt;[key] , [value]&gt;</code>,当你有很多元素的时候可以用它。在使用前，你必须声明它的键类型和值类型</p>
<h3 id="数组、ArrayList和List三者的区别">数组、<code>ArrayList</code>和List三者的区别</h3>
<ul>
<li>数组:针对特定类型固定长度，值不可为null,在声明数组的时候必须指定数组的长度，可有多个维度</li>
<li>Array:数组的另一种创建方式，抽象类Array，作为所有数组的基类，针对任意类型固定长度,值可以为null,输出会被替换为0</li>
<li>ArrayList:针对任意类型、任意长度的,值可以为null,存储或检索值类型时通常发生装箱和取消装箱操作，带来很大的性能耗损</li>
<li>List<T>:强类型的集合,固定类型、任意长度,值不可为null,是类型安全的，</li>
</ul>
<h3 id="索引器-Indexer">索引器(<code>Indexer</code>)</h3>
<p><code>Indexer</code>是一种特殊类型的属性，允许以与其内部集合的数组相同的方式访问类或结构。除了使用带有方括号和参数的此关键字定义的属性外，它与属性相同。<br>
要点：</p>
<ul>
<li>索引器与属性相同，除了它使用带有方括号的此关键字定义，该参数具有参数。</li>
<li>可以通过具有不同类型的参数来覆盖索引器。</li>
<li>不支持使用索引器的<code>Ref</code>和<code>out</code>参数。</li>
<li>索引器可以作为接口成员包含在内。</li>
</ul>
<p>语法</p>
<pre><code class="language-cs">Public &lt;return typethis[&lt;parameter typeindex]
{
    Get{
    // return the value from the specified index
    }
    Set{
    // set values at the specified index
    }
}
</code></pre>
<pre><code class="language-cs">public class Indexer
{
        private int[] indexs = new int[10];

        public int this[int index]
        {
            get { return indexs[index]; }
            set
            {
                indexs[index] = value;
            }
        }
}
/*
Indexer是一种特殊类型的属性，允许以与其内部集合的数组相同的方式访问类或结构。
除了使用带有方括号和参数的此关键字定义的属性外，它与属性相同。
*/
Console.WriteLine(&quot;********************索引器*********************&quot;);

Indexer indexer = new Indexer();
indexer[0] = 1;
indexer[1] = 2;
Console.WriteLine(indexer[1]);
</code></pre>
<h1>面向对象三大特性:封装、继承、多态</h1>
<h2 id="继承类型">继承类型</h2>
<p>在面向对象编程中,有两种截然不同的继承类型,实现继承和接口继承;<code>C#</code>中不支持多重继承,<code>C#</code>类可以派生自另一个类和任意多的接口</p>
<ul>
<li>实现继承:表示一个类型派生自一个基类型,它拥有该基类型的所有成员字段和函数,在需要给现有类型添加功能或者许多相关类型共享一组重要的公共功能时.这种类型继承非常有用</li>
<li>接口继承:表示一个类型只继承了函数的签名,没有继承任何的实现代码</li>
</ul>
<h3 id="实现继承virtual-override">实现继承<code>virtual</code>/<code>override</code></h3>
<pre><code class="language-cs">    /// &lt;summary&gt;
    /// 基类
    /// &lt;/summary&gt;
    class Person
    {
        /// &lt;summary&gt;
        /// 使用virtual关键字定义的方法允许在派生类中使用override重写
        /// &lt;/summary&gt;
        public virtual void SayHello()
        {
            Console.WriteLine(&quot;基类的SayHello&quot;);
        }
    }

    /// &lt;summary&gt;
    /// 派生自Person
    /// &lt;/summary&gt;
    class ChinaPerson : Person
    {
        /// &lt;summary&gt;
        /// 使用override关键字重写基类的SayHello方法
        /// &lt;/summary&gt;
        public override void SayHello()
        {
            Console.WriteLine(&quot;你好&quot;);
        }
    }

    /// &lt;summary&gt;
    /// 派生自Person
    /// &lt;/summary&gt;
    class ThailandPerson : Person
    {
        public override void SayHello()
        {
            Console.WriteLine(&quot;萨瓦迪卡&quot;);
        }
    }
</code></pre>
<p>把一个基类函数声明为<code>virtual</code>,就可以在任何派生类中重写该函数,<code>virtual</code>也适用于属性;需要注意:成员字段和静态函数都不能声明为<code>virtual</code>,因为这个概念只对类中的实例成员有意义</p>
<h3 id="隐藏方法">隐藏方法</h3>
<p>如果签名相同的方法在基类和派生类中都进行了声明,但该方法没有声明为<code>virtual</code>和<code>override</code>,派生类会隐藏基类方法</p>
<pre><code class="language-cs"> class Person
    {
        public void SayHello()
        {
            Console.WriteLine(&quot;基类的SayHello&quot;);
        }
    }

    class ChinaPerson : Person
    {
        //提示:隐藏继承的成员Person.SayHello,如果有意的,请使用关键字new
        public void SayHello()
        {
            Console.WriteLine(&quot;你好&quot;);
        }
    }
</code></pre>
<h3 id="调用函数的基类版本">调用函数的基类版本</h3>
<p>C#中可以使用<code>base.</code>这种语法来调用方法的基类版本</p>
<pre><code class="language-cs">class Person
    {
        public virtual void SayHello()
        {
            Console.WriteLine(&quot;基类的SayHello&quot;);
        }
    }

    class ChinaPerson : Person
    {
        public override void SayHello()
        {
            base.SayHello();
            Console.WriteLine(&quot;你好&quot;);
        }
    }
</code></pre>
<h3 id="接口继承">接口继承</h3>
<p>表示一个类型只继承了函数的签名,没有继承任何实现代码。在需要指定该类型具有某些可用的特性时,最好使用这种类型的继承。接口名称通常上以字母I开头,以便知道这是一个接口。C#支持多接口继承和单一实现继承</p>
<p>接口继承中又分为隐式实现和显式实现</p>
<pre><code class="language-cs">    interface IPerson
    {
        void SayHello();
    }

    /// &lt;summary&gt;
    /// 隐式实现接口
    /// &lt;/summary&gt;
    class ChinaPerson : IPerson
    {
        public void SayHello()
        {
            Console.WriteLine(&quot;你好&quot;);
        }
    }

    /// &lt;summary&gt;
    /// 显式实现接口
    /// &lt;/summary&gt;
    class ThailandPerson : IPerson
    {      
        void IPerson.SayHello()
        {
            Console.WriteLine(&quot;莎娃迪卡&quot;);
        }
    }

</code></pre>
<p>对于隐式实现的接口调用这两种方式都可以</p>
<pre><code class="language-cs">            ChinaPerson chinaPerson = new ChinaPerson();
            IPerson person = new ChinaPerson();
            person.SayHello();
            chinaPerson.SayHello();
</code></pre>
<p>对于显式实现的接口调用:只能使用接口调用</p>
<pre><code class="language-cs">            IPerson thailandPerson = new ThailandPerson();
            thailandPerson.SayHello();
</code></pre>
<h3 id="抽象类和抽象函数">抽象类和抽象函数</h3>
<ul>
<li>C#中允许把类或函数声明为<code>abstract</code>,抽象类不能被实例化.抽象函数也不能直接实现,必须在非抽象的派生类中重写</li>
<li>如果类包含抽象函数,则该类也必须被声明为抽象的</li>
<li>抽象方法只在派生类中真正实现，这表明抽象方法只存放函数原型，不涉及主体代码</li>
<li>派生自抽象类的类需要实现其基类的抽象方法，才能实例化对象</li>
<li>使用<code>override</code>关键子可在派生类中实现抽象方法，经<code>override</code>声明重写的方法称为重写基类方法，其签名必须与<code>override</code>方法的签名相同</li>
</ul>
<h3 id="密封类和密封方法">密封类和密封方法</h3>
<p>C#允许把类和方法声明为<code>sealed</code>,对于类,这表示不能继承；对于方法，这表示不能重写该方法</p>
<h3 id="派生类的构造函数执行顺序">派生类的构造函数执行顺序</h3>
<p>首先定义基类<code>A</code>,为了方便查看，显式指明基类的无参构造函数</p>
<pre><code class="language-cs">    class A
    {
        public int Age { get; set; }

        public string Name { get; set; }

        public A()
        {
            Console.WriteLine(&quot;A类无参构造函数&quot;);
        }
    }
</code></pre>
<p>然后定义<code>B</code>类继承自<code>A</code>类</p>
<pre><code class="language-cs"> class B : A
    {
        public B()
        {
            Console.WriteLine(&quot;B类无参构造函数&quot;);
        }
    }
</code></pre>
<p>实例化<code>B</code>类</p>
<pre><code class="language-cs">static void Main(string[] args)
        {
            B b = new B();
            Console.ReadKey();
        }
</code></pre>
<p>输出：<br>
A类无参构造函数<br>
B类无参构造函数</p>
<p>实例化子类时，只可以<code>new</code>子类，执行顺序为：先执行父类构造函数=&gt;再执行子类构造函数</p>
<p>如果父类存在多个构造函数会怎么样？</p>
<pre><code class="language-cs">    class A
    {
        public int Age { get; set; }

        public string Name { get; set; }

        public A()
        {
            Console.WriteLine(&quot;A类无参构造函数&quot;);
        }
        public A(int age, string name)
        {
            Console.WriteLine(&quot;A类带参构造函数&quot;);
        }
    }
    class B : A
    {
        public B()
        {
            Console.WriteLine(&quot;B类无参构造函数&quot;);
        }
    }
</code></pre>
<p>再次实例化<code>B</code>类</p>
<pre><code class="language-cs">static void Main(string[] args)
        {
            B b = new B();
            Console.ReadKey();
        }
</code></pre>
<p>输出：<br>
A类无参构造函数<br>
B类无参构造函数</p>
<p>实例化子类时，会先执行父类的构造函数(默认为父类的无参构造函数)，也可以在子类中使用<code>base</code>关键字指定调用父类的哪个构造函数</p>
<pre><code class="language-cs">    class Program
    {
        static void Main(string[] args)
        {
            B b = new B(3);
            Console.ReadKey();
        }
    }

    class A
    {
        public int Age { get; set; }

        public A()
        {
            Console.WriteLine(&quot;A类无参构造函数&quot;);
        }
        public A(int age)
        {
            Console.WriteLine(&quot;A类带参构造函数&quot;);
        }
    }

    class B : A
    {
        public B() : base(3)
        {
            Console.WriteLine(&quot;B类无参构造函数调用父类带参构造函数&quot;);
        }
    }
</code></pre>
<p>输出：<br>
A类带参构造函数<br>
B类无参构造函数调用父类带参构造函数</p>
<p>总结：</p>
<ul>
<li>实例化父类时，可以使用<code>new</code>子类，执行构造函数顺序为：执行父类构造函数=&gt;执行子类构造函数</li>
<li>实例化子类时，只可以<code>new</code>子类，执行顺序同上</li>
<li>父类实例化后，只能执行父类的方法，获得父类的属性等</li>
<li>实例化子类后，可同时执行子类和父类的方法和属性，如同名方法，则执行子类的方法</li>
<li>子类构造函数可以使用<code>base</code>关键字指定调用的父类构造函数</li>
</ul>
<h3 id="抽象类和接口的区别">抽象类和接口的区别</h3>
<p>相同点：</p>
<ul>
<li>1.都可以被继承</li>
<li>2.都不能被实例化</li>
<li>3.都包含方法声明</li>
<li>4.派生类必须实现未实现的方法</li>
</ul>
<p>区别:<br>
抽象基类可以定义字段/属性/方法实现.接口只能定义属性/索引器/事件/方法声明<br>
抽象类是一个不完整的类,需要通过集成进一步细化.而接口更像是一个行为规范,表名我能做什么<br>
接口是可以被多重实现的,可以有多个类实现接口,因为类的单一继承性,抽象类只能被单一继承<br>
抽象类实现继承需要使用<code>override</code>关键字,接口则不用<br>
如果抽象类实现接口,可以把接口方法映射到抽象类中作为抽象方法不必实现,而在抽象类的子类中实现接口方法<br>
抽象类表示的是:这个对象是什么;接口表示的是:这个对象能做什么;使用抽象类是为了代码的复用,使用接口是为了实现多态性</p>
<h3 id="普通类和抽象类的区别">普通类和抽象类的区别</h3>
<p>都可以被继承<br>
抽象类不能实例化,普通类允许实例化<br>
抽象方法值包含方法声明而且必须包含在抽象类中<br>
子类继承抽象类必须实现抽象类中的抽象方法除非子类也是抽象类<br>
抽象类中可以包含抽象方法和实例方法</p>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>压力测试(k6)</title>
    <url>/2020/11/18/%E6%B5%8B%E8%AF%95%E7%9B%B8%E5%85%B3/%E5%8E%8B%E5%8A%9B%E6%B5%8B%E8%AF%95(k6)/</url>
    <content><![CDATA[<p>k6是GitHub上提供的开源负载测试工具。使用Go编写，并运行用JavaScript编写的测试脚本</p>
<a id="more"></a>
<h1>K6单接口压测</h1>
<h2 id="先决条件">先决条件</h2>
<ul>
<li>安装K6 <a href="https://k6.io/docs/getting-started/installation" target="_blank" rel="noopener">https://k6.io/docs/getting-started/installation</a></li>
<li>环境变量配置、cd到压测脚本目录</li>
</ul>
<h2 id="概念">概念</h2>
<ul>
<li>vu：虚拟用户</li>
<li>duration:持续时长</li>
</ul>
<h2 id="入门">入门</h2>
<pre><code class="language-cs">import http from 'k6/http';
import { sleep } from 'k6';

export default function() {
  http.get('http://localhost:5000/api/values');
}
</code></pre>
<p><code>k6 run ValuesApiK6.js</code></p>
<h2 id="多Vu和指定时长">多Vu和指定时长</h2>
<p><code>k6 run --vus 10 --duration 10s ValuesApiK6.js</code></p>
<h2 id="使用配置选项">使用配置选项</h2>
<pre><code>import http from &quot;k6/http&quot;;
import { sleep,check } from &quot;k6&quot;;

export let options = {
  vus: 10,
  duration: '10s',
};

// 脚本至少必须包含一个default函数,这定义了VU的入口点，类似于许多其他语言中的main()函数
export default function() {
  let res = http.get(&quot;http://localhost:5000/api/values&quot;);
  check(res, { 'status was 200': r =&gt; r.status == 200 });
  sleep(1);
}
</code></pre>
<h2 id="测试过程中设置阶段使VU级别上升和下降">测试过程中设置阶段使VU级别上升和下降</h2>
<pre><code>import http from &quot;k6/http&quot;;
import { sleep, check } from &quot;k6&quot;;

export let options = {
  // 阶段负载
  stages: [
    { duration: &quot;3s&quot;, target: 2 },
    { duration: &quot;5s&quot;, target: 3 },
    { duration: &quot;6s&quot;, target: 10 },
  ],
};

// 脚本至少必须包含一个default函数,这定义了VU的入口点，类似于许多其他语言中的main()函数
export default function () {
  let res = http.get(&quot;http://localhost:5000/api/values&quot;);
  check(res, { &quot;status was 200&quot;: (r) =&gt; r.status == 200 });
  sleep(1);
}
</code></pre>
<p>可能出现的异常：<code>using multiple execution config shortcuts (</code>duration<code>and</code>stages<code>) simultaneously is not allowed</code>  不允许同时使用多个执行配置快捷方式(“duration”和“stages”)</p>
<h2 id="断言">断言</h2>
<p>需要<code>import { sleep, check } from &quot;k6&quot;;</code> 引入check模块</p>
<p><code>check(res, { &quot;status was 200&quot;: (r) =&gt; r.status == 200 });</code></p>
<h2 id="测试结果">测试结果</h2>
<pre><code> execution: local-
     script: ValuesApiK6.js
     output: -

  scenarios: (100.00%) 1 executors, 10 max VUs, 44s max duration (incl. graceful stop):
           * default: Up to 10 looping VUs for 14s over 3 stages (gracefulRampDown: 30s, gracefulStop: 30s)

I
running (16.0s), 00/10 VUs, 38 complete and 0 interrupted iterations
default ✓ [======================================] 00/10 VUs  14s9


    ✓ status was 200 
 
    checks.....................: 100.00% ✓ 38   ✗ 0
    data_received..............: 176 MB  11 MB/s
    data_sent..................: 3.3 kB  208 B/s
    http_req_blocked...........: avg=79.1µs   min=0s       med=0s       max=2ms     p(90)=0s      p(95)=149.71µs
    http_req_connecting........: avg=52.85µs  min=0s       med=0s       max=1.01ms  p(90)=0s      p(95)=149.71µs
    http_req_duration..........: avg=682.14ms min=332.4ms  med=516.59ms max=1.66s   p(90)=1.22s   p(95)=1.44s
    http_req_receiving.........: avg=10.39ms  min=3.98ms   med=8.97ms   max=37.89ms p(90)=16.85ms p(95)=23.37ms
    http_req_sending...........: avg=0s       min=0s       med=0s       max=0s      p(90)=0s      p(95)=0s
    http_req_tls_handshaking...: avg=0s       min=0s       med=0s       max=0s      p(90)=0s      p(95)=0s
    http_req_waiting...........: avg=671.75ms min=323.42ms med=508.61ms max=1.65s   p(90)=1.21s   p(95)=1.43s
    http_reqs..................: 38      2.374567/s
    iteration_duration.........: avg=1.68s    min=1.33s    med=1.51s    max=2.66s   p(90)=2.22s   p(95)=2.44s
    iterations.................: 38      2.374567/s
    vus........................: 0       min=0  max=9
    vus_max....................: 10      min=10 max=10
</code></pre>
<p>测试细节</p>
<ul>
<li>execution: local k6执行模式（本地或云）</li>
<li>output: -测试结果的输出。默认值为stdout</li>
<li>script: script.js 显示正在执行的脚本的名称</li>
<li>duration: 测试运行时间</li>
<li>iterations: -VU 迭代的总数</li>
<li>vus: 100 测试将开始运行的VU的初始数量</li>
<li>max: 100 测试将扩展的VU的最大数量</li>
</ul>
<p>测试指标分析<br>
参考：<a href="https://k6.io/docs/using-k6/metrics" target="_blank" rel="noopener">https://k6.io/docs/using-k6/metrics</a></p>
<h2 id="测试结果输出第三方平台">测试结果输出第三方平台</h2>
<p>参考：<a href="https://k6.io/docs/getting-started/results-output#output-plugins" target="_blank" rel="noopener">https://k6.io/docs/getting-started/results-output#output-plugins</a></p>
<h2 id="测试结果输出Json">测试结果输出Json</h2>
<p><code>k6 run --summary-export=export.json test.js</code></p>
<h2 id="使用–out-o选项使k6输出JSON格式的详细统计信息">使用–out/-o选项使k6输出JSON格式的详细统计信息</h2>
<p><code>k6 run --out json=my_test_result.json test.js</code></p>
<h2 id="使用tags给请求打标签分组">使用tags给请求打标签分组</h2>
<p>参考：<a href="https://k6.io/docs/using-k6/http-requests#url-grouping" target="_blank" rel="noopener">https://k6.io/docs/using-k6/http-requests#url-grouping</a></p>
<h2 id="K6内置指标">K6内置指标</h2>
<p>参考：<a href="https://k6.io/docs/using-k6/metrics#custom-metrics" target="_blank" rel="noopener">https://k6.io/docs/using-k6/metrics#custom-metrics</a></p>
<h2 id="HTTP内置指标">HTTP内置指标</h2>
<p>参考：<a href="https://k6.io/docs/using-k6/metrics#metric-types" target="_blank" rel="noopener">https://k6.io/docs/using-k6/metrics#metric-types</a></p>
<h2 id="阈值">阈值</h2>
<p>阈值是通过/失败标准，用于指定被测系统的性能期望</p>
<p>预期示例（阈值）：</p>
<p>系统产生的错误不超过1％<br>
95％的请求的响应时间应小于200ms<br>
99％的请求的响应时间应低于400毫秒<br>
特定端点必须始终在300毫秒内响应<br>
阈值分析性能指标并确定最终测试结果（通过/失败）。阈值对于负载测试自动化至关重要</p>
<p>参考：<a href="https://k6.io/docs/using-k6/thresholds" target="_blank" rel="noopener">https://k6.io/docs/using-k6/thresholds</a></p>
<h2 id="分析结果可视化">分析结果可视化</h2>
<h3 id="influxdb">influxdb</h3>
<p>安装目录下运行启动<code>influxdb</code></p>
<p>启动客户端输入命令开始操作<br>
<code>influx</code></p>
<p><code>k6 run --out influxdb=http://localhost:8080/test ValuesApiK6.js</code></p>
<h2 id="Grafana">Grafana</h2>
<p><img src="/images/2020-11-18-00-11-44.png" alt=""></p>
]]></content>
      <categories>
        <category>Testing</category>
      </categories>
  </entry>
  <entry>
    <title>自动化UI测试(Selenium)</title>
    <url>/2020/11/18/%E6%B5%8B%E8%AF%95%E7%9B%B8%E5%85%B3/%E8%87%AA%E5%8A%A8%E5%8C%96UI%E6%B5%8B%E8%AF%95(Selenium)/</url>
    <content><![CDATA[<p>Selenium可以用来做自动化UI测试，支持多语言，这里使用C#编写测试脚本</p>
<a id="more"></a>
<h2 id="UI自动化测试-Selenium-PageObject-DDT">UI自动化测试(Selenium+PageObject+DDT)</h2>
<h3 id="Selenium">Selenium</h3>
<h2 id="NetFramework">NetFramework</h2>
<p>核心库:<code>Selenium.Support</code>，<code>Selenium.WebDriver</code>,<code>Selenium.RC</code></p>
<h2 id="dotnetCore">dotnetCore</h2>
<p>核心库:<code>Selenium.Support</code>，<code>Selenium.WebDriver</code>,<code>Selenium.Chrome.WebDriver</code></p>
<blockquote>
<p>Selenium.RC包在dotnetcore会存在不兼容的问题，无法正常找到<code>chromedriver.exe</code>文件</p>
</blockquote>
<p><strong>Exception</strong></p>
<blockquote>
<p>The chromedriver.exe file does not exist in the current directory or in a directory on the PATH environment variable. The driver can be downloaded at <a href="http://chromedriver.storage.googleapis.com/index.html.%E2%80%9D" target="_blank" rel="noopener">http://chromedriver.storage.googleapis.com/index.html.”</a></p>
</blockquote>
<p><strong>解决方法</strong></p>
<blockquote>
<p>nuget Selenium.Chrome.WebDriver。将ChromeDriver.exe放入构建目录中</p>
</blockquote>
<p><strong>注意</strong>：.netcore2.1中引入了<code>Selenium.Chrome.WebDriver</code>包依旧会出现上面的异常</p>
<h2 id="浏览器驱动下载（需下载本机对应版本，不区分32-64）">浏览器驱动下载（需下载本机对应版本，不区分32/64）</h2>
<p><a href="https://npm.taobao.org/mirrors/chromedriver/" target="_blank" rel="noopener">https://npm.taobao.org/mirrors/chromedriver/</a></p>
<h2 id="PageObject">PageObject</h2>
<blockquote>
<p>解决的问题：版本迭代太快，UI层元素的属性经常变换，导致维护人员需要花大把的时间去维护代码，为了节省维护成本及时间，就可以利用PageObject 这种设计模式，它就大大的减少了维护时间</p>
</blockquote>
<h2 id="实践">实践</h2>
<p>TestBase</p>
<blockquote>
<p>创建对应浏览器配置，创建WebDriver对象</p>
</blockquote>
<pre><code class="language-cs">public abstract class TestBase
{
    protected IWebDriver driver;

    public void WebDriverInit(string url)
    {
        ChromeOptions options = new ChromeOptions();
        // 窗口最大化
        options.AddArgument(&quot;start-maximized&quot;);
        driver = new ChromeDriver(options);
        driver.Navigate().GoToUrl(url);
        Thread.Sleep(1000);
    }
}
</code></pre>
<p>ClockInPage</p>
<blockquote>
<p>页面模型，封装页面标签属性以及对应操作</p>
</blockquote>
<pre><code>public class ClockInPage
{
    public static class PagePath
    {
        public static readonly string input_loginName_id = &quot;tb_LoginID_Account_589f1fc067ff4031&quot;;
        public static readonly string input_passWord_id = &quot;tb_Password&quot;;
        public static readonly string submit_login_Name = &quot;ctl10&quot;;
    }

    public void LoginClick(IWebDriver driver, string logiName, string passWord)
    {
        driver.FindElement(By
            .Id(PagePath.input_loginName_id)).SendKeys(logiName);
        driver.FindElement(By
            .Id(PagePath.input_passWord_id)).SendKeys(passWord);
        driver.FindElement(By
            .Name(PagePath.submit_login_Name)).Click();
        Thread.Sleep(1000);
    }
}
</code></pre>
<p>ClockInTests</p>
<blockquote>
<p>测试方法,采用DDT数据驱动方式提供测试数据</p>
</blockquote>
<pre><code>[TestCategory(&quot;ui\\CodedUITest&quot;)]
[TestClass()]
public class ClockInTests : TestBase
{
    [TestCleanup]
    public void Clean()
    {
        driver.Dispose();
    }

    [DataTestMethod]
    [DataRow(&quot;http://11.11.141.10/OMSP//Login/JSS.aspx&quot;, &quot;wangpengliang&quot;, &quot;Wpl-19950815&quot;, true)]
    [DataRow(&quot;http://11.11.141.10/OMSP//Login/JSS.aspx&quot;, &quot;111111&quot;, &quot;222222&quot;, false)]
    public void ClockIn_Login_01_Normal(string url, string loginName, string passWord, bool expect)
    {
        WebDriverInit(url);
        ClockInPage page = new ClockInPage();
        page.LoginClick(driver, loginName, passWord);
        Assert.AreEqual(expect, driver.Url != url);
    }
}
</code></pre>
]]></content>
      <categories>
        <category>Testing</category>
      </categories>
  </entry>
  <entry>
    <title>Azure Devops安装(Version-2020)</title>
    <url>/2020/11/17/azureDevOps/Azure%20Devops%E5%AE%89%E8%A3%85(Version-2020)/</url>
    <content><![CDATA[<p>介绍如何安装<code>Azure Devops</code>,版本为2020</p>
<a id="more"></a>
<h2 id="DevOps">DevOps</h2>
<p>DevOps 是一个完整的面向IT运维的工作流，来自于Development和Operations的组合，突出重视软件开发人员和运维人员的沟通合作，通过自动化流程来使得软件构建、测试、发布更加快捷、频繁和可靠，以 IT 自动化以及持续集成（CI）、持续部署（CD）为基础，来优化程式开发、测试、系统运维等所有环节</p>
<h2 id="Azure-DevOps">Azure DevOps</h2>
<p>Azure DevOps provides developer services to support teams to plan work, collaborate on code development, and build and deploy applications. Developers can work in the cloud using Azure DevOps Services or on-premises using Azure DevOps Server. Azure DevOps Server was formerly named Visual Studio Team Foundation Server (TFS).</p>
<p>Azure DevOps provides integrated features that you can access through your web browser or IDE client. You can use one or more of the following services based on your business needs:</p>
<ul>
<li>
<p>Azure Repos provides Git repositories or Team Foundation Version Control (TFVC) for source control of your code</p>
</li>
<li>
<p>Azure Pipelines provides build and release services to support continuous integration and delivery of your apps</p>
</li>
<li>
<p>Azure Boards delivers a suite of Agile tools to support planning and tracking work, code defects, and issues using Kanban and Scrum methods</p>
</li>
<li>
<p>Azure Test Plans provides several tools to test your apps, including manual/exploratory testing and continuous testing</p>
</li>
<li>
<p>Azure Artifacts allows teams to share Maven, npm, and NuGet packages from public and private sources and integrate package sharing into your CI/CD pipelines<br>
You can also use collaboration tools such as:</p>
</li>
<li>
<p>Customizable team dashboards with configurable widgets to share information, progress, and trends</p>
</li>
<li>
<p>Built-in wikis for sharing information</p>
</li>
<li>
<p>Configurable notifications<br>
Azure DevOps supports adding extensions and integrating with other popular services, such as: Campfire, Slack, Trello, UserVoice, and more, and developing your own custom extensions.</p>
</li>
</ul>
<p>For information on the differences between the cloud versus on-premises platforms, see Azure DevOps Services and Azure DevOps Server.</p>
<p>Microsoft renamed Visual Studio Team Services (VSTS) to Azure DevOps Services. For more information about this change, see Introducing Azure DevOps.</p>
<p>Team Foundation Server became Azure DevOps Server 2019.</p>
<p>Choose Azure DevOps Services<br>
Choose Azure DevOps Services when you want:</p>
<ul>
<li>Quick set-up</li>
<li>Maintenance-free operations</li>
<li>Easy collaboration across domains</li>
<li>Elastic scale</li>
<li>Rock-solid security<br>
To learn more about data protection in Azure DevOps Services, see Data protection overview.</li>
</ul>
<p>Azure DevOps Services also gives you access to cloud build and deployment servers, and application insights.</p>
<p>We’ve made it easy for you to start for free and try out our services. Sign up for free by creating an organization. Then, either upload your code to share or source control. Begin tracking your work using Scrum, Kanban, or a combination of methods.</p>
<p>You can use all the services included with Azure DevOps, or choose just what you need to complement your existing workflows.</p>
<ul>
<li>Azure Boards. Plan, track, and discuss work across your teams.</li>
<li>Azure Pipelines. Continuously build, test, and deploy to any platform and cloud.</li>
<li>Azure Repos. Get unlimited, cloud-hosted private Git repositories for your project.</li>
</ul>
<h2 id="安装部署">安装部署</h2>
<h3 id="安装软件">安装软件</h3>
<p>Azure DevOps Server2020的安装过程，还是集成之前版本的习惯，主要分为两个部分：安装软件和配置系统</p>
<p>安装软件的过程基本是傻瓜式的，下载镜像文件后，装载ISO文件，允许AzureDevOpsServer2020.exe文件，系统会自动将必要软件安装你的系统中</p>
<p>注意，如果服务器中已经安装过其他版本的Azure DevOps Server，安装软件不会自动删除之前的文件，而是将2020安装到一个新的目录中，默认文件地址是：<code>C:\Program Files\Azure DevOps Server 2020</code></p>
<p><img src="/images/2020-11-17-23-18-11.png" alt=""></p>
<h3 id="系统配置">系统配置</h3>
<p>按照系统提示重启服务器后，系统会自动打开Azure DevOps Server 的配置界面</p>
<p><img src="/images/2020-11-17-23-18-20.png" alt=""></p>
<p>设置好各个环节的配置，如出错就挨个解决掉，比如</p>
<p><img src="/images/2020-11-17-23-18-31.png" alt=""></p>
<p>配置完成</p>
<p><img src="/images/2020-11-17-23-18-40.png" alt=""></p>
<p>浏览器访问</p>
<p><img src="/images/2020-11-17-23-18-47.png" alt=""></p>
<h2 id="Agent安装">Agent安装</h2>
<p><img src="/images/2020-11-17-23-18-56.png" alt=""></p>
<h3 id="CI-Agent">CI-Agent</h3>
<pre><code>PS D:\AzureDevopsServer\install\agent-CI&gt; .\config.cmd

  ___                      ______ _            _ _
 / _ \                     | ___ (_)          | (_)
/ /_\ \_____   _ _ __ ___  | |_/ /_ _ __   ___| |_ _ __   ___  ___
|  _  |_  / | | | '__/ _ \ |  __/| | '_ \ / _ \ | | '_ \ / _ \/ __|
| | | |/ /| |_| | | |  __/ | |   | | |_) |  __/ | | | | |  __/\__ \
\_| |_/___|\__,_|_|  \___| \_|   |_| .__/ \___|_|_|_| |_|\___||___/
                                   | |
        agent v2.170.1             |_|          (commit bb272bb)


&gt;&gt; 连接:

输入 服务器 URL &gt; http://localhost:9090/
输入 身份验证类型 (对于 Integrated 按 Enter) &gt;
正在连接到服务器...

&gt;&gt; 注册代理:

输入 代理池 (对于 default 按 Enter) &gt;
输入 代理名称 (对于 WANGPENGLIANG 按 Enter) &gt;
正在扫描工具功能。
正在连接到服务器。
已成功添加代理
正在测试代理连接。
输入 工作文件夹 (对于 _work 按 Enter) &gt;
2020-11-02 07:17:03Z: 设置已保存。
输入 是否将代理作为服务运行? (Y/N) (对于 N 按 Enter) &gt; y
输入 要用于服务的用户帐户 (对于 NT AUTHORITY\NETWORK SERVICE 按 Enter) &gt;
将文件权限授予“NT AUTHORITY\NETWORK SERVICE”。
已成功安装服务 vstsagent.localhost.Default.WANGPENGLIANG
已成功设置服务 vstsagent.localhost.Default.WANGPENGLIANG 的恢复选项
服务 vstsagent.localhost.Default.WANGPENGLIANG 已成功设置为延迟自动启动
已成功配置服务 vstsagent.localhost.Default.WANGPENGLIANG
已成功启动服务 vstsagent.localhost.Default.WANGPENGLIANG
</code></pre>
<p><img src="/images/2020-11-17-23-19-12.png" alt=""></p>
<h3 id="CD-Agent">CD-Agent</h3>
<p>复制了一份<code>agent</code>文件夹内容，并且命令修改为<code>.\config.cmd --deploymentpool</code></p>
<pre><code>PS D:\AzureDevopsServer\install\agent-CD&gt; .\config.cmd --deploymentpool

  ___                      ______ _            _ _
 / _ \                     | ___ (_)          | (_)
/ /_\ \_____   _ _ __ ___  | |_/ /_ _ __   ___| |_ _ __   ___  ___
|  _  |_  / | | | '__/ _ \ |  __/| | '_ \ / _ \ | | '_ \ / _ \/ __|
| | | |/ /| |_| | | |  __/ | |   | | |_) |  __/ | | | | |  __/\__ \
\_| |_/___|\__,_|_|  \___| \_|   |_| .__/ \___|_|_|_| |_|\___||___/
                                   | |
        agent v2.170.1             |_|          (commit bb272bb)


&gt;&gt; 连接:

输入 服务器 URL &gt; http://localhost:9090/
输入 身份验证类型 (对于 Integrated 按 Enter) &gt;
正在连接到服务器...

&gt;&gt; 注册代理:

输入 部署池名称 &gt; deploymentpool
输入 代理名称 (对于 WANGPENGLIANG 按 Enter) &gt;
正在扫描工具功能。
正在连接到服务器。
已成功添加代理
正在测试代理连接。
输入 工作文件夹 (对于 _work 按 Enter) &gt;
2020-11-02 07:14:25Z: 设置已保存。
输入 是否将代理作为服务运行? (Y/N) (对于 N 按 Enter) &gt; y
输入 要用于服务的用户帐户 (对于 NT AUTHORITY\SYSTEM 按 Enter) &gt;
诊断日志中报告了 Error。请查看日志了解更多详细信息。
    - D:\AzureDevopsServer\install\agent-CD\_diag\Agent_20201102-071319-utc.log
将文件权限授予“NT AUTHORITY\SYSTEM”。
已成功安装服务 vstsagent.localhost.deploymentpool.WANGPENGLIANG
已成功设置服务 vstsagent.localhost.deploymentpool.WANGPENGLIANG 的恢复选项
服务 vstsagent.localhost.deploymentpool.WANGPENGLIANG 已成功设置为延迟自动启动
已成功配置服务 vstsagent.localhost.deploymentpool.WANGPENGLIANG
已成功启动服务 vstsagent.localhost.deploymentpool.WANGPENGLIANG
</code></pre>
<p><img src="/images/2020-11-17-23-19-19.png" alt=""></p>
]]></content>
      <categories>
        <category>AzureDevops</category>
      </categories>
  </entry>
  <entry>
    <title>Azure Devops常用功能</title>
    <url>/2020/11/17/azureDevOps/Azure%20Devops%E5%B8%B8%E7%94%A8%E5%8A%9F%E8%83%BD/</url>
    <content><![CDATA[<p>介绍<code>Azure Devops</code>中比较常用的功能点</p>
<a id="more"></a>
<h2 id="代码托管">代码托管</h2>
<p>将项目代码推送到<code>Azure Devops Server</code>的存储库中，没难度不详述</p>
<p><img src="/images/2020-11-17-23-21-42.png" alt=""></p>
<h2 id="分支策略">分支策略</h2>
<p>新建<code>dev</code>开发分支</p>
<p><img src="/images/2020-11-17-23-21-59.png" alt=""></p>
<p>给<code>master</code>分支设置分支策略，具体策略可根据实际项目情况设置</p>
<p><img src="/images/2020-11-17-23-22-07.png" alt=""></p>
<pre><code>发布到远程存储库时遇到错误: rejected master -&gt; master (TF402455: 不允许推送到此分支；必须使用拉取请求来更新此分支。)
Switched to branch 'feature/dev'
已签出 feature/dev
已在存储库 C:\Users\WangPengLiang\Desktop\TestApplication 中本地创建提交 41b86a08
正在推送 feature/dev
Enumerating objects: 7, done.
Delta compression using up to 8 threads
Total 4 (delta 3), reused 0 (delta 0)
POST git-receive-pack (494 bytes)
Pushing to http://localhost:9090/DefaultCollection/TestApplication/_git/TestApplication
To http://localhost:9090/DefaultCollection/TestApplication/_git/TestApplication
 * [new branch]      feature/dev -&gt; feature/dev
updating local tracking ref 'refs/remotes/origin/feature/dev'
Branch 'feature/dev' set up to track remote branch 'feature/dev' from 'origin'.
</code></pre>
<p>至此，<code>master</code>分支不再支持直接push代码，必须要通过<code>pull-request</code>的方式进行代码合并，<code>code review</code>可以在这个时候来做，保证代码质量</p>
<h2 id="CI-持续集成">CI(持续集成)</h2>
<p>创建CI管道，由一系列Job组成的一个CI过程</p>
<p><img src="/images/2020-11-17-23-23-30.png" alt=""></p>
<h2 id="任务组">任务组</h2>
<p>将一组<code>Job</code>封装成一个<code>Task Group</code>，以便所有CI管道都可使用</p>
<p><img src="/images/2020-11-17-23-24-56.png" alt=""></p>
<p><img src="/images/2020-11-17-23-25-11.png" alt=""></p>
<h2 id="持续集成">持续集成</h2>
<p>分支有代码更改时自动触发<code>CI</code>，不再需要人工去做代码合并和发布，系统会自动构建并运行不同级别的自动化测试（如果写了测试的话）来验证更改</p>
<p><img src="/images/2020-11-17-23-26-18.png" alt=""></p>
<p>也可手动运行<br>
<img src="/images/2020-11-17-23-26-37.png" alt=""></p>
<h2 id="CD-持续交付-持续部署">CD(持续交付/持续部署)</h2>
<p>设置部署对应信息，选择对应的<code>CI</code>源</p>
<p><img src="/images/2020-11-17-23-27-03.png" alt=""></p>
<h2 id="多环境发布">多环境发布</h2>
<ul>
<li>创建多环境，CD中添加多个CI管道源实现多环境发布</li>
<li>CD中使用替换环境变量的方式实现多环境发布</li>
</ul>
<h2 id="变量替换">变量替换</h2>
<p>参考：<a href="https://docs.microsoft.com/en-us/azure/devops/pipelines/tasks/transforms-variable-substitution?view=azure-devops&amp;viewFallbackFrom=vsts&amp;tabs=Classic#json-variable-substitution" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/azure/devops/pipelines/tasks/transforms-variable-substitution?view=azure-devops&amp;viewFallbackFrom=vsts&amp;tabs=Classic#json-variable-substitution</a></p>
<h2 id="备注">备注</h2>
<p>这里只是简单过了一遍，具体细节还要自己多尝试才能熟练运用。还有一些相关知识，<code>部署池</code>、<code>代理池</code>、<code>变量组</code>、<code>定时运行CI/CD</code>、等后续总结</p>
]]></content>
      <categories>
        <category>AzureDevops</category>
      </categories>
  </entry>
  <entry>
    <title>SonarQube安装配置</title>
    <url>/2020/11/17/sonarqube/SonarQube%E5%AE%89%E8%A3%85%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<p>SonarQube是一种自动代码审查工具，用于检测代码中的错误，漏洞和冗余代码。可以与现有的工作流程集成，以便在项目分支和拉取请求之间进行连续的代码检查</p>
<a id="more"></a>
<h2 id="简介">简介</h2>
<p>SonarQube集成<code>Azure Devops</code>，在代码构建的时候自动进行代码分析，生成代码报告</p>
<p>SonarQube集成<code>gitlab</code>，在分支推送的时候自动进行代码检查分析，生成代码报告</p>
<p>SonarQube集成<code>jenkins</code>，在代码构建的时候自动进行代码分析，生成代码报告</p>
<p>总之，SonarQube可以配合一切好用的CI/CD工具进行自动化分析代码并生成代码报告，通过报告可以看到代码中可能存在的bug和代码债务</p>
<h2 id="安装">安装</h2>
<p>注意</p>
<ul>
<li>SonarQube 依赖于Java 环境，也就是需要安装Jre，不同版本对Jre的版本要求不同</li>
<li>SonarQube 支持的数据库有：PostgreSQL，Microsoft SQL Server，Oracle，7.9版本已经不对MySql进行官方支持</li>
</ul>
<p>下载地址：<a href="https://www.sonarqube.org/downloads" target="_blank" rel="noopener">https://www.sonarqube.org/downloads/</a></p>
<p>文档地址：<a href="https://docs.sonarqube.org/latest/" target="_blank" rel="noopener">https://docs.sonarqube.org/latest/</a></p>
<p><img src="/images/2020-11-17-22-48-17.png" alt=""></p>
<p>目录</p>
<ul>
<li><code>bin</code>目录存放各环境的启动脚本</li>
<li><code>conf</code>目录存放sonarqube的配置文件</li>
<li><code>logs</code>目录存放启动和运行时的日志文件</li>
</ul>
<h2 id="配置">配置</h2>
<p>数据库连接字符串配置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">conf&#x2F;sonar.properties</span><br></pre></td></tr></table></figure>
<p>启动SonarQube</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bin&#x2F;$(SystemName)&#x2F;StartSonar.bat</span><br></pre></td></tr></table></figure>
<p>访问Web页面,默认端口<code>9000</code></p>
<p><img src="/images/2020-11-17-22-49-26.png" alt=""></p>
<h2 id="插件安装">插件安装</h2>
<p>这里是安装了中文插件的结果,其余自行发挥</p>
<p><img src="/images/2020-11-17-22-50-25.png" alt=""></p>
<h2 id="SonarQube连接MSSQL">SonarQube连接MSSQL</h2>
<p>参考<br>
<a href="http://www.codewrecks.com/blog/index.php/2015/10/30/installing-sonarqube-on-windows-and-sql-server/" target="_blank" rel="noopener">http://www.codewrecks.com/blog/index.php/2015/10/30/installing-sonarqube-on-windows-and-sql-server/</a></p>
<h2 id="其他">其他</h2>
<ul>
<li><code>/key</code>(简写k)：对应projectKey即项目的唯一代码，如两套源代码使用同一个projectKey那扫描的结果将混在一起，所以一个项目需要有一个单独的projectKey</li>
<li><code>/name</code>(简写n)：对应projectName即项目的名称，为项目的一个显示的名称，建立使用完整的项目名称</li>
<li><code>/version</code>(简写v)：对应projectVersion即项目的版本，项目在不同的时期版本也是不一样的，如果方便，可以在SonarQube的服务器中查看到不同的版本代码其中问题的变化</li>
</ul>
<p>SQLServer2016打开配置管理器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SQLServerManager13.msc</span><br></pre></td></tr></table></figure>
<p>es端口更改</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\SonarQube\temp\conf\es\elasticsearch.yml</span><br></pre></td></tr></table></figure>
<p>Sonar端口更改</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SonarQube\conf\sonar.properties</span><br></pre></td></tr></table></figure>
<p>参考：</p>
<ul>
<li><a href="https://sonarcloud.io/documentation/analysis/scan/sonarscanner-for-msbuild/" target="_blank" rel="noopener">https://sonarcloud.io/documentation/analysis/scan/sonarscanner-for-msbuild/</a></li>
<li><a href="https://www.cnblogs.com/jinjiangongzuoshi/p/11648785.html" target="_blank" rel="noopener">https://www.cnblogs.com/jinjiangongzuoshi/p/11648785.html</a></li>
<li><a href="https://www.cnblogs.com/7tiny/p/11342902.html" target="_blank" rel="noopener">https://www.cnblogs.com/7tiny/p/11342902.html</a></li>
</ul>
]]></content>
      <categories>
        <category>SonarQube</category>
      </categories>
  </entry>
  <entry>
    <title>SonarQube集成AzureDevops</title>
    <url>/2020/11/17/sonarqube/SonarQube%E9%9B%86%E6%88%90AzureDevops/</url>
    <content><![CDATA[<p>介绍如何将SonarQube集成到<code>Azure Devops</code></p>
<a id="more"></a>
<h2 id="CI管道添加SonarQube">CI管道添加SonarQube</h2>
<p><img src="/images/2020-11-17-23-10-58.png" alt=""></p>
<p>大象装冰箱第一步<br>
<img src="/images/2020-11-17-23-11-42.png" alt=""></p>
<p><code>ServerUrl</code>:SonarQube地址</p>
<p><code>Token</code>:身份验证所需令牌，在SonarQube中生成<br>
<img src="/images/2020-11-17-23-11-51.png" alt=""></p>
<h2 id="配置">配置</h2>
<p>这里因为jdk问题，没有使用SonarQube提供的job而是使用PowerShell脚本方式执行，没什么区别<br>
<img src="/images/2020-11-17-23-12-00.png" alt=""></p>
<p><img src="/images/2020-11-17-23-12-10.png" alt=""></p>
<p><img src="/images/2020-11-17-23-12-21.png" alt=""></p>
<p>实际情况可以结合<code>AzureDevops</code>的变量将对应配置参数化</p>
]]></content>
      <categories>
        <category>SonarQube</category>
      </categories>
  </entry>
  <entry>
    <title>SonarQube代码检查</title>
    <url>/2020/11/17/sonarqube/SonarQube%E4%BB%A3%E7%A0%81%E6%A3%80%E6%9F%A5/</url>
    <content><![CDATA[<p>介绍如何使用SonarQube对<code>dotnetcore</code>项目进行代码检查</p>
<a id="more"></a>
<h2 id="创建项目">创建项目</h2>
<p>创建一个.dotnet core项目</p>
<p><img src="/images/2020-11-17-22-58-22.png" alt=""></p>
<p>推荐安装vs扩展<code>SonarLint for Visual Studio 2019</code>,在开发阶段就可以发现代码中可能存在的问题</p>
<h2 id="使用SonarQube分析代码">使用SonarQube分析代码</h2>
<p>安装<code>MSBuild .NET Core Global Tool</code>全局工具</p>
<p><a href="https://docs.sonarqube.org/latest/analysis/scan/sonarscanner-for-msbuild/" target="_blank" rel="noopener">相关工具及命令</a></p>
<p>大象放冰箱总共分三步</p>
<ol>
<li>开始分析<br>
这里使用了指定的SonarScanner.MSBuild版本</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dotnet D:\SonarScannerMsbuild\SonarScanner.MSBuild.dll begin &#x2F;k:项目唯一编码 &#x2F;n:显示的项目名称 &#x2F;v:当前执行活动号（可以动态递增或使用时间戳）</span><br></pre></td></tr></table></figure>
<ol start="2">
<li>编译</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dotnet build $&#123;xx.csproj or xx.sln&#125;</span><br></pre></td></tr></table></figure>
<ol start="3">
<li>将分析结果推送到SonarQube站点</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dotnet sonarscanner end &#x2F;d:sonar.login&#x3D;&quot;账号&quot; &#x2F;d:sonar.password&#x3D;&quot;密码&quot;</span><br></pre></td></tr></table></figure>
<h2 id="问题记录">问题记录</h2>
<p><code>MSBuild</code>版本问题</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\WangPengLiang\Desktop\TestApplication\.sonarqube\bin\targets\SonarQube.Integration.targets(145,5): error : SonarQube analysis is only supported with MSBuild 14 or MSBuild 15 [C:\Users\WangPengLiang\Desktop\TestApplication\TestApplication\TestApplication.csproj]</span><br></pre></td></tr></table></figure>
<p>解决方法<br>
安装<code>MSBuild</code>14以上版本，运行时使用指定的<code>MSBuild</code>版本进行<code>build</code></p>
<p><img src="/images/2020-11-17-23-01-32.png" alt=""></p>
<p>示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dotnet D:\SonarScannerMsbuild\SonarScanner.MSBuild.dll begin &#x2F;k:testApplication &#x2F;n:TestApplication &#x2F;v:1.0 &#x2F;d:sonar.host.url&#x3D;&quot;http:&#x2F;&#x2F;localhost:9000&quot; &#x2F;d:sonar.login&#x3D;&quot;admin&quot; &#x2F;d:sonar.password&#x3D;&quot;admin&quot;</span><br><span class="line"></span><br><span class="line">dotnet build TestApplication.sln</span><br><span class="line"></span><br><span class="line">dotnet sonarscanner end &#x2F;d:sonar.login&#x3D;&quot;admin&quot; &#x2F;d:sonar.password&#x3D;&quot;admin&quot;</span><br></pre></td></tr></table></figure>
<p><img src="/images/2020-11-17-23-02-20.png" alt=""></p>
<p><img src="/images/2020-11-17-23-02-39.png" alt=""></p>
<p><img src="/images/2020-11-17-23-03-39.png" alt=""></p>
<p><img src="/images/2020-11-17-23-04-13.png" alt=""></p>
<p><img src="/images/2020-11-17-23-04-31.png" alt=""></p>
<h2 id="单元测试覆盖率">单元测试覆盖率</h2>
<p>SonarQube只能通过已经生成的测试结果文件和代码覆盖率结果文件来展示覆盖率，如何生成测试结果文件参考测试相关笔记，SonarQube只需要在对应项目配置找到对应开发语言下设置测试结果文件和覆盖率文件存储位置即可</p>
<p><img src="/images/2020-11-17-23-05-01.png" alt=""></p>
<h2 id="SonarQube-多分支扫描">SonarQube 多分支扫描</h2>
<ul>
<li>SonarQube Community 版本不支持多分支扫描</li>
<li>SonarQube Developer Edition: 及以上版本支持多分支扫描</li>
</ul>
<p>想实现社区版多分支扫描可以使用两种方式来做</p>
<ul>
<li>开源插件：<code>sonarqube-community-branch-plugin</code></li>
<li>替换 <code>sonar.projectKey</code>，porjectKey 相等于 Sonar 中每个项目的主键 ID，替换后就会以新项目创建</li>
</ul>
<p>这里要注意<code>sonarqube-community-branch-plugin</code>对<code>SonarQube</code>版本支持有限制，先看好自己<code>SonarQube</code>是什么版本</p>
<p><img src="/images/2020-11-17-23-05-12.png" alt=""></p>
<p>我的是8.4，下载对应版本的开源插件</p>
<p><a href="https://github.com/mc1arke/sonarqube-community-branch-plugin/releases" target="_blank" rel="noopener">下载地址</a></p>
<p>下载插件放到<code>${SONAR_HOME}/extensions/plugins</code>和<code>${SONAR_HOME}/lib/common</code>目录下，重启SonarQube</p>
<p>扫描时，增加<code>-d sonar.branch.name=${GIT_BRANCH}</code>即可</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dotnet D:\SonarScannerMsbuild\SonarScanner.MSBuild.dll begin &#x2F;k:test &#x2F;n:test &#x2F;v:2.0 &#x2F;d:sonar.host.url&#x3D;&quot;http:&#x2F;&#x2F;localhost:9000&quot; &#x2F;d:sonar.login&#x3D;&quot;admin&quot; &#x2F;d:sonar.password&#x3D;&quot;admin&quot; &#x2F;d:sonar.branch.name&#x3D;&#39;$(Build.SourceBranchName)&#39;</span><br></pre></td></tr></table></figure>
<p><img src="/images/2020-11-17-23-06-01.png" alt=""></p>
]]></content>
      <categories>
        <category>SonarQube</category>
      </categories>
  </entry>
  <entry>
    <title>四种Exchange介绍及代码示例</title>
    <url>/2020/11/17/rabbitMQ/%E5%9B%9B%E7%A7%8DExchange%E4%BB%8B%E7%BB%8D%E5%8F%8A%E4%BB%A3%E7%A0%81%E7%A4%BA%E4%BE%8B/</url>
    <content><![CDATA[<h2 id="ExchangesType">ExchangesType</h2>
<p>发布,路由,通配符这三种模式可以算为一种模式，区别仅仅是交互机类型不同.发送者将消息发送发送到交换机，接收者创建各自的消息队列绑定到交换机</p>
<p><img src="/images/2020-11-17-23-43-49.png" alt=""></p>
<h3 id="worker">worker</h3>
<blockquote>
<p>worker模式其实是一对多的模式,但是这个一对多并不是像发布订阅那种,而是信息以顺序的传输给每个接收者</p>
</blockquote>
<p>生产者</p>
<pre><code class="language-cs">   static void Main(string[] args)
        {
            while (true)
            {
                Console.WriteLine(&quot;Publisher(Worker):Input Message Content:&quot;);
                string message = Console.ReadLine();
                if (!string.IsNullOrEmpty(message))
                {
                    // RabbitMQ连接工厂
                    var factory = new ConnectionFactory()
                    {
                        HostName = &quot;localhost&quot;,
                        // 用户名
                        UserName = &quot;guest&quot;,
                        // 密码
                        Password = &quot;guest&quot;,
                        // 网络故障自动恢复连接
                        AutomaticRecoveryEnabled = true,
                        // 心跳处理
                        RequestedHeartbeat = new TimeSpan(5000)
                    };
                    // 建立连接
                    using var connection = factory.CreateConnection();
                    // 创建信道
                    using var channel = connection.CreateModel();
                    // 创建名称为hello的队列
                    channel.QueueDeclare(&quot;hello&quot;, false, false, false, null);
                    // 构建消息数据包
                    byte[] body = Encoding.UTF8.GetBytes(message);

                    // 消息发送
                    channel.BasicPublish(
                        exchange: &quot;&quot;,
                        routingKey: &quot;hello&quot;,
                        basicProperties: null,
                        body: body);
                }
            }
        }
</code></pre>
<p>消费者</p>
<pre><code class="language-cs">  static void Main(string[] args)
        {
            Console.WriteLine($&quot;{nameof(WorkerConsumerClient1)}:&quot;);
            // RabbitMQ连接工厂
            var factory = new ConnectionFactory()
            {
                HostName = &quot;localhost&quot;,
                // 用户名
                UserName = &quot;guest&quot;,
                // 密码
                Password = &quot;guest&quot;,
                // 网络故障自动恢复连接
                AutomaticRecoveryEnabled = true,
                // 心跳处理
                RequestedHeartbeat = new TimeSpan(5000)
            };
            // 建立连接
            using var connection = factory.CreateConnection();
            // 创建信道
            using var channel = connection.CreateModel();

            string queueName = &quot;hello&quot;;
            // 申明队列
            channel.QueueDeclare(
                queue: queueName,
                durable: false,
                exclusive: false,
                autoDelete: false,
                arguments: null);

            EventingBasicConsumer consumer =
                new EventingBasicConsumer(channel);

            // 每次只能向消费者发送一条信息,在消费者未确认之前,不再向它发送信息
            channel.BasicQos(0, 1, false);
            // 绑定消息接收后的事件委托
            consumer.Received += (model, ea) =&gt;
            {
                string message =
                       Encoding.UTF8.GetString(ea.Body.ToArray());
                Console.WriteLine($&quot;Message:{message}&quot;);

                channel.BasicAck(
                    deliveryTag: ea.DeliveryTag,
                    // 是否一次性确认多条数据
                    multiple: false);
            };
            channel.BasicConsume(queue: queueName,
                autoAck: false,
                consumer: consumer);

            Console.ReadLine();
        }
</code></pre>
<p>这里定义了两个消费者来消费Queue,结果如下:</p>
<p><img src="/images/2020-11-17-23-44-04.png" alt=""></p>
<h3 id="fanout">fanout</h3>
<blockquote>
<p>发布订阅模式(fanout),消息发送到Exchange,所有订阅了当前Exchange的Queue都可以收到消息</p>
</blockquote>
<p>发布</p>
<pre><code class="language-cs">var factory = new ConnectionFactory()
{
    HostName = &quot;localhost&quot;,
    // 用户名
    UserName = &quot;guest&quot;,
    // 密码
    Password = &quot;guest&quot;,
    // 网络故障自动恢复连接
    AutomaticRecoveryEnabled = true,
    // 心跳处理
    RequestedHeartbeat = new TimeSpan(5000)
};
using var connection = factory.CreateConnection();
using var channel = connection.CreateModel();

string exchangeName = $&quot;testExchange_fanout&quot;;
// 声明交换机
channel.ExchangeDeclare(
    exchange: exchangeName,
    type: &quot;fanout&quot;);

byte[] body = Encoding.UTF8.GetBytes(message);

channel.BasicPublish(
    exchange: exchangeName,
    routingKey: &quot;&quot;,
    basicProperties: null,
    body: body);


</code></pre>
<p>消费端</p>
<pre><code class="language-cs">// RabbitMQ连接工厂
var factory = new ConnectionFactory()
{
    HostName = &quot;localhost&quot;,
    // 用户名
    UserName = &quot;guest&quot;,
    // 密码
    Password = &quot;guest&quot;,
    // 网络故障自动恢复连接
    AutomaticRecoveryEnabled = true,
    // 心跳处理
    RequestedHeartbeat = new TimeSpan(5000)
};
// 建立连接
using var connection = factory.CreateConnection();
// 创建信道
using var channel = connection.CreateModel();

string exchangeName = $&quot;testExchange_fanout&quot;;
//声明交换机并指定类型
channel.ExchangeDeclare(
    exchange: exchangeName,
    type: &quot;fanout&quot;);

string queueName = $&quot;{exchangeName}_{nameof(ExchangeFanoutConsumerClient1)}&quot;；
//声明队列
channel.QueueDeclare(queue: queueName,
    durable: false,
    exclusive: false,
    autoDelete: false,
    arguments: null);

//将队列与交换机进行绑定
channel.QueueBind(
    queue: queueName,
    exchange: exchangeName,
    routingKey: &quot;&quot;);

EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

// 每次只能向消费者发送一条信息,在消费者未确认之前,不再向它发送信息
channel.BasicQos(0, 1, false);
// 绑定消息接收后的事件委托
consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
    Console.WriteLine($&quot;Message:{message}&quot;);

    channel.BasicAck(
        deliveryTag: ea.DeliveryTag,
        // 是否一次性确认多条数据
        multiple: false);
};
channel.BasicConsume(queue: queueName,
    autoAck: false,
    consumer: consumer);
</code></pre>
<h3 id="direct">direct</h3>
<blockquote>
<p>direct跟Fanout的区别在于多了routekey,消息发送到Exchange,所有订阅了当前Exchange并且routekey完全匹配的Queue都可以收到消息</p>
</blockquote>
<p>发布</p>
<pre><code class="language-cs">var factory = new ConnectionFactory()
{
    HostName = &quot;localhost&quot;,
    // 用户名
    UserName = &quot;guest&quot;,
    // 密码
    Password = &quot;guest&quot;,
    // 网络故障自动恢复连接
    AutomaticRecoveryEnabled = true,
    // 心跳处理
    RequestedHeartbeat = new TimeSpan(5000)
};
using var connection = factory.CreateConnection();
using var channel = connection.CreateModel();

string exchangeName = $&quot;testExchange_direct&quot;;

string routeKeyName = &quot;testExchange_routeKey&quot;;

// 声明交换机并设置类型为direct
channel.ExchangeDeclare(
    exchange: exchangeName,
    type: &quot;direct&quot;);

byte[] body = Encoding.UTF8.GetBytes(message);

channel.BasicPublish(
    exchange: exchangeName,
    routingKey: routeKeyName,
    basicProperties: null,
    body: body);

</code></pre>
<p>消费端</p>
<pre><code class="language-cs">// RabbitMQ连接工厂
var factory = new ConnectionFactory()
{
    HostName = &quot;localhost&quot;,
    // 用户名
    UserName = &quot;guest&quot;,
    // 密码
    Password = &quot;guest&quot;,
    // 网络故障自动恢复连接
    AutomaticRecoveryEnabled = true,
    // 心跳处理
    RequestedHeartbeat = new TimeSpan(5000)
};
// 建立连接
using var connection = factory.CreateConnection();
// 创建信道
using var channel = connection.CreateModel();

string exchangeName = $&quot;testExchange_direct&quot;;

string routeKey = &quot;testExchange_routeKey&quot;;

//声明交换机并指定类型
channel.ExchangeDeclare(
    exchange: exchangeName,
    type: &quot;direct&quot;);

string queueName = $&quot;{exchangeName}_{nameof(ExchangeDirectConsumerClient1)}&quot;;
// 声明队列
channel.QueueDeclare(queue: queueName,
    durable: false,
    exclusive: false,
    autoDelete: false,
    arguments: null);

// 将队列与交换机进行绑定
channel.QueueBind(queue: queueName,
    exchange: exchangeName,
    routingKey: routeKey);

EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

channel.BasicQos(0, 1, false);

consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
    Console.WriteLine($&quot;Message:{message}&quot;);

    channel.BasicAck(
        deliveryTag: ea.DeliveryTag,
        // 是否一次性确认多条数据
        multiple: false);
};
channel.BasicConsume(queue: queueName,
      autoAck: false,
      consumer: consumer);
</code></pre>
<h3 id="topic">topic</h3>
<blockquote>
<p>topic符模式与路由模式一致，只不过通配符模式中的路由可以声明为模糊查询，RabbitMQ拥有两个通配符</p>
</blockquote>
<ul>
<li>#：匹配0-n个字符语句</li>
<li>*：匹配一个字符语句</li>
</ul>
<p>注意：RabbitMQ中通配符并不像正则中的单个字符，而是一个以“.”分割的字符串，如 ”topic1.*“匹配的规则以topic1开始并且&quot;.&quot;后只有一段语句的路由<br>
例：“topic1.aaa”，“<a href="http://topic1.bb" target="_blank" rel="noopener">topic1.bb</a>”</p>
<p>发布</p>
<pre><code class="language-cs">var factory = new ConnectionFactory()
{
    HostName = &quot;localhost&quot;,
    // 用户名
    UserName = &quot;guest&quot;,
    // 密码
    Password = &quot;guest&quot;,
    // 网络故障自动恢复连接
    AutomaticRecoveryEnabled = true,
    // 心跳处理
    RequestedHeartbeat = new TimeSpan(5000)
};
using var connection = factory.CreateConnection();
using var channel = connection.CreateModel();

string exchangeName = $&quot;testExchange_topic&quot;;

string routeKeyName = &quot;testExchange_routeKey.*&quot;;

// 声明交换机并设置类型为topic
channel.ExchangeDeclare(
    exchange: exchangeName,
    type: &quot;topic&quot;);

byte[] body = Encoding.UTF8.GetBytes(message);

channel.BasicPublish(
    exchange: exchangeName,
    routingKey: routeKeyName,
    basicProperties: null,
    body: body);
</code></pre>
<p>消费端</p>
<pre><code class="language-cs"> // RabbitMQ连接工厂
 var factory = new ConnectionFactory()
 {
     HostName = &quot;localhost&quot;,
     // 用户名
     UserName = &quot;guest&quot;,
     // 密码
     Password = &quot;guest&quot;,
     // 网络故障自动恢复连接
     AutomaticRecoveryEnabled = true,
     // 心跳处理
     RequestedHeartbeat = new TimeSpan(5000)
 };
 // 建立连接
 using var connection = factory.CreateConnection();
 // 创建信道
 using var channel = connection.CreateModel();

 string exchangeName = $&quot;testExchange_topic&quot;;

 string routeKey = &quot;testExchange_routeKey.*&quot;;

 //声明交换机并指定类型
 channel.ExchangeDeclare(
     exchange: exchangeName,
     type: &quot;topic&quot;);

 string queueName = $&quot;{exchangeName}_{nameof(ExchangeTopicConsumerClient1)}&quot;;
 // 声明队列
 channel.QueueDeclare(queue: queueName,
     durable: false,
     exclusive: false,
     autoDelete: false,
     arguments: null);

 // 将队列与交换机进行绑定
 channel.QueueBind(queue: queueName,
     exchange: exchangeName,
     routingKey: routeKey);

 EventingBasicConsumer consumer =
     new EventingBasicConsumer(channel);

 channel.BasicQos(0, 1, false);

 consumer.Received += (model, ea) =&gt;
 {
     string message =
            Encoding.UTF8.GetString(ea.Body.ToArray());
     Console.WriteLine($&quot;Message:{message}&quot;);

     channel.BasicAck(
         deliveryTag: ea.DeliveryTag,
         // 是否一次性确认多条数据
         multiple: false);
 };
 channel.BasicConsume(queue: queueName,
       autoAck: false,
       consumer: consumer);
</code></pre>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>消息确认及持久化</title>
    <url>/2020/11/17/rabbitMQ/%E6%B6%88%E6%81%AF%E7%A1%AE%E8%AE%A4%E5%8F%8A%E6%8C%81%E4%B9%85%E5%8C%96/</url>
    <content><![CDATA[<h2 id="消息确认机制">消息确认机制</h2>
<h3 id="生产端消息确认">生产端消息确认</h3>
<h4 id="tx机制">tx机制</h4>
<blockquote>
<p>tx机制可以叫做事务机制，RabbitMQ中有三个与tx机制的方法：<code>txSelect()</code>, <code>txCommit()</code>和<code>txRollback()</code>。 channel.txSelect() 用于将当前channel设置成transaction模式， channel.txCommit() 提交事务， channel.txRollback() 回滚事务。使用tx机制，首先要通过txSelect方法开启事务，然后发布消息给broker服务器，如果txCommit提交成功，则说明消息成功被broker接收;如果在txCommit执行之前broker异常崩溃或者由于其他原因抛出异常，这个时候可以捕获异常，通过txRollback回滚事务</p>
</blockquote>
<pre><code class="language-cs">// 创建名称为hello的队列
channel.QueueDeclare(&quot;hello&quot;, false, false, false, null);
for (int i = 0; i &lt; 5; i++)
{
    // 构建消息数据包
    byte[] body = Encoding.UTF8.GetBytes(i.ToString());
    try
    {
        // 开启事务机制
        channel.TxSelect();

        // 消息发送
        channel.BasicPublish(
            exchange: &quot;&quot;,
            routingKey: &quot;hello&quot;,
            basicProperties: null,
            body: body);

        //事务提交
        channel.TxCommit();
    }
    catch (Exception ex)
    {
        channel.TxRollback();
        Assert.Fail(ex.Message);
    }
}

</code></pre>
<h4 id="Confirm模式">Confirm模式</h4>
<blockquote>
<p>C#的RabbitMQ API中，有三个与Confirm相关的方法：<code>ConfirmSelect()</code>,<code>WaitForConfirms()</code>和<code>WaitForConfirmOrDie</code>。 <code>channel.ConfirmSelect()</code> 表示开启Confirm模式； <code>channel.WaitForConfirms()</code> 等待所有消息确认，如果所有的消息都被服务端成功接收返回true，只要有一条没有被成功接收就返回false。 <code>channel.WaitForConfirmsOrDie()</code>和<code>WaitForConfirms</code>作用类型，也是等待所有消息确认，区别在于该方法没有返回值(Void)，如果有任意一条消息没有被成功接收，该方法会立即抛出一个<code>OperationInterrupedException</code>类型异常</p>
</blockquote>
<pre><code class="language-cs">channel.QueueDeclare(
    queue: &quot;hello&quot;,
    durable: false,
    exclusive: false,
    autoDelete: false,
    arguments: null);

string message = &quot;hello world&quot;;
byte[] messageBody = Encoding.UTF8.GetBytes(message);

// 开启Confirm模式
channel.ConfirmSelect();

// 消息发送
channel.BasicPublish(
        exchange: &quot;&quot;,
        routingKey: &quot;hello&quot;,
        basicProperties: null,
        body: messageBody);

// WaitForConfirms确认消息(可以同时确认多条消息)是否发送成功
if (channel.WaitForConfirms())
{
    Console.WriteLine($&quot;Message发送成功&quot;);
}
else
{
    Assert.Fail();
}
</code></pre>
<h3 id="消费端消息确认">消费端消息确认</h3>
<h4 id="自动确认">自动确认</h4>
<blockquote>
<p>当RabbbitMQ将消息发送给消费者后，消费者端接收到消息后，不等待消息处理结束，立即自动回送一个确认回执。自动确认的用法十分简单，设置消费方法的参数autoAck为true即可</p>
</blockquote>
<pre><code class="language-cs">EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
};
channel.BasicConsume(queue: &quot;hello&quot;, autoAck: true, consumer: consumer);
</code></pre>
<p>可能存在的问题</p>
<ol>
<li>丢失数据:：Broker会在接收到确认回执时删除消息，如果消费者接收到消息并返回了确认回执，然后这个消费者在处理消息时挂了，那么这条消息就再也找不回来了</li>
<li>只要队列不空，RabbitMQ会源源不断的把消息推送给客户端，而不管客户端能否消费的完，如果其中一个消费端消费的较慢,会极大的浪费性能</li>
</ol>
<h4 id="手动确认-BasicAck">手动确认(BasicAck)</h4>
<blockquote>
<p>消费从队列中获取消息后,服务器会将该消息处于不可用状态,等待消费者反馈,Resume方法的参数autoAck设置为false，然后在消费端使用代码 <code>channel.BasicAck()</code>/<code>BasicReject()</code>等方法来确认和拒绝消息即可实现手动确认</p>
</blockquote>
<pre><code class="language-cs">EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());

    channel.BasicAck(
        deliveryTag: ea.DeliveryTag,
        multiple: false);
};

channel.BasicConsume(queue: &quot;hello&quot;,
    autoAck: false,
    consumer: consumer);
</code></pre>
<p>改为手动确认方式只需改两处</p>
<ol>
<li>开启监听时将autoAck参数改为false,</li>
<li>消息消费成功后返回确认</li>
</ol>
<p>这段代码中，先处理消息，完成后，再做ack响应，失败就不做ack响应，这样消息会储存在MQ的Unacked消息里，不会丢失，看起来没啥问题，但是如果其中一条消息在处理时抛出了异常，将导致后续所有消息都会无法消费</p>
<h2 id="消息拒绝">消息拒绝</h2>
<h3 id="BasicNack">BasicNack()</h3>
<blockquote>
<p>与<code>BasicReject()</code>不同的是同时支持多个消息，可以nack该消费者先前接收未ack的所有消息</p>
</blockquote>
<pre><code class="language-cs">EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
    try
    {
        /* 消费到某条消息时出错
         * 导致Broker无法拿到正常回执信息引发后续消息都无法被正常消费
         * 如果MQ没得到ack响应，这些消息会堆积在Unacked消息里,不会丢弃,直至客户端断开重连时，才变回ready
         * 如果Consumer客户端不断开连接，这些Unacked消息，永远不会变回ready状态
         * Unacked消息多了,占用内存越来越大,就会异常
         */
        MessageConsumer(ea);
        channel.BasicAck(
                   deliveryTag: ea.DeliveryTag,
                   multiple: false);
    }
    catch (Exception ex)
    {
        // 出错了，发nack，并通知MQ把消息塞回的队列头部（不是尾部）
        channel.BasicNack(
            deliveryTag: ea.DeliveryTag,
            multiple: false,
            requeue: true);
    }
};

channel.BasicConsume(queue: &quot;hello&quot;,
    autoAck: false,
    consumer: consumer);
</code></pre>
<p>这里将代码调整为消费正常就ack，不正常就nack，并等下一次重新消费。看起来没问题，但是如果某条消息在消费时又抛出异常，该消息将会被<code>Nack</code>机制重新扔回<strong>队列头部</strong>，下一步又消费这条会出异常的消息，又出错，塞回队列……进入死循环，所以要谨慎使用<code>Nack</code>机制，这里可以在<code>catch</code>中记录错误日志依旧使用<code>ack</code>确认消费</p>
<h3 id="BasicReject">BasicReject()</h3>
<blockquote>
<p>消费端告诉服务器这个消息拒绝接收,不处理,可以设置是否放回到队列中还是丢掉，注意(只能一次拒绝一个消息)</p>
</blockquote>
<pre><code class="language-cs"> MessagePublisher(&quot;hello&quot;, $&quot;1&quot;);
 MessagePublisher(&quot;hello&quot;, $&quot;2&quot;);
 MessagePublisher(&quot;hello&quot;, $&quot;3&quot;);

 channel.QueueDeclare(
     queue: &quot;hello&quot;,
     durable: false,
     exclusive: false,
     autoDelete: false,
     arguments: null);

 EventingBasicConsumer consumer =
     new EventingBasicConsumer(channel);

 channel.BasicQos(0, 1, false);

 consumer.Received += (model, ea) =&gt;
 {
     string message =
            Encoding.UTF8.GetString(ea.Body.ToArray());

     if (message == &quot;2&quot;)
     {
         Console.WriteLine($&quot;Message：{message}&quot;);
         channel.BasicAck(
             deliveryTag: ea.DeliveryTag,
             multiple: false);
     }
     else
     {
         Console.WriteLine($&quot;拒绝处理&quot;);
         /* BasicReject用于拒绝消息
            requeue参数指定了拒绝后是否重新放回queue
            一次只能拒绝一条消息
            设置为true: 消息会被重新仍回queue中
            设置为false:消息将被丢弃
         */
         channel.BasicReject(
             deliveryTag: ea.DeliveryTag,
             requeue: true);
     }
 };

 channel.BasicConsume(queue: &quot;hello&quot;,
     autoAck: false,
     consumer: consumer);

</code></pre>
<h3 id="BasicRecover">BasicRecover()</h3>
<blockquote>
<p>路由不成功的消息可以使用recovery重新发送到队列中,参数是是否requeue，true则重新入队列，并且尽可能的将之前recover的消息投递给其他消费者消费，而不是自己再次消费。false则消息会重新被投递给自己。</p>
</blockquote>
<h2 id="消息持久化-优先级">消息持久化/优先级</h2>
<h3 id="Persistent">Persistent</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>重启RabbitMQ</th>
</tr>
</thead>
<tbody>
<tr>
<td>exchange.durable=fasle/queue.durable=false</td>
<td>exchange/queue将会被丢弃</td>
</tr>
<tr>
<td>exchange.durable=fasle</td>
<td>exchange将会被丢弃</td>
</tr>
<tr>
<td>queue.durable=fasle</td>
<td>queue将会被丢弃</td>
</tr>
<tr>
<td>exchange.durable=fasle/queue.durable=true</td>
<td>exchange将会被丢弃,queue虽然会存在,但队列内消息会全部丢失</td>
</tr>
<tr>
<td>exchange.durable=true/queue.durable=true</td>
<td>exchange/queue会存在,但队列内消息会全部丢失</td>
</tr>
<tr>
<td>exchange.durable=true/queue.durable=true/消息发布时(persistent=true)</td>
<td>消息真正的持久化</td>
</tr>
</tbody>
</table>
<pre><code class="language-cs">for (int i = 0; i &lt; 100; i++)
{
    byte[] messageBody = Encoding.UTF8.GetBytes(i.ToString());

    // 设置消息持久化
    var props = channel.CreateBasicProperties();
    props.Persistent = true;

    // 消息发送
    channel.BasicPublish(
        exchange: &quot;TestExchange&quot;,
        routingKey: &quot;&quot;,
        basicProperties: props,
        body: messageBody);
}
</code></pre>
<h3 id="Priority">Priority</h3>
<blockquote>
<p>queue是先进先出的，即先发送的消息，先被消费。但是在具体业务中可能会遇到要提前处理某些消息的需求，如一个常见的需求：普通客户的消息按先进先出的顺序处理，Vip客户的消息要提前处理。消息实现优先级控制的实现方式是：首先在声明queue是设置队列的x-max-priority属性，然后在publish消息时，设置消息的优先级等级即可</p>
</blockquote>
<p>生产者</p>
<pre><code class="language-cs"> // 声明交换机exchang
 channel.ExchangeDeclare(exchange: &quot;myexchange&quot;,
                         type: ExchangeType.Fanout,
                         durable: true,
                         autoDelete: false,
                         arguments: null);
 // 声明队列queue
 channel.QueueDeclare(queue: &quot;myqueue&quot;,
                    durable: true,
                    exclusive: false,
                    autoDelete: false,
                    arguments: new Dictionary&lt;string, object&gt;() {
                    //队列优先级最高为10，不加x-max-priority的话，消息发布时设置了消息的优先级也不会生效
                    {&quot;x-max-priority&quot;,10 }
                    });
 // 绑定exchange和queue
 channel.QueueBind(queue: &quot;myqueue&quot;, exchange: &quot;myexchange&quot;, routingKey: &quot;mykey&quot;);
 Console.WriteLine(&quot;生产者准备就绪....&quot;);
 // 测试数据
 string[] msgs = { &quot;vip1&quot;, &quot;hello1&quot;, &quot;hello2&quot;, &quot;hello3&quot;, &quot;vip5&quot; };

 // 设置消息优先级
 var props = channel.CreateBasicProperties();
 foreach (string msg in msgs)
 {
     // vip开头的消息，优先级设置为9
     if (msg.StartsWith(&quot;vip&quot;))
     {
         props.Priority = 9;
         channel.BasicPublish(exchange: &quot;myexchange&quot;,
                              routingKey: &quot;mykey&quot;,
                              basicProperties: props,
                              body: Encoding.UTF8.GetBytes(msg));
     }
     // 其他消息优先级为1
     else
     {
         props.Priority = 1;
         channel.BasicPublish(exchange: &quot;myexchange&quot;,
                              routingKey: &quot;mykey&quot;,
                              basicProperties: props,
                              body: Encoding.UTF8.GetBytes(msg));
     }
</code></pre>
<p>消费端</p>
<pre><code class="language-cs">EventingBasicConsumer consumer =
    new EventingBasicConsumer(channel);

// 绑定消息接收后的事件委托
consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
    Console.WriteLine($&quot;Message：{message}&quot;);
    Assert.IsNotNull(message);
};

channel.BasicConsume(
    queue: &quot;myqueue&quot;,
    autoAck: true,
    consumer: consumer);
</code></pre>
<p><img src="/images/2020-11-17-23-46-52.png" alt=""></p>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>两种消费模式和QOS的实现</title>
    <url>/2020/11/17/rabbitMQ/%E4%B8%A4%E7%A7%8D%E6%B6%88%E8%B4%B9%E6%A8%A1%E5%BC%8F%E5%92%8CQOS%E7%9A%84%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h2 id="消费模式">消费模式</h2>
<h3 id="EventingBasicConsumer">EventingBasicConsumer</h3>
<blockquote>
<p>EventingBasicConsumer是发布/订阅模式的消费者，即只要订阅的queue中有了新消息，Broker就会立即把消息推送给消费者，这种模式可以保证消息及时地被消费者接收到。EventingBasicConsumer是长连接的：只需要创建一个Connection,然后在Connection的基础上创建通道channel，消息的发送都是通过channel来执行的，这样可以减少Connection的创建，比较节省资源。之前一直使用的就是<code>EventingBasicConsumer</code>不再赘述</p>
</blockquote>
<h3 id="BasicGet">BasicGet</h3>
<blockquote>
<p>EventingBasicConsumer可以让消费者最及时地获取到消息，使用EventingBasicConsumer模式时消费者在被动的接收消息，即消息是推送过来的，Broker是主动的一方，如果想让消费者作为主动的一方什么时候想要消息了，就自己发送一个请求去找Broker可以使用<code>Get</code>方式。<code>Get</code>方式是短连接的，消费者每次想要消息的时候，首先建立一个connection，发送一次请求，Broker接收到请求后，响应一条消息给消费者，然后断开连接。RabbitMQ中Get方式和HTTP的请求响应流程基本一样，Get方式的实时性比较差，也比较耗费资源</p>
</blockquote>
<pre><code>BasicGetResult result = channel.BasicGet(queue: &quot;test&quot;, autoAck: true);
Assert.IsNotNull(result.Body.ToArray());
Console.WriteLine($&quot;接收到消息{Encoding.UTF8.GetString(result.Body.ToArray())}&quot;);
// 打印exchange和routingKey
Console.WriteLine($&quot;exchange：{result.Exchange},routingKey:{result.RoutingKey}&quot;);

</code></pre>
<h3 id="QueueBaicConsumer">QueueBaicConsumer</h3>
<blockquote>
<p>用法和Get方式类似，QueueBaicConsumer在官方API中标记已过时，不再介绍</p>
</blockquote>
<h2 id="BasicQos">BasicQos</h2>
<ul>
<li>当消息有十万，百万条时，一股脑把消息发送给消费者，可能会造成消费者内存爆满</li>
<li>当消息处理比较慢的时，单一的消费者处理这些消息可能很长时间，自然想到再添加一个消费者加快消息的处理速度，但是这些消息都被原来的消费者接收了，状态为<code>Unacked</code>，所以这些消息不会再发送给新添加的消费者</li>
</ul>
<p>RabbitMQ提供的<code>Qos(服务质量)</code>可以完美解决上边的问题，使用Qos时，Broker不会再把消息全部发送给消费者，可以设置每次传输给消费者的消息条数n，消费者把这n条消息处理完成后，再获取n条数据进行处理，这样就不用担心消息丢失、服务端内存爆满的问题，因为没有发送的消息状态都是Ready，所以新增一个消费者时，消息也可以立即发送给新增的消费者</p>
<p>使用Qos的方式十分简单,在消费端调用<code>channel.BasicQos()</code>方法即可</p>
<pre><code class="language-cs">channel.BasicQos(prefetchSize: 0, prefetchCount: 2, global: false);
</code></pre>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>RabbitMQ常用命令</title>
    <url>/2020/11/21/rabbitMQ/RabbitMQ%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="用户角色分类">用户角色分类</h2>
<h3 id="none">none</h3>
<p>通常就是普通的生产者和消费者,无法登录控制台,不能访问<code>management plugin</code>，通常就是普通的生产者和消费者</p>
<h3 id="management">management</h3>
<p>普通管理者，仅可登陆管理控制台(启用<code>management plugin</code>的情况下)，无法看到节点信息，也无法对<code>policies</code>进行管理。可以通过<code>AMQP</code>做的任何事外加：<br>
列出自己可以通过<code>AMQP</code>登入的<code>virtual hosts</code><br>
查看自己的<code>virtual hosts</code>中的<code>queues, exchanges</code> 和 <code>bindings</code><br>
查看和关闭自己的<code>channels</code> 和 <code>connections</code><br>
查看有关自己的<code>virtual hosts</code>的“全局”的统计信息，包含其他用户在这些<code>virtual hosts</code>中的活动</p>
<h3 id="policymaker">policymaker</h3>
<p>策略制定者，<code>management</code>可以做的任何事外加：<br>
查看、创建和删除自己的<code>virtual hosts</code>所属的<code>policies</code>和<code>parameters</code></p>
<h3 id="monitoring">monitoring</h3>
<p>监控者，<code>management</code>可以做的任何事外加：<br>
列出所有<code>virtual hosts</code>，包括他们不能登录的<code>virtual hosts</code><br>
查看其他用户的<code>connections</code>和<code>channels</code><br>
查看节点级别的数据如<code>clustering</code>和<code>memory</code>使用情况<br>
查看真正的关于所有<code>virtual hosts</code>的全局的统计信息<br>
同时可以查看<code>rabbitmq</code>节点的相关信息(进程数，内存使用情况，磁盘使用情况等)</p>
<h3 id="administrator">administrator</h3>
<p>，超级管理员，<code>policymaker</code>和<code>monitoring</code>可以做的任何事外加:<br>
创建和删除<code>virtual hosts</code><br>
查看、创建和删除<code>users</code><br>
查看创建和删除<code>permissions</code><br>
关闭其他用户的<code>connections</code></p>
<h2 id="RabbitMQ-Control">RabbitMQ Control</h2>
<blockquote>
<p>使用Web管理界面可以实现RabbitMQ的大部分常用功能，但是有些功能WebUI是做不到的，如：开启/关闭RabbitMQ应用程序和集群的管理等。RabbitMQ Control是RabbitMQ的命令行管理工具，可以调用所有的RabbitMQ内置功能，主命令是<code>rabbitmqctl </code></p>
</blockquote>
<h3 id="创建用户">创建用户</h3>
<p><code>rabbitmqctl add_user {用户名} {密码}</code></p>
<h3 id="设置权限">设置权限</h3>
<p><code>rabbitmqctl set_user_tags {用户名} {权限}</code><br>
例：创建一个超级用户</p>
<pre><code>rabbitmqctl add_user admin1 admin1
rabbitmqctl set_user_tags admin1 administrator
</code></pre>
<h3 id="查看用户列表">查看用户列表</h3>
<p><code>rabbitmqctl list_users</code></p>
<h3 id="为用户赋权">为用户赋权</h3>
<pre><code>// 使用户user1具有vhost1这个virtual host中所有资源的配置、写、读权限以便管理其中的资源
rabbitmqctl  set_permissions -p vhost1 user1 '.*' '.*' '.*' 
</code></pre>
<h3 id="查看权限">查看权限</h3>
<p><code>rabbitmqctl list_user_permissions user1</code><br>
<code>rabbitmqctl list_permissions -p vhost1</code></p>
<h3 id="清除权限">清除权限</h3>
<p><code>rabbitmqctl clear_permissions [-p VHostPath] User</code></p>
<h3 id="删除用户">删除用户</h3>
<p><code>rabbitmqctl delete_user Username</code></p>
<h3 id="修改用户密码">修改用户密码</h3>
<p><code>rabbitmqctl change_password Username Newpassword</code></p>
<h3 id="激活RabbitMQ’s-Management-Plugin">激活RabbitMQ’s Management Plugin</h3>
<p>找到安装rabbitMQ的路径，然后切换到<code>sbin</code>的文件夹，管理员运行Shell脚本<br>
<code>rabbitmq-pluginsenable rabbitmq_management</code></p>
<h3 id="启动监控管理器">启动监控管理器</h3>
<p><code>rabbitmq-plugins enable rabbitmq_management</code></p>
<h3 id="关闭监控管理器">关闭监控管理器</h3>
<p><code>rabbitmq-plugins disable rabbitmq_management</code></p>
<h3 id="启动rabbitmq">启动rabbitmq</h3>
<p><code>net start RabbitMQ</code></p>
<h3 id="关闭rabbitmq">关闭rabbitmq</h3>
<p><code>net stop RabbitMQ</code></p>
<h3 id="查看所有的队列">查看所有的队列</h3>
<p><code>rabbitmqctl list_queues</code></p>
<h3 id="清除所有的队列">清除所有的队列</h3>
<p><code>rabbitmqctl reset</code></p>
<h3 id="查看已有用户及用户的角色">查看已有用户及用户的角色</h3>
<p><code>rabbitmqctl list_users</code></p>
<h3 id="新增虚拟主机">新增虚拟主机</h3>
<p><code>rabbitmqctl add_vhost  vhost_name</code></p>
<h3 id="将新虚拟主机授权给新用户">将新虚拟主机授权给新用户</h3>
<p><code>rabbitmqctl set_permissions -p vhost_name username '.*' '.*' '.*'</code></p>
<h3 id="设置用户权限">设置用户权限</h3>
<p><code>rabbitmqctl  set_permissions  -p  VHostPath  username  ConfP  WriteP  ReadP</code></p>
<h3 id="查看-指定hostpath-所有用户的权限信息">查看(指定hostpath)所有用户的权限信息</h3>
<p><code>rabbitmqctl  list_permissions  [-p  VHostPath]</code></p>
<h3 id="查看指定用户的权限信息">查看指定用户的权限信息</h3>
<p><code>rabbitmqctl  list_user_permissions  username</code></p>
<h3 id="清除用户的权限信息">清除用户的权限信息</h3>
<p><code>rabbitmqctl  clear_permissions  [-p VHostPath]  username</code></p>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>RabbitMQ的概念及安装</title>
    <url>/2020/11/17/rabbitMQ/RabbitMQ%E6%A6%82%E5%BF%B5%E5%8F%8A%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<blockquote>
<p>消息(Message)是指在应用间传送的数据。消息可以非常简单，比如只包含文本字符串，也可以更复杂，可能包含嵌入对象</p>
</blockquote>
<p>消息队列（Message Queue）是一种应用间的通信方式，消息发送后可以立即返回，由消息系统来确保消息的可靠传递。消息发布者只管把消息发布到 MQ 中而不用管谁来取，消息使用者只管从MQ中取消息而不管是谁发布的。发布者和使用者都不用知道对方的存在</p>
<h2 id="消息队列应用场景">消息队列应用场景</h2>
<p>消息队列是一种应用间的异步协作机制，以常见的订单系统为例，用户点击【下单】按钮之后的业务逻辑可能包括：扣减库存、生成相应单据、发红包、发短信通知。在业务发展初期这些逻辑可能放在一起同步执行，随着业务的发展订单量增长，需要提升系统服务的性能，这时可以将一些不需要立即生效的操作拆分出来异步执行，比如发放红包、发短信通知等。这种场景下就可以用MQ，在下单的主流程（比如扣减库存、生成相应单据）完成之后发送一条消息到MQ让主流程快速完结，而由另外的单独线程拉取MQ的消息（或者由MQ 推送消息），当发现MQ中有发红包或发短信之类的消息时，执行相应的业务逻辑。<br>
以上是用于业务解耦的情况，其它常见场景包括最终一致性、广播、错峰流控等</p>
<h2 id="Features">Features</h2>
<blockquote>
<p>RabbitMQ 是一个由 Erlang 语言开发的 AMQP 的开源实现</p>
</blockquote>
<p>AMQP(Advanced Message)</p>
<blockquote>
<p>高级消息队列协议。它是应用层协议的一个开放标准，为面向消息的中间件设计，基于此协议的客户端与消息中间件可传递消息，并不受产品、开发语言等条件的限制</p>
</blockquote>
<p>可靠性(Reliability)</p>
<blockquote>
<p>RabbitMQ 使用一些机制来保证可靠性，如持久化、传输确认、发布确认</p>
</blockquote>
<p>灵活的路由(Flexible Routing)</p>
<blockquote>
<p>在消息进入队列之前，通过 Exchange 来路由消息的。对于典型的路由功能，RabbitMQ 已经提供了一些内置的 Exchange 来实现。针对更复杂的路由功能，可以将多个 Exchange绑定在一起，也通过插件机制实现自己的 Exchange</p>
</blockquote>
<p>消息集群(Clustering)</p>
<blockquote>
<p>多个RabbitMQ服务器可以组成一个集群,形成一个逻辑 Broker</p>
</blockquote>
<p>高可用(Highly Available Queues)</p>
<blockquote>
<p>队列可以在集群中的机器上进行镜像，使得在部分节点出问题的情况下队列仍然可用</p>
</blockquote>
<p>多种协议(Multi-protocol)</p>
<blockquote>
<p>RabbitMQ 支持多种消息队列协议，比如 STOMP、MQTT 等</p>
</blockquote>
<p>多语言客户端(Many Clients)</p>
<blockquote>
<p>RabbitMQ 几乎支持所有常用语言，比如 Java、.NET、Ruby 等</p>
</blockquote>
<p>管理界面(Management UI)</p>
<blockquote>
<p>RabbitMQ 提供了一个易用的用户界面，使得用户可以监控和管理消息Broker 的许多方面</p>
</blockquote>
<p>跟踪机制(Tracing)</p>
<blockquote>
<p>如果消息异常，RabbitMQ 提供了消息跟踪机制，使用者可以找出发生了什么</p>
</blockquote>
<p>插件机制(Plugin System)</p>
<blockquote>
<p>RabbitMQ 提供了许多插件，来从多方面进行扩展，也可以编写自己的插件</p>
</blockquote>
<h2 id="概念模型">概念模型</h2>
<h3 id="消息模型">消息模型</h3>
<blockquote>
<p>所有 MQ 产品从模型抽象上来说都是一样的过程：消费者（consumer）订阅某个队列。生产者（producer）创建消息，然后发布到队列（queue）中，最后将消息发送到监听的消费者</p>
</blockquote>
<p><img src="/images/2020-11-17-23-39-56.png" alt=""></p>
<h3 id="RabbitMQ-基本概念">RabbitMQ 基本概念</h3>
<p>上面只是最简单抽象的描述，具体到 RabbitMQ则有更详细的概念需要解释。上面介绍过 RabbitMQ 是 AMQP协议的一个开源实现，所以其内部实际上也是 AMQP 中的基本概念<br>
<img src="/images/2020-11-17-23-40-06.png" alt=""></p>
<h3 id="名词解释">名词解释</h3>
<p>Message</p>
<blockquote>
<p>消息，消息是不具名的，它由消息头和消息体组成。消息体是不透明的，而消息头则由一系列的可选属性组成，这些属性包括routing-key（路由键）、priority（相对于其他消息的优先权）、delivery-mode（指出该消息可能需要持久性存储）等</p>
</blockquote>
<p>Publisher</p>
<blockquote>
<p>消息的生产者，也是一个向交换器发布消息的客户端应用程序</p>
</blockquote>
<p>Exchange</p>
<blockquote>
<p>交换器，用来接收生产者发送的消息并将这些消息路由给服务器中的队列</p>
</blockquote>
<p>Binding</p>
<blockquote>
<p>绑定，用于消息队列和交换器之间的关联。一个绑定就是基于路由键将交换器和消息队列连接起来的路由规则，所以可以将交换器理解成一个由绑定构成的路由表</p>
</blockquote>
<p>Queue</p>
<blockquote>
<p>消息队列，用来保存消息直到发送给消费者。它是消息的容器，也是消息的终点。一个消息可投入一个或多个队列。消息一直在队列里面，等待消费者连接到这个队列将其取走</p>
</blockquote>
<p>Connection</p>
<blockquote>
<p>网络连接，比如一个TCP连接</p>
</blockquote>
<p>Channel</p>
<blockquote>
<p>信道，多路复用连接中的一条独立的双向数据流通道。信道是建立在真实的TCP连接内地虚拟连接，AMQP 命令都是通过信道发出去的，不管是发布消息、订阅队列还是接收消息，这些动作都是通过信道完成。因为对于操作系统来说建立和销毁 TCP 都是非常昂贵的开销，所以引入了信道的概念，以复用一条 TCP 连接</p>
</blockquote>
<p>Consumer</p>
<blockquote>
<p>消息的消费者，表示一个从消息队列中取得消息的客户端应用程序</p>
</blockquote>
<p>Virtual Host</p>
<blockquote>
<p>虚拟主机，表示一批交换器、消息队列和相关对象。虚拟主机是共享相同的身份认证和加密环境的独立服务器域。每个 vhost 本质上就是一个 mini 版的 RabbitMQ 服务器，拥有自己的队列、交换器、绑定和权限机制。vhost 是 AMQP 概念的基础，必须在连接时指定，RabbitMQ 默认的 vhost 是 /</p>
</blockquote>
<p>Broker</p>
<blockquote>
<p>表示消息队列服务器实体</p>
</blockquote>
<p>消息路由</p>
<blockquote>
<p>AMQP中增加了Exchange 和 Binding 的角色。生产者把消息发布到 Exchange 上，消息最终到达队列并被消费者接收，而Binding决定交换器的消息应该发送到哪个队列</p>
</blockquote>
<p><img src="/images/2020-11-17-23-40-23.png" alt=""></p>
<h2 id="Exchange-类型">Exchange 类型</h2>
<p>Exchange分发消息时根据类型的不同分发策略有区别，目前共四种类型：<code>direct</code>、<code>fanout</code>、<code>topic</code>、<code>headers</code> 。<code>headers</code> 匹配 AMQP 消息的 <code>header</code> 而不是路由键，此外 <code>headers</code> 交换器和 <code>direct</code>交换器完全一致，但性能差很多，目前几乎用不到了，所以直接看另外三种类型</p>
<h3 id="direct">direct</h3>
<blockquote>
<p>消息中的路由键（routing key）如果和 Binding 中的 binding key 一致，交换器就将消息发到对应的队列中。路由键与队列名完全匹配，如果一个队列绑定到交换机要求路由键为“dog”，则只转发 routing key标记为“dog”的消息，不会转发“dog.puppy”，也不会转发“dog.guard”等等。它是完全匹配、单播的模式</p>
</blockquote>
<p><img src="/images/2020-11-17-23-40-40.png" alt=""></p>
<h3 id="fanout">fanout</h3>
<blockquote>
<p>每个发到 fanout 类型交换器的消息都会分到所有绑定的队列上去。fanout 交换器不处理路由键，只是简单的将队列绑定到交换器上，每个发送到交换器的消息都会被转发到与该交换器绑定的所有队列上。很像子网广播，每台子网内的主机都获得了一份复制的消息。fanout 类型转发消息是最快的</p>
</blockquote>
<p><img src="/images/2020-11-17-23-40-48.png" alt=""></p>
<h3 id="topic">topic</h3>
<blockquote>
<p>topic 交换器通过模式匹配分配消息的路由键属性，将路由键和某个模式进行匹配，此时队列需要绑定到一个模式上。它将路由键和绑定键的字符串切分成单词，这些单词之间用点隔开。它同样也会识别两个通配符：符号“#”和符号“”。#匹配0个或多个单词，匹配不多不少一个单词</p>
</blockquote>
<p><img src="/images/2020-11-17-23-40-55.png" alt=""></p>
<h2 id="RabbitMQ安装">RabbitMQ安装</h2>
<h3 id="Windows">Windows</h3>
<h4 id="安装Erlang">安装Erlang</h4>
<blockquote>
<p>因为RabbitMQ是用Erlang语言编写的，所以要安装RabbitMQ先要安装Erlang<br/></p>
</blockquote>
<ol>
<li>下载地址: <a href="http://www.erlang.org/downloads" target="_blank" rel="noopener">http://www.erlang.org/downloads</a> 根据需要选择版本<br/></li>
<li>下载完成后直接安装，一直NEXT。（路径自行选择）</li>
<li>命令行输入:<code>erl</code>，提示版本信息: Eshell V10.6  (abort with ^G), 说明Erlang安装 成功</li>
</ol>
<h4 id="安装RabbitMQ">安装RabbitMQ</h4>
<ol>
<li>下载地址:  <a href="https://www.rabbitmq.com/install-windows.html" target="_blank" rel="noopener">https://www.rabbitmq.com/install-windows.html</a><br></li>
<li>根据需要选择版本(注意：RabbitMQ和Erlang版本可能存在不匹配情况)<br/></li>
<li>下载完成后直接安装，一直NEXT（路径自行选择）</li>
</ol>
<h4 id="环境变量配置">环境变量配置</h4>
<ol>
<li>新建环境变量<code>ERLANG_HOME</code>，路径为Erlang安装路径</li>
<li>新建环境变量<code>RABBITMQ_SERVER</code>，路径为RabbitMQ安装路径</li>
<li>在<code>Path</code>中添加<code>ERLANG_HOME</code>和<code>RABBITMQ_SERVER</code></li>
</ol>
<h4 id="激活RabbitMQ’s-Management-Plugin可视化插件">激活RabbitMQ’s Management Plugin可视化插件</h4>
<ol>
<li>进入到RabbitMQ安装目录, <code>D:\RabbitMQ\rabbitmq_server-3.8.2\sbin</code></li>
<li>运行命令: <code>rabbitmq-plugins.bat enable rabbitmq_management</code></li>
<li>浏览器访问: <a href="http://localhost:15672/" target="_blank" rel="noopener">http://localhost:15672/</a> 用户名/密码均为: <code>guest</code></li>
</ol>
<h3 id="Centos">Centos</h3>
<blockquote>
<p>采用的虚拟机系统为<code>Centos7</code>，采用的安装方式是<code>yum</code>安装，为了简单，直接使用官方提供的<code>erlang</code>和<code>RabbitMQ-server</code>的自动安装脚本(官方安装文档)，逐行执行下边的代码就可以安装完成erlang和RabbitMQ</p>
</blockquote>
<h4 id="安装socat">安装socat</h4>
<p><code>yum install socat</code></p>
<h4 id="安装erlang">安装erlang</h4>
<pre><code>curl -s https://packagecloud.io/install/repositories/rabbitmq/erlang/script.rpm.sh | sudo bash
yum -y install erlang
</code></pre>
<h4 id="安装rabbitmq-server">安装rabbitmq-server</h4>
<pre><code>curl -s https://packagecloud.io/install/repositories/rabbitmq/rabbitmq-server/script.rpm.sh | sudo bash
yum -y install rabbitmq-server
</code></pre>
<h4 id="启动rabbitmq服务">启动rabbitmq服务</h4>
<p><code>systemctl start rabbitmq-server</code></p>
<h4 id="添加web管理插件">添加web管理插件</h4>
<p><code>rabbitmq-plugins enable rabbitmq_management</code></p>
<h4 id="开放端口">开放端口</h4>
<pre><code>firewall-cmd --zone=public --add-port=5672/tcp --permanent
firewall-cmd --zone=public --add-port=15672/tcp --permanent
</code></pre>
<h4 id="重新加载防火墙">重新加载防火墙</h4>
<p><code>firewall-cmd --reload</code></p>
<h4 id="访问Web界面">访问Web界面</h4>
<blockquote>
<p>默认账号密码都是<code>guest</code>   ，但是如果使用guest登录，会出现报错如下：</p>
</blockquote>
<p><img src="/images/2020-11-21-21-45-20.png" alt=""></p>
<p>原因是<code>RabbitMQ3.3</code>以后，<code>guest</code>账号只能在本机登录。创建其他登录账号然后重启<code>RabbitMQ</code>即可</p>
<h4 id="创建用户">创建用户</h4>
<blockquote>
<p>如果是登录虚机中的RabbitMq无法使用<code>guest</code>账户,需要创建其他登录账户</p>
</blockquote>
<p>创建用户名<code>admin</code>，密码<code>admin</code>的用户<br>
<code>rabbitmqctl add_user admin admin</code></p>
<p>设置admin为超级管理员<br>
<code>rabbitmqctl set_user_tags admin administrator</code></p>
<p>授权远程访问（也可以登录后，可视化配置）<br>
<code>rabbitmqctl set_permissions -p / admin &quot;.&quot; &quot;.&quot; &quot;.*&quot;</code></p>
<p>创建完成后，重启RabbitMQ<br>
<code>systemctl restart rabbitmq-server</code></p>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>RabbitMQ常见问题</title>
    <url>/2020/11/21/rabbitMQ/RabbitMQ%E5%B8%B8%E8%A7%81%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h2 id="为什么要使用消息队列">为什么要使用消息队列?</h2>
<h3 id="解耦">解耦</h3>
<p>传统模式：系统间耦合度强 如系统A直接调用系统B系统C的代码，如果再有系统D接入，则系统A还要修改代码<br>
中间件模式：系统A将消息写入消息队列，系统B,系统C 订阅消息队列，如果再有系统D介入，直接订阅消息队列即可 系统A不必修改代码</p>
<h3 id="异步">异步</h3>
<p>传统模式：一些非必要得业务逻辑以同步得方式运行浪费时间<br>
中间件模式：将消息写入消息队列 一些非必要的业务逻辑以异步得方式运行 提高响应速度</p>
<h3 id="削峰-限流">削峰/限流</h3>
<p>传统模式：并发量大得时候所有请求全部到数据库，造成数据库连接异常<br>
中间件模式：系统按照数据库能处理得并发量从消息队列中慢慢拉取消息。在生产环境中这种短暂得高峰期积压是允许的</p>
<h2 id="消息队列的使用场景有哪些">消息队列的使用场景有哪些?</h2>
<blockquote>
<p>消息队列常见的使用场景很多，比较核心的有3个：<code>解耦</code>、<code>异步</code>、<code>削峰</code></p>
</blockquote>
<h2 id="什么是RabbitMQ">什么是RabbitMQ?</h2>
<p>即一个消息队列，主要是用来实现应用程序的异步和解耦，同时也能起到消息缓冲，消息分发的作用。RabbitMQ使用的是<code>AMQP</code>协议，它是一种二进制协议</p>
<p><code>AMQP</code>，即<code>Advanced Message Queuing Protocol</code>(高级消息排队协议),一个提供统一消息服务的应用层标准高级消息队列协议,是应用层协议的一个开放标准,为面向消息的中间件设计。基于此协议的客户端与消息中间件可传递消息，并不受客户端/中间件不同产品，不同的开发语言等条件的限制</p>
<h2 id="为什么使用RabbitMQ">为什么使用RabbitMQ?</h2>
<p>这里涉及一个消息中间件选型的问题<br>
<img src="/images/2020-11-22-14-31-18.png" alt=""></p>
<table>
<thead>
<tr>
<th>特性</th>
<th>ActiveMQ</th>
<th>RabbitMQ</th>
<th>RocketMQ</th>
<th>kafka</th>
</tr>
</thead>
<tbody>
<tr>
<td>开发语言</td>
<td>java</td>
<td>erlang</td>
<td>java</td>
<td>scala</td>
</tr>
<tr>
<td>单机吞吐量</td>
<td>万级</td>
<td>万级</td>
<td>10万级</td>
<td>10万级</td>
</tr>
<tr>
<td>时效性</td>
<td>ms级</td>
<td>us级</td>
<td>ms级</td>
<td>ms级以内</td>
</tr>
<tr>
<td>可用性</td>
<td>高(主从架构)</td>
<td>高(主从架构)</td>
<td>非常高(分布式架构)</td>
<td>非常高(分布式架构)</td>
</tr>
<tr>
<td>功能特性</td>
<td>成熟的产品，在很多公司得到应用；有较多的文档；各种协议支持较好</td>
<td>基于erlang开发，所以并发能力很强，性能极其好，延时很低;管理界面较丰富</td>
<td>MQ功能比较完备，扩展性佳</td>
<td>只支持主要的MQ功能，像一些消息查询，消息回溯等功能没有提供，毕竟是为大数据准备的，在大数据领域应用广</td>
</tr>
</tbody>
</table>
<p>(1)中小型软件公司，建议选<code>RabbitMQ</code>.一方面，<code>erlang</code>语言天生具备高并发的特性，而且他的管理界面用起来十分方便。正所谓，成也萧何，败也萧何！他的弊端也在这里，虽然<code>RabbitMQ</code>是开源的，然而国内有几个能定制化开发erlang的程序员呢？所幸，RabbitMQ的社区十分活跃，可以解决开发过程中遇到的bug，这点对于中小型公司来说十分重要。不考虑rocketmq和kafka的原因是，一方面中小型软件公司不如互联网公司，数据量没那么大，选消息中间件，应首选功能比较完备的，所以kafka排除。不考虑rocketmq的原因是，rocketmq是阿里出品，如果阿里放弃维护rocketmq，中小型公司一般抽不出人来进行rocketmq的定制化开发，因此不推荐</p>
<p>(2)大型软件公司，根据具体使用在rocketMq和kafka之间二选一。一方面，大型软件公司，具备足够的资金搭建分布式环境，也具备足够大的数据量。针对rocketMQ,大型软件公司也可以抽出人手对rocketMQ进行定制化开发，毕竟国内有能力改JAVA源码的人，还是相当多的。至于kafka，根据业务场景选择，如果有日志采集功能，肯定首选kafka</p>
<blockquote>
<p>具体该选哪个，应该根据实际场景考虑，不能为了用而用</p>
</blockquote>
<h2 id="使用消息队列会有什么缺点">使用消息队列会有什么缺点?</h2>
<p>系统可用性降低:本来其他系统只要运行好好的，那你的系统就是正常的。现在加了消息队列进去，那消息队列挂了，你的系统也就挂了。因此，系统可用性降低<br>
系统复杂性增加:要多考虑很多方面的问题，比如一致性问题、如何保证消息不被重复消费，如何保证保证消息可靠传输。因此，需要考虑的东西更多，系统复杂性增大</p>
<h2 id="如何实现RabbitMQ的高可用">如何实现RabbitMQ的高可用?</h2>
<p>参考：搭建RabbitMQ高可用集群</p>
<h2 id="RabbitMQ中重要角色有哪些">RabbitMQ中重要角色有哪些?</h2>
<p><code>生产者</code>：消费的创建者，负责创建和推送数据到消息服务器<br>
<code>消费者</code>：消息的接收方，用于处理数据和确认消息<br>
<code>代理</code>：指RabbitMQ本身，本身不生产消息，只扮演“快递”的角色</p>
<h2 id="RabbitMQ中重要的组件有哪些">RabbitMQ中重要的组件有哪些?</h2>
<p><code>ConnectionFactory</code>（连接管理器）：应用程序与Rabbit之间建立连接的管理器，程序代码中使用<br>
<code>Channel</code>（信道）：消息推送使用的通道<br>
<code>Exchange</code>（交换器）：用于接收，分配消息<br>
<code>Queue</code>（队列）：用于存储生产者的消息<br>
<code>RoutingKey</code>（路由键）：用于把生产者的数据分配到交换器上<br>
<code>BindingKey</code>（绑定键）：用于把交换器的消息绑定到队列上</p>
<h2 id="RabbitMQ中vhost的作用">RabbitMQ中vhost的作用?</h2>
<p>每个RabbitMQ都能创建多个<code>vhost</code>，称为虚拟主机，每个虚拟主机都是mini版的RabbitMQ，它拥有自己的队列，交换器和绑定，拥有自己的权限机制</p>
<h2 id="RabbitMQ中的消息如何发送">RabbitMQ中的消息如何发送?</h2>
<p>首先客户端必须连接到RabbitMQ服务器才能发布和消费消息，客户端和 rabbit server 之间会创建一个tcp连接，一旦tcp打开并通过了认证（认证就是你发送给rabbit服务器的用户名和密码），你的客户端和 就创建一条 amqp 信道（channel），信道是创建在“真实”tcp上的虚拟连接，amqp命令都是通过信道发送出去的，每个信道都会有唯一的id，不论是发布消息，订阅队列都是通过这个信道完成</p>
<h2 id="RabbitMQ怎么保证消息的可靠性传输">RabbitMQ怎么保证消息的可靠性传输?</h2>
<p>从三个角度分析：生产者弄丢数据 消息队列弄丢数据 消费者弄丢数据</p>
<h3 id="生产者丢失数据">生产者丢失数据</h3>
<p>提供<code>Transacton</code> 和<code>confirm</code>机制来确保生产者不丢消息</p>
<p><code>Transacton</code>: 发消息前开启事务（channel.txSelect()),然后发送消息，如果发送过程中有什么异常，事务就会回滚（channel,txRollback()),如果发送成功则提交事务（channel.txCommit()).然而缺点是吞吐量下降了。因此生产用<code>confirm</code>模式居多。一但channel 进入confirm 模式，所有该信道上面发送的消息都将会指派一个唯一的ID（从1开始），一但消息被投递到所有匹配的队列之后，rabbitMQ就会发送一个ACK给生产者（包含消息的唯一ID),这就使生产这知道消息已经到达消息队列了。如果RabbitMQ没有处理该消息，则会发送一个Nack消息给你，你可以进行重试操作</p>
<h3 id="消息队列丢失数据">消息队列丢失数据</h3>
<p>处理消息队列丢失数据的情况，一把是开启持久化磁盘的配置。这个持久话配置可以和confirm机制配合使用，你可以在持久化磁盘之后，再给生产这发送一个Ack信号。这样如果持久化磁盘之前，rabbitMQ阵亡了，那么生产者收不到Ack信号，生产者会自动重发</p>
<p>持久化一般分两步</p>
<ol>
<li>将queue的持久化标识<code>durable</code>设置为<code>true</code>,则代表一个持久化队列</li>
<li>发送消息的时候将<code>deliveryMode=2</code></li>
</ol>
<p>这样设置之后，rabbitMQ就算挂掉了，重启后也能恢复数据</p>
<h3 id="消费者丢数据">消费者丢数据</h3>
<p>消费者丢数据一般是因为采用了自动确认消息模式，这种模式下，消费者会自动确认收到信息。这时rabbitMQ会立即将消息删除，这种情况下如果消费者出现异常而没能处理该消息，就会丢失该消息</p>
<p>解决方案：采用手动确认消息即可，在处理完消息后才发送<code>ack</code></p>
<h2 id="RabbitMQ怎么避免消息丢失">RabbitMQ怎么避免消息丢失?</h2>
<p>将数据写到消息队列上，系统B和C还没来得及取消息队列的数据，就挂掉了。如果没有做任何的措施，数据就丢了，这时候就要考虑数据的持久化，<code>磁盘</code>、<code>数据库</code>、<code>Redis</code>等，是同步存储还是异步存储</p>
<h2 id="RabbitMQ消息持久化成功的条件有哪些">RabbitMQ消息持久化成功的条件有哪些?</h2>
<p>声明队列必须设置持久化<code>durable</code>设置为<code>true</code><br>
消息推送投递模式必须设置持久化， <code>deliveryMode</code>设置为2（持久）<br>
消息已经到达持久化交换器<br>
消息已经到达持久化队列</p>
<h2 id="持久化有什么缺点">持久化有什么缺点?</h2>
<p>持久化的缺点就是降低了服务器的吞吐量，因为使用的是磁盘而非内存存储，从而降低了吞吐量。可使用SSD硬盘来缓解吞吐量的问题</p>
<h2 id="RabbitMQ有几种广播类型">RabbitMQ有几种广播类型?</h2>
<p><code>direct</code>（默认方式）：最基础最简单的模式，发送方把消息发送给订阅方，如果有多个订阅者，默认采用轮询的方式进行消息发送<br>
<code>headers</code>：与direct类似，只是性能很差，实际开发使用很少<br>
<code>fanout</code>：分发模式，把消费分发给所有订阅者<br>
<code>topic</code>：匹配订阅模式，使用正则匹配到消息队列，能匹配到的都能接收到</p>
<h2 id="RabbitMQ中延迟消息队列如何实现">RabbitMQ中延迟消息队列如何实现?</h2>
<p>消息过期后进入<code>死信交换器</code>，再由交换器转发到延迟消费队列，实现延迟功能<br>
使用<code>RabbitMQ-delayed-message-exchange</code>插件实现延迟功能</p>
<h2 id="RabbitMQ集群有什么用">RabbitMQ集群有什么用?</h2>
<p>高可用：某个服务器出现问题，整个RabbitMQ还可以继续使用<br>
高容量：集群可以承载更多的消息量</p>
<h2 id="RabbitMQ节点的类型有哪些">RabbitMQ节点的类型有哪些?</h2>
<p>磁盘节点：消息会存储到磁盘<br>
内存节点：消息都存储到内存中，重启服务器消息丢失，性能高于磁盘类型</p>
<h2 id="RabbitMQ集群搭建需要注意哪些问题">RabbitMQ集群搭建需要注意哪些问题?</h2>
<p>各节点之间使用“-link”连接，此属性不能忽略<br>
各节点使用的<code>erlang cookie</code>值必须相同，此值相当于“秘钥”的功能，用于各节点的认证<br>
整个集群必须包含一个磁盘节点</p>
<h2 id="RabbitMQ节点拷贝问题">RabbitMQ节点拷贝问题?</h2>
<p>每个节点是其他节点的完整拷贝吗?为什么<br>
不是，原因有以下两个：</p>
<p>存储空间的考虑：如果每个节点都拥有所有队列的完全拷贝，这样新增节点不但没有新增存储空间，反而增加了更多的冗余数据<br>
性能的考虑：如果每条消息都需要完整拷贝每一个集群节点，那新增节点并没有提升处理消息的能力，最多是保持和单节点相同的性能甚至更糟</p>
<h2 id="RabbitMQ集群中唯一一个磁盘节点崩溃会发生什么情况">RabbitMQ集群中唯一一个磁盘节点崩溃会发生什么情况?</h2>
<p>如果唯一磁盘的磁盘节点崩溃，不能进行一下操作：</p>
<p>不能创建队列<br>
不能创建交换器<br>
不能创建绑定<br>
不能添加用户<br>
不能更改权限<br>
不能添加和删除集群节点<br>
唯一磁盘节点崩溃了，集群是可以保持运行的，但不能更改任何东西</p>
<h2 id="RabbitMQ对集群节点停止顺序有要求吗">RabbitMQ对集群节点停止顺序有要求吗?</h2>
<p>对集群的停止的顺序是有要求的，应该先关闭内存节点，最后关闭磁盘节点。如果顺序恰好相反的话，可能造成消息的丢失</p>
<h2 id="Kafka与RabbitMQ的区别">Kafka与RabbitMQ的区别?</h2>
<h3 id="应用场景方面">应用场景方面</h3>
<p>RabbitMQ：用于实时的，对可靠性要求较高的消息传递上<br>
kafka：用于处于活跃的流式数据，大数据量的数据处理上</p>
<h3 id="架构模型方面">架构模型方面</h3>
<p>producer，broker，consumer<br>
RabbitMQ：以broker为中心，有消息的确认机制<br>
kafka：以consumer为中心，无消息的确认机制</p>
<h3 id="吞吐量方面">吞吐量方面</h3>
<p>RabbitMQ：支持消息的可靠的传递，支持事务，不支持批量操作，基于存储的可靠性的要求存储可以采用内存或硬盘，吞吐量小<br>
kafka：内部采用消息的批量处理，数据的存储和获取是本地磁盘顺序批量操作，消息处理的效率高，吞吐量高</p>
<h3 id="集群负载均衡方面">集群负载均衡方面</h3>
<p>RabbitMQ：本身不支持负载均衡，需要loadbalancer的支持<br>
kafka：采用zookeeper对集群中的broker，consumer进行管理，可以注册topic到zookeeper上，通过zookeeper的协调机制，producer保存对应的topic的broker信息，可以随机或者轮询发送到broker上，producer可以基于语义指定分片，消息发送到broker的某个分片上。<br>
kafka通过zk和分区机制实现：zk记录broker信息，生产者可以获取到并通过策略完成负载均衡；通过分区，投递消息到不同分区，消费者通过服务组完成均衡消费</p>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>搭建RabbitMQ高可用集群</title>
    <url>/2020/11/17/rabbitMQ/%E6%90%AD%E5%BB%BARabbitMQ%E9%AB%98%E5%8F%AF%E7%94%A8%E9%9B%86%E7%BE%A4/</url>
    <content><![CDATA[<p>RabbitMQ集群</p>
<a id="more"></a>
<p>TODO：虚机未安装好，后续补充</p>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>Channel常见方法</title>
    <url>/2020/11/17/rabbitMQ/Channel%E5%B8%B8%E8%A7%81%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<h2 id="channel-exchangeDeclare">channel.exchangeDeclare()</h2>
<ul>
<li>type：有direct、fanout、topic三种</li>
<li>durable：true、false true：服务器重启会保留下来Exchange。警告：仅设置此选项，不代表消息持久化。即不保证重启后消息还在。原文：true if we are declaring a durable exchange (the exchange will survive a server restart)</li>
<li>autoDelete:true、false.true:当已经没有消费者时，服务器是否可以删除该Exchange。原文1：true if the server should delete the exchange when it is no longer in use。</li>
</ul>
<pre><code class="language-cs"> /**
     * Declare an exchange.
     * @see com.rabbitmq.client.AMQP.Exchange.Declare
     * @see com.rabbitmq.client.AMQP.Exchange.DeclareOk
     * @param exchange the name of the exchange
     * @param type the exchange type
     * @param durable true if we are declaring a durable exchange (the exchange will survive a server restart)
     * @param autoDelete true if the server should delete the exchange when it is no longer in use
     * @param arguments other properties (construction arguments) for the exchange
     * @return a declaration-confirm method to indicate the exchange was successfully declared
     * @throws java.io.IOException if an error is encountered
     */
    Exchange.DeclareOk exchangeDeclare(String exchange, String type, boolean durable, boolean autoDelete,
                                       Map&lt;String, Object&gt; arguments) throws IOException;
</code></pre>
<h2 id="chanel-basicQos">chanel.basicQos()</h2>
<ul>
<li>prefetchSize：0</li>
<li>prefetchCount：会告诉RabbitMQ不要同时给一个消费者推送多于N个消息，即一旦有N个消息还没有ack，则该consumer将block掉，直到有消息ack</li>
<li>global：true\false 是否将上面设置应用于channel，简单点说，就是上面限制是channel级别的还是consumer级别<br>
备注：据说prefetchSize 和global这两项，rabbitmq没有实现，暂且不研究</li>
</ul>
<pre><code class="language-cs">    /**
     * Request specific &quot;quality of service&quot; settings.
     *
     * These settings impose limits on the amount of data the server
     * will deliver to consumers before requiring acknowledgements.
     * Thus they provide a means of consumer-initiated flow control.
     * @see com.rabbitmq.client.AMQP.Basic.Qos
     * @param prefetchSize maximum amount of content (measured in
     * octets) that the server will deliver, 0 if unlimited
     * @param prefetchCount maximum number of messages that the server
     * will deliver, 0 if unlimited
     * @param global true if the settings should be applied to the
     * entire channel rather than each consumer
     * @throws java.io.IOException if an error is encountered
     */
    void basicQos(int prefetchSize, int prefetchCount, boolean global) throws IOException;
</code></pre>
<h2 id="channel-basicPublish">channel.basicPublish()</h2>
<ul>
<li>routingKey：路由键，#匹配0个或多个单词，*匹配一个单词，在topic exchange做消息转发用</li>
<li>mandatory：true：如果exchange根据自身类型和消息routeKey无法找到一个符合条件的queue，那么会调用basic.return方法将消息返还给生产者。false：出现上述情形broker会直接将消息扔掉</li>
<li>immediate：true：如果exchange在将消息route到queue(s)时发现对应的queue上没有消费者，那么这条消息不会放入队列中。当与消息routeKey关联的所有queue(一个或多个)都没有消费者时，该消息会通过basic.return方法返还给生产者。</li>
<li>BasicProperties ：需要注意的是BasicProperties.deliveryMode，0:不持久化 1：持久化 这里指的是消息的持久化，配合channel(durable=true),queue(durable)可以实现，即使服务器宕机，消息仍然保留</li>
</ul>
<blockquote>
<p>简单来说：mandatory标志告诉服务器至少将该消息route到一个队列中，否则将消息返还给生产者；immediate标志告诉服务器如果该消息关联的queue上有消费者，则马上将消息投递给它，如果所有queue都没有消费者，直接把消息返还给生产者，不用将消息入队列等待消费者了</p>
</blockquote>
<pre><code class="language-cs">  /**
     * Publish a message.
     *
     * Publishing to a non-existent exchange will result in a channel-level
     * protocol exception, which closes the channel.
     *
     * Invocations of &lt;code&gt;Channel#basicPublish&lt;/code&gt; will eventually block if a
     * &lt;a href=&quot;http://www.rabbitmq.com/alarms.html&quot;&gt;resource-driven alarm&lt;/a&gt; is in effect.
     *
     * @see com.rabbitmq.client.AMQP.Basic.Publish
     * @see &lt;a href=&quot;http://www.rabbitmq.com/alarms.html&quot;&gt;Resource-driven alarms&lt;/a&gt;.
     * @param exchange the exchange to publish the message to
     * @param routingKey the routing key
     * @param mandatory true if the 'mandatory' flag is to be set
     * @param immediate true if the 'immediate' flag is to be
     * set. Note that the RabbitMQ server does not support this flag.
     * @param props other properties for the message - routing headers etc
     * @param body the message body
     * @throws java.io.IOException if an error is encountered
     */
    void basicPublish(String exchange, String routingKey, boolean mandatory, boolean immediate, BasicProperties props, byte[] body)
            throws IOException;
</code></pre>
<h2 id="channel-basicAck">channel.basicAck()</h2>
<ul>
<li>deliveryTag:该消息的index</li>
<li>multiple：是否批量.true:将一次性ack所有小于deliveryTag的消息。</li>
</ul>
<pre><code class="language-cs">    /**
     * Acknowledge one or several received
     * messages. Supply the deliveryTag from the {@link com.rabbitmq.client.AMQP.Basic.GetOk}
     * or {@link com.rabbitmq.client.AMQP.Basic.Deliver} method
     * containing the received message being acknowledged.
     * @see com.rabbitmq.client.AMQP.Basic.Ack
     * @param deliveryTag the tag from the received {@link com.rabbitmq.client.AMQP.Basic.GetOk} or {@link com.rabbitmq.client.AMQP.Basic.Deliver}
     * @param multiple true to acknowledge all messages up to and
     * including the supplied delivery tag; false to acknowledge just
     * the supplied delivery tag.
     * @throws java.io.IOException if an error is encountered
     */
    void basicAck(long deliveryTag, boolean multiple) throws IOException;
</code></pre>
<h2 id="channel-basicNack-delivery-getEnvelope-getDeliveryTag-false-true">channel.basicNack(delivery.getEnvelope().getDeliveryTag(), false, true)</h2>
<ul>
<li>deliveryTag:该消息的index</li>
<li>multiple：是否批量.true:将一次性拒绝所有小于deliveryTag的消息。</li>
<li>requeue：被拒绝的是否重新入队列</li>
</ul>
<pre><code class="language-cs">    /**
     * Reject one or several received messages.
     *
     * Supply the &lt;code&gt;deliveryTag&lt;/code&gt; from the {@link com.rabbitmq.client.AMQP.Basic.GetOk}
     * or {@link com.rabbitmq.client.AMQP.Basic.GetOk} method containing the message to be rejected.
     * @see com.rabbitmq.client.AMQP.Basic.Nack
     * @param deliveryTag the tag from the received {@link com.rabbitmq.client.AMQP.Basic.GetOk} or {@link com.rabbitmq.client.AMQP.Basic.Deliver}
     * @param multiple true to reject all messages up to and including
     * the supplied delivery tag; false to reject just the supplied
     * delivery tag.
     * @param requeue true if the rejected message(s) should be requeued rather
     * than discarded/dead-lettered
     * @throws java.io.IOException if an error is encountered
     */
    void basicNack(long deliveryTag, boolean multiple, boolean requeue)
            throws IOException;
</code></pre>
<h2 id="channel-basicReject-delivery-getEnvelope-getDeliveryTag-false">channel.basicReject(delivery.getEnvelope().getDeliveryTag(), false)</h2>
<ul>
<li>deliveryTag:该消息的index</li>
<li>requeue：被拒绝的是否重新入队列</li>
</ul>
<blockquote>
<p>channel.basicNack 与 channel.basicReject 的区别在于basicNack可以拒绝多条消息，而basicReject一次只能拒绝一条消息</p>
</blockquote>
<pre><code class="language-cs"> /**
     * Reject a message. Supply the deliveryTag from the {@link com.rabbitmq.client.AMQP.Basic.GetOk}
     * or {@link com.rabbitmq.client.AMQP.Basic.Deliver} method
     * containing the received message being rejected.
     * @see com.rabbitmq.client.AMQP.Basic.Reject
     * @param deliveryTag the tag from the received {@link com.rabbitmq.client.AMQP.Basic.GetOk} or {@link com.rabbitmq.client.AMQP.Basic.Deliver}
     * @param requeue true if the rejected message should be requeued rather than discarded/dead-lettered
     * @throws java.io.IOException if an error is encountered
     */
    void basicReject(long deliveryTag, boolean requeue) throws IOException;
</code></pre>
<h2 id="channel-basicConsume-queueName-true-consumer">channel.basicConsume(queueName, true, consumer)</h2>
<ul>
<li>autoAck：是否自动ack，如果不自动ack，需要使用channel.ack、channel.nack、channel.basicReject 进行消息应答</li>
</ul>
<pre><code class="language-cs">    /**
     * Start a non-nolocal, non-exclusive consumer, with
     * a server-generated consumerTag.
     * @param queue the name of the queue
     * @param autoAck true if the server should consider messages
     * acknowledged once delivered; false if the server should expect
     * explicit acknowledgements
     * @param callback an interface to the consumer object
     * @return the consumerTag generated by the server
     * @throws java.io.IOException if an error is encountered
     * @see com.rabbitmq.client.AMQP.Basic.Consume
     * @see com.rabbitmq.client.AMQP.Basic.ConsumeOk
     * @see #basicConsume(String, boolean, String, boolean, boolean, Map, Consumer)
     */
    String basicConsume(String queue, boolean autoAck, Consumer callback) throws IOException;
</code></pre>
<h2 id="chanel-exchangeBind">chanel.exchangeBind()</h2>
<p>channel.queueBind(queueName, EXCHANGE_NAME, bindingKey);<br>
用于通过绑定bindingKey将queue到Exchange，之后便可以进行消息接收</p>
<pre><code class="language-cs">    /**
     * Bind an exchange to an exchange, with no extra arguments.
     * @see com.rabbitmq.client.AMQP.Exchange.Bind
     * @see com.rabbitmq.client.AMQP.Exchange.BindOk
     * @param destination the name of the exchange to which messages flow across the binding
     * @param source the name of the exchange from which messages flow across the binding
     * @param routingKey the routine key to use for the binding
     * @return a binding-confirm method if the binding was successfully created
     * @throws java.io.IOException if an error is encountered
     */
    Exchange.BindOk exchangeBind(String destination, String source, String routingKey) throws IOException;
</code></pre>
<h2 id="channel-queueDeclare-queueName-false-false-false-null">channel.queueDeclare(queueName, false, false, false, null)</h2>
<ul>
<li>durable：true、false true：在服务器重启时，能够存活</li>
<li>exclusive ：是否为当前连接的专用队列，在连接断开后，会自动删除该队列，生产环境中应该很少用到吧。</li>
<li>autodelete：当没有任何消费者使用时，自动删除该队列(this means that the queue will be deleted when there are no more processes consuming messages from it.)</li>
</ul>
<pre><code class="language-cs"> /**
     * Declare a queue
     * @see com.rabbitmq.client.AMQP.Queue.Declare
     * @see com.rabbitmq.client.AMQP.Queue.DeclareOk
     * @param queue the name of the queue
     * @param durable true if we are declaring a durable queue (the queue will survive a server restart)
     * @param exclusive true if we are declaring an exclusive queue (restricted to this connection)
     * @param autoDelete true if we are declaring an autodelete queue (server will delete it when no longer in use)
     * @param arguments other properties (construction arguments) for the queue
     * @return a declaration-confirm method to indicate the queue was successfully declared
     * @throws java.io.IOException if an error is encountered
     */
    Queue.DeclareOk queueDeclare(String queue, boolean durable, boolean exclusive, boolean autoDelete,
                                 Map&lt;String, Object&gt; arguments) throws IOException;
</code></pre>
<p>arguments中可以设置的常用属性</p>
<table>
<thead>
<tr>
<th>参数名</th>
<th>作用</th>
<th>示例</th>
<th>示例描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Message TTL</td>
<td>设置队列中消息的有效时间</td>
<td>{ “x-message-ttl”,1000*8}</td>
<td>设置队列中的所有消息的有效期为8s</td>
</tr>
<tr>
<td>Auto expire</td>
<td>一定的时间内队列没有被使用，则自动删除队列</td>
<td>{“x-expires”,1000*60}</td>
<td>如果60s没有队列被访问，则删除队列</td>
</tr>
<tr>
<td>Max length</td>
<td>队列能保存消息的最大条数</td>
<td>{“x-max-length”,100 }</td>
<td>设置队列最多保存100条消息</td>
</tr>
<tr>
<td>Max length bytes</td>
<td>队列中ready类型消息的总字节数</td>
<td>{“x-max-length-bytes”,1000 }</td>
<td>设置队列中ready类型消息总共不能超过1000字节</td>
</tr>
<tr>
<td>Overflow behaviour</td>
<td>当队列消息满了时，再接收消息时的处理方法。有两种处理方案：默认为&quot;drop-head&quot;模式，表示从队列头部丢弃消息；&quot;reject-publish&quot;表示不接收后续的消息</td>
<td>{“x-overflow”,“reject-publish” }</td>
<td>设置当队列消息满了时,丢弃传来后续消息</td>
</tr>
<tr>
<td>Dead letter exchange</td>
<td>用于存储被丢弃的消息的交换机名。Overflow behaviour 的两种处理方案中丢弃的消息都会发送到这个交换机</td>
<td>{“x-dead-letter-exchange”,“beiyongExchange” }</td>
<td>设置丢弃的消息发送到名字位beiyongExchange的交换机</td>
</tr>
<tr>
<td>Dead letter routing key</td>
<td>被丢弃的消息发送到Dead letter exchange时的使用的routing Key</td>
<td>{“x-dead-letter-routing-key”,“deadKey” }</td>
<td>设置丢弃的消息发送到beiyongExchange交换机时的RoutingKey值是&quot;deadKey&quot;</td>
</tr>
<tr>
<td>Maximum priority</td>
<td>设置队列中消息优先级的最大等级，在publish消息时可以设置单条消息的优先级等级</td>
<td>{“x-max-priority”,10 }</td>
<td>设置中消息优先级的最大等级为10</td>
</tr>
<tr>
<td>Lazy mode</td>
<td>设置队列的模式，如果设置为Lazy表示队列中消息尽可能存放在磁盘中，以减少内存占用；不设置时消息都存放在队列中，用以尽可能快的处理消息</td>
<td>{“x-queue-mode”,“lazy”}</td>
<td>3.6以后版本可用，设置队列中消息尽可能存放在磁盘中，以减少内存占用。在消息拥堵时和消息持久化配置使用可以减少内存占用</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>RabbitMQ常见策略</title>
    <url>/2020/11/17/rabbitMQ/RabbitMQ%E5%B8%B8%E8%A7%81%E7%AD%96%E7%95%A5/</url>
    <content><![CDATA[<h2 id="死信队列">死信队列</h2>
<blockquote>
<p>死信队列：DLX，dead-letter-exchang,利用DLX，当消息在一个队列中变成死信 (dead message) 之后，它能被重新publish到另一个Exchange，这个Exchange就是DLX</p>
</blockquote>
<h3 id="消息-死信判断条件">消息=&gt;死信判断条件</h3>
<ul>
<li>消息被拒绝(basic.reject / basic.nack)，并且requeue = false</li>
<li>消息TTL过期</li>
<li>队列达到最大长度</li>
</ul>
<h3 id="死信处理过程">死信处理过程</h3>
<ul>
<li>DLX也是一个正常的Exchange，和一般的Exchange没有区别，它能在任何的队列上被指定，实际上就是设置某个队列的属性</li>
<li>当这个队列中有死信时，RabbitMQ就会自动的将这个消息重新发布到设置的Exchange上去，进而被路由到另一个队列</li>
<li>可以通过监听这个死信队列的消息做相应的处理</li>
</ul>
<h3 id="死信队列设置">死信队列设置</h3>
<ol>
<li>首先设置死信队列的exchange和queue，然后进行绑定</li>
</ol>
<ul>
<li>Exchange: dlx.exchange<br>
<img src="/images/2020-11-17-23-53-38.png" alt=""></li>
<li>Queue: dlx.queue<br>
<img src="/images/2020-11-17-23-53-51.png" alt=""></li>
<li>RoutingKey: # (#表示只要有消息到达了Exchange，那么都会路由到这个queue上)<br>
<img src="/images/2020-11-17-23-53-59.png" alt=""></li>
</ul>
<ol start="2">
<li>正常声明交换机、队列、绑定，只不过需要在声明队列时加上一个参数即可：<code>arguments.put(&quot; x-dead-letter-exchange&quot;，&quot;dlx.exchange&quot;);</code>这样消息在过期、requeue、队列在达到最大长度时，消息就可以直接路由死信队列</li>
</ol>
<p>声明交换机<br>
<img src="/images/2020-11-17-23-54-07.png" alt=""></p>
<p>声明队列并设置<code>arguments.put(&quot; x-dead-letter-exchange&quot;，&quot;dlx.exchange&quot;);</code><br>
<img src="/images/2020-11-17-23-54-14.png" alt=""></p>
<p>绑定<br>
<img src="/images/2020-11-17-23-54-20.png" alt=""></p>
<p>生产者</p>
<pre><code class="language-cs"> for (int i = 0; i &lt; 100; i++)
 {
     byte[] messageBody = Encoding.UTF8.GetBytes(i.ToString());

     // 设置消息持久化
     var props = channel.CreateBasicProperties();
     props.Persistent = true;

     // 消息发送
     channel.BasicPublish(
         exchange: &quot;exchange&quot;,
         routingKey: &quot;&quot;,
         basicProperties: props,
         body: messageBody);
 }
</code></pre>
<p><img src="/images/2020-11-17-23-54-39.png" alt=""><br>
消费端</p>
<pre><code class="language-cs">static void MessageConsumer(BasicDeliverEventArgs ea)
            {
                string message = Encoding.UTF8.GetString(ea.Body.ToArray());
                Console.WriteLine($&quot;Message:{message}&quot;);
                if (message == &quot;50&quot;)
                {
                    throw new Exception(&quot;error&quot;);
                }
            }

EventingBasicConsumer consumer =
   new EventingBasicConsumer(channel);

channel.BasicQos(0, 1, false);

consumer.Received += (model, ea) =&gt;
{
    string message =
           Encoding.UTF8.GetString(ea.Body.ToArray());
    try
    {
        MessageConsumer(ea);
        channel.BasicAck(
                   deliveryTag: ea.DeliveryTag,
                   multiple: false);
    }
    catch (Exception ex)
    {
        // 出错了，发nack
        channel.BasicNack(
            deliveryTag: ea.DeliveryTag,
            multiple: false,
            requeue: false);
    }
};

channel.BasicConsume(queue: &quot;myqueue&quot;,
    autoAck: false,
    consumer: consumer);
</code></pre>
<p>Queue被消费完<br>
<img src="/images/2020-11-17-23-54-51.png" alt=""></p>
<p>死信Quque加了一条数据<br>
<img src="/images/2020-11-17-23-55-08.png" alt=""></p>
<blockquote>
<p>实际环境还需要对死信队列进行一个监听和处理，具体的处理逻辑和业务相关，这里只简单演示死信队列是否生效</p>
</blockquote>
<h2 id="延时队列">延时队列</h2>
<blockquote>
<p>延迟队列就是进入该队列的消息会被延迟消费的队列。而一般的队列，消息一旦入队了之后就会被消费者马上消费</p>
</blockquote>
<p>延迟队列能做什么？延迟队列多用于需要延迟工作的场景。最常见的是以下两种场景：</p>
<ul>
<li>延迟消费。比如：用户生成订单之后，需要过一段时间校验订单的支付状态，如果订单仍未支付则需要及时关闭订单</li>
<li>延迟重试。比如消费者从队列里消费消息时失败了，但是想要延迟一段时间后自动重试</li>
</ul>
<h3 id="实现思路">实现思路</h3>
<p>实现依赖于RabbitMQ的两个特性</p>
<ul>
<li>Time-To-Live Extensions</li>
</ul>
<blockquote>
<p>RabbitMQ允许我们为消息或者队列设置TTL（time to live），也就是过期时间。TTL表明了一条消息可在队列中存活的最大时间，单位为毫秒。也就是说，当某条消息被设置了TTL或者当某条消息进入了设置了TTL的队列时，这条消息会在经过TTL秒后“死亡”，成为Dead Letter。如果既配置了消息的TTL，又配置了队列的TTL，那么较小的那个值会被取用。更多资料请查阅<a href="https://www.rabbitmq.com/ttl.html" target="_blank" rel="noopener">官方文档</a></p>
</blockquote>
<ul>
<li>Dead Letter Exchanges</li>
</ul>
<blockquote>
<p>被设置了TTL的消息在过期后会成为Dead Letter。之前说过在RabbitMQ中，一共有三种消息的“死亡”形式：</p>
</blockquote>
<ul>
<li>消息被拒绝。通过调用basic.reject或者basic.nack并且设置的requeue参数为false</li>
<li>消息因为设置了TTL而过期</li>
<li>消息进入了一条已经达到最大长度的队列</li>
</ul>
<p>如果队列设置了Dead Letter Exchange（DLX），那么这些Dead Letter就会被重新publish到Dead Letter Exchange，通过Dead Letter Exchange路由到其他队列</p>
<h3 id="延迟消费">延迟消费</h3>
<blockquote>
<p>生产者产生的消息首先会进入缓冲队列（图中红色队列）。通过RabbitMQ提供的TTL扩展，这些消息会被设置过期时间，也就是延迟消费的时间。等消息过期之后，这些消息会通过配置好的DLX转发到实际消费队列（图中蓝色队列），以此达到延迟消费的效果</p>
</blockquote>
<p><img src="/images/2020-11-17-23-55-22.png" alt=""></p>
<h3 id="延时重试">延时重试</h3>
<blockquote>
<p>消费者发现该消息处理出现了异常，比如是因为网络波动引起的异常。那么如果不等待一段时间，直接就重试的话，很可能会导致在这期间内一直无法成功，造成一定的资源浪费。可以将其先放在缓冲队列中（图中红色队列，可以理解为重试定义的exchange），等消息经过一段的延迟时间后再次进入实际消费队列中（图中蓝色队列），此时由于已经过了“较长”的时间了，异常的一些波动通常已经恢复，这些消息可以被正常消费</p>
</blockquote>
<p><img src="/images/2020-11-17-23-55-28.png" alt=""></p>
<h2 id="重试">重试</h2>
<blockquote>
<p>RabbitMQ<code>channel.basicNack()</code>能够让消息回到队列中，这样可以实现重试。但是无法明确重试次数，如果当前的消息一直重试的话，则后面的消息就会堆积起来，导致后面的消息无法消费。这是一个致命的缺点。因此需要设置重试次数来解决类似问题</p>
</blockquote>
<ul>
<li>使用redis或者mongo等第三方存储当前重试次数</li>
<li>在header中添加重试次数,使用channel.basicPublish()方法重新将消息发送后将重试次数加1</li>
</ul>
<p>可以设置重试次数到达阈值时转发到指定的<code>exchange</code></p>
<p>相关代码示例：<br>
<a href="https://github.com/wangpengliang815/CodeSnippet" target="_blank" rel="noopener">https://github.com/wangpengliang815/CodeSnippet</a></p>
<p>参考：</p>
<ul>
<li><a href="https://www.rabbitmq.com/ttl.html" target="_blank" rel="noopener">https://www.rabbitmq.com/ttl.html</a></li>
<li><a href="https://www.cnblogs.com/xishuai/p/spring-boot-rabbitmq-delay-queue.html" target="_blank" rel="noopener">https://www.cnblogs.com/xishuai/p/spring-boot-rabbitmq-delay-queue.html</a></li>
<li><a href="https://www.cnblogs.com/lori/p/9984760.html" target="_blank" rel="noopener">https://www.cnblogs.com/lori/p/9984760.html</a></li>
<li><a href="https://www.jianshu.com/p/537cb84ba72f" target="_blank" rel="noopener">https://www.jianshu.com/p/537cb84ba72f</a></li>
<li><a href="https://www.jianshu.com/p/986ee5eb78bc" target="_blank" rel="noopener">https://www.jianshu.com/p/986ee5eb78bc</a></li>
<li><a href="https://www.cnblogs.com/wyy1234/category/1321800.html" target="_blank" rel="noopener">https://www.cnblogs.com/wyy1234/category/1321800.html</a></li>
</ul>
]]></content>
      <categories>
        <category>RabbitMQ</category>
      </categories>
  </entry>
  <entry>
    <title>集成测试</title>
    <url>/2020/12/07/%E6%B5%8B%E8%AF%95%E7%9B%B8%E5%85%B3/%E9%9B%86%E6%88%90%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<h2 id="集成测试">集成测试</h2>
<p>集成测试可在包含应用支持基础结构（如数据库、文件系统和网络）的级别上确保应用组件功能正常</p>
<p>单元测试关注是的软件最小可执行单元是否能够正常执行，但软件是由一个个最小执行单元组成的集合体，单元与单元之间存在着种种依赖或联系，所以在开发时仅仅确保最小单元的正确往往不够，为了保证软件能够正确运行，单元与单元之间的集成测试是非常必要。</p>
<p>与单元测试相比，集成测试可在更广泛的级别上评估应用的组件。 单元测试用于测试独立软件组件，如单独的类方法。 集成测试确认两个或更多应用组件一起工作以生成预期结果，可能包括完整处理请求所需的每个组件。</p>
<h3 id="TestServer">TestServer</h3>
<p>使用<code>TestServer</code>测试API接口</p>
<pre><code>       protected TestServer CreateSingleTestServer()
       {
           if (testServer == null)
           {
               lock (singleton_Lock)
               {
                   if (testServer == null)
                   {
                       string path = Assembly.GetAssembly(typeof(TestBase)).Location;

                       IWebHostBuilder hostBuilder =
                           new WebHostBuilder()
                               .UseContentRoot(Path.GetDirectoryName(path))
                               .ConfigureAppConfiguration(cb =&gt;
                               {
                                   cb.AddJsonFile(&quot;appsettings.test.json&quot;, true, true);
                               })
                               .UseEnvironment(&quot;Development&quot;)
                               .UseStartup&lt;TestStartup&gt;();

                       testServer = new TestServer(hostBuilder);
                   }
               }
           }
           return testServer;
       }

       public class TestStartup : Startup
       {
           public TestStartup(
               IConfiguration configuration,
               IHostingEnvironment env) : base(configuration, env)
           {
if debug
               string connectionString =
                   Configuration.GetSection(&quot;Database:ConnectString&quot;).Value;

               testDbContext =
                   new ApplicationDbContext(new DbContextOptionsBuilder&lt;ApplicationDbContext&gt;()
                       .UseSqlServer(connectionString)
                       .Options);
endif
           }
       }
   }

</code></pre>
<pre><code>    [TestClass]
    [TestCategory(&quot;integration&quot;)]
    public class ApiTest : TestBase
    {
        private HttpClient client;


        [TestInitialize]
        public void Init()
        {
            client = testServer.CreateClient();
        }

        [TestCleanup]
        public void Cleanup()
        {
            client.Dispose();
        }

        [TestMethod]
        public async Task GetAsync()
        {
            var response =
               await client.GetAsync($&quot;/api/values&quot;);

            Assert.AreEqual(HttpStatusCode.OK, response.StatusCode);
        }
    }

</code></pre>
<p>参考：</p>
<ul>
<li><a href="https://www.cnblogs.com/selimsong/p/9263957.html" target="_blank" rel="noopener">https://www.cnblogs.com/selimsong/p/9263957.html</a></li>
<li><a href="https://docs.microsoft.com/zh-cn/dotnet/core/testing/?pivots=mstest" target="_blank" rel="noopener">https://docs.microsoft.com/zh-cn/dotnet/core/testing/?pivots=mstest</a></li>
<li><a href="https://docs.microsoft.com/zh-cn/aspnet/core/test/integration-tests?view=aspnetcore-3.1#test-app-prerequisites" target="_blank" rel="noopener">https://docs.microsoft.com/zh-cn/aspnet/core/test/integration-tests?view=aspnetcore-3.1#test-app-prerequisites</a></li>
<li><a href="https://www.cnblogs.com/selimsong/p/9306221.html" target="_blank" rel="noopener">https://www.cnblogs.com/selimsong/p/9306221.html</a></li>
</ul>
]]></content>
      <categories>
        <category>Testing</category>
      </categories>
  </entry>
  <entry>
    <title>VMware安装Centos</title>
    <url>/2020/12/07/centos/VMware%E5%AE%89%E8%A3%85Centos/</url>
    <content><![CDATA[<h2 id="准备">准备</h2>
<p>软件：<code>VMware</code><br>
镜像：<code>CentOS7</code></p>
<h2 id="虚拟机准备">虚拟机准备</h2>
<p>1.打开VMware选择新建虚拟机<br>
<img src="/images/2020-11-22-15-40-35.png" alt=""></p>
<p>2.典型安装与自定义安装</p>
<p>典型安装：VMware会将主流的配置应用在虚拟机的操作系统上，对于新手来很友好<br>
自定义安装：自定义安装可以针对性的把一些资源加强，把不需要的资源移除。避免资源浪费</p>
<p>这里选择典型安装<br>
<img src="/images/2020-11-22-15-42-05.png" alt=""></p>
<p>3.选择稍后安装操作系统<br>
<img src="/images/2020-11-22-15-43-14.png" alt=""></p>
<p>4.选择操作系统为<code>Linux</code><br>
<img src="/images/2020-11-22-15-44-09.png" alt=""></p>
<p>5.输入虚机名称和存储位置<br>
<img src="/images/2020-11-22-15-45-48.png" alt=""></p>
<p>6.设置虚机磁盘大小<br>
<img src="/images/2020-11-22-15-46-27.png" alt=""></p>
<p>7.自定义硬件<br>
<img src="/images/2020-11-22-15-46-57.png" alt=""></p>
<p><img src="/images/2020-11-22-15-49-01.png" alt=""><br>
根据自己电脑实际情况分配<br>
内存：2G<br>
处理器：处理器数量：2 内核数量：2<br>
网络适配器：NAT模式<br>
移除：打印机和声卡</p>
<h2 id="安装Centos">安装Centos</h2>
<p>1.在创建好的虚机上右键设置<br>
<img src="/images/2020-11-22-15-51-20.png" alt=""></p>
<p>2.开启虚拟机<br>
<img src="/images/2020-11-22-15-52-41.png" alt=""></p>
<p>3.安装操作系统<br>
开启虚拟机后会出现以下界面:</p>
<p>Install CentOS 7 安装CentOS 7<br>
Test this media &amp; install CentOS 7 测试安装文件并安装CentOS 7<br>
Troubleshooting 修复故障<br>
选择第一项，安装直接CentOS 7，回车，进入下面的界面<br>
<img src="/images/2020-11-22-15-53-16.png" alt=""></p>
<p>选择安装过程中使用的语言<br>
<img src="/images/2020-11-22-15-55-05.png" alt=""></p>
<p>安装位置及分区设置：这里分区使用自动配置<br>
<img src="/images/2020-11-22-15-56-37.png" alt=""></p>
<p>软件选择：最小安装<br>
<img src="/images/2020-11-22-15-57-43.png" alt=""></p>
<p>网络和主机名：设置主机名称开启网络连接<br>
<img src="/images/2020-11-22-15-58-54.png" alt=""></p>
<p>开始安装<br>
<img src="/images/2020-11-22-16-00-36.png" alt=""></p>
<p>设置Root密码并创建新的用户admin<br>
<img src="/images/2020-11-22-16-01-35.png" alt=""></p>
<p>等待安装完成重启系统即可</p>
]]></content>
      <categories>
        <category>Centos</category>
      </categories>
  </entry>
  <entry>
    <title>单元测试</title>
    <url>/2020/11/18/%E6%B5%8B%E8%AF%95%E7%9B%B8%E5%85%B3/%E5%8D%95%E5%85%83%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<h2 id="常见测试类型">常见测试类型</h2>
<ul>
<li>单元测试</li>
<li>集成测试</li>
<li>Web测试</li>
<li>负载测试</li>
<li>其他</li>
</ul>
<h2 id="单元测试">单元测试</h2>
<blockquote>
<p>软件应用程序有多种类型的测试。 其中包括集成测试、Web 测试、负载测试和其他测试。 “单元测试”用于对软件中的最小可测试单元进行检查和验证。 单元测试仅应测试开发人员控件内的代码。 它们不应测试基础结构问题。 基础结构问题包括数据库、文件系统和网络资源。</p>
</blockquote>
<p>编写单元测试时，尽量不要引入基础结构依赖项。这些依赖项会降低测试速度，使测试更加脆弱，应将其保留供集成测试使用。 可以通过遵循<code>Explicit Dependencies Principle</code>（显式依赖项原则）和使用<code>Dependency Injection</code>（依赖项注入）避免应用程序中的这些依赖项。还可以将单元测试保留在单独的项目中，与集成测试相分隔。 这可确保单元测试项目没有引用或依赖于基础结构包</p>
<h3 id="使用依赖倒置（DIP）原则编写代码">使用依赖倒置（DIP）原则编写代码</h3>
<p>很多情况下,一个类或者是方法它会依赖一些外部组件，如其他开发写的代码、第三方类库、数据库、网络等，当被测试的代码与这些组件紧耦合时，这段代码将可能是不可测的，如一个方法中依赖一个数据库组件去访问数据库，那么在执行这个方法时，必然要与数据库交互，如果没有数据库，那么该方法就无法运行。所以单元测试不仅是对代码逻辑进行检查，同时还对整个代码结构有所限制，面向对象编程时应当遵循“依赖倒置”原则，模块应该依赖抽象，抽象不应该依赖实现。并且所依赖的抽象，应该显示的通过构造或者方法参数进行暴露，让组件的使用者对组件的依赖一目了然,在单元测试时为了屏蔽这些抽象依赖，不同测试框架中提供了<code>stub</code>、<code>mock</code>、<code>fake</code>等方式对抽象进行模拟，以便于代码能够正常执行</p>
<h3 id="单元测试的好处">单元测试的好处</h3>
<ul>
<li>比执行功能测试节省时间</li>
<li>防止回归：新功能上线时可通过单元测试来确保之前存在的功能正常运行不会破坏现有功能</li>
<li>减少耦合代码：当代码紧密耦合时，可能很难进行单元测试，如果不为代码编写单元测试，耦合可能就不太明显了</li>
</ul>
<h3 id="优质单元测试的特征">优质单元测试的特征</h3>
<ul>
<li>快速： 对成熟项目进行数千次单元测试，这很常见。 应花非常少的时间来运行单元测试</li>
<li>独立： 单元测试是独立的，可以单独运行，并且不依赖文件系统或数据库等任何外部因素</li>
<li>可重复： 运行单元测试的结果应该保持一致，也就是说，如果在运行期间不更改任何内容，总是返回相同的结果</li>
<li>自检查： 测试应该能够在没有任何人工交互的情况下，自动检测测试是否通过</li>
</ul>
<h3 id="代码覆盖率">代码覆盖率</h3>
<p>高代码覆盖率百分比通常与较高的代码质量相关联。 但该度量值本身无法确定代码的质量。 设置过高的代码覆盖率百分比目标可能会适得其反。</p>
<p><strong>高代码覆盖率百分比不代表代码质量就高， 它仅仅表示单元测试所涵盖的代码量</strong></p>
<p>代码覆盖率工具有两种类型：</p>
<ul>
<li>数据收集器： 数据收集器监视测试执行并收集有关测试运行的信息。 它们以各种输出格式（例如 XML 和 JSON）报告收集的信息</li>
<li>报表生成器： 使用从测试运行收集的数据生成报表，通常为带样式的 HTML</li>
</ul>
<p>若要通过<code>Coverlet</code>获得代码覆盖率，现有单元测试项目必须具有相应的包依赖项，或者依赖于 .net 全局工具和对应的<code>coverlet.console NuGet</code>包。</p>
<p>代码覆盖率是单元测试运行的代码量（行、分支或方法）的度量值，企业版的VS自带了代码覆盖率分析工具<br>
详情参考：<br>
<a href="https://docs.microsoft.com/en-us/visualstudio/test/using-code-coverage-to-determine-how-much-code-is-being-tested" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/visualstudio/test/using-code-coverage-to-determine-how-much-code-is-being-tested</a><br>
<a href="https://github.com/Microsoft/vstest-docs/blob/master/docs/analyze.md#coverage" target="_blank" rel="noopener">https://github.com/Microsoft/vstest-docs/blob/master/docs/analyze.md#coverage</a></p>
<p>.netcore下使用（暂不支持.NetFramework）<code>coverlet(数据收集器)</code>=&gt;<code>ReportGenerator(报表生成器)</code>=&gt;代码覆盖率报告</p>
<h3 id="dotnet-core中的单元测试框架">dotnet core中的单元测试框架</h3>
<p>dotnet core中常用的单元测试的框架有<code>MSTest</code>、<code>NUnit</code>和<code>xUnit.net</code>，使用方法都非常相似，都是通过特性标记的方式声明测试方法，然后在方法中使用断言<code>Assertions</code>来判别方法执行结果是否达到预期</p>
<p><a href="https://docs.microsoft.com/zh-cn/dotnet/core/testing/?pivots=mstest" target="_blank" rel="noopener">.net平台下单元测试各框架介绍</a></p>
<p><a href="http://https://xunit.github.io/docs/comparisons" target="_blank" rel="noopener">框架特性及断言比较参考</a></p>
<h3 id="dotnet-test">dotnet test</h3>
<p><img src="/images/2020-11-18-00-08-59.png" alt=""></p>
<h4 id="MSBuild">MSBuild</h4>
<p>使用<code>MSBuild</code>方式运行测试并生成指定名称的测试结果文件，输出到指定目录</p>
<pre><code>dotnet test TestApplication.UnitTests.csproj  /p:CollectCoverage=true /p:CoverletOutputFormat=opencover /p:CoverletOutput=./TestResults/
</code></pre>
<p><img src="/images/2020-11-18-00-09-09.png" alt=""><br>
这里有个问题：对使用依赖注入的项目这里获取的覆盖率为0，详情：<br>
<a href="https://github.com/coverlet-coverage/coverlet/blob/master/Documentation/KnownIssues.md" target="_blank" rel="noopener">https://github.com/coverlet-coverage/coverlet/blob/master/Documentation/KnownIssues.md</a><br>
<a href="https://github.com/coverlet-coverage/coverlet/issues/922" target="_blank" rel="noopener">https://github.com/coverlet-coverage/coverlet/issues/922</a></p>
<h4 id="VSTest">VSTest</h4>
<p>使用coverlet命令生成覆盖率Xml</p>
<pre><code>dotnet test TestApplication.UnitTests.csproj --collect:&quot;XPlat Code Coverage&quot;
</code></pre>
<h4 id="Reportgenerator">Reportgenerator</h4>
<p>使用reportgenerator命令覆盖率xml文件渲染成Html，因为<code>Msbuild</code>和<code>VsTest</code>方式生成的目录结果有所不同所以渲染命令也有所不同</p>
<h5 id="MSBuild-2">MSBuild</h5>
<pre><code>reportgenerator &quot;-reports:./TestResults/*.xml;&quot;&quot;--targetdir:D:\RunTime\TestReport&quot;  -reporttypes:Html
</code></pre>
<h5 id="VSTest-2">VSTest</h5>
<pre><code>reportgenerator &quot;-reports:./TestResults/*/*.xml&quot; &quot;--targetdir:D:\RunTime\TestReport&quot;  -reporttypes:Html
</code></pre>
<h5 id="合并渲染">合并渲染</h5>
<p>找到<code>xml</code>文件路径，<code>-reports</code>命令中以<code>;</code>分割即可</p>
<pre><code>reportgenerator &quot;-reports:TestApplication.UnitTests/TestResults/*.xml;TestApplication.IntegrationTests/TestResults/*.xml;&quot; &quot;--targetdir:D:\RunTime\TestReport&quot;  -reporttypes:Html
</code></pre>
<p><strong>注意当前的工作目录</strong></p>
<p>参考：</p>
<ul>
<li><a href="https://www.cnblogs.com/cgzl/p/9326127.html" target="_blank" rel="noopener">https://www.cnblogs.com/cgzl/p/9326127.html</a></li>
<li><a href="https://github.com/coverlet-coverage/coverlet" target="_blank" rel="noopener">https://github.com/coverlet-coverage/coverlet</a></li>
<li><a href="https://github.com/danielpalme/ReportGenerator" target="_blank" rel="noopener">https://github.com/danielpalme/ReportGenerator</a></li>
</ul>
<h3 id="测试可重复性">测试可重复性</h3>
<ul>
<li>使用COM+自动事务(TestFixture)</li>
<li>使用数据清理来保证测试用例的独立和可重复性<code>TestInitialize/TestCleanup</code></li>
</ul>
<h3 id="Mock">Mock</h3>
<p>面向对象编程应该显式依赖抽象，单元测试时应屏蔽依赖的影响(无论是依赖还未实现，或者实现的依赖会阻碍代码执行)，为了满足这一需求出现了Mock、Fake等方式，其原理就是创建一个&quot;假&quot;的&quot;空&quot;的依赖，并用其替代真实依赖，以确保代码能够运行</p>
<pre><code>public class OrgPublisherServiceTests : TestBase
{
    private readonly Mock&lt;ILogRecorder&lt;OrgPublisherService&gt;&gt; _logger;

    public OrgPublisherServiceTests()
    {
        _logger = new Mock&lt;ILogRecorder&lt;OrgPublisherService&gt;&gt;();
    }
}
</code></pre>
<p><a href="http://https://docs.microsoft.com/zh-cn/dotnet/core/testing/unit-testing-best-practices" target="_blank" rel="noopener">单元测试最佳实践</a></p>
<p><a href="https://github.com/dotnet/aspnetcore/wiki/Engineering-guidelines#unit-tests-and-functional-tests" target="_blank" rel="noopener">单元测试规范</a></p>
<h3 id="注意">注意</h3>
<p>单元测试仅能保证软件的最小可执行单元是正确的，真正的软件是由这些最小可执行单元组成的一个整体，单元的正确性无法保证整体的正确性</p>
]]></content>
      <categories>
        <category>Testing</category>
      </categories>
  </entry>
  <entry>
    <title>Centos常用命令</title>
    <url>/2020/12/07/centos/%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p>记录Centos常用命令</p>
<a id="more"></a>
<h2 id="Vi">Vi</h2>
<h3 id="编辑模式">编辑模式</h3>
<p>使用<code>vi</code>进入文本后，按<code>i</code>编辑文本</p>
<pre><code>vi /etc/hosts　　
</code></pre>
<h3 id="退出编辑模式">退出编辑模式</h3>
<p>按<code>ESC</code>键，然后：退出vi<br>
<code>:q!</code>  不保存文件，强制退出vi命令<br>
<code>:w</code>   保存文件，不退出vi命令<br>
<code>:wq</code>  保存文件，退出vi命令</p>
<h2 id="防火墙">防火墙</h2>
<blockquote>
<p>注意：添加后需要重新加载防火墙才能生效</p>
</blockquote>
<h3 id="添加端口">添加端口</h3>
<p><code>firewall-cmd --add-port=8080/tcp --permanent</code><br>
<code>firewall-cmd --add-port=8888/tcp --permanent</code></p>
<h3 id="重新加载">重新加载</h3>
<p><code>firewall-cmd --reload</code></p>
<h3 id="防火墙启动关闭">防火墙启动关闭</h3>
<pre><code>systemctl start firewalld.service
systemctl stop firewalld.service
systemctl enable firewalld.service
</code></pre>
<p>或</p>
<pre><code>service firewalld restart #重启
service firewalld start #开启
service firewalld stop #关闭
</code></pre>
<h3 id="查看端口列表">查看端口列表</h3>
<pre><code>firewall-cmd --permanent --list-port
firewall-cmd --list-all
</code></pre>
]]></content>
      <categories>
        <category>Centos</category>
      </categories>
  </entry>
  <entry>
    <title>异常处理</title>
    <url>/2020/12/08/csharp/Exception/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>Autofac依赖注入</title>
    <url>/2020/12/08/autofac/Autofac%E4%BE%9D%E8%B5%96%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Autofac</category>
      </categories>
  </entry>
  <entry>
    <title>EF Codefirst</title>
    <url>/2020/12/08/entityFramework/EF%20Codefirst/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>EntityFramework</category>
      </categories>
  </entry>
  <entry>
    <title>Linq</title>
    <url>/2020/12/08/linq/Linq/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Linq</category>
      </categories>
  </entry>
  <entry>
    <title>Domain Driven Design(DDD)</title>
    <url>/2020/12/08/%E9%A2%86%E5%9F%9F%E9%A9%B1%E5%8A%A8/Domain%20Driven%20Design/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Domain Driven Design</category>
      </categories>
  </entry>
  <entry>
    <title>委托和事件</title>
    <url>/2020/11/17/csharp/Delegate&amp;Event/</url>
    <content><![CDATA[<p><strong>委托是一个类，它定义了方法的类型，指明了这个委托类型的变量可接受的函数，表示对具有特定参数列表和返回类型的方法的引用，使得可以将方法当作另一个方法的参数来进行传递，不管什么函数只要返回值类型和参数能匹配委托所指定的，那么这个函数就能存储为一个委托变量的引用</strong></p>
<a id="more"></a>
<h2 id="为什么需要委托？">为什么需要委托？</h2>
<ul>
<li>委托可以将方法（即逻辑）作为参数</li>
<li>逻辑解耦，保持稳定</li>
<li>代码复用，保证项目规范</li>
</ul>
<h2 id="委托使用步骤">委托使用步骤</h2>
<ul>
<li>使用<code>delegate</code>关键字定义委托</li>
<li>声明委托对应的方法</li>
<li>实例化委托将方法作为参数传入</li>
</ul>
<pre><code>    class DelegateTest
    {
        //step01：使用delegate关键字定义委托
        public delegate int Sum(int x, int y);

        static void Main(string[] args)
        {
            // step03：实例化委托将方法作为参数传入
            Sum sum = new Sum(new DelegateTest().Add);
            int result = sum.Invoke(1, 2);
            Console.WriteLine(result);
            Console.ReadKey();
        }
        // step02：声明委托对应的方法
        public int Add(int x, int y)
        {
            return x + y;
        }
    }
</code></pre>
<p>step01：使用delegate关键字定义委托</p>
<p>step02：声明委托对应的方法</p>
<p>step03：实例化委托将方法作为参数传入</p>
<p>至此,一个委托就完成了</p>
<h2 id="匿名方法">匿名方法</h2>
<blockquote>
<p>上面说到完成一个委托要分三步走，缺一步都不行,如果要跨大步，步子大了容易扯着蛋.但是微软不怕扯着蛋,非要把三步做成两步来走！所以微软就用匿名方法来简化上边的三个步骤</p>
</blockquote>
<pre><code>//step01：首先用delegate定义一个委托 
public delegate int Sum(int x, int y);

static void Main(string[] args)
{
    //step02：使用匿名方法的写法把一个方法赋值给委托
    Sum sum = delegate (int x, int y) { return x + y; };
    int result = sum.Invoke(1, 2);
    Console.WriteLine(result);
    Console.ReadKey();
}
</code></pre>
<p>step01：使用delegate关键字定义委托</p>
<p>step02：使用匿名方法的写法把一个方法赋值给委托,其实这种写法就是匿名方法</p>
<p>这时会发现这里省略了定义方法这一步,将三步简化成了两步</p>
<h2 id="Lambda表达式">Lambda表达式</h2>
<blockquote>
<p>原本很简单的程序,加上几个delegate关键字,一下就搞的深奥了,但是微软对C#的设计理念是简单易用。微软对匿名方法的方式依旧不太满意，就想方设法的来简化 <code>delegate(int x, int y) { return x + y; }</code> 这个匿名方法，Lambda就出现了</p>
</blockquote>
<p>lambda运算符&quot;=&gt;&quot;左边列出了需要的参数,右边定义了赋予lambda变量的方法实现代码</p>
<pre><code>//step01：首先用delegate定义一个委托 
public delegate int Sum(int x, int y);

static void Main(string[] args)
{
    //方法一：
    Sum sum1 = (int x, int y) =&gt; { return x + y; };
    int result1 = sum1(1, 2);

    //方法二：
    Sum sum2 = (x, y) =&gt; { return x + y; };
    int result2 = sum2(1, 2);

    //方法三：
    Sum sum3 = (x, y) =&gt; x + y;
    int result3 = sum3(1, 2);
}
</code></pre>
<p>方法一：简单的把delegate去掉，在 <code>()</code> 与 <code>{}</code> 之间加上 <code>=&gt;</code></p>
<p>方法二：在方法一的基础上把参数类型都干掉了</p>
<p>方法三：要干就干彻底些，把 <code>{}</code>以及 <code>return</code> 关键字都去掉了</p>
<p>这几种方法随便怎么写都行,不过就是害苦了初学者,一会儿看到这种写法,一会儿看到那种写法，把人搞的神魂颠倒</p>
<h2 id="Lambda表达式简写">Lambda表达式简写</h2>
<blockquote>
<p>如果lambda表达式只有一句.方法块内就可以省略花括号和return语句,这时编译器会添加一条隐式的return语句</p>
</blockquote>
<pre><code>Func&lt;double, double&gt; func = param =&gt; param * param;
</code></pre>
<p>等价于</p>
<pre><code>Func&lt;double, double&gt; func = param =&gt;
{
    return param * 2;
};
</code></pre>
<h2 id="泛型委托">泛型委托</h2>
<blockquote>
<p>随着.net版本的不断升级,微软又来玩新花样了,不管是匿名方法还是Lambda表达式,完成一个委托的应用,都逃不过两个步骤,一步是定义一个委托,另一步是用一个方法来实例化一个委托.微软干脆把这两步都合成一步来走了。用Func来简化一个委托的定义</p>
</blockquote>
<pre><code>static void Main(string[] args)
{
    //方法一：
    Func&lt;int, int, int&gt; add1 = (int x, int y) =&gt; { return x + y; };
    int result1 = add1(1, 2);

    //方法二：
    Func&lt;int, int, int&gt; add2 = (x, y) =&gt; { return x + y; };
    int result2 = add2(1, 2);

    //方法三：
    Func&lt;int, int, int&gt; add3 = (x, y) =&gt; x + y;
    int result3 = add3(1, 2);
}
</code></pre>
<p>至此一个委托的应用就可用 <code>Func&lt;int, int, int&gt; cAdD3 = (x, y) =&gt; x + y;</code> 一句话来完成了,其中的 <code>Func</code> 就是所谓的泛型委托</p>
<p>微软提供了 <code>Action&lt;T&gt;</code>和 <code>Func&lt;T&gt;</code>两种泛型委托,用于简化方法定义</p>
<h3 id="Action">Action</h3>
<blockquote>
<p>表示引用一个void返回类型的方法,可以传递最多16种不同的参数类型,没有泛型参数的Action类可调用没有参数的方法</p>
</blockquote>
<pre><code>    // Action：无参数
    Action action1 = () =&gt; { Console.WriteLine(&quot;啦啦啦啦&quot;); };
    action1();
    // Action：一个参数
    Action&lt;string&gt; action2 = p =&gt; { Console.WriteLine(&quot;啦啦啦啦,name:{0}&quot;, p); };
    action2(&quot;wang&quot;);
    // Action：多个参数
    Action&lt;string, int&gt; action3 = (name, age) =&gt; { Console.WriteLine(&quot;啦啦啦啦,    name:{0},age:{1}&quot;, name, age); };
    action3(&quot;wang&quot;, 25);

</code></pre>
<h3 id="Func">Func</h3>
<blockquote>
<p>Func<T>允许调用带返回类型的方法,可以传递16种不同类型的参数和一个返回类型,Func<out TResult>委托类型可以调用带返回类且无参数的方法</p>
</blockquote>
<p>总结</p>
<ul>
<li><code>Action&lt;T&gt;</code> 用于没有返回值的方法（参数根据自己情况进行传递）</li>
<li><code>Func&lt;T&gt;</code> 用于有返回值的方法（参数根据自己情况传递）</li>
</ul>
<p>记住无返回就用 <code>Action&lt;T&gt;</code>，有返回就用 <code>Func&lt;T&gt;</code></p>
<h2 id="表达式树">表达式树</h2>
<blockquote>
<p>表达式树其实与委托已经没什么关系了,如果非要扯上关系,表达式树是存放委托的容器.如果非要说的更专业一些，表达式树是存取Lambda表达式的一种数据结构。要用Lambda表达式的时候,直接从表达式中获取出来 <code>Compile()</code> 就可以直接用了</p>
</blockquote>
<pre><code>static void Main(string[] args)
{
    Expression&lt;Func&lt;int, int, int&gt;&gt; exp = (x, y) =&gt; x + y;
    Func&lt;int, int, int&gt; fun = exp.Compile();
    int result = fun(1, 2);
}

</code></pre>
<h2 id="Invoke">Invoke</h2>
<pre><code> Sum sum = delegate (int x, int y) { return x + y; };
 int result = sum.Invoke(1, 2);
 //等价于
 int result = sum(1, 2);
</code></pre>
<h2 id="委托数组">委托数组</h2>
<blockquote>
<p>定义Math类提供两个静态方法接收一个double类型的参数,用于计算倍数和阶乘</p>
</blockquote>
<pre><code>class Math
    {
        public static double MultipleTwo(double value)
        {
            return value * 2;
        }
        public static double Square(double value)
        {
            return value * value;
        }
    }
</code></pre>
<p>添加MathOperation操作方法,传递委托和double类型参数</p>
<pre><code>class Program
{
    public delegate double MyDelegate(double value);
    static void Main(string[] args)
    {
        //定义委托数组
        MyDelegate[] myDelegates = {
            Math.MultipleTwo,
            Math.Square
        };
        //使用委托数组
        for (int i = 0; i &lt; myDelegates.Length; i++)
        {
            MathOperation(myDelegates[i], 3.7);
            MathOperation(myDelegates[i], 3.0);
        }
        Console.ReadKey();
    }
    public static void MathOperation(MyDelegate myDelegate, double value)
    {
        var result = myDelegate(value);
        Console.WriteLine(&quot;Delegate is {0},value is {1}&quot;, myDelegate,result);
    }
}
</code></pre>
<h2 id="多播委托">多播委托</h2>
<blockquote>
<p>之前的每个委托都只包含一个方法调用,调用委托的次数与调用方法的次数相同,如果要调用多个方法,就需要多次显式调用这个委托,但是委托中也可以包含多个方法,称为多播委托,多播委托可以按顺序调用多个方法,为此委托的签名必须返回void,否则就只能得到委托最后调用的最后一个方法的结果</p>
</blockquote>
<pre><code>Func&lt;double, double&gt; func = Math.MultipleTwo;
func += Math.Square;
var result = func(3.0);
Console.WriteLine(result);

MyDelegate myDelegate = Math.MultipleTwo;
myDelegate += Math.Square;
var result2 = myDelegate(3.0);
Console.WriteLine(result2);
Console.ReadKey(); 
</code></pre>
<p>只返回了3.0阶乘的值</p>
<p>这里需要再回头仔细了解一下看这种说法到底是否正确:即多播委托返回值必须为void</p>
<h3 id="和">+= 和 -=</h3>
<blockquote>
<p>多播委托使用+=和-=,在委托中增加或删除方法调用</p>
</blockquote>
<pre><code class="language-cs">static void Main(string[] args)
{
    Action action = Print.First;
    action += Print.Second;
    action();

    Action action2 = Print.First;
    action2 += Print.Second;
    action2 -= Print.First;
    action2();
    Console.ReadKey();
}
class Print
{
    public static void First()
    {
        Console.WriteLine(&quot;FirstMethod&quot;);
    }
    public static void Second()
    {
        Console.WriteLine(&quot;SecondMethod&quot;);
    }
}
</code></pre>
<p>如果要使用多播委托,就要知道对同一个委托调用方法链的顺序并未正式定义,因为要避免编写依赖于特定顺序调用方法的代码</p>
<h3 id="多播委托异常处理">多播委托异常处理</h3>
<blockquote>
<p>使用多播委托,意味着多播委托里包含一个逐个调用的委托集合,如果集合其中一个方法抛出异常.整个迭代就会停止</p>
</blockquote>
<pre><code class="language-cs">class Program
    {
        public delegate double MyDelegate(out double value);

        static void Main(string[] args)
        {
            Action action = Print.First;
            action += Print.Second;
            action();
            Console.ReadKey();
        }
    }
    class Print
    {
        public static void First()
        {
            Console.WriteLine(&quot;FirstMethod&quot;);
            throw new Exception(&quot;Error&quot;);
        }
        public static void Second()
        {
            Console.WriteLine(&quot;SecondMethod&quot;);          
        }
    }
</code></pre>
<p>委托只调用了第一个方法,因为第一个方法抛出了异常,委托的迭代停止,不再调用 <code>Second()</code>方法</p>
<h3 id="GetInvocationList">GetInvocationList</h3>
<blockquote>
<p>使用 <code>Delegate的GetInvocationList()</code> 方法自己迭代方法列表</p>
</blockquote>
<pre><code class="language-cs">            Action action = Print.First;
            action += Print.Second;

            Delegate[] delegates = action.GetInvocationList();
            foreach (Action item in delegates)
            {
                try
                {
                    item();
                }
                catch (Exception error)
                {
                    Console.WriteLine(error.Message);
                }
            }
            Console.ReadKey();
</code></pre>
<p>修改后,程序在捕获异常后,会迭代下一个方法</p>
<h2 id="闭包的陷阱">闭包的陷阱</h2>
<blockquote>
<p><a href="https://www.cnblogs.com/aehyok/p/3730417.html" target="_blank" rel="noopener">https://www.cnblogs.com/aehyok/p/3730417.html</a></p>
</blockquote>
<p>源码：</p>
<pre><code class="language-cs">List&lt;Action&gt; list = new List&lt;Action&gt;();
for (int i = 0; i &lt; 5; i++)
{
    Action t = () =&gt; Console.WriteLine(i.ToString());
    list.Add(t);
}
foreach (Action t in list)
{
    t();
}
</code></pre>
<p>IL反编译</p>
<pre><code class="language-cs">List&lt;Action&gt; list = new List&lt;Action&gt;();
TempClass tempClass = new TempClass();
for (tempClass.i = 0; tempClass.i &lt; 5; tempClass.i++)
{
    Action t = tempClass.TempFunc;
    list.Add(t);
}
foreach (Action t in list)
{
    t();
}
</code></pre>
<pre><code class="language-cs"> public class TempClass
 {
     public int i;
     public void TempFunc()
     {
         Console.WriteLine(i.ToString());
     }
 }
</code></pre>
<blockquote>
<p>所谓的闭包对象，指的是上面这种情形中的TempClass对象，如果匿名方法（Lambda表达式）引用了某个局部变量，编译器就会自动将该引用提升到该闭包对象中，即将for循环中的变量i修改成了引用闭包对象的公共变量i。这样一来，即使代码执行后离开了原局部变量i的作用域(如for循环)，包含该闭包对象的作用域也还存在</p>
</blockquote>
<p><img src="/images/2020-11-17-21-27-16.png" alt=""></p>
<p>修改一下源代码</p>
<pre><code class="language-cs">List&lt;Action&gt; list = new List&lt;Action&gt;();
for (int i = 0; i &lt; 5; i++)
{
    int temp = i;
    Action t = () =&gt; Console.WriteLine(temp.ToString());
    list.Add(t);
}
foreach (Action t in list)
{
    t();
}
</code></pre>
<p><img src="/images/2020-11-17-21-28-16.png" alt=""></p>
<h2 id="事件">事件</h2>
<p><strong>事件是一种引用类型</strong>，实际上也是一种特殊的委托。<strong>事件基于委托，是提供了发布/订阅机制的委托，事件是将委托封装，并对外公布了订阅和取消订阅的接口</strong></p>
<h3 id="Example-01">Example_01</h3>
<blockquote>
<p>不使用委托和事件的代码，耦合度高</p>
</blockquote>
<pre><code class="language-CS">public class Player
{
    public void Die()
    {
        GameConfiguration configuration = new GameConfiguration();
        configuration.OnPlayerDeath();
        UI ui = new UI();
        ui.OnPlayerDeath();
    }
}

public class UI
{
    public void OnPlayerDeath()
    {
        Console.Write(&quot;GameOver&quot;);
    }
}

public class GameConfiguration
{
    public int DeathNumber { get; set; }

    public void OnPlayerDeath()
    {
        DeathNumber++;
        Console.WriteLine(DeathNumber);
    }
}
</code></pre>
<pre><code class="language-cs">[TestMethod]
public void Example_01()
{
    Player player = new Player();
    player.Die();
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="Example-02">Example_02</h3>
<blockquote>
<p>使用委托和事件解耦代码</p>
</blockquote>
<pre><code class="language-cs">/// &lt;summary&gt;
/// 热水器
/// &lt;/summary&gt;
public class Heater
{
    /// &lt;summary&gt;
    /// 温度字段
    /// &lt;/summary&gt;
    private int temperature;

    /// &lt;summary&gt;
    /// 事件委托
    /// &lt;/summary&gt;
    /// &lt;param name=&quot;param&quot;&gt;&lt;/param&gt;
    public delegate void BoilHandler(int param);

    /// &lt;summary&gt;
    /// 将委托封装，并对外公布订阅和取消订阅的接口
    /// &lt;/summary&gt;
    public event BoilHandler BoilEvent;

    public void BoilWater()
    {
        for (int i = 0; i &lt;= 100; i++)
        {
            temperature = i;
            if (temperature &gt; 95)
            {
                // 调用所有注册对象的方法
                BoilEvent?.Invoke(temperature);
            }
        }
    }
}

/// &lt;summary&gt;
/// 警报器
/// &lt;/summary&gt;
public class Alarm
{
    public void Alert(int param)
    {
        Console.WriteLine(&quot;Alarm：dddddddd，水已经 {0} 度了：&quot;, param);
    }
}

/// &lt;summary&gt;
/// 显示器
/// &lt;/summary&gt;
public static class Display
{
    public static void ShowMsg(int param)
    {
        Console.WriteLine(&quot;Display：水已烧开，当前温度：{0}度。&quot;, param);
    }
}
</code></pre>
<pre><code class="language-cs"> [TestMethod]
 public void Example_02()
 {
     Heater heater = new Heater();
     Alarm alarm = new Alarm();
     // 注册方法
     heater.BoilEvent += alarm.Alert;
     //heater.BoilEvent += (new Alarm()).MakeAlert; // 匿名对象注册方法
     // 注册静态方法
     heater.BoilEvent += Display.ShowMsg;
     // 烧水，会自动调用注册过对象的方法
     heater.BoilWater();
     Assert.IsTrue(true);
 }
</code></pre>
<h3 id="Example-03">Example_03</h3>
<ul>
<li>委托类型的名称都应该以<code>EventHandler</code>结束</li>
<li>委托的原型定义：有一个<code>void</code>返回值，并接受两个输入参数：一个<code>Object</code>类型，一个<code>EventArgs</code>类型(或继承自<code>EventArgs</code>)</li>
<li>事件的命名为 委托去掉 <code>EventHandler</code>之后剩余的部分。</li>
<li>继承自<code>EventArgs</code>的类型应该以<code>EventArgs</code>结尾</li>
</ul>
<p>委托声明原型中的Object类型的参数代表了Subject，也就是监视对象，在本例中是 Heater(热水器)。回调函数(比如Alarm的Alert)可以通过它访问触发事件的对象(Heater),EventArgs 对象包含了Observer所感兴趣的数据，在本例中是temperature</p>
<pre><code class="language-cs">public class Heater
{
    /// &lt;summary&gt;
    /// 温度
    /// &lt;/summary&gt;
    private int temperature;

    /// &lt;summary&gt;
    /// 添加型号作为演示
    /// &lt;/summary&gt;
    public string type = &quot;01&quot;;

    /// &lt;summary&gt;
    /// 添加产地作为演示
    /// &lt;/summary&gt;
    public string area = &quot;China&quot;;

    public delegate void BoiledEventHandler(object sender, BoiledEventArgs e);

    public event BoiledEventHandler Boiled;

    /// &lt;summary&gt;
    /// 定义 BoiledEventArgs类，传递给 Observer 所感兴趣的信息
    /// &lt;/summary&gt;
    public class BoiledEventArgs : EventArgs
    {
        public readonly int temperature;

        public BoiledEventArgs(int temperature)
        {
            this.temperature = temperature;
        }
    }

    /// &lt;summary&gt;
    /// 提供继承自Heater的类重写，以便继承类拒绝其他对象对它的监视
    /// &lt;/summary&gt;
    /// &lt;param name=&quot;e&quot;&gt;&lt;/param&gt;
    protected virtual void OnBoiled(BoiledEventArgs e)
    {
        // 如果有对象注册
        Boiled?.Invoke(this, e);  // 调用所有注册对象的方法
    }

    public void BoilWater()
    {
        for (int i = 0; i &lt;= 100; i++)
        {
            temperature = i;
            if (temperature &gt; 95)
            {
                // 建立BoiledEventArgs 对象。
                BoiledEventArgs e = new BoiledEventArgs(temperature);
                OnBoiled(e); // 调用 OnBolied 方法
            }
        }
    }
}

// 警报器
public class Alarm
{
    public void MakeAlert(object sender, Heater.BoiledEventArgs e)
    {
        Heater heater = (Heater)sender;
        Console.WriteLine(&quot;Alarm：{0} - {1}: &quot;, heater.area, heater.type);
        Console.WriteLine(&quot;Alarm: 嘀嘀嘀，水已经 {0} 度了：&quot;, e.temperature);
        Console.WriteLine();
    }
}

// 显示器
public static class Display
{
    public static void ShowMsg(object sender, Heater.BoiledEventArgs e)
    {
        //静态方法
        Heater heater = (Heater)sender;
        Console.WriteLine(&quot;Display：{0} - {1}: &quot;, heater.area, heater.type);
        Console.WriteLine(&quot;Display：水快烧开了，当前温度：{0}度。&quot;, e.temperature);
        Console.WriteLine();
    }
}
</code></pre>
<pre><code class="language-cs">[TestMethod]
public void Example_03()
{
    Heater heater = new Heater();
    Alarm alarm = new Alarm();
    heater.Boiled += alarm.MakeAlert;   //注册方法
    //heater.Boiled += (new Alarm()).MakeAlert;      //给匿名对象注册方法
    //heater.Boiled += new Heater.BoiledEventHandler(alarm.MakeAlert);    //也可以这么注册
    heater.Boiled += Display.ShowMsg;       //注册静态方法
    heater.BoilWater();   //烧水，会自动调用注册过对象的方法
    Assert.IsTrue(true);
}
</code></pre>
<p><a href="http://www.cnblogs.com/jujusharp/archive/2011/08/04/2127999.html" target="_blank" rel="noopener">http://www.cnblogs.com/jujusharp/archive/2011/08/04/2127999.html</a></p>
<p><a href="https://www.cnblogs.com/HQFZ/p/4903400.html" target="_blank" rel="noopener">https://www.cnblogs.com/HQFZ/p/4903400.html</a></p>
<p><a href="https://www.cnblogs.com/wangjiming/p/8300103.html" target="_blank" rel="noopener">https://www.cnblogs.com/wangjiming/p/8300103.html</a></p>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>Bpmn</title>
    <url>/2020/12/09/bpmn/bpmn/</url>
    <content><![CDATA[<h1>简介</h1>
<p>BPMN(Business Process Model and Notation)，业务流程建模和标注，<strong>简单来说就是使用BPMN的图形来表达业务流程</strong></p>
<p><img src="/images/2020-12-16-11-04-53.png" alt=""></p>
<h1>BPMN2.0基础概念</h1>
<h2 id="Activity">Activity</h2>
<p>一个activity表示一份待完成的任务或工作，用圆角矩形表示。分为任务(task)和子流程(subprocess),一个任务表示工作需要被外部实体完成，比如人工或自动服务。 任务的类型显示在矩形的左上角，用小图标区别。根据任务的类型，引擎会执行不同的功能。</p>
<table>
<thead>
<tr>
<th>流程任务</th>
<th>属性</th>
</tr>
</thead>
<tbody>
<tr>
<td>Service Task（服务任务）</td>
<td>可以用于调外部服务或自动执行程序</td>
</tr>
<tr>
<td>Send Task（发送任务）</td>
<td>用于处理外部流程参与人发送消息的工作。eg：EmailTask</td>
</tr>
<tr>
<td>Receive Taks（接收任务）</td>
<td>等待外部流程参与者发送消息的任务</td>
</tr>
<tr>
<td>User Task（用户任务）</td>
<td>表示需要人参与的任务，可以配置候选人/组/代理人等</td>
</tr>
<tr>
<td>Script Task（脚本任务）</td>
<td>用于执行定义好的脚本程序，流程执行到这个结点自动执行脚本</td>
</tr>
</tbody>
</table>
<h3 id="用户任务-User-Task">用户任务(User Task)</h3>
<p>用户任务表示需要人来执行的任务，有一个输入和一个输出。</p>
<h3 id="服务任务-Service-Task">服务任务(Service Task)</h3>
<p>Service Task是一个自动活动，它会调用一些服务， 比如web service，java service等等，必须有一个输入和一个输出。</p>
<h3 id="脚本任务-Script-Task">脚本任务(Script Task)</h3>
<p>脚本任务时一个自动活动，当到达这个任务的时候流程引擎会执行一个脚本。必须有一个输入和一个输出。支持的脚本语言有Java，JavScript，XPath1.0，mvel。</p>
<p>脚本任务与服务任务的区别。服务任务一般用来处理和外部服务之间的交互。脚本任务只用来执行一些简单的逻辑。</p>
<h3 id="规则任务-Business-Rule-Task">规则任务(Business Rule Task)</h3>
<p>规则任务用来执行使用Drools定义的规则集，规则集通过ruleflow-group来识别。</p>
<h2 id="子流程-subprocess">子流程(subprocess)</h2>
<p>子流程表示多个activity的组合。子流程内部的元件禁止和外部的元件直连，只能作为一个整体与父流程的元件相连接。</p>
<h2 id="网关-gateway">网关(gateway)</h2>
<p>网关用来控制业务流程走向。分为如下四个之类，每个类型网关都需要设置gateway direction属性。下面的值可以使用：<br>
converging：网关必须拥有多个进入顺序流， 但是只能有一个外出顺序流。<br>
diverging：网关必须拥有一个进入顺序流， 和多个外出顺序流。</p>
<h3 id="唯一网关-Exclusive-Gateway">唯一网关(Exclusive Gateway)</h3>
<p>用一个内部包含X的菱形表示</p>
<h4 id="Diverging-发散">Diverging(发散)</h4>
<p>表示只有一个外向顺序流被执行。在执行时，必须确保至少一个外向顺序流上面的条件为true。</p>
<h4 id="Converging-会聚">Converging(会聚)</h4>
<p>每个入口顺序流执行完成之后，都会触发一次唯一网关后面的顺序流。</p>
<h3 id="并行网关-Parallel-Gateway">并行网关(Parallel Gateway)</h3>
<h4 id="Diverging-发散-2">Diverging(发散)</h4>
<p>表示多个外向顺心流会同时执行。</p>
<h4 id="Converging-会聚-2">Converging(会聚)</h4>
<p>等待所有的入口顺序流完成之后，才会触发出口顺序流。</p>
<p>包含网关(Inclusive Gateway)</p>
<h3 id="包含网关">包含网关</h3>
<p>Diverging(发散)<br>
只要外向顺心流上面的条件为true，则都会被执行。<br>
Converging(会聚)<br>
等待所有的active入口顺序流完成之后，才会触发出口顺序流。</p>
<h2 id="事件">事件</h2>
<h3 id="开始事件">开始事件</h3>
<p>表示业务流程是如何开始的。用一个细线圆表示，园中的图标表示触发的方式。<br>
空启动事件: 表示没有指明触发者。子流程必须有一个空启动事件。<br>
消息启动事件: 由外部消息来触发流程的执行。<br>
定时器启动事件: 由时间来触发流程的执行。</p>
<h3 id="结束事件">结束事件</h3>
<p>使用粗线圆表示，意味着流程的一个顺序流的结束。和启动事件不一样，在一个流程中出现多个结束事件是非常常见的。</p>
<h3 id="空结束事件">空结束事件</h3>
<p>表示流程中一个路径的结束，不返回任何结果。<br>
消息结束事件<br>
表示流程中一个路径的结束，并发送一个消息。<br>
Terminate结束事件<br>
结束整个流程的执行，即使有并行路径在执行。</p>
<h2 id="顺序流-Sequence-Flow">顺序流(Sequence Flow)</h2>
<p>表示顺序执行的顺序，用实线箭头表达。</p>
<h2 id="数据-Data-Object">数据(Data Object)</h2>
<p>Data Object可以理解为流程实例的局部临时变量，流程实例结束后Data Object也被释放。</p>
<h2 id="Artifact">Artifact</h2>
<p>artifact表示没有执行语义，也就是说对流程的执行没有任何影响。BPMN中有两种artifact，注释和分组。</p>
<h2 id="注释-Text-Annotation">注释(Text Annotation)</h2>
<p>用于对流程图中的元件进行解释说明。</p>
<h2 id="分组-Group">分组(Group)</h2>
<p>用带虚线的矩形框表达，本质上没有任何执行相关的含义。</p>
<h1>实践</h1>
<h2 id="流程图">流程图</h2>
<p><img src="/images/2020-12-16-10-51-25.png" alt=""></p>
<h2 id="生成的XML文件">生成的XML文件</h2>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;bpmn2:definitions xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot; xmlns:bpmn2=&quot;http://www.omg.org/spec/BPMN/20100524/MODEL&quot; xmlns:bpmndi=&quot;http://www.omg.org/spec/BPMN/20100524/DI&quot; xmlns:dc=&quot;http://www.omg.org/spec/DD/20100524/DC&quot; xmlns:di=&quot;http://www.omg.org/spec/DD/20100524/DI&quot; id=&quot;sample-diagram&quot; targetNamespace=&quot;http://bpmn.io/schema/bpmn&quot; xsi:schemaLocation=&quot;http://www.omg.org/spec/BPMN/20100524/MODEL BPMN20.xsd&quot;&gt;
  &lt;bpmn2:process id=&quot;Process_1&quot; isExecutable=&quot;false&quot;&gt;

    &lt;bpmn2:startEvent id=&quot;StartEvent_1&quot;&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1pkwsp2&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:startEvent&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1pkwsp2&quot; sourceRef=&quot;StartEvent_1&quot; targetRef=&quot;Task_08a6w5d&quot; /&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0o2t9pt&quot; sourceRef=&quot;Task_0f8o5aw&quot; targetRef=&quot;Task_0i5bfbz&quot; /&gt;

    &lt;bpmn2:exclusiveGateway id=&quot;ExclusiveGateway_139zs30&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_16yinb7&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0o1558l&lt;/bpmn2:outgoing&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1bi3f0i&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:exclusiveGateway&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_16yinb7&quot; sourceRef=&quot;Task_0i5bfbz&quot; targetRef=&quot;ExclusiveGateway_139zs30&quot; /&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0o1558l&quot; sourceRef=&quot;ExclusiveGateway_139zs30&quot; targetRef=&quot;Task_122uxom&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status=="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;

    &lt;bpmn2:exclusiveGateway id=&quot;ExclusiveGateway_1u1tohc&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0yj5u4u&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_05546n3&lt;/bpmn2:outgoing&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_04274wh&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:exclusiveGateway&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0yj5u4u&quot; sourceRef=&quot;Task_122uxom&quot; targetRef=&quot;ExclusiveGateway_1u1tohc&quot; /&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_05546n3&quot; sourceRef=&quot;ExclusiveGateway_1u1tohc&quot; targetRef=&quot;Task_1g1imuh&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status=="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;

    &lt;bpmn2:exclusiveGateway id=&quot;ExclusiveGateway_1qorevu&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1iorjgw&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0q208gh&lt;/bpmn2:outgoing&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0bzccir&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:exclusiveGateway&gt;

    &lt;bpmn2:endEvent id=&quot;EndEvent_0h4s3ey&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1c9ogpj&lt;/bpmn2:incoming&gt;
    &lt;/bpmn2:endEvent&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_15p4qmb&quot; sourceRef=&quot;Task_08a6w5d&quot; targetRef=&quot;Task_0f8o5aw&quot; /&gt;
    
    &lt;bpmn2:userTask id=&quot;Task_08a6w5d&quot; name=&quot;填写申请表单&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1pkwsp2&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1bi3f0i&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0r0u448&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_04274wh&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0bzccir&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_15p4qmb&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    &lt;bpmn2:userTask id=&quot;Task_0f8o5aw&quot; name=&quot;上传申请表单扫描件&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_15p4qmb&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0o2t9pt&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    &lt;bpmn2:userTask id=&quot;Task_0i5bfbz&quot; name=&quot;审核扫描件&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0o2t9pt&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_16yinb7&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    &lt;bpmn2:userTask id=&quot;Task_122uxom&quot; name=&quot;专家中心审核&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0o1558l&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0yj5u4u&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    &lt;bpmn2:userTask id=&quot;Task_1g1imuh&quot; name=&quot;信息技术服务中心费用审查&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_05546n3&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1iorjgw&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    &lt;bpmn2:userTask id=&quot;Task_0tblwxb&quot; name=&quot;信息管理部审批&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_0q208gh&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1mvfpx8&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0q208gh&quot; sourceRef=&quot;ExclusiveGateway_1qorevu&quot; targetRef=&quot;Task_0tblwxb&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status=="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;

    &lt;bpmn2:exclusiveGateway id=&quot;ExclusiveGateway_0wvl0q1&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1mvfpx8&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1ij0j7w&lt;/bpmn2:outgoing&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_0r0u448&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:exclusiveGateway&gt;

    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1ij0j7w&quot; sourceRef=&quot;ExclusiveGateway_0wvl0q1&quot; targetRef=&quot;Task_0w6v3ig&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status=="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1c9ogpj&quot; sourceRef=&quot;Task_0w6v3ig&quot; targetRef=&quot;EndEvent_0h4s3ey&quot; /&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1bi3f0i&quot; sourceRef=&quot;ExclusiveGateway_139zs30&quot; targetRef=&quot;Task_08a6w5d&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status!="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0r0u448&quot; sourceRef=&quot;ExclusiveGateway_0wvl0q1&quot; targetRef=&quot;Task_08a6w5d&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status!="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_04274wh&quot; sourceRef=&quot;ExclusiveGateway_1u1tohc&quot; targetRef=&quot;Task_08a6w5d&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status!="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1mvfpx8&quot; sourceRef=&quot;Task_0tblwxb&quot; targetRef=&quot;ExclusiveGateway_0wvl0q1&quot; /&gt;
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_0bzccir&quot; sourceRef=&quot;ExclusiveGateway_1qorevu&quot; targetRef=&quot;Task_08a6w5d&quot;&gt;
      &lt;bpmn2:conditionExpression xsi:type=&quot;bpmn2:tFormalExpression&quot;&gt;{{status!="同意"}}&lt;/bpmn2:conditionExpression&gt;
    &lt;/bpmn2:sequenceFlow&gt;

    &lt;bpmn2:userTask id=&quot;Task_0w6v3ig&quot; name=&quot;专家中心组织实施&quot;&gt;
      &lt;bpmn2:incoming&gt;SequenceFlow_1ij0j7w&lt;/bpmn2:incoming&gt;
      &lt;bpmn2:outgoing&gt;SequenceFlow_1c9ogpj&lt;/bpmn2:outgoing&gt;
    &lt;/bpmn2:userTask&gt;
    
    &lt;bpmn2:sequenceFlow id=&quot;SequenceFlow_1iorjgw&quot; sourceRef=&quot;Task_1g1imuh&quot; targetRef=&quot;ExclusiveGateway_1qorevu&quot; /&gt;
  &lt;/bpmn2:process&gt;

  &lt;bpmndi:BPMNDiagram id=&quot;BPMNDiagram_1&quot;&gt;
    &lt;bpmndi:BPMNPlane id=&quot;BPMNPlane_1&quot; bpmnElement=&quot;Process_1&quot;&gt;
      &lt;bpmndi:BPMNShape id=&quot;_BPMNShape_StartEvent_2&quot; bpmnElement=&quot;StartEvent_1&quot;&gt;
        &lt;dc:Bounds x=&quot;246&quot; y=&quot;133&quot; width=&quot;36&quot; height=&quot;36&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;219&quot; y=&quot;169&quot; width=&quot;90&quot; height=&quot;20&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1pkwsp2_di&quot; bpmnElement=&quot;SequenceFlow_1pkwsp2&quot;&gt;
        &lt;di:waypoint x=&quot;282&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;349&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;315.5&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0o2t9pt_di&quot; bpmnElement=&quot;SequenceFlow_0o2t9pt&quot;&gt;
        &lt;di:waypoint x=&quot;645&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;742&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;693.5&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;ExclusiveGateway_139zs30_di&quot; bpmnElement=&quot;ExclusiveGateway_139zs30&quot; isMarkerVisible=&quot;true&quot;&gt;
        &lt;dc:Bounds x=&quot;937&quot; y=&quot;126&quot; width=&quot;50&quot; height=&quot;50&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;962&quot; y=&quot;180&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_16yinb7_di&quot; bpmnElement=&quot;SequenceFlow_16yinb7&quot;&gt;
        &lt;di:waypoint x=&quot;842&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;937&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;889.5&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0o1558l_di&quot; bpmnElement=&quot;SequenceFlow_0o1558l&quot;&gt;
        &lt;di:waypoint x=&quot;987&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;1055&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1021&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;ExclusiveGateway_1u1tohc_di&quot; bpmnElement=&quot;ExclusiveGateway_1u1tohc&quot; isMarkerVisible=&quot;true&quot;&gt;
        &lt;dc:Bounds x=&quot;1244&quot; y=&quot;126&quot; width=&quot;50&quot; height=&quot;50&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1269&quot; y=&quot;180&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0yj5u4u_di&quot; bpmnElement=&quot;SequenceFlow_0yj5u4u&quot;&gt;
        &lt;di:waypoint x=&quot;1155&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;1244&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1199.5&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_05546n3_di&quot; bpmnElement=&quot;SequenceFlow_05546n3&quot;&gt;
        &lt;di:waypoint x=&quot;1294&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;1348&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1321&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;ExclusiveGateway_1qorevu_di&quot; bpmnElement=&quot;ExclusiveGateway_1qorevu&quot; isMarkerVisible=&quot;true&quot;&gt;
        &lt;dc:Bounds x=&quot;1524&quot; y=&quot;126&quot; width=&quot;50&quot; height=&quot;50&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1549&quot; y=&quot;186&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;EndEvent_0h4s3ey_di&quot; bpmnElement=&quot;EndEvent_0h4s3ey&quot;&gt;
        &lt;dc:Bounds x=&quot;1380&quot; y=&quot;465&quot; width=&quot;36&quot; height=&quot;36&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1398&quot; y=&quot;505&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_15p4qmb_di&quot; bpmnElement=&quot;SequenceFlow_15p4qmb&quot;&gt;
        &lt;di:waypoint x=&quot;449&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;545&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;497&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_1hs0nix_di&quot; bpmnElement=&quot;Task_08a6w5d&quot;&gt;
        &lt;dc:Bounds x=&quot;349&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_11y33o8_di&quot; bpmnElement=&quot;Task_0f8o5aw&quot;&gt;
        &lt;dc:Bounds x=&quot;545&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_0joc7sr_di&quot; bpmnElement=&quot;Task_0i5bfbz&quot;&gt;
        &lt;dc:Bounds x=&quot;742&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_1t9tk6x_di&quot; bpmnElement=&quot;Task_122uxom&quot;&gt;
        &lt;dc:Bounds x=&quot;1055&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_1ymdptf_di&quot; bpmnElement=&quot;Task_1g1imuh&quot;&gt;
        &lt;dc:Bounds x=&quot;1348&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_1uqp059_di&quot; bpmnElement=&quot;Task_0tblwxb&quot;&gt;
        &lt;dc:Bounds x=&quot;1654&quot; y=&quot;111&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0q208gh_di&quot; bpmnElement=&quot;SequenceFlow_0q208gh&quot;&gt;
        &lt;di:waypoint x=&quot;1574&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;1654&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1614&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;ExclusiveGateway_0wvl0q1_di&quot; bpmnElement=&quot;ExclusiveGateway_0wvl0q1&quot; isMarkerVisible=&quot;true&quot;&gt;
        &lt;dc:Bounds x=&quot;1679&quot; y=&quot;313&quot; width=&quot;50&quot; height=&quot;50&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1739&quot; y=&quot;332&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1ij0j7w_di&quot; bpmnElement=&quot;SequenceFlow_1ij0j7w&quot;&gt;
        &lt;di:waypoint x=&quot;1704&quot; y=&quot;363&quot; /&gt;
        &lt;di:waypoint x=&quot;1704&quot; y=&quot;483&quot; /&gt;
        &lt;di:waypoint x=&quot;1583&quot; y=&quot;483&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1719&quot; y=&quot;417&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1c9ogpj_di&quot; bpmnElement=&quot;SequenceFlow_1c9ogpj&quot;&gt;
        &lt;di:waypoint x=&quot;1483&quot; y=&quot;483&quot; /&gt;
        &lt;di:waypoint x=&quot;1416&quot; y=&quot;483&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1449.5&quot; y=&quot;462&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1bi3f0i_di&quot; bpmnElement=&quot;SequenceFlow_1bi3f0i&quot;&gt;
        &lt;di:waypoint x=&quot;962&quot; y=&quot;126&quot; /&gt;
        &lt;di:waypoint x=&quot;962&quot; y=&quot;-35&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;-35&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;111&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;680.5&quot; y=&quot;-56&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0r0u448_di&quot; bpmnElement=&quot;SequenceFlow_0r0u448&quot;&gt;
        &lt;di:waypoint x=&quot;1679&quot; y=&quot;338&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;338&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;191&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1039&quot; y=&quot;317&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_04274wh_di&quot; bpmnElement=&quot;SequenceFlow_04274wh&quot;&gt;
        &lt;di:waypoint x=&quot;1269&quot; y=&quot;126&quot; /&gt;
        &lt;di:waypoint x=&quot;1269&quot; y=&quot;-118&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;-118&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;111&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;834&quot; y=&quot;-139&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1mvfpx8_di&quot; bpmnElement=&quot;SequenceFlow_1mvfpx8&quot;&gt;
        &lt;di:waypoint x=&quot;1704&quot; y=&quot;191&quot; /&gt;
        &lt;di:waypoint x=&quot;1704&quot; y=&quot;313&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1719&quot; y=&quot;246&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_0bzccir_di&quot; bpmnElement=&quot;SequenceFlow_0bzccir&quot;&gt;
        &lt;di:waypoint x=&quot;1549&quot; y=&quot;126&quot; /&gt;
        &lt;di:waypoint x=&quot;1549&quot; y=&quot;-189&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;-189&quot; /&gt;
        &lt;di:waypoint x=&quot;399&quot; y=&quot;111&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;974&quot; y=&quot;-210&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNShape id=&quot;UserTask_1koiwyj_di&quot; bpmnElement=&quot;Task_0w6v3ig&quot;&gt;
        &lt;dc:Bounds x=&quot;1483&quot; y=&quot;443&quot; width=&quot;100&quot; height=&quot;80&quot; /&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge id=&quot;SequenceFlow_1iorjgw_di&quot; bpmnElement=&quot;SequenceFlow_1iorjgw&quot;&gt;
        &lt;di:waypoint x=&quot;1448&quot; y=&quot;151&quot; /&gt;
        &lt;di:waypoint x=&quot;1524&quot; y=&quot;151&quot; /&gt;
        &lt;bpmndi:BPMNLabel&gt;
          &lt;dc:Bounds x=&quot;1486&quot; y=&quot;130&quot; width=&quot;0&quot; height=&quot;12&quot; /&gt;
        &lt;/bpmndi:BPMNLabel&gt;
      &lt;/bpmndi:BPMNEdge&gt;
    &lt;/bpmndi:BPMNPlane&gt;
  &lt;/bpmndi:BPMNDiagram&gt;
&lt;/bpmn2:definitions&gt;
</code></pre>
<h2 id="节点描述">节点描述</h2>
<h3 id="process">process</h3>
<blockquote>
<p>流程定义根元素，代表了一个流程定义的开始</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>流程定义ID，代表该流程的唯一性，启动该流程时需要使用该ID</td>
</tr>
<tr>
<td>isExecutable</td>
<td>表示该流程是否可执行，其值有true和false，默认为true</td>
</tr>
<tr>
<td>name</td>
<td>流程名称</td>
</tr>
<tr>
<td>type</td>
<td>流程类型</td>
</tr>
<tr>
<td>isClosed</td>
<td>流程是否已关闭,关闭不能执行</td>
</tr>
</tbody>
</table>
<h3 id="startEvent">startEvent</h3>
<blockquote>
<p>流程启动事件，一个<code>process</code>只能有一个,且必须为流程起始元素</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>启动节点id</td>
</tr>
<tr>
<td>name</td>
<td>启动节点名称</td>
</tr>
</tbody>
</table>
<h3 id="sequenceFlow">sequenceFlow</h3>
<blockquote>
<p>顺序流</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>顺序流id</td>
</tr>
<tr>
<td>sourceRef</td>
<td>上一个节点id</td>
</tr>
<tr>
<td>targetRef</td>
<td>下一个节点id</td>
</tr>
</tbody>
</table>
<h3 id="exclusiveGateway">exclusiveGateway</h3>
<blockquote>
<p>排它性网关,即多个<code>sequenceFlow</code>以网关节点开始时,只根据条件执行其中一条流,其他流不再判断</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>节点id</td>
</tr>
<tr>
<td>name</td>
<td>节点名称</td>
</tr>
<tr>
<td>gatewayDirection</td>
<td>网关方向,Unspecified</td>
</tr>
</tbody>
</table>
<h3 id="userTask">userTask</h3>
<blockquote>
<p>流程中间用户任务,夹在startEvent与endEvent之间的节点，表示需要待办人处理的节点</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>任务节点id，必须是唯一的</td>
</tr>
<tr>
<td>name</td>
<td>任务节点名称</td>
</tr>
<tr>
<td>activiti:assignee</td>
<td>任务所属用户,只能指定用户完成这个任务,即任务办理人</td>
</tr>
<tr>
<td>activiti:candidateUsers</td>
<td>多个任务办理人</td>
</tr>
<tr>
<td>activiti:candidateGroups</td>
<td>任务处理人候选组,处理人必须在这个组内</td>
</tr>
<tr>
<td>activiti:exclusive</td>
<td>独家的,好像是在排它性网关中使用,意思应该是在有并行分支情况下,只会走其中一条</td>
</tr>
<tr>
<td>activiti:dueDate</td>
<td>设置用户任务到期日期</td>
</tr>
<tr>
<td>activiti:priority</td>
<td>用户任务优先级,0-100</td>
</tr>
</tbody>
</table>
<h3 id="endEvent">endEvent</h3>
<blockquote>
<p>流程结束事件,一个<code>process</code>只能有一个,且必须为流程结束元素</p>
</blockquote>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td>结束节点id</td>
</tr>
<tr>
<td>name</td>
<td>结束节点名称</td>
</tr>
</tbody>
</table>
<h1>bpmnlint校验</h1>
<h2 id="git">git</h2>
<p><a href="https://github.com/bpmn-io/bpmnlint-playground" target="_blank" rel="noopener">https://github.com/bpmn-io/bpmnlint-playground</a></p>
<h2 id="校验">校验</h2>
<p><img src="/images/2020-12-16-11-09-37.png" alt=""></p>
]]></content>
      <categories>
        <category>Bpmn</category>
      </categories>
  </entry>
  <entry>
    <title>反射</title>
    <url>/2020/12/14/csharp/Reflect/</url>
    <content><![CDATA[<h2 id="定义">定义</h2>
<p>反射提供了封装程序集、模块和类型的对象（Type类型）。可以使用反射动态创建类型的实例，将类型绑定到现有对象，或从现有对象获取类型并调用其方法或访问其字段和属性</p>
<h2 id="使用场景">使用场景</h2>
<ul>
<li>需要访问程序元数据的特性</li>
<li>检查和实例化程序集中的类型</li>
<li>在运行时构建新类型。使用System.Reflection.Emit中的类</li>
<li>执行后期绑定，访问在运行时创建的类型的方法</li>
</ul>
<h2 id="用途">用途</h2>
<table>
<thead>
<tr>
<th>类型</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>Assembly</td>
<td>定义和加载程序集，加载程序集清单中列出的模块，以及从此程序集中查找类型并创建该类型的实例</td>
</tr>
<tr>
<td>Module</td>
<td>了解包含模块的程序集以及模块中的类等，还可以获取在模块上定义的所有全局方法或其他特定的非全局方法</td>
</tr>
<tr>
<td>ConstructorInfo</td>
<td>了解构造器的名称、参数、访问修饰符（如public或private）和实现详细信息（如abstract或virtual）等。使用Type的GetConstructors或GetConstructor方法来调用特定的构造函数</td>
</tr>
<tr>
<td>MethodInfo</td>
<td>了解方法的名称、返回类型、参数、访问修饰符（如public或private）和实现详细信息（如abstract或virtual）等。使用Type的GetMethods或GetMethod方法来调用特定的方法</td>
</tr>
<tr>
<td>FieldInfo</td>
<td>了解字段的名称、访问修饰符（如public或private）和实现详细信息（如static）等，并获取或设置字段值</td>
</tr>
<tr>
<td>EventInfo</td>
<td>了解事件的名称、事件处理程序数据类型、自定义特性、声明类型和反射类型等，并添加或移除事件处理程序</td>
</tr>
<tr>
<td>PropertyInfo</td>
<td>了解属性的名称、数据类型、声明类型、反射类型和只读或可写状态等，并获取或设置属性值</td>
</tr>
<tr>
<td>ParameterInfo</td>
<td>了解参数的名称、数据类型、参数是输入参数还是输出参数等，以及参数在方法签名中的位置等</td>
</tr>
</tbody>
</table>
<p>（1）使用<code>Assembly</code>定义和加载程序集，加载在程序集中的所有模块以及从此程序集中查找类型并创建该类型的实例<br>
（2）使用<code>Module</code>了解包含模块的程序集以及模块中的类等，还可以获取在模块上定义的所有全局方法或其他特定的非全局方法<br>
（3）使用<code>ConstructorInfo</code>了解构造函数的名称、参数、访问修饰符（如pulic 或private）和实现详细信息（如abstract或virtual）等<br>
（4）使用<code>MethodInfo</code>了解方法的名称、返回类型、参数、访问修饰符（如pulic 或private）和实现详细信息（如abstract或virtua）等<br>
（5）使用<code>FiedInfo</code>了解字段的名称、访问修饰符（如public或private）和实现详细信息（如static）等，并获取或设置字段值<br>
（6）使用<code>EventInfo</code>了解事件的名称、事件处理程序数据类型、自定义属性、声明类型和反射类型等，添加或移除事件处理程序<br>
（7）使用<code>PropertyInfo</code>了解属性的名称、数据类型、声明类型、反射类型和只读或可写状态等，获取或设置属性值<br>
（8）使用<code>ParameterInfo</code>了解参数的名称、数据类型、是输入参数还是输出参数，以及参数在方法签名中的位置等</p>
<h2 id="反射用到的命名空间">反射用到的命名空间</h2>
<ul>
<li>System.Reflection</li>
<li>System.Type</li>
<li>System.Reflection.Assembly</li>
</ul>
<h2 id="反射用到的主要类">反射用到的主要类</h2>
<ul>
<li>System.Type 类－－通过这个类可以访问任何给定数据类型的信息。</li>
<li>System.Reflection.Assembly 类－－它可以用于访问给定程序集的信息，或者把这个程序集加载到程序中</li>
</ul>
<h2 id="System-Type">System.Type</h2>
<p><code>System.Type</code>类对于反射有很重要的作用。它是一个抽象的基类，<code>Type</code>有与每种数据类型对应的派生类，我们使用这个派生类的对象的方法、字段、属性来查找有关该类型的所有信息。<br>
获取给定类型的Type值有三种常用方式：</p>
<h3 id="typeof运算符"><code>typeof</code>运算符</h3>
<p><code>Type t=typeof(string);</code></p>
<h3 id="对象的GetType-方法">对象的<code>GetType()</code>方法</h3>
<p><code>string s=”guo”;Type t=s.GetType();</code></p>
<h3 id="Type类的静态方法GetType">Type类的静态方法<code>GetType()</code></h3>
<p><code>Type t=Type.GetType(&quot;System.String&quot;);</code></p>
<h3 id="Type类属性">Type类属性</h3>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Name</td>
<td>数据类型名</td>
</tr>
<tr>
<td>FullName</td>
<td>数据类型的完全限定名(包括命名空间名)</td>
</tr>
<tr>
<td>Namespace</td>
<td>定义数据类型的命名空间名</td>
</tr>
<tr>
<td>IsAbstract</td>
<td>指示该类型是否是抽象类型</td>
</tr>
<tr>
<td>IsArray</td>
<td>指示该类型是否是数组</td>
</tr>
<tr>
<td>IsClass</td>
<td>指示该类型是否是类</td>
</tr>
<tr>
<td>IsEnum</td>
<td>指示该类型是否是枚举</td>
</tr>
<tr>
<td>IsInterface</td>
<td>指示该类型是否是接口</td>
</tr>
<tr>
<td>IsPublic</td>
<td>指示该类型是否是公有的</td>
</tr>
<tr>
<td>IsSealed</td>
<td>指示该类型是否是密封类</td>
</tr>
<tr>
<td>IsValueType</td>
<td>指示该类型是否是值类型</td>
</tr>
</tbody>
</table>
<h3 id="Type类方法">Type类方法</h3>
<table>
<thead>
<tr>
<th>方法名称</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>GetConstructor(), GetConstructors()</td>
<td>返回ConstructorInfo类型，用于取得该类的构造函数的信息</td>
</tr>
<tr>
<td>GetEvent(), GetEvents()</td>
<td>返回EventInfo类型，用于取得该类的事件的信息</td>
</tr>
<tr>
<td>GetField(), GetFields()</td>
<td>返回FieldInfo类型，用于取得该类的字段（成员变量）的信息</td>
</tr>
<tr>
<td>GetInterface(), GetInterfaces()</td>
<td>返回InterfaceInfo类型，用于取得该类实现的接口的信息</td>
</tr>
<tr>
<td>GetMember(), GetMembers()</td>
<td>返回MemberInfo类型，用于取得该类的所有成员的信息</td>
</tr>
<tr>
<td>GetMethod(), GetMethods()</td>
<td>返回MethodInfo类型，用于取得该类的方法的信息</td>
</tr>
<tr>
<td>GetProperty(), GetProperties()</td>
<td>返回PropertyInfo类型，用于取得该类的属性的信息</td>
</tr>
</tbody>
</table>
<p>可以调用这些成员，其方式是调用Type的InvokeMember()方法，或者调用MethodInfo, PropertyInfo和其他类的Invoke()方法</p>
<pre><code class="language-cs">public class ReflectClass
{
    public string Address;

    public int Age { get; set; }

    public string Sex { get; set; }

    public string Name { get; set; }

    public ReflectClass() { }

    public ReflectClass(string name)
        {
            this.Name = name;
        }

    public ReflectClass(string name, string sex)
        {
            this.Name = name;
            this.Sex = sex;
        }

    public void Show()
        {
            Console.WriteLine(&quot;姓名：&quot; + Name + &quot;\n&quot; + &quot;年龄：&quot; + Age + &quot;\n&quot; + &quot;性别：&quot; + Sex);
        }
}
</code></pre>
<h3 id="获取所有构造函数">获取所有构造函数</h3>
<pre><code class="language-cs"> [TestMethod]
 public void GetConstructors()
 {
     Type t = new ReflectClass().GetType();
     // 获取类的所有构造函数
     ConstructorInfo[] constructorInfos = t.GetConstructors();
     foreach (ConstructorInfo ci in constructorInfos)
     {
         // 获取每个构造函数的参数
         ParameterInfo[] parameterInfos = ci.GetParameters();
         foreach (ParameterInfo p in parameterInfos)
         {
             Console.WriteLine(p.ParameterType.ToString() + &quot;\n&quot; + p.Name + &quot;\n&quot;);
         }
     }
     Assert.IsTrue(constructorInfos.Length &gt; 0);
 }
</code></pre>
<h3 id="动态创建对象">动态创建对象</h3>
<pre><code class="language-cs">[TestMethod]
public void DynamicCreateObject()
{
    Type t = typeof(ReflectClass);
    Type[] pt = new Type[2];
    pt[0] = typeof(string);
    pt[1] = typeof(string);
    //根据参数类型获取构造函数
    ConstructorInfo ci = t.GetConstructor(pt);
    //构造Object数组，作为构造函数的输入参数
    object[] obj = new object[2] { &quot;wang&quot;, &quot;男&quot; };
    //调用构造函数生成对象
    object @object = ci.Invoke(obj);
    //调用生成的对象的方法测试是否对象生成成功
    ((ReflectClass)@object).Show();
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="使用Activator动态创建对象">使用Activator动态创建对象</h3>
<pre><code class="language-cs">[TestMethod]
public void ActivatorDynamicCreateObject()
{
    Type t = typeof(ReflectClass);
    object[] obj = new object[2] { &quot;wang&quot;, &quot;男&quot; };
    //用Activator的CreateInstance静态方法，生成新对象
    object @object = Activator.CreateInstance(t, obj);
    ((ReflectClass)@object).Show();
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="获取类中的Public属性">获取类中的Public属性</h3>
<pre><code class="language-cs">[TestMethod]
public void GetProperties()
{
    Type t = new ReflectClass().GetType();
    PropertyInfo[] propertyInfos = t.GetProperties();
    foreach (PropertyInfo p in propertyInfos)
    {
        Console.WriteLine(p.Name);
    }
    Assert.IsTrue(true);
}

</code></pre>
<h3 id="获取类中Public方法">获取类中Public方法</h3>
<pre><code class="language-cs">[TestMethod]
public void GetPublicMethod()
{
    Type t = new ReflectClass().GetType();
    MethodInfo[] mi = t.GetMethods();
    foreach (MethodInfo method in mi)
    {
        Console.WriteLine(method.ReturnType + &quot;|&quot; + method.Name);
    }
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="获取类中Public字段">获取类中Public字段</h3>
<pre><code>[TestMethod]
public void GetField()
{
    Type t = new ReflectClass().GetType();
    FieldInfo[] fieldInfos = t.GetFields();
    foreach (FieldInfo fieldInfo in fieldInfos)
    {
        Console.WriteLine(fieldInfo.Name);
    }
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="Example-01">Example_01</h3>
<pre><code class="language-cs">[TestMethod]
public void Example_01()
{
    ReflectClass rc = new ReflectClass();
    Type t = rc.GetType();
    object obj = Activator.CreateInstance(t);

    FieldInfo address = t.GetField(&quot;Address&quot;);
    address.SetValue(obj, &quot;Beijing&quot;);

    PropertyInfo name = t.GetProperty(&quot;Name&quot;);
    name.SetValue(obj, &quot;wang&quot;, null);

    PropertyInfo age = t.GetProperty(&quot;Age&quot;);
    age.SetValue(obj, 20, null);

    MethodInfo method = t.GetMethod(&quot;Show&quot;);
    method.Invoke(obj, null);

    Console.WriteLine(&quot;Address为：&quot; + ((ReflectClass)obj).Address);
    Assert.IsTrue(true);
}
</code></pre>
<h2 id="Assembly">Assembly</h2>
<h3 id="GetAssembly">GetAssembly</h3>
<pre><code class="language-cs">[TestMethod]
public void GetAssembly()
{
    // 通过程序集名称返回Assembly对象
    Assembly assembly = Assembly.Load(&quot;CodeSnippet&quot;);

    // 通过Assembly获取程序集中类(参数必须是类的全名)
    Type type = assembly.GetType(&quot;CodeSnippet.Csharp.ReflectClass&quot;);
    Assert.IsNotNull(type);

    // 通过Assembly获取程序集中所有的类
    Type[] types = assembly.GetTypes();
    Assert.IsNotNull(types);

    // 通过DLL文件名称返回Assembly对象
warning disable S3885 // &quot;Assembly.Load&quot; should be used
    Assembly assembly2 = Assembly.LoadFrom(&quot;CodeSnippet.dll&quot;);
warning restore S3885 // &quot;Assembly.Load&quot; should be used

    // 通过Assembly获取程序集中类(参数必须是类的全名)
    Type type2 = assembly2.GetType(&quot;CodeSnippet.Csharp.ReflectClass&quot;);
    Assert.IsNotNull(type2);

    // 通过Assembly获取程序集中所有的类
    Type[] types2 = assembly2.GetTypes();
    Assert.IsNotNull(types2);
}
</code></pre>
<h3 id="通过程序集的名称反射">通过程序集的名称反射</h3>
<pre><code class="language-cs">[TestMethod]
public void Example_02()
{
    Assembly assembly = Assembly.Load(&quot;CodeSnippet&quot;);
    //参数必须是类的全名
    Type t = assembly.GetType(&quot;CodeSnippet.Csharp.ReflectClass&quot;);
    object o = Activator.CreateInstance(t, &quot;男&quot;);
    MethodInfo mi = t.GetMethod(&quot;Show&quot;);
    mi.Invoke(o, null);
    Assert.IsNotNull(o);
}
</code></pre>
<h3 id="Example-03">Example_03</h3>
<pre><code class="language-cs">[TestMethod]
public void Example_03()
{
warning disable S3885 // &quot;Assembly.Load&quot; should be used
    Assembly assembly = Assembly.LoadFrom(&quot;CodeSnippet.dll&quot;);
warning restore S3885 // &quot;Assembly.Load&quot; should be used
    Type[] types = assembly.GetTypes();
    foreach (Type t in types)
    {
        if (t.FullName == &quot;CodeSnippet.Csharp.ReflectClass&quot;)
        {
            object o = Activator.CreateInstance(t);
            Assert.IsNotNull(o);
        }
    }
}
</code></pre>
<h3 id="通过DLL文件全名反射其中的所有类型">通过DLL文件全名反射其中的所有类型</h3>
<pre><code class="language-cs">[TestMethod]
public void Example_03()
{
warning disable S3885 // &quot;Assembly.Load&quot; should be used
    Assembly assembly = Assembly.LoadFrom(&quot;CodeSnippet.dll&quot;);
warning restore S3885 // &quot;Assembly.Load&quot; should be used
    Type[] types = assembly.GetTypes();
    foreach (Type t in types)
    {
        if (t.FullName == &quot;CodeSnippet.Csharp.ReflectClass&quot;)
        {
            object o = Activator.CreateInstance(t);
            Assert.IsNotNull(o);
        }
    }
}
</code></pre>
<h2 id="BindingFlags">BindingFlags</h2>
<pre><code class="language-cs">//     Specifies flags that control binding and the way in which the search for members
//     and types is conducted by reflection.
</code></pre>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>多线程</title>
    <url>/2020/12/08/csharp/%E5%A4%9A%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<p>使用多线程可以充分利用<code>CPU</code>资源.提高<code>CPU</code>的使用率,采用多线程的方式去同时完成几件事情而不互相干扰.在处理大量的<code>IO</code>操作或处理的情况需要花费大量的时间时(如:读写文件,视频图像的采集,处理,显示,保存等)有较大优势</p>
<a id="more"></a>
<p><strong>优点</strong></p>
<ul>
<li>多线程可以把占据时间长的程序中的任务放到后台去处理而不影响主程序的运行</li>
<li>程序的运行效率可能会提高</li>
<li>在一些等待的任务实现上如用户输入,文件读取和网络收发数据等,线程比较有用</li>
</ul>
<p><strong>不足</strong></p>
<ul>
<li>如果有大量的线程,会影响性能,因为操作系统需要在它们之间切换</li>
<li>更多的线程需要更多的内存空间</li>
</ul>
<h2 id="概念了解">概念了解</h2>
<h3 id="并发-Concurrency">并发(<code>Concurrency</code>)</h3>
<p>逻辑上的同时发生，一个处理器（在不同时刻或者说在同一时间间隔内）&quot;同时&quot;处理多个任务。宏观上是并发的，微观上是按排队等待、唤醒、执行的步骤序列执行。并发性是对有限物理资源强制行使多用户共享（多路复用）以提高效率</p>
<h3 id="并行-Parallel">并行(<code>Parallel</code>)</h3>
<p>物理上的同时发生，多核处理器或多个处理器（在同一时刻）同时处理多个任务。并行性允许多个程序同一时刻可在不同 CPU 上同时执行</p>
<h3 id="进程-Process">进程(<code>Process</code>)</h3>
<p>程序在计算机上的一次执行活动。运行一个程序、启动一个进程。程序是死的（静态的），进程是活的（动态的）。<code>Windows</code>系统利用进程把工作划分为多个独立的区域，每个应用程序实例对应一个进程。进程是操作系统分配和使用系统资源的基本单位。进程包含一个运行-ing 应用程序的所有资源、进程（占用的资源）间相互独立</p>
<h3 id="线程-Thread">线程(<code>Thread</code>)</h3>
<p>轻量级进程，是进程的一个实体（线程本质上是进程中一段并发运行的代码），执行线程、体现程序的真实执行情况，是处理器上系统独立调度和时间分配的最基本的执行单元。同一进程的所有线程共享相同的资源和内存（共享代码，全局变量，环境字符串等），使得线程间上下文切换更快、可以在同一地址空间内访问内存</p>
<h3 id="同步">同步</h3>
<p>如果一个程序调用某个方法，等待其执行所有处理后才继续执行，称这样的方法是同步的</p>
<h3 id="异步">异步</h3>
<p>如果一个程序调用某个方法，在该方法处理完成之前就返回到调用方法，则这个方法是异步的</p>
<h2 id="线程创建">线程创建</h2>
<h3 id="默认创建">默认创建</h3>
<p>C#中使用<code>Thread</code>类创建和控制线程,该类允许创建线程,以及设置线程的优先级</p>
<pre><code class="language-cs">/// &lt;summary&gt;
/// 线程创建
/// &lt;/summary&gt;
public static void ThreadCreate_Basic()
{
    static void ThreadMethod()
    {
        Thread.Sleep(2000);
        Console.WriteLine(&quot;子线程结束&quot;);
    }

    Console.WriteLine(&quot;程序在启动时创建一个线程,称为主线程&quot;);
    //创建线程
    Thread t = new Thread(ThreadMethod);
    //启动线程
    t.Start();
    Console.WriteLine(&quot;主线程结束&quot;);
}
</code></pre>
<p><code>Thread</code>构造函数接收<code>ParameterrizeThreadStart</code>和<code>ThreadStart</code>委托参数,所以也可以这么写</p>
<p><code>Thread t = new Thread(new ThreadStart(ThreadMethod));</code></p>
<h3 id="lambad-表达式创建">lambad 表达式创建</h3>
<pre><code class="language-cs">Thread t = new Thread(() =&gt; Console.WriteLine(&quot;ThreadMethod&quot;));
</code></pre>
<h2 id="线程调用顺序">线程调用顺序</h2>
<p>备注:不同 PC 运行结果可能不一致,只作示例</p>
<pre><code class="language-cs"> /// &lt;summary&gt;
 /// 线程调用顺序
 /// &lt;/summary&gt;
 /// &lt;remarks&gt;
 /// 观察输出结果说明
 /// 线程是由操作系统调度的,每次哪个线程先被执行不确定,线程的调度是无序的
 ///&lt;/remarks&gt;
 public static void Thread_Order()
 {
     Console.WriteLine(&quot;程序在启动时创建一个线程,称为主线程&quot;);

     Thread t = new Thread(() =&gt; Console.WriteLine(&quot;A&quot;));
     Thread t1 = new Thread(() =&gt; Console.WriteLine(&quot;B&quot;));
     Thread t2 = new Thread(() =&gt; Console.WriteLine(&quot;C&quot;));
     Thread t3 = new Thread(() =&gt; Console.WriteLine(&quot;D&quot;));
     t.Start();
     t1.Start();
     t2.Start();
     t3.Start();
     Console.WriteLine(&quot;主线程结束&quot;);
 }
</code></pre>
<p>第一次:<br>
<img src="/images/2020-12-08-17-17-10.png" alt=""></p>
<p>第二次:<br>
<img src="/images/2020-12-08-17-17-30.png" alt=""></p>
<p>第三次:<br>
<img src="/images/2020-12-08-17-17-55.png" alt=""></p>
<p><strong>说明</strong>:线程是由操作系统调度的,每次哪个线程先被执行可以不同,就是说该例子中线程的调度是无序的</p>
<h2 id="线程传递数据">线程传递数据</h2>
<p>使用带<code>ParameterrizeThreadStart</code>委托参数的<code>Thread</code>构造函数<br>
创建自定义类.把线程方法定义为实例方法,之后初始化实例数据,启动线程</p>
<h3 id="不带参数">不带参数</h3>
<pre><code class="language-cs">Thread t = new Thread(() =&gt; Console.WriteLine(&quot;ThreadMethod&quot;));
</code></pre>
<h3 id="一个参数">一个参数</h3>
<pre><code class="language-cs">Thread t = new Thread((object message) =&gt; Console.WriteLine(message));
</code></pre>
<h3 id="多个参数-自定义类">多个参数(自定义类)</h3>
<pre><code class="language-cs">class Data
    {
        public string name;
        public int age;
        public Data(string name, int age)
        {
            this.name = name;
            this.age = age;
        }
        public void Write()
        {
            Console.WriteLine(&quot;name:{0},age:{1}&quot;, this.name, this.age);
        }
    }
</code></pre>
<pre><code class="language-cs">var data = new Data(&quot;Wang&quot;, 24);
Thread t = new Thread(data.Write);
       t.Start();
</code></pre>
<h2 id="后台线程">后台线程</h2>
<p>默认情况下:用<code>Thread</code>类创建的线程总是前台线程,线程池中的线程总是后台线程</p>
<p>前台线程和后台线程的区别在于</p>
<ul>
<li>前台线程:应用程序必须运行完所有的前台线程才可以退出</li>
<li>后台线程:应用程序可以不考虑其是否已经运行完毕而直接退出，所有的后台线程在应用程序退出时都会自动结束</li>
</ul>
<h3 id="前台线程阻止进程的关闭">前台线程阻止进程的关闭</h3>
<pre><code class="language-cs">Thread thread = new Thread(() =&gt;
{
    Thread.Sleep(5000);
    Console.WriteLine(&quot;前台线程执行&quot;);
});
thread.Start();
Console.WriteLine(&quot;主线程执行完毕&quot;);
</code></pre>
<p>这里主线程马上执行完成，并不马上关闭，前台线程等待 5 秒再执行输出,然后控制台退出</p>
<h3 id="后台线程不阻止进程的关闭">后台线程不阻止进程的关闭</h3>
<pre><code class="language-cs">Thread thread = new Thread(() =&gt;
{
    Thread.Sleep(5000);
    Console.WriteLine(&quot;前台线程执行&quot;);
})
{ IsBackground = true };

thread.Start();
Console.WriteLine(&quot;主线程执行完毕&quot;);
</code></pre>
<p>输出结果:不等后台线程执行完毕,主线程执行完毕后立即退出,控制台立即退出</p>
<h2 id="线程优先级">线程优先级</h2>
<p>之前说到,线程是由操作系统调度的,给线程指定优先级,可以影响调度顺序,C#中<code>Thread</code>类的<code>Priority</code>属性提供了五种线程优先级别,这是一个枚举对象</p>
<ul>
<li>Normal(正常,默认值)</li>
<li>Highest (最高)</li>
<li>AboseNormal(高于正常)</li>
<li>BelowNormal(低于正常)</li>
<li>Lowest(最低)</li>
</ul>
<pre><code class="language-cs">Thread normal = new Thread(() =&gt;
{
    Console.WriteLine(&quot;优先级为正常线程&quot;);
});
normal.Start();

Thread aboseNormal = new Thread(() =&gt;
{
    Console.WriteLine(&quot;优先级为高于正常线程&quot;);
})
{ Priority = ThreadPriority.AboveNormal };
aboseNormal.Start();

Thread belowNormal = new Thread(() =&gt;
{
    Console.WriteLine(&quot;优先级为低于正常线程&quot;);
})
{ Priority = ThreadPriority.BelowNormal };
belowNormal.Start();

Thread highest = new Thread(() =&gt;
{
    Console.WriteLine(&quot;优先级最高线程&quot;);
})
{ Priority = ThreadPriority.Highest };
highest.Start();

Thread lowest = new Thread(() =&gt;
{
    Console.WriteLine(&quot;优先级最低线程&quot;);
})
{ Priority = ThreadPriority.Lowest };
lowest.Start();

Console.WriteLine(&quot;主线程执行完毕&quot;);
</code></pre>
<p><img src="/images/2020-12-08-17-24-24.png" alt=""></p>
<p>结果可知:设置优先级并不是会指定线程固定执行的顺序,设置线程优先级只是提高了线程被调用的概率，并不是定义<code>CPU</code>调用线程的顺序，具体还是要由操作系统内部来调度</p>
<h2 id="线程控制">线程控制</h2>
<p><code>Thread</code>类常用的属性</p>
<table>
<thead>
<tr>
<th style="text-align:left">属性</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">CurrentThread</td>
<td style="text-align:left">获取当前正在运行的线程</td>
</tr>
<tr>
<td style="text-align:left">IsAlive</td>
<td style="text-align:left">获取一个值，该值指示当前线程的执行状态</td>
</tr>
<tr>
<td style="text-align:left">Name</td>
<td style="text-align:left">获取或设置线程的名称</td>
</tr>
<tr>
<td style="text-align:left">Priority</td>
<td style="text-align:left">获取或设置一个值，该值指示线程的调度优先级</td>
</tr>
<tr>
<td style="text-align:left">ThreadState</td>
<td style="text-align:left">获取一个值，该值包含当前线程的状态</td>
</tr>
<tr>
<td style="text-align:left"><strong>方法</strong></td>
<td style="text-align:left"><strong>描述</strong></td>
</tr>
<tr>
<td style="text-align:left">Abort</td>
<td style="text-align:left">调用此方法通常会终止线程</td>
</tr>
<tr>
<td style="text-align:left">Join</td>
<td style="text-align:left">阻止调用线程，直到某个线程终止时为止</td>
</tr>
<tr>
<td style="text-align:left">Resume</td>
<td style="text-align:left">继续已挂起的线程</td>
</tr>
<tr>
<td style="text-align:left">Sleep</td>
<td style="text-align:left">将当前线程阻止指定的毫秒数</td>
</tr>
<tr>
<td style="text-align:left">Start</td>
<td style="text-align:left">使线程被安排进行执行</td>
</tr>
<tr>
<td style="text-align:left">Suspend</td>
<td style="text-align:left">挂起线程，或者如果线程已挂起，则不起作用</td>
</tr>
</tbody>
</table>
<h3 id="暂停线程">暂停线程</h3>
<pre><code class="language-cs">static void ThreadMethod()
{
    for (int i = 0; i &lt; 10; i++)
    {
        Console.WriteLine(&quot;a&quot;);
    }
}

static void ThreadMethodSleep()
{
    for (int i = 0; i &lt; 10; i++)
    {
        //暂停2s
        Thread.Sleep(TimeSpan.FromSeconds(2));
        Console.WriteLine(&quot;b&quot;);
    }
}
Thread t = new Thread(ThreadMethodSleep);
t.Start();
ThreadMethod();
</code></pre>
<p>工作原理<br>
当程序运行时,主线程创建,而后创建线程<code>t</code>,该线程首先执行<code>ThreadMethodSleep</code>方法中的代码。然后会立即执行<code>ThreadMethod</code>方法。关键之处在于在<code>ThreadMethodSleep</code>方法中加入了<code>Thread.Sleep</code>方法调用。这将导致线程执行该代码时,在打印任何数字之前会等待指定的时间(本例中是 2 秒钟),当线程处于休眠状态时,它会占用尽可能少的 CPU 时间。结果发现通常后运行的<code>ThreadMethod</code>方法中的代码会比独立线程中的<code>ThreadMethodSleep</code>方法中的代码先执行。</p>
<h3 id="线程等待Thread-Join">线程等待<code>Thread.Join()</code></h3>
<pre><code class="language-cs">static void ThreadMethod()
{
    for (int i = 0; i &lt; 10; i++)
    {
        Console.WriteLine(&quot;a&quot;);
    }
}static void ThreadMethodSleep()
{
    for (int i = 0; i &lt; 20; i++)
    {
        //暂停2s
        Thread.Sleep(TimeSpan.FromSeconds(2));
        Console.WriteLine(&quot;b&quot;);
    }
}

Thread t = new Thread(ThreadMethodSleep);
t.Start();
Thread.Sleep(TimeSpan.FromSeconds(5));
t.Abort();

static void ThreadMethodSleep()
{
    for (int i = 0; i &lt; 10; i++)
    {
        //暂停2s
        Thread.Sleep(TimeSpan.FromSeconds(2));
        Console.WriteLine(&quot;b&quot;);
    }
}
Thread t = new Thread(ThreadMethodSleep);
t.Start();
t.Join();
ThreadMethod();
</code></pre>
<p>工作原理<br>
程序运行后,创建线程<code>t</code>,调用<code>ThreadMethodSleep()</code>方法,该方法循环打印 3 个&quot;b&quot;,但是每次打印前都要暂停 2s,在主程序中调用<code>t.join</code>方法,该方法允许等待线程<code>t</code>完成,只有<code>t</code>线程完成后才会继续执行主程序的代码,这个例子中就是主线程等待<code>t</code>线程完成后再继续执行,主线程等待时处于阻塞状态</p>
<h3 id="Suspend和Resume方法来同步线程"><code>Suspend</code>和<code>Resume</code>方法来同步线程</h3>
<p>需要多线程编程时为了挂起与恢复线程可以使用<code>Thread</code>类的<code>Suspend()</code>与<code>Resume()</code>方法，可是 VS 提示这两个方法已经过时,原因 MSDN:<a href="https://docs.microsoft.com/en-us/dotnet/api/system.threading.thread.suspend?redirectedfrom=MSDN&amp;view=netframework-4.7.2#System_Threading_Thread_Suspend" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/dotnet/api/system.threading.thread.suspend?redirectedfrom=MSDN&amp;view=netframework-4.7.2#System_Threading_Thread_Suspend</a></p>
<h3 id="终止线程Thread-Abort">终止线程<code>Thread.Abort()</code></h3>
<pre><code class="language-cs">static void ThreadMethodSleep()
{
    for (int i = 0; i &lt; 20; i++)
    {
        //暂停2s
        Thread.Sleep(TimeSpan.FromSeconds(2));
        Console.WriteLine(&quot;b&quot;);
    }
}

Thread t = new Thread(ThreadMethodSleep);
t.Start();
Thread.Sleep(TimeSpan.FromSeconds(5));
t.Abort();
</code></pre>
<p>工作原理<br>
程序运行后,创建线程<code>t</code>,调用<code>ThreadMethodSleep()</code>方法,该方法循环打印 20 个&quot;b&quot;,但是每次打印前都要暂停 2s,在主线程中设置等待 6s 后调用<code>t.Abort()</code>方法,这有可能给线程注入了<code>ThreadAbortException</code>异常,导致线程被终结。这非常危险,因为该异常可以在任何时刻发生并可能彻底摧毁应用程序。另外,使用该技术也不一定总能终止线程。目标线程可以通过处理该异常并调用<code>Thread.ResetAbort</code>方法来拒绝被终止。因此并不推荐使用<code>Abort</code>方法来关闭线程。可优先使用一些其他方法,比如提供一个<code>CancellationToken</code>方法来,取消线程的执行</p>
<h3 id="线程的状态监测">线程的状态监测</h3>
<p>通过<code>Thread.ThreadState</code>属性读取当前线程状态</p>
<pre><code class="language-cs">static void ThreadMethodSleep()
{
    for (int i = 0; i &lt; 3; i++)
    {
        //暂停2s
        Thread.Sleep(TimeSpan.FromSeconds(2));
        Console.WriteLine(&quot;b&quot;);
    }
}

Thread t = new Thread(ThreadMethodSleep);
Console.WriteLine(&quot;创建线程,线程状态:{0}&quot;, t.ThreadState);
t.Start();
Console.WriteLine(&quot;线程调用Start()方法,线程状态:{0}&quot;, t.ThreadState);
Thread.Sleep(TimeSpan.FromSeconds(5));
Console.WriteLine(&quot;线程休眠5s,线程状态:{0}&quot;, t.ThreadState);
t.Join();
Console.WriteLine(&quot;等待线程结束,线程状态:{0}&quot;, t.ThreadState);
</code></pre>
<p>工作原理<br>
程序运行后,创建线程<code>t</code>(Unstarted)-&gt;<code>t.start()</code>(Running)-&gt;<code>t.sleep()</code>(WaitSleepJoin)-&gt;<code>t.join()</code>线程结束(Stopped)</p>
<h2 id="异常处理">异常处理</h2>
<pre><code class="language-cs">static void ThreadMethodA()
{
    throw new Exception(&quot;AError&quot;);
}
static void ThreadMethodB()
{
    try
    {
        throw new Exception(&quot;BError&quot;);
    }
    catch (Exception ex)
    {
        Console.WriteLine(&quot;Error:{0}&quot;, ex.Message);
    }
}

Thread t = new Thread(ThreadMethodB);
t.Start();
t.Join();
try
{
    Thread t1 = new Thread(ThreadMethodA);
    t1.Start();
}
catch (Exception e)
{
    Console.WriteLine(&quot;Error:{0}&quot;, e.Message);
}
static void AddThread(object e)
{
    Console.WriteLine(&quot;当前线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);
}

ThreadPool.GetMaxThreads(out int workThread, out int ioThread);
Console.WriteLine(&quot;工作线程数:{0},io线程数{1}&quot;, workThread, ioThread);

for (int i = 0; i &lt; 5; i++)
{
    ThreadPool.QueueUserWorkItem(AddThread);
    workThread = ioThread = 0;
}
Console.WriteLine($&quot;{workThread},{ioThread}&quot;);
</code></pre>
<p>工作原理</p>
<p>程序运行后,定义了两个会抛出异常的线程,其中一个对异常进行了处理,另一个没有,可以看到<code>ThreadMethodB()</code>方法中的异常没有被主程序包裹线程启动的<code>try/catch</code>代码块捕获到,所以如果直接使用线程,一般不要在线程中抛出异常,而在线程代码中使用<code>try/catch</code>代码块</p>
<h2 id="线程池-ThreadPool">线程池 ThreadPool</h2>
<p>创建线程需要时间.如果有不同的短任务要完成,就可以事先创建许多线程,在应完成这些任务时发出请求,这个线程数最好在需要更多的线程时增加,在需要释放资源时减少.C#中不需要自己创建维护这样一个列表,该列表由<code>ThreadPool</code>类托管.该类会在需要时增减池中线程的线程数,直到最大线程数,线程数的值是可配置的,如果线程池中个数到达了设置的极限,还是有更多的作业要处理,最新的作业就要排队,且必须等待线程完成其任务</p>
<pre><code class="language-cs">static void AddThread(object e)
{
    Console.WriteLine(&quot;当前线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);
}

ThreadPool.GetMaxThreads(out int workThread, out int ioThread);
Console.WriteLine(&quot;工作线程数:{0},io线程数{1}&quot;, workThread, ioThread);

for (int i = 0; i &lt; 5; i++)
{
    ThreadPool.QueueUserWorkItem(AddThread);
    workThread = ioThread = 0;
}
Console.WriteLine($&quot;{workThread},{ioThread}&quot;);
</code></pre>
<p><code>ThreadPool.GetMaxThreads(out workThread, out ioThread)</code>接收两个<code>out int</code>类型参数返回最大工作线程数和 io 线程数,for 循环中使用<code>ThreadPool.QueueUserWorkItem()</code>方法传递<code>WaitCallback</code>类型委托,将<code>AddThread()</code>方法赋予线程池中的线程,线程池收到请求后,如果线程池还没有运行,就会创建一个线程池,并启动第一个线程,如果已经启动,且有一个空闲线程来完成任务,就把该任务传递给这个线程</p>
<h2 id="线程池使用限制">线程池使用限制</h2>
<p>1:线程池中所有线程都是后台线程,如果进程的所有前台线程都结束了,所有的后台线程就会停止,不能把入池的线程改为前台线程</p>
<p>2:不能给入池的线程设置优先级或名称</p>
<p>3:入池的线程只能用于时间较短的任务,如果线程要一直运行,就应使用<code>Thread</code>类创建一个线程</p>
<p>4:对于<code>COM</code>对象,入池的所有线程都是多线程单元(MTA)线程,许多 COM 对象都需要单线程单元(STA)</p>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>线程同步机制</title>
    <url>/2020/12/08/csharp/%E7%BA%BF%E7%A8%8B%E5%90%8C%E6%AD%A5%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>当有一个线程在对内存进行操作时，其他线程都不可以对这个内存地址进行操作，直到该线程完成操作，其他线程才能对该内存地址进行操作，而其他线程又处于等待状态，这种时候很容易出现问题</p>
<h2 id="争用条件">争用条件</h2>
<p>如果两个或多个线程访问相同的对象,并且对共享状态的访问没有同步,就会出现争用条件</p>
<pre><code class="language-cs">class StateObject
{
    private int state = 5;

    public void ChangeState()
        {
            state++;
            if (state == 5)
            {
                Console.WriteLine(&quot;value=5&quot;);
            }
            state = 5;
        }
}
</code></pre>
<pre><code class="language-cs">public static void ThreadSync_Lock_Example_01()
{
    StateObject m = new StateObject();
    Thread t1 = new Thread(ChangeState);
    t1.Start(m);
    Console.ReadKey();

    static void ChangeState(object o)
    {
        StateObject m = o as StateObject;
        while (true)
        {
            m.ChangeState();
        }
    }
}
</code></pre>
<p>此时运行程序是没有输出的,因为<code>StateObject</code>类中<code>state</code>初始值是5,<code>if</code>条件不会进入,随后又将<code>state</code>从新初始化为5</p>
<p>两个线程执行</p>
<pre><code class="language-cs">public static void ThreadSync_Lock_Example_02()
{
    StateObject m = new StateObject();
    Thread t1 = new Thread(ChangeState);
    Thread t2 = new Thread(ChangeState);
    t1.Start(m);
    t2.Start(m);
    Console.ReadKey();

    static void ChangeState(object o)
    {
        StateObject m = o as StateObject;
        while (true)
        {
            m.ChangeState();
        }
    }
}
</code></pre>
<p>此时会不停的打印&quot;value=5&quot;,原因在于:一个线程在判断语句处时，另一个线程可能又将<code>state</code>的值改为了5，而导致输出合法</p>
<h2 id="死锁">死锁</h2>
<pre><code class="language-cs">class Deadlock
{
    static StateObject o1 = new StateObject();
    static StateObject o2 = new StateObject();
    public static void DeadlockA(object o)
    {
        lock (o1)
        {
            Console.WriteLine(&quot;我是线程{0},我锁定了对象o1&quot;, o);
            lock (o2)
            {
                Console.WriteLine(&quot;我是线程{0},我锁定了对象o2&quot;, o);
            }
        }
    }

    public static void DeadlockB(object o)
    {
        lock (o2)
        {
            Console.WriteLine(&quot;我是线程{0},我锁定了对象o2&quot;, o);
            lock (o1)
            {
                Console.WriteLine(&quot;我是线程{0},我锁定了对象o1&quot;, o);
            }
        }
    }
}
</code></pre>
<pre><code class="language-cs"> Thread t1 = new Thread(Deadlock.DeadlockA);
 Thread t2 = new Thread(Deadlock.DeadlockB);
        t1.Start(&quot;t1&quot;);
        t2.Start(&quot;t2&quot;);
</code></pre>
<p><img src="/images/2020-12-15-10-12-00.png" alt=""><br>
<code>t1</code>线程执行<code>DeadlockA()</code>方法顺序锁定<code>o1</code>和<code>o2</code><br>
<code>t2</code>线程执行<code>DeadlockB()</code>方法顺序锁定<code>o2</code>和<code>o1</code><br>
当前结果显示t1线程锁定了<code>o1</code>后,<code>t2</code>线程在<code>t1</code>线程锁定<code>o1</code>后抢占进来,锁定了<code>o2</code>,<code>t2</code>在等<code>t1</code>解锁,<code>t1</code>在等<code>t2</code>解锁,都处于挂起状态在等对方解锁,这就形成了死锁,线程将无限等待下去</p>
<p>这个问题应该从一开始就设计好锁定顺序,也可以为锁定义超时时间来处理,保证“上锁”这个操作在一个线程上执行也是避免死锁的方法之一</p>
<p>C#中有多个用于多线程的同步技术</p>
<ul>
<li>lock语句</li>
<li>Interlocked类</li>
<li>Monitor类</li>
<li>SpinLock类</li>
<li>WaitHandle类</li>
<li>Mutex类</li>
<li>Semapphore类</li>
<li>Event类</li>
<li>Barrier类</li>
<li>ReaderWriteLockSlim类</li>
</ul>
<p>其中lock语句/Interlocked类/Monitor类可用于进程内存的同步,其它几个提供了多进程之间的线程同步</p>
<h2 id="Lock">Lock</h2>
<p>C#使用<code>lock</code>语句锁定在线程中共享的变量,如果一个线程锁定了变量,另一个线程就必须等待该锁定的解除</p>
<pre><code class="language-cs">static void ChangeState(object o)
        {
            StateObject m = o as StateObject;
            while (true)
            {
　　　　　　　　　 //给变量m加锁
                lock (m)
                {
                    m.ChangeState();
                }
            }
        }
</code></pre>
<p>因为实例的对象也可以用于外部的同步访问,而且不能在类自身控制这种访问,所以应采用<code>SyncRoot</code>模式,创建私有对象,将这个对象用于<code>lock</code>语句</p>
<pre><code class="language-cs">        private static object syncRoot= new object();
        static void ChangeState(object o)
        {
            StateObject m = o as StateObject;
            while (true)
            {
                lock (aync)
                {
                    m.ChangeState();
                }
            }
        }
</code></pre>
<p>需要注意:</p>
<ol>
<li><code>lock</code>只能锁定对象，即引用类型，不能锁定值类型.</li>
<li><code>lock</code>不能锁定空值,因为<code>null</code>是不需要被释放的</li>
<li>不能锁定<code>string</code>类型,虽然它也是引用类型的。因为字符串类型被<code>CLR暂留</code>，这意味着整个程序中任何给定字符串都只有一个实例，具有相同内容的字符串上放置了锁，就将锁定应用程序中该字符串的所有实例</li>
<li>避免锁定<code>public</code>类型,如果该实例可以被公开访问，则 <code>lock(this)</code> 可能会有问题，因为不受控制的代码也可能会锁定该对象</li>
</ol>
<h2 id="锁是否必须是静态类型？">锁是否必须是静态类型？</h2>
<p>如果被锁定的方法是静态的，那么这个锁必须是静态类型。这样就是在全局锁定了该方法，不管该类有多少个实例，都要排队执行</p>
<p>如果被锁定的方法不是静态的，那么不能使用静态类型的锁，因为被锁定的方法是属于实例的，只要该实例调用锁定方法不产生损坏就可以，不同实例间是不需要锁的。这个锁只锁该实例的方法，而不是锁所有实例的方法</p>
<pre><code class="language-cs">class ThreadSafe
{
 private static object _locker = new object();
 
  void Go()
  {
    lock (_locker)
    {
      ......//共享数据的操作 （Static Method）,使用静态锁确保所有实例排队执行
    }
  }

private object _locker2=new object();
  void GoTo()
  {
    lock(_locker2)
    //共享数据的操作，非静态方法，是用非静态锁，确保同一个实例的方法调用者排队执行
  }
}
</code></pre>
<p>同步对象可以兼作它lock的对象<br>
如：</p>
<pre><code class="language-cs">class ThreadSafe
{
 private List &lt;string&gt; _list = new List &lt;string&gt;(); 
  void Test()
  {
    lock (_list)
    {
      _list.Add (&quot;Item 1&quot;);
    }
  }
}
</code></pre>
<h2 id="Monitors">Monitors</h2>
<p>lock其实是Monitors的简洁写法</p>
<pre><code class="language-cs">lock (syncRoot)
{
    m.ChangeState();
}
</code></pre>
<p>两者其实是一样的</p>
<pre><code class="language-cs">Monitor.Enter(syncRoot);
try
{
    m.ChangeState();
}
finally
{
    Monitor.Exit(syncRoot);
}
</code></pre>
<h2 id="Mutex">Mutex</h2>
<blockquote>
<p>互斥锁是一个互斥的同步对象，同一时间有且仅有一个线程可以获取它。可以实现进程级别上线程的同步</p>
</blockquote>
<pre><code class="language-CS">public static void ThreadSync_Mutex_Example_01()
{
    for (int i = 0; i &lt; 3; i++)
    {
        //在不同的线程中调用受互斥锁保护的方法
        Thread test = new Thread(MutexMethod);
        test.Start();
    }
    Console.Read();

    static void MutexMethod()
    {
        //实例化一个互斥锁
        Mutex mutex = new Mutex();

        Console.WriteLine(&quot;{0} 请求获取互斥锁&quot;, Thread.CurrentThread.ManagedThreadId);
        mutex.WaitOne();
        Console.WriteLine(&quot;{0} 已获取到互斥锁&quot;, Thread.CurrentThread.ManagedThreadId);
        Console.WriteLine(&quot;{0} 准备释放互斥锁&quot;, Thread.CurrentThread.ManagedThreadId);
        // 释放互斥锁
        mutex.ReleaseMutex();
        Console.WriteLine(&quot;{0} 已经释放互斥锁&quot;, Thread.CurrentThread.ManagedThreadId);
    }
}
</code></pre>
<p>互斥锁的带有三个参数的构造函数</p>
<ul>
<li>initiallyOwned: 如果initiallyOwned为true，互斥锁的初始状态就是被所实例化的线程所获取，否则实例化的线程处于未获取状态</li>
<li>name:该互斥锁的名字，在操作系统中只有一个命名为name的互斥锁mutex，如果一个线程得到这个name的互斥锁，其他线程就无法得到这个互斥锁了，必须等待那个线程对这个线程释放</li>
<li>createNew:如果指定名称的互斥体已经存在就返回false，否则返回true</li>
</ul>
<p>参考<br>
<a href="https://www.cnblogs.com/JoeSnail/p/7155815.html" target="_blank" rel="noopener">https://www.cnblogs.com/JoeSnail/p/7155815.html</a><br>
<a href="https://www.cnblogs.com/wyt007/p/9486752.html" target="_blank" rel="noopener">https://www.cnblogs.com/wyt007/p/9486752.html</a><br>
<a href="https://www.cnblogs.com/yunfeifei/p/3993401.html" target="_blank" rel="noopener">https://www.cnblogs.com/yunfeifei/p/3993401.html</a></p>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>并行和异步编程</title>
    <url>/2020/12/08/csharp/%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>并行开发要做的事情就是将任务分摊给硬件线程去并行执行来达到负载和加速,传统的代码都是串行的，就一个主线程，为了实现加速而开了很多工作线程，这些工作线程就是软件线程</p>
<h2 id="Parallel">Parallel</h2>
<p><code>Parallel</code>类是对线程的抽象,位于<code>System.Threading.Tasks</code>名称空间下,提供了<code>任务和数据并行性</code>.在<code>Parallel</code>下有三个常用的方法<code>Invoke</code>,<code>For</code>和<code>ForEach</code>,其中<code>Parallel</code>.<code>Invoke</code>用于任务并行性,<code>Parallel.ForEach</code>/<code>Parallel.For</code>用于数据并行性</p>
<h2 id="任务并行性">任务并行性</h2>
<h3 id="Parallel-Invoke">Parallel.Invoke</h3>
<p>如果多个任务应并行运行,就可以使用<code>Parallel.Invoke()</code>方法将串行的代码并行化</p>
<pre><code class="language-cs">public void Parallel_Example_01()
{
    var watch = Stopwatch.StartNew();
    watch.Start();
    Run1();
    Run2();
    Run3();
    watch.Stop();
    Console.WriteLine(&quot;串行开发,总耗时{0}&quot;, watch.ElapsedMilliseconds);

    watch.Restart();

    Parallel.Invoke(Run1, Run2, Run3);
    watch.Stop();
    Console.WriteLine(&quot;并行开发,总耗时{0}&quot;, watch.ElapsedMilliseconds);

    static void Run1()
    {
        Console.WriteLine(&quot;Run1,我需要1s&quot;);
        Thread.Sleep(1000);
    }
    static void Run2()
    {
        Console.WriteLine(&quot;Run2,我需要2s&quot;);
        Thread.Sleep(2000);
    }
    static void Run3()
    {
        Console.WriteLine(&quot;Run3,我需要3s&quot;);
        Thread.Sleep(3000);
    }
    Assert.IsTrue(true);
}
</code></pre>
<p><img src="/images/2020-12-17-10-53-37.png" alt=""><br>
主程序启动时,先顺序调用<code>Run1()</code>,<code>Run()2</code>,<code>Run3()</code>方法,这是串行的,而后使用<code>Parallel.Invoke()</code>将三个方法并行调用</p>
<h2 id="执行顺序">执行顺序</h2>
<pre><code class="language-cs">public void Parallel_Example_02()
{
    Console.WriteLine(&quot;主线程启动,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);

    Parallel.Invoke(
        () =&gt; Run1(&quot;task1&quot;),
        () =&gt; Run2(&quot;task2&quot;),
        () =&gt; Run3(&quot;task3&quot;));

    Console.WriteLine(&quot;主线程结束,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);

    static void Run1(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;, taskName, Thread.CurrentThread.ManagedThreadId);
        for (int i = 0; i &lt; 5; i++)
        {
            Console.WriteLine(&quot;a&quot;);
        }
    }
    static void Run2(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;, taskName, Thread.CurrentThread.ManagedThreadId);
        for (int i = 0; i &lt; 5; i++)
        {
            Console.WriteLine(&quot;b&quot;);
        }
    }
    static void Run3(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;, taskName, Thread.CurrentThread.ManagedThreadId);
        for (int i = 0; i &lt; 5; i++)
        {
            Console.WriteLine(&quot;c&quot;);
        }
    }
    Assert.IsTrue(true);
}
</code></pre>
<p><img src="/images/2020-12-17-10-51-14.png" alt=""><br>
结果可知：<br>
没有固定顺序，每个<code>Task</code>可能是不同的线程去执行，也可能是相同的<br>
主线程必须等<code>Invoke</code>中的所有方法执行完成后返回才继续向下执行,以后设计并行的时候，要考虑每个<code>Task</code>任务尽可能差不多，如果相差很大，比如一个时间非常长，其他都比较短，这样一个线程可能会影响整个任务的性能。这点非常重要(就是说<code>Invoke</code>会阻塞主线程)</p>
<h2 id="数据并行性">数据并行性</h2>
<h3 id="Parallel-For">Parallel.For</h3>
<p><code>Parallel.For</code>是 <code>for</code> 的多线程实现,串行代码中也有一个<code>for</code>，但是那个<code>for</code>并没有用到多核，而<code>Paraller.For</code>它会在底层根据硬件线程的运行状况来充分的使用所有的可利用的硬件线程</p>
<pre><code class="language-cs">public void Parallel_Example_For()
{
    for (int i = 0; i &lt; 3; i++)
    {
        ConcurrentBag&lt;int&gt; bag = new ConcurrentBag&lt;int&gt;();
        var watch = Stopwatch.StartNew();
        watch.Start();

        for (int j = 0; j &lt; 20000000; j++)
        {
            bag.Add(i);
        }
        watch.Stop();
        Console.WriteLine(&quot;串行添加,总数20000000,耗时{0}&quot;, watch.ElapsedMilliseconds);
        watch.Restart();
        Parallel.For(0, 20000000, j =&gt;
        {
            bag.Add(j);
        });
        watch.Stop();
        Console.WriteLine(&quot;并行添加,总数20000000,耗时{0}&quot;, watch.ElapsedMilliseconds);
        Console.WriteLine(&quot;***********************************&quot;);
    }
    Assert.IsTrue(true);
}
</code></pre>
<p>向一个线程安全的集合插入数据,使用串行的 for 耗时与使用并行的 Parallel.For 差异<br>
<img src="/images/2020-12-17-10-57-19.png" alt=""></p>
<h3 id="Parallel-ForEach">Parallel.ForEach</h3>
<p><code>Parallel.ForEach</code> 是 <code>foreach</code> 的多线程实现，都能对<code>IEnumerable&lt;T&gt;</code> 类型对象进行遍历，<code>Parallel.ForEach</code>的特殊之处在于它使用多线程来执行循环体内的代码段</p>
<pre><code class="language-cs">public void Parallel_Example_ForEach()
{
    ConcurrentBag&lt;int&gt; bag = new ConcurrentBag&lt;int&gt;();
    Parallel.For(0, 10, j =&gt;
    {
        bag.Add(j);
    });
    Console.WriteLine(&quot;集合总数:{0}&quot;, bag.Count);
    Parallel.ForEach(bag, item =&gt;
    {
        Console.WriteLine(item);
    });
    Assert.IsTrue(true);
}
</code></pre>
<h2 id="中断">中断</h2>
<p><code>Parallel.For</code>:添加<code>ParallelLoopState</code>参数，该实例提供了<code>Break</code>和<code>Stop</code>方法来帮助实现中断</p>
<p><code>ParallelLoopState.Break()</code>：在完成当前的这轮工作之后，不再执行后继的工作，但在当前这轮工作开始之前“已经在执行”的工作，则必须完成。但并不能执行完所有的循环。<br>
<code>ParallelLoopState.Stop()</code>：不但不会再创建新的线程执行并行循环，而且当前“已经在执行”的工作也应该被中止。</p>
<h3 id="ParallelLoopState-Break">ParallelLoopState.Break()</h3>
<pre><code class="language-cs">public void Parallel_Example_For_Break()
{
    int maxCount = 1000;
    ConcurrentBag&lt;int&gt; bag = new ConcurrentBag&lt;int&gt;();
    var watch = Stopwatch.StartNew();
    watch.Start();
    Parallel.For(0, 2000, (j, state) =&gt;
    {
        if (bag.Count == maxCount)
        {
            state.Break();
            //return是必须的,否则依旧会继续执行
            return;
        }
        bag.Add(j);
    });
    watch.Stop();
    Console.WriteLine(&quot;集合元素个数{0}&quot;, bag.Count);
    Assert.AreEqual(maxCount, bag.Count);
}
</code></pre>
<h3 id="ParallelLoopState-Stop">ParallelLoopState.Stop()</h3>
<pre><code class="language-cs">public void Parallel_Example_For_Stop()
{
    int maxCount = 1000;
    ConcurrentBag&lt;int&gt; bag = new ConcurrentBag&lt;int&gt;();
    for (int j = 0; j &lt; 5; j++)
    {
        bag = new ConcurrentBag&lt;int&gt;();
        Parallel.For(0, 2000, (i, state) =&gt;
        {
            if (bag.Count == maxCount)
            {
                state.Stop();
                return;
            }
            bag.Add(i);
        });
        Console.WriteLine(&quot;集合元素个数{0}&quot;, bag.Count);
        Console.WriteLine(&quot;*************************************************&quot;);
        Assert.AreEqual(maxCount, bag.Count);
    }           
}
</code></pre>
<h3 id="注意">注意</h3>
<p><code>Stop</code>仅仅通知其他迭代尽快结束，而<code>Break</code>不仅通知其他迭代尽快结束，同时还要保证退出之前要完成<code>LowestBreakIteration</code>之前的迭代。 例如，对于从 0 到 1000 并行迭代的<code>for</code>循环，如果从第 100 此迭代开始调用<code>Break</code>，则低于 100 的所有迭代仍会运行，从 101 到 1000 的迭代则不必要。而调用<code>Stop</code>方法不保证低于 100 的所有迭代都会运行</p>
<h2 id="异常处理">异常处理</h2>
<p>任务是并行计算的，处理过程中可能会产生n多的异常</p>
<h3 id="Exception">Exception</h3>
<p><code>Exception</code>是可以捕获到两个异常的</p>
<pre><code class="language-cs">public void Parallel_Example_Exception()
{
    Console.WriteLine(&quot;主线程启动,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);
    try
    {
        Parallel.Invoke(() =&gt; Run1(&quot;task1&quot;), () =&gt; Run2(&quot;task2&quot;), () =&gt; Run3(&quot;task3&quot;));
    }
    catch (Exception ex)
    {
        Console.WriteLine(ex.Message);
    }
    Console.WriteLine(&quot;主线程结束,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);


    static void Run1(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
        throw new Exception(&quot;Run1出现异常&quot;);
    }
    static void Run2(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
    }
    static void Run3(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
        throw new Exception(&quot;Run3出现异常&quot;);
    }
    Assert.IsTrue(true);
}
</code></pre>
<p><img src="/images/2020-12-17-15-32-34.png" alt=""></p>
<h3 id="AggregateException">AggregateException</h3>
<pre><code class="language-cs">public void Parallel_Example_AggregateException()
{
    Console.WriteLine(&quot;主线程启动,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);
    try
    {
        Parallel.Invoke(() =&gt; Run1(&quot;task1&quot;), () =&gt; Run2(&quot;task2&quot;), () =&gt; Run3(&quot;task3&quot;));
    }
    catch (AggregateException ex)
    {
        // AggregateException捕获并行产生的一组异常集合
        foreach (var item in ex.InnerExceptions)
        {
            Console.WriteLine(item);
        }
    }
    static void Run1(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
        throw new Exception(&quot;Run1出现异常&quot;);
    }

    static void Run2(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
    }

    static void Run3(string taskName)
    {
        Console.WriteLine(&quot;任务名：{0}线程ID:{1}&quot;,
            taskName, Thread.CurrentThread.ManagedThreadId);
        throw new Exception(&quot;Run3出现异常&quot;);
    }

    Console.WriteLine(&quot;主线程结束,线程ID:{0}&quot;, Thread.CurrentThread.ManagedThreadId);
    Assert.IsTrue(true);
}
</code></pre>
<p><code>Invoke</code>方法中调用了一个产生异常的方法,但是结果显示异常并不会影响其它方法及主线程的执行</p>
<h2 id="Parallel-For肯定快吗">Parallel.For肯定快吗?</h2>
<p>在实现多线程时，为了防止多个线程同时处理同一个变量而导致变量处于&quot;薛定谔状态&quot;，引入了&quot;锁&quot;的概念，即在每一时刻只有获得&quot;锁&quot;的线程才能操作目标变量。如果在<code>Parallel.For</code>中也需要操作一个全局变量，就意味着即使这是并行计算，大家也需要排队操作全局变量，此时Parallel.For可能远远不如传统的for循环来的快</p>
<pre><code class="language-cs">public void Parallel_Example_Performance()
{
    Stopwatch stopWatch = new Stopwatch();

    var obj = new object();
    long num = 0;

    stopWatch.Start();
    for (int i = 0; i &lt; 10000; i++)
    {
        for (int j = 0; j &lt; 60000; j++)
        {
            num++;
        }
    }
    stopWatch.Stop();
    Console.WriteLine(&quot;for run &quot; + stopWatch.ElapsedMilliseconds + &quot; ms.&quot;);

    stopWatch.Reset();
    stopWatch.Start();
    Parallel.For(0, 10000, item =&gt;
    {
        for (int j = 0; j &lt; 60000; j++)
        {
            lock (obj)
            {
                num++;
            }
        }
    });
    stopWatch.Stop();
    Console.WriteLine(&quot;ParallelFor run &quot; + stopWatch.ElapsedMilliseconds + &quot; ms.&quot;);
    Assert.IsTrue(true);
}
</code></pre>
<p><img src="/images/2020-12-17-15-45-08.png" alt=""></p>
<h2 id="ParallelOptions类">ParallelOptions类</h2>
<table>
<thead>
<tr>
<th style="text-align:left">属性</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">CancellationToken</td>
<td style="text-align:left">获取或设置与此 ParallelOptions 实例关联的 CancellationToken</td>
</tr>
<tr>
<td style="text-align:left">MaxDegreeOfParallelism</td>
<td style="text-align:left">获取或设置此 ParallelOptions 实例所允许的并发任务的最大数目</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
  <entry>
    <title>异步编程</title>
    <url>/2020/12/17/csharp/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1>概要</h1>
<p>对于多线程，经常使用的是<code>Thread</code>。在了解<code>Task</code>之前，如果要使用多核的功能可能就会自己来开线程，然而这种线程模型在<code>.net 4.0</code>之后被一种称为<code>基于“任务的编程模型”</code>所冲击，这就是<code>Task</code>,<code>Task</code>会比<code>Thread</code>具有更小的性能开销,<code>Task</code>是架构在<code>Thread</code>之上的就是说<code>Task</code>最终还是会抛给线程去做,并且任务跟线程不是一对一的关系，比如开 10 个任务并不是说会开 10 个线程，这一点任务有点类似线程池，但是任务相比线程池有很小的开销和精确的控制。<code>Task</code>类的表示单个操作不返回一个值，通常以异步方式执行,<code>Task</code> 对象是<code>&quot;基于任务的异步模式&quot;</code>首次引入<code>.NET Framework 4</code> 中。因为由执行工作 <code>Task</code>对象通常以异步方式执行在线程池线程上而不是以同步方式在主应用程序线程，可以使用<code>Status</code>属性，以及 <code>IsCanceled</code>,<code>IsCompleted</code>, 和 <code>IsFaulted</code>属性，以确定任务的状态</p>
<h1>相关概念理解</h1>
<ul>
<li>同步（Synchronous）</li>
<li>异步 (Asynchronous)</li>
<li>阻塞 (Blocking)</li>
<li>非阻塞(Nonblocking)</li>
</ul>
<p>同步/异步指的是在客户端<br>
同步意味着：客户端提出了一个请求以后，在回应之前只能等待<br>
异步意味着：客户端提出一个请求以后，还可以继续提其他请求</p>
<p>阻塞/非阻塞指的是服务器端<br>
阻塞意味着：服务器接受一个请求后，在返回结果以前不能接受其他请求<br>
非阻塞意味着：服务器接受一个请求后，尽管没有返回结果，还是可以继续接受其他请求</p>
<h2 id="同步与异步">同步与异步</h2>
<blockquote>
<p>同步和异步关注的是消息通信机制 (synchronous communication/ asynchronous communication)</p>
</blockquote>
<p>所谓同步，就是在发出一个<strong>调用</strong>时，在没有得到结果之前，该<strong>调用</strong>就不返回。但是一旦调用返回，就得到返回值了。换句话说，就是由<strong>调用者</strong>主动等待这个<strong>调用</strong>的结果</p>
<p>而异步则是相反，<strong>调用</strong>在发出之后，这个调用就直接返回了，所以没有返回结果。换句话说，当一个异步过程调用发出后，调用者不会立刻得到结果,而是在**调用*发出后，<strong>被调用者</strong>通过状态、通知来通知调用者，或通过回调函数处理这个调用。(典型的异步编程模型比如Node.js)</p>
<p>Example</p>
<p>同步通信机制<br>
你打电话问书店老板有没有《分布式系统》这本书，如果是同步通信机制，书店老板会说，你稍等，“我查一下”，然后开始查啊查，等查好了（可能是5秒，也可能是一天）告诉你结果（返回结果）</p>
<p>异步通信机制<br>
书店老板直接告诉你我查一下啊，查好了打电话给你，然后直接挂电话了（不返回结果）。然后查好了，他会主动打电话给你。在这里老板通过&quot;回电&quot;这种方式来回调</p>
<h2 id="阻塞与非阻塞">阻塞与非阻塞</h2>
<blockquote>
<p>阻塞和非阻塞关注的是程序在等待调用结果（消息，返回值）时的状态</p>
</blockquote>
<p>阻塞调用是指调用结果返回之前，当前线程会被挂起。调用线程只有在得到结果之后才会返回</p>
<p>非阻塞调用指在不能立刻得到结果之前，该调用不会阻塞当前线程</p>
<p>Example<br>
你打电话问书店老板有没有《分布式系统》这本书，你如果是阻塞式调用，你会一直把自己“挂起”，直到得到这本书有没有的结果，如果是非阻塞式调用，你不管老板有没有告诉你，你自己先一边去玩了， 当然你也要偶尔过几分钟check一下老板有没有返回结果，在这里阻塞与非阻塞与是否同步异步无关。跟老板通过什么方式回答你结果无关</p>
<h2 id="模型图例">模型图例</h2>
<h3 id="单线程同步模型">单线程同步模型</h3>
<p><img src="/images/2020-12-18-10-47-48.png" alt=""></p>
<h3 id="多线程模型">多线程模型</h3>
<p><img src="/images/2020-12-18-10-50-46.png" alt=""></p>
<h3 id="异步模型">异步模型</h3>
<p><img src="/images/2020-12-18-11-03-10.png" alt=""></p>
<h1>基础</h1>
<h2 id="创建任务">创建任务</h2>
<h3 id="new-TaskFactory">new TaskFactory()</h3>
<blockquote>
<p>使用实例化的TaskFactory类</p>
</blockquote>
<pre><code class="language-cs"> var t1 = new TaskFactory()
     .StartNew(() =&gt; Console.WriteLine(&quot;new TaskFactory().StartNew&quot;));
</code></pre>
<h3 id="Task-Factory">Task.Factory</h3>
<blockquote>
<p>使用Task静态属性Factory</p>
</blockquote>
<pre><code class="language-cs">var t2 = Task.Factory
                .StartNew(() =&gt; Console.WriteLine(&quot;Task.Factory&quot;));
</code></pre>
<h3 id="new-Task">new Task()</h3>
<blockquote>
<p>使用<code>Task</code>的构造函数, 实例化<code>Task</code>对象时,任务不会立即执行,而是指定<code>Created</code>状态,通过<code>Task.Start()</code>方法启动</p>
</blockquote>
<pre><code class="language-cs">var t3 = new Task(() =&gt; Console.WriteLine(&quot;Task Constructor&quot;));
            t3.Start();
</code></pre>
<h3 id="Task-Run">Task.Run</h3>
<p>.Net4.5 新增功能使用<code>Task</code>类的<code>Run</code>方法</p>
<pre><code class="language-cs">var t4 = Task.Run(() =&gt; Console.WriteLine(&quot;Task.Run&quot;));
</code></pre>
<p>注意:使用<code>Task.Run/Task.Factory.StartNew/new TaskFactory().StartNew()</code>方法运行的任务会立即开始工作,无需显式地调用这些任务的<code>Start</code>方法</p>
<h2 id="同步任务">同步任务</h2>
<blockquote>
<p>任务不一定要使用线程池中的线程,也可以使用其他线程,任务也可以同步进行,以相同的线程作为主调线程</p>
</blockquote>
<pre><code class="language-cs">static void Main(string[] args)
        {
            var t1 = new Task(() =&gt; TaskMethod(&quot;t1&quot;));
            t1.Start();
            Console.WriteLine(&quot;主线程调用结束&quot;);
            Console.ReadKey();
        }

        public static void TaskMethod(string taskName)
        {
            Console.WriteLine(&quot;Task {0} 运行在线程id为{1}的线程上。是否是线程池中线程？:{2}&quot;,
taskName, Thread.CurrentThread.ManagedThreadId, Thread.CurrentThread.IsThreadPoolThread);
        }
</code></pre>
<pre><code class="language-cs">var t1 = new Task(() =&gt; TaskMethod(&quot;t1&quot;));
    t1.RunSynchronously();
    Console.WriteLine(&quot;主线程调用结束&quot;);
    Console.ReadKey();
</code></pre>
<h2 id="使用单独线程的任务">使用单独线程的任务</h2>
<blockquote>
<p>如果任务的代码应该长时间运行,应该使用<code>TaskCreationOptions.LongRuning</code>告诉任务调度器创建一个新线程,而不是使用线程池中的线程,此时,线程可以不受线程池管理</p>
</blockquote>
<pre><code class="language-cs"> var t1 = new Task(TaskMethod, TaskCreationOptions.LongRunning);
     t1.Start();
</code></pre>
<h2 id="Task生命周期">Task生命周期</h2>
<table>
<thead>
<tr>
<th style="text-align:left">状态</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Created</td>
<td style="text-align:left">该任务已初始化，但尚未被计划</td>
</tr>
<tr>
<td style="text-align:left">WaitingForActivation</td>
<td style="text-align:left">该任务正在等待 .NET Framework 基础结构在内部将其激活并进行计划</td>
</tr>
<tr>
<td style="text-align:left">WaitingToRun</td>
<td style="text-align:left">该任务已被计划执行，但尚未开始执行</td>
</tr>
<tr>
<td style="text-align:left">Running</td>
<td style="text-align:left">该任务正在运行，但尚未完成</td>
</tr>
<tr>
<td style="text-align:left">WaitingForChildrenToComplete</td>
<td style="text-align:left">该任务已完成执行，正在隐式等待附加的子任务完成</td>
</tr>
<tr>
<td style="text-align:left">RanToCompletion</td>
<td style="text-align:left">已成功完成执行的任务</td>
</tr>
<tr>
<td style="text-align:left">Canceled</td>
<td style="text-align:left">该任务已通过对其自身的 CancellationToken 引发 OperationCanceledException 对取消进行了确认，此时该标记处于已发送信号状态；或者在该任务开始执行之前，已向该任务的 CancellationToken 发出了信号</td>
</tr>
<tr>
<td style="text-align:left">Faulted</td>
<td style="text-align:left">由于未处理异常的原因而完成的任务</td>
</tr>
</tbody>
</table>
<h2 id="Task任务控制">Task任务控制</h2>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Task.Wait</td>
<td style="text-align:left">task1.Wait();就是等待任务执行完成，task 的状态变为 Completed</td>
</tr>
<tr>
<td style="text-align:left">Task.WaitAll</td>
<td style="text-align:left">待所有的任务都执行完成</td>
</tr>
<tr>
<td style="text-align:left">Task.WaitAny</td>
<td style="text-align:left">等待任何一个任务完成就继续向下执行</td>
</tr>
<tr>
<td style="text-align:left">Task.ContinueWith</td>
<td style="text-align:left">第一个 Task 完成后自动启动下一个 Task，实现 Task 的延续 CancellationTokenSource</td>
</tr>
<tr>
<td style="text-align:left">CancellationTokenSource</td>
<td style="text-align:left">通过 cancellation 的 tokens 来取消一个 Task</td>
</tr>
</tbody>
</table>
<h2 id="Task返回类型">Task返回类型</h2>
<h3 id="void">void</h3>
<blockquote>
<p>不关心结果，返回 void 类型</p>
</blockquote>
<pre><code class="language-CS">[TestMethod]
public void Task_Result_Void()
{
    Console.WriteLine($&quot;不关心结果,返回Void,--Start,线程Id:{Thread.CurrentThread.ManagedThreadId}&quot;);
    Print();
    Console.WriteLine($&quot;不关心结果,返回Void,--End,线程Id:{Thread.CurrentThread.ManagedThreadId}&quot;);
    Assert.IsTrue(true);


    static async void Print()
    {
        await Task.Run(() =&gt;
       {
           Console.WriteLine($&quot;Hello, 线程Id:{ Thread.CurrentThread.ManagedThreadId}&quot;);
       });
    }
}
</code></pre>
<h3 id="Task">Task</h3>
<blockquote>
<p>关心是否完成，返回 Task 类型</p>
</blockquote>
<pre><code class="language-cs">public void Task_Result_Task()
{
    Console.WriteLine($&quot;看电视中...,线程Id:{Thread.CurrentThread.ManagedThreadId}&quot;);
    Console.WriteLine(&quot;突然停电，看下是不是跳闸了&quot;);
    var task = OpenMainsSwitch();
    Console.WriteLine($&quot;没电了先玩会儿手机吧，线程Id为：{Thread.CurrentThread.ManagedThreadId}&quot;);
    Thread.Sleep(100);
    // 等着电源开关被打开
    task.Wait();
    Console.WriteLine($&quot;又有电了,继续看电视...,线程Id:{Thread.CurrentThread.ManagedThreadId}&quot;);
    Assert.IsTrue(true);


    static async Task OpenMainsSwitch()
    {
        Console.WriteLine($&quot;准备打开电源开关，线程Id：{ Thread.CurrentThread.ManagedThreadId}&quot;);
        await Task.Run(() =&gt;
        {
            Console.WriteLine($&quot;打开电源开关, 线程Id:{ Thread.CurrentThread.ManagedThreadId}&quot;);
            Thread.Sleep(2000);
        });
        Console.WriteLine($&quot;电源开关打开了，线程Id：{ Thread.CurrentThread.ManagedThreadId}&quot;);
    }
}
</code></pre>
<h3 id="Task-T"><code>Task&lt;T&gt;</code></h3>
<blockquote>
<p>调用方法要从调用中获取一个<code>T</code>类型的值，异步方法的返回类型就必须是<code>Task&lt;T&gt;</code>。调用方法从<code>Task</code>的<code>Result</code>属性获取的就是<code>T</code>类型的值</p>
</blockquote>
<pre><code class="language-CS">public async Task Task_Result_TaskTAsync()
{
    string message = $&quot;Today is {DateTime.Today:D}\n&quot; + &quot;Today's hours of leisure: &quot; + $&quot;{await GetLeisureHoursAsync()}&quot;;
    Console.WriteLine(message);
    Assert.IsTrue(true);


    static async Task&lt;int&gt; GetLeisureHoursAsync()
    {
        DayOfWeek today = await Task.FromResult(DateTime.Now.DayOfWeek);

        int leisureHours =
            today is DayOfWeek.Saturday || today is DayOfWeek.Sunday
            ? 16 : 5;

        return leisureHours;
    }
}
</code></pre>
<h2 id="连续任务">连续任务</h2>
<blockquote>
<p>在指定任务完成后调用另一个指定任务</p>
</blockquote>
<pre><code class="language-cs"> public void Task_ContinueWith_Example()
 {
     Task&lt;string&gt; t1 = new Task&lt;string&gt;(
         () =&gt; TaskMethod1(&quot;t1&quot;));
     Console.WriteLine(&quot;Task1-创建,状态为:{0}&quot;, t1.Status);
     t1.Start();
     Console.WriteLine(&quot;Task1-启动,状态为:{0}&quot;, t1.Status);
     Console.WriteLine(t1.Result);
     Console.WriteLine(&quot;Task1-完成,状态为:{0}&quot;, t1.Status);
     Task t2 = t1.ContinueWith(TaskMethod2);
     Console.WriteLine(&quot;Task2,状态为:{0}&quot;, t2.Status);

     static string TaskMethod1(string taskName)
     {
         var result = string.Format($&quot;Task:{taskName} 运行在线程id:{ Thread.CurrentThread.ManagedThreadId}的线程上,&quot; +
             $&quot;是否是线程池中线程？:{Thread.CurrentThread.IsThreadPoolThread}&quot;);
         return result;
     }

     static void TaskMethod2(Task t)
     {
         Console.WriteLine($&quot;TaskID:{ t.Id} 运行在线程id:{Thread.CurrentThread.ManagedThreadId}的线程上。是否是线程池中线程？:{Thread.CurrentThread.IsThreadPoolThread}&quot;);

     }
     Assert.IsTrue(true);
 }

</code></pre>
<p>使用<code>TaskContinuationOptions</code>枚举的值可以指定连续任务只有在起始任务成功或失败结束时启动</p>
<pre><code class="language-cs">Task t2 = t1.ContinueWith(TaskMethod2, TaskContinuationOptions.NotOnCanceled);
</code></pre>
<h2 id="嵌套Task">嵌套Task</h2>
<h3 id="关联嵌套">关联嵌套</h3>
<blockquote>
<p>在创建<code>cTask</code>时，加入了以参数，<code>TaskCreationOptions.AttachedToParent</code>，这个时候，<code>cTask</code>和<code>pTask</code>就会建立关联，<code>cTask</code>就会成为<code>pTask</code>的一部分</p>
</blockquote>
<pre><code class="language-cs">[TestMethod]
public void Task_Relevance_Example()
{
    var pTask = Task.Factory.StartNew(() =&gt;
    {
       var cTask= Task.Factory.StartNew(() =&gt;
         {
             Console.WriteLine(&quot;Childen task finished!&quot;);
         }, TaskCreationOptions.AttachedToParent);
        Console.WriteLine(&quot;Parent task finished!&quot;);
    });
    pTask.Wait();
    Console.WriteLine(&quot;Flag&quot;);
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="非关联嵌套">非关联嵌套</h3>
<blockquote>
<p>外层的pTask运行完后，并不会等待内层的cTask，直接向下走先输出了Flag。这种嵌套有时候相当于我们创建两个Task，但是嵌套在一起的话，在Task比较多时会方便查找和管理，并且还可以在一个Task中途加入多个Task，让进度并行前进</p>
</blockquote>
<pre><code class="language-cs">[TestMethod]
public void Task_NoRelevance_Example()
{
    var pTask = Task.Factory.StartNew(() =&gt;
    {
        Task.Factory.StartNew(() =&gt;
        {
            Console.WriteLine(&quot;Childen task finished!&quot;);
        });
        Console.WriteLine(&quot;Parent task finished!&quot;);
    });
    pTask.Wait();
    Console.WriteLine(&quot;Flag&quot;);
    Assert.IsTrue(true);
}
</code></pre>
<blockquote>
<p>在<code>Task</code>内部创建<code>Task</code>,如果父任务在子任务之前结束,父任务状态就显示为<code>WaitingForChilderenToComplete</code>,所有的子任务也结束时,父任务的状态就改为<code>RanToCompletion</code>,如果使用<code>TaskContinuationOptions</code>枚举值创建子任务时会有不同结果,取消父任务也会取消子任务</p>
</blockquote>
<h2 id="Task取消">Task取消</h2>
<h3 id="单个任务">单个任务</h3>
<pre><code class="language-cs">[TestMethod]
public void Task_CancellationToken_SingleTask_Example()
{
    CancellationTokenSource cts = new CancellationTokenSource();
    Task t1 = Task.Run(() =&gt;
    {
        for (int i = 0; i &lt; 30; i++)
        {
            if (cts.Token.IsCancellationRequested)
            {
                cts.Token.ThrowIfCancellationRequested();
            }
            else
            {
                Thread.Sleep(500);
                Console.WriteLine(&quot;任务t1,共执行30次,当前第{0}次&quot;, i + 1);
            }
        }
    }, cts.Token);
    Thread.Sleep(2000);
    // 传达取消请求
    cts.Cancel();
    Console.WriteLine($&quot;已停止,Status{t1.Status}&quot;);
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="多个任务">多个任务</h3>
<pre><code class="language-cs">[TestMethod]
public void Task_CancellationToken_MultiTask_Example()
{
    CancellationTokenSource cts1 = new CancellationTokenSource();
    CancellationTokenSource cts2 = new CancellationTokenSource();

    // 任何Task处于取消状态时其余也将取消
    CancellationTokenSource ctsCombine =
         CancellationTokenSource.CreateLinkedTokenSource(cts1.Token, cts2.Token);
    Task t1 = Task.Run(() =&gt;
    {
        for (int i = 0; i &lt; 30; i++)
        {
            if (!ctsCombine.IsCancellationRequested)
            {
                Thread.Sleep(500);
                Console.WriteLine(&quot;任务t1,共执行30次,当前第{0}次&quot;, i + 1);
            }
        }
    }, ctsCombine.Token);

    Task t2 = Task.Run(() =&gt;
    {
        for (int i = 0; i &lt; 30; i++)
        {
            if (!ctsCombine.IsCancellationRequested)
            {
                Thread.Sleep(500);
                Console.WriteLine(&quot;任务t2,共执行30次,当前第{0}次&quot;, i + 1);
            }
        }
    }, ctsCombine.Token);
    Thread.Sleep(2000);
    cts1.Cancel();
    Console.WriteLine($&quot;t1:Status_{t1.Status},t2:Status_{t2.Status}&quot;);
    Assert.IsTrue(true);
}
</code></pre>
<h3 id="定时取消">定时取消</h3>
<pre><code class="language-cs">static void Main(string[] args)
{
    CancellationTokenSource cts = new CancellationTokenSource();
    cts.CancelAfter(8000);
    Task t1 = Task.Run(() =&gt;
    {
        for (int i = 0; i &lt; 30; i++)
        {
            if (cts.Token.IsCancellationRequested)
            {
                cts.Token.ThrowIfCancellationRequested();
            }
            else
            {
                Thread.Sleep(500);
                Console.WriteLine(&quot;任务t1,共执行30次,当前第{0}次&quot;, i);
            }
        }
    }, cts.Token);
    try
    {
        t1.Wait();
    }
    catch (AggregateException e)
    {
        foreach (var item in e.InnerExceptions)
        {
            Console.WriteLine(item);
        }
    }
    Console.ReadKey();
}
</code></pre>
<p><img src="/images/2019-04-24-18-45-02.png" alt=""><br>
工作原理<br>
程序运行主线程创建-&gt;创建<code>CancellationTokenSource</code>对象-&gt;设置<code>task</code>在指定毫秒数后取消(这里是 8000)-&gt;创建<code>task</code>并传入<code>CancellationTokenSource</code>对象生成的<code>token</code>,循环打印 1~30,取消标记为<code>true</code>则抛出异常中止任务,<code>false</code>则正常输出,在输出前等待 500 毫秒(避免 8000 毫秒还没到任务就已经执行完成)-&gt;使用<code>try/catch</code>包裹<code>t1.Wait()</code>等待任务执行完成语句,并捕获处理异常.这里任务在执行完 15 次的时候被取消.</p>
<h2 id="取消时回调">取消时回调</h2>
<blockquote>
<p><code>CancellationTokenSource.Token.Register()</code>,使用<code>Register</code>，向取消标记登记一个回调方法。应用程序调用<code>CancellationTokenSource</code> 对象的 <code>Cancel</code> 方法时，这个回调就会运行。但是不能保证这个方法在什么时候执行,可能在任务执行完自己的取消处理之前或之后,也可能在那个过程之中</p>
</blockquote>
<pre><code class="language-cs"> [TestMethod]
 public void Task_CancellationToken_Register_Example()
 {
     CancellationTokenSource cts = new CancellationTokenSource();
     var token = cts.Token;
     cts.CancelAfter(8000);
     token.Register(Callback);
     Task t1 = Task.Run(() =&gt;
     {
         for (int i = 0; i &lt; 30; i++)
         {
             if (token.IsCancellationRequested)
             {
                 token.ThrowIfCancellationRequested();
             }
             else
             {
                 Thread.Sleep(500);
                 Console.WriteLine(&quot;任务t1,共执行30次,当前第{0}次&quot;, i);
             }
         }
     }, token);
     try
     {
         t1.Wait();
     }
     catch (AggregateException e)
     {
         foreach (var item in e.InnerExceptions)
         {
             Console.WriteLine(item);
         }
     }
     static void Callback()
     {
         Console.WriteLine(&quot;Register登记的任务取消回调函数&quot;);
     }
     Assert.IsTrue(true);
 }
</code></pre>
<h1>问题记录</h1>
<h2 id="async-await并不能提升性能">async/await并不能提升性能?</h2>
<p>问题描述：所谓的异步操作就是A线程在执行任务的时候，执行到一半再把任务交给B线程，然后A线程开始等待B的执行完成了，这不还是同步的吗?</p>
<blockquote>
<p>异步编程不能提高性能。异步编程只是提供一种简单的编程模型来提高系统的响应能力，比如，如果在UI线程上执行长时间IO操作，在操作完成之前界面就处在无法操作的状态。<br>
可以提高性能的是并行技术，比如任务并行库（TPL，Task Parallel Library）、并行LINQ（PLINQ）等，这些技术可以充分利用CPU来提高计算性能。<br>
一般来说，耗时的IO操作使用异步简化代码，耗时的计算操作使用并行来提高性能</p>
</blockquote>
<blockquote>
<p>比如服务端最大处理请求是100个线程，那么同步模式下，150个请求过来，就有50个请求需要等待执行。async/await 模式下，正在执行的100个线程 可以空闲出来 处理后来的50个请求，前面100个请求异步完成后，再通过上下文切换到当前的100个线程，处理完后续的流程。工作线程是在不停的切换过程中提升了并发效率。而在单个请求中，async/await是有线程上下文切换的性能损耗，所以在处理一个本来就很快速的逻辑,例如读一个Redis缓存，那么性能会有比较显著的下降</p>
</blockquote>
<p>总结：异步编程只是提高了线程的利用率，针对一个本来就很“快”的操作，可能异步还不如同步，因为线程上下文的切换也会带来性能损耗</p>
<h2 id="async-await死锁问题">async/await死锁问题</h2>
<h3 id="可能发生死锁的程序类型">可能发生死锁的程序类型</h3>
<ul>
<li><code>WPF/WinForm</code>程序</li>
<li><a href="http://asp.net" target="_blank" rel="noopener">asp.net</a>(<a href="http://xn--asp-628d60u5v0a.net" target="_blank" rel="noopener">不包括asp.net</a> core)程序</li>
</ul>
<h3 id="什么情况下会产生死锁">什么情况下会产生死锁?</h3>
<p>调用 <code>Task.Wait()</code> 或者 <code>Task.Result</code> 立刻产生死锁的充分条件</p>
<ul>
<li>调用 <code>Wait()</code>或<code>Result</code>的代码位于<code>UI</code>线程</li>
<li><code>Task</code>的实际执行在其他线程，且需要返回<code>UI</code>线程</li>
</ul>
<h3 id="死锁的原因">死锁的原因?</h3>
<blockquote>
<p><code>UWP</code>、<code>WPF</code>、<code>Windows Forms</code>程序的 UI 线程都是单线程的。为了让使用了<code>async/await</code>的代码像使用同步代码一样简单，WPF 程序的<code>Application</code>类在构造的时候会将<code>主UI线程</code> Task的同步上下文设置为<code>DispatcherSynchronizationContext</code>的实例，当<code>Task</code>的任务结束时，会从<code>AsyncMethodStateMachine</code>中调用<code>Awaiter</code>的<code>OnComplete()</code>方法，而<code>await</code>后续方法的执行靠的就是<code>OnComplete()</code>方法中一层层调用到 <code>DispatcherSynchronizationContext</code>里的<code>Post</code>方法</p>
</blockquote>
<pre><code class="language-cs">/// &lt;summary&gt;
/// Asynchronously invoke the callback in the SynchronizationContext.
/// &lt;/summary&gt;
public override void Post(SendOrPostCallback d, Object state)
{
    // Call BeginInvoke with the cached priority.  Note that BeginInvoke
    // preserves the behavior of passing exceptions to
    // Dispatcher.UnhandledException unlike InvokeAsync.  This is
    // desireable because there is no way to await the call to Post, so
    // exceptions are hard to observe.
    _dispatcher.BeginInvoke(_priority, d, state);
}
</code></pre>
<p>这里就是问题的关键！！！</p>
<p>如果<code>_dispatcher.BeginInvoke(_priority, d, state);</code>这句代码在后台线程，那么此时<code>UI线程</code>处于<code>Wait()/Result</code>调用中的阻塞状态，<code>BeginInvoke</code>中的任务是无论如何也无法执行到的,于是无论如何都无法完成这个<code>Post</code>任务，即无论如何也无法退出此异步任务的执行，于是<code>Wait()</code>便无法完成等待，产生死锁</p>
<p>Example</p>
<pre><code class="language-cs">DoAsync().Wait();

async Task DoAsync()
{
    await Task.Run(() =&gt; { });
}
</code></pre>
<p>无论是<code>WPF</code>还是<code>UWP</code>，只要在<code>UI</code>线程上调用上述代码，必然死锁</p>
<h3 id="There-Is-No-Thread">There Is No Thread</h3>
<blockquote>
<p>读写文件，访问网络，这些 IO 阻塞的操作执行时，里面根本就没有线程，详情请阅读：<a href="https://blog.stephencleary.com/2013/11/there-is-no-thread.html" target="_blank" rel="noopener">There Is No Thread</a></p>
</blockquote>
<p>还有另一些操作，也没有后台线程的参与，于是也不存在从后台线程回到主线程导致死锁的情况。如<code>Task.Yield</code>，还有<code>InvokeAsync</code>，它们也不会造成死锁。如果是控制台程序，或者一个普通的非UI线程，其<code>SynchronizationContext</code>为<code>null</code>，那么异步任务执行完后不需要回到原有线程，也不会造成死锁</p>
<h3 id="不会造成死锁的充分条件">不会造成死锁的充分条件</h3>
<ul>
<li>异步操作执行完后不需要回到原有线程（例如非 UI 线程和控制台线程）</li>
<li>异步操作不需要单独的线程执行任务</li>
</ul>
<h3 id="如何避免死锁">如何避免死锁?</h3>
<ul>
<li>在 UI 线程，如果使用了<code>async/await</code>，就尽量不要再使用<code>Task.Wait()/Task.Result</code>了，就一直异步一条路走到黑好了（微软称其为<code>Async All the Way</code>）</li>
<li>如果可能，尽量在异步任务后添加<code>.ConfigureAwait(false);</code>这样，异步任务后面继续执行的代码就不会回到原<code>UI</code>线程了，而是直接从线程池中再取出一个线程执行；这样，即便<code>UI</code>线程后续可能有别的原因造成阻塞，也不会产生死锁了<br>
把原来的代码改成这样，就不会死锁了：</li>
</ul>
<p>只能是一路<code>async/await</code>。微软将其描述为：<code>async/await 会像病毒一样在你的代码中传播</code></p>
<blockquote>
<p>Others have also noticed the spreading behavior of asynchronous programming and have called it “contagious” or compared it to a zombie virus.</p>
</blockquote>
<p>这句话的原文参见：<a href="https://docs.microsoft.com/en-us/archive/msdn-magazine/2013/march/async-await-best-practices-in-asynchronous-programming" target="_blank" rel="noopener">Async/Await - Best Practices in Asynchronous Programming</a></p>
<p>如果你是类库提供者，因为不确定调用者程序是<code>WPF</code>/<code>Winform</code>…,为了防止代码调用者在同步方法中使用<code>Wait()/Result</code>调用异步方法，还需要考虑添加<code>.ConfigureAwait(false);</code></p>
<pre><code class="language-cs">async Task DoAsync()
{
    await Task.Run(() =&gt; { }).ConfigureAwait(false);
}
</code></pre>
<p>这一句的目的是防止执行上下文切换回 UI 线程。</p>
<p>这样，即便真的使用 DoAsync().Wait() 也不会发生死锁。注意，整个方法调用链都需要使用<code>.ConfigureAwait(false)</code>才能够防止线程切换时，在调用方的 Wait() 方法中发生死锁。详见我的另一篇博客 在编写异步方法时，使用 ConfigureAwait(false) 避免使用者死锁。）</p>
<h3 id="更多死锁相关">更多死锁相关</h3>
<p><a href="https://blog.walterlv.com/post/deadlock-in-task-wait.html" target="_blank" rel="noopener">使用 Task.Wait()？立刻死锁（deadlock） - walterlv</a><br>
<a href="https://blog.walterlv.com/post/deadlock-of-invoke-in-lazy.html" target="_blank" rel="noopener">不要使用 Dispatcher.Invoke，因为它可能在你的延迟初始化 Lazy 中导致死锁 - walterlv</a><br>
<a href="https://blog.walterlv.com/post/deadlock-if-await-in-ui-lock-context.html" target="_blank" rel="noopener">在有 UI 线程参与的同步锁（如 AutoResetEvent）内部使用 await 可能导致死锁</a><br>
<a href="https://blog.walterlv.com/post/task-wait-may-cause-long-time-waiting.html" target="_blank" rel="noopener">.NET 中小心嵌套等待的 Task，它可能会耗尽你线程池的现有资源，出现类似死锁的情况 - walterlv</a></p>
<p>解决方法</p>
<p><a href="https://blog.walterlv.com/post/using-configure-await-to-avoid-deadlocks.html" target="_blank" rel="noopener">在编写异步方法时，使用 ConfigureAwait(false) 避免使用者死锁 - walterlv</a><br>
<a href="https://walterlv.com/post/convert-async-to-sync-by-push-frame.html" target="_blank" rel="noopener">将 async/await 异步代码转换为安全的不会死锁的同步代码（使用 PushFrame） - walterlv</a></p>
<h1>参考</h1>
<p><a href="https://blog.csdn.net/WPwalter/article/details/78370706" target="_blank" rel="noopener">https://blog.csdn.net/WPwalter/article/details/78370706</a><br>
<a href="https://docs.microsoft.com/en-us/archive/msdn-magazine/2013/march/async-await-best-practices-in-asynchronous-programming" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/archive/msdn-magazine/2013/march/async-await-best-practices-in-asynchronous-programming</a><br>
<a href="https://www.cnblogs.com/qingfenglin/p/12058267.html" target="_blank" rel="noopener">https://www.cnblogs.com/qingfenglin/p/12058267.html</a><br>
<a href="https://www.cnblogs.com/liqingwen/p/5844095.html" target="_blank" rel="noopener">https://www.cnblogs.com/liqingwen/p/5844095.html</a><br>
<a href="https://www.cnblogs.com/zhaoshujie/p/11082753.html" target="_blank" rel="noopener">https://www.cnblogs.com/zhaoshujie/p/11082753.html</a><br>
<a href="https://q.cnblogs.com/q/107005/" target="_blank" rel="noopener">https://q.cnblogs.com/q/107005/</a><br>
<a href="https://rubikscode.net/2018/06/11/asynchronous-programming-in-net-benefits-and-tradeoffs-of-using-valuetask/" target="_blank" rel="noopener">https://rubikscode.net/2018/06/11/asynchronous-programming-in-net-benefits-and-tradeoffs-of-using-valuetask/</a><br>
<a href="https://blog.scooletz.com/2018/05/14/task-async-await-valuetask-ivaluetasksource-and-how-to-keep-your-sanity-in-modern-net-world/" target="_blank" rel="noopener">https://blog.scooletz.com/2018/05/14/task-async-await-valuetask-ivaluetasksource-and-how-to-keep-your-sanity-in-modern-net-world/</a><br>
<a href="https://www.cnblogs.com/ms27946/p/understanding-task-valuetask.html" target="_blank" rel="noopener">https://www.cnblogs.com/ms27946/p/understanding-task-valuetask.html</a><br>
<a href="https://www.cnblogs.com/ittranslator/p/13703279.html" target="_blank" rel="noopener">https://www.cnblogs.com/ittranslator/p/13703279.html</a><br>
<a href="https://devblogs.microsoft.com/dotnet/understanding-the-whys-whats-and-whens-of-valuetask/" target="_blank" rel="noopener">https://devblogs.microsoft.com/dotnet/understanding-the-whys-whats-and-whens-of-valuetask/</a><br>
<a href="https://www.cnblogs.com/xiaoxiaotank/p/13206569.html" target="_blank" rel="noopener">https://www.cnblogs.com/xiaoxiaotank/p/13206569.html</a><br>
<a href="https://www.cnblogs.com/wcrBlog/p/11690460.html" target="_blank" rel="noopener">https://www.cnblogs.com/wcrBlog/p/11690460.html</a><br>
<a href="https://docs.microsoft.com/zh-cn/dotnet/csharp/programming-guide/concepts/async/async-return-types#generalized-async-return-types-and-valuetasktresult" target="_blank" rel="noopener">https://docs.microsoft.com/zh-cn/dotnet/csharp/programming-guide/concepts/async/async-return-types#generalized-async-return-types-and-valuetasktresult</a><br>
<a href="https://q.cnblogs.com/q/124633/" target="_blank" rel="noopener">https://q.cnblogs.com/q/124633/</a><br>
<a href="https://www.cnblogs.com/dudu/p/9860959.html#!comments" target="_blank" rel="noopener">https://www.cnblogs.com/dudu/p/9860959.html#!comments</a><br>
<a href="http://labs.criteo.com/2018/10/net-threadpool-starvation-and-how-queuing-makes-it-worse/" target="_blank" rel="noopener">http://labs.criteo.com/2018/10/net-threadpool-starvation-and-how-queuing-makes-it-worse/</a><br>
<a href="https://www.cnblogs.com/xishuai/p/asp-net-sync-over-async.html" target="_blank" rel="noopener">https://www.cnblogs.com/xishuai/p/asp-net-sync-over-async.html</a></p>
]]></content>
      <categories>
        <category>CSharp</category>
      </categories>
  </entry>
</search>
