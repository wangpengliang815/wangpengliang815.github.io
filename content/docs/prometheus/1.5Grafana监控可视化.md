[关于 Grafana 更多资料参考](https://grafana.com/)

# Grafana 安装

这里采用 Dokcer 安装 Grafana，如果防火墙处于开启状态需要开放对应端口。

```bash
docker run -d -p 3000:3000 --user root --name=grafana -v /home/grafana:/var/lib/grafana grafana/grafana:7.3.6
```

# Grafana 登录

访问：http://ipaddress:3000，默认账号/密码：`admin/admin`

![](/images/2022-07-13-16-54-53.png)

# 添加数据源

登陆成功后，点击 "datasources" 按钮，跳转到添加数据源页面，Type 选择 Prometheus 配置如下：

- `Name`:  数据源名称

- `URL`:  Prometheus 地址

- `Access`:  Server(Default)

其余默认，点击 "Save & Test"，如下所示：

![](/images/2022-07-13-16-58-48.png)

# 导入 Dashboard

从 Grafana 官网下载相关 dashboaed 到本地，如：https://grafana.com/dashboards/405

Grafana首页 => 左上角图标 => Dashboard => Import

![](/images/2022-07-13-17-02-28.png)

上传已下载至本地的 `json `文件或者直接使用 `dashboard id`，[更多 Dashboard 参考](https://grafana.com/grafana/dashboards/)，数据源选择之前添加的数据源，点击导入按钮。

# 查看 Dashboard

Grafana首页 => 左上角图标 => Dashboard => Home，双击查看创建的 Dashboard 如下：

![](/images/2022-07-13-17-06-55.png)

